<?xml version="1.0" encoding="utf-8"?>
<feed xmlns="http://www.w3.org/2005/Atom">
  <title>λ</title>
  <subtitle>(conj clojurians me)</subtitle>
  <link href="/atom.xml" rel="self"/>
  
  <link href="https://lameta.com/"/>
  <updated>2017-04-18T11:44:18.000Z</updated>
  <id>https://lameta.com/</id>
  
  <author>
    <name>lambeta</name>
    <email>qianyan.lambda@gmail.com</email>
  </author>
  
  <generator uri="http://hexo.io/">Hexo</generator>
  
  <entry>
    <title>如何高效地学习编程语言</title>
    <link href="https://lameta.com/2017/04/18/Techniques-for-Efficiently-Learning-Programming-Languages/"/>
    <id>https://lameta.com/2017/04/18/Techniques-for-Efficiently-Learning-Programming-Languages/</id>
    <published>2017-04-18T07:15:09.000Z</published>
    <updated>2017-04-18T11:44:18.000Z</updated>
    
    <content type="html"><![CDATA[<blockquote>
<p>这是Clojure好书《Clojure for the Brave and True》作者 Daniel Higginbotham 写于2017年4月16日的博文。从作者的丰富的经验来看，本文非常具有指导意义。</p>
</blockquote>
<p>学习编程语言是一种技巧：做好了，你会感受到掌握新事物之后的快感（dopamine：多巴胺）；做不好，就会接二连三的沮丧，甚至放弃。下面这些学习编程语言的最佳技巧是我从多年的<a href="http://www.braveclojure.com/" target="_blank" rel="external">著书</a><a href="http://www.flyingmachinestudios.com/programming/the-unofficial-guide-to-rich-hickeys-brain/" target="_blank" rel="external">写作</a>，<a href="https://www.youtube.com/watch?v=eRq5UBx6cbA" target="_blank" rel="external">演讲</a>和<a href="http://www.braveclojure.com/training/" target="_blank" rel="external">培训</a>中总结出来的。这里头很多技巧来源于对高效学习前沿研究做了解释的书籍。你可以在 <a href="http://www.communitypicks.com/r/learnprogramming" target="_blank" rel="external">Community Picks Learn Programming</a> 中找到那些书（还有其它牛x的编程书籍）。</p>
<h2 id="持续测试自己以抵抗胜任力错觉"><a href="#持续测试自己以抵抗胜任力错觉" class="headerlink" title="持续测试自己以抵抗胜任力错觉"></a>持续测试自己以抵抗胜任力错觉</h2><p>最不济的学习方法中的一种就是重读或者重看材料。这种重复会给你一种感觉——似乎不用花什么气力，你就理解了话题所涵盖的内容。研究者们把这种现象称作胜任力错觉。</p>
<p>更好的一种方式（你可以掌握的最佳技巧之一）则是持续地测试自己。不要重读一个函数、类或者一个对象是什么，而是让自己定义这些概念或者把它们用到短小的程序当中——强迫你用某种方式显示自己的理解。这一过程常常很不舒服的，但是对形成长期记忆非常有效。更进一步，你可以在阅读材料之前先去行测试，举个例子，尝试在阅读一个章节之前做做练习。值得一提的是，这也被证明有助于记忆的形成。</p>
<p>测试对于学习的显著影响被称为<strong>测试效用</strong>，下面是一些具体方法可以利用：</p>
<ul>
<li>在阅读章节或者看视频之前，尝试猜测你将要学习的东西，并写下来。</li>
<li>在阅读章节之前先做做这个章节的练习题。</li>
<li>一直做练习，即使是最难的那些。暂时（永远）放弃一个练习也是可以的，不过至少要尝试一遍。（下个章节会详细谈到）</li>
<li>阅读短小的程序并且尝试不看源码重新写一个。或者，再小一些，写个函数也行。</li>
<li>在学习了对象、类、方法或者高阶函数等新概念之后，立即编码做示例。</li>
<li>创建阐述这些概念的图示，以及这些概念之间的区别和联系。</li>
<li>把你刚刚学到的概念写成博客。</li>
<li>尝试把概念解释给非技术的朋友听。（在写《Clojure for Brave and True》的时候，我常常这么干。这样能够以外行的话阐述一个想法，进而迫使你深入理解想法本身。）</li>
</ul>
<p>这些技巧的大部分都要规约到编写代码上！说到编程，由于程序都是重文本和概念性的，所以很容易以为我们仅仅通过阅读（代码）就在学习。但是程序同时也是一门手艺，就像其它手艺一样你得操练才能娴熟。编码是暴露你对程序作有错误假设的最佳方式。你越快地这么做，就会越快地纠正错误和提升技能。</p>
<p>如果你想了解更多测试效应的事情，敬请查看<a href="http://www.communitypicks.com/r/learnprogramming/s/17592186047889-make-it-stick-the-science-of-successful-learning" target="_blank" rel="external">坚持：成功学习的科学（make it stick： The Science of Successful Learning）</a>。</p>
<h2 id="花时间放空自己"><a href="#花时间放空自己" class="headerlink" title="花时间放空自己"></a>花时间放空自己</h2><p>如果你纠结在一个问题上，或者对刚才读到的东西不能理解，就去散散步甚或洗个澡 —— 只要能进入一种舒缓、放空的状态就行。解除障碍的最佳方式之一就是歇一会儿，这可能听上去有点反直觉，但确实如此。</p>
<p>问题是，当全神贯注解决问题时，我们很容易陷入思维障碍（mental blinder）。我的意思是，这差不多就是“关注”（字面上）的意思。不过，全神贯注会导致我们只能一直探索解决方案空间的一小部分。一旦放空，我们的潜意识就可以探索并联结我们经验中的广泛领域。</p>
<p>对我来说，这就像当你试图在纸质地图上找到一个目的地（是否还记得？）。你不用刻意就确信你想抵达的城市应该就在这里！在地图的左上角的区域，所以你看了一遍又一遍，都没有成功。然后你放下地图，做了深呼吸并让目光游离了一会儿。当你重新看地图时，确切的地点立马映入眼帘。</p>
<p>我们曾经都有过这样的经验，在洗澡的时候突发灵感。现在你对于为什么这么做有了更好的了解，那么也就能刻意地使用这个技巧。个人来讲，如果纠结在某事上，我真的会洗个澡，这个技巧的功效显著。另一方面，我又是多么干净（注：洗澡这件事）。</p>
<p>如果你想多学一些关于思考的关注和分散模式，敬请查看<a href="http://www.communitypicks.com/r/learnprogramming/s/17592186047884-a-mind-for-numbers-how-to-excel-at" target="_blank" rel="external">A Mind for Numbers: How to Excel at Math and Science (Even If You FLunked Algebra)</a></p>
<h2 id="别浪费时间沮丧"><a href="#别浪费时间沮丧" class="headerlink" title="别浪费时间沮丧"></a>别浪费时间沮丧</h2><p>和上一个章节相关：别浪费时间为代码沮丧了。沮丧会让我们做一些愚蠢的事情，比如重新编译或者重刷浏览器，期望这次会有所不同。</p>
<p>把沮丧看作你的知识有差距的信号。一旦你意识到自己沮丧了，它可以帮你后退一步，清晰地识别问题。如果你写的代码不起作用，坦率地向自己或者别人说明你期望的结果。使用科学的方法，就非预期行为的根因提出一个假说。然后测试你的假说。再次试验后，如果依然解决不了，就把这个问题放到一边，待会儿回来。</p>
<p>在一些似乎没法解决的问题上，我不知有多少次恼怒地扔掉了自己的笔记本电脑。隔天再看，一个显而易见的解决方案立马跳入脑海。甚至上周就发生过。</p>
<h2 id="确认你正在处理语言的哪个方面"><a href="#确认你正在处理语言的哪个方面" class="headerlink" title="确认你正在处理语言的哪个方面"></a>确认你正在处理语言的哪个方面</h2><p>个人观点，我觉得记住这些是有用的——当学习一门编程语言的时候，你实际上正在学四件事情。</p>
<ul>
<li>怎么写代码：语法、语义以及资源管理</li>
<li>语言的范式：面向对象，函数式，逻辑等</li>
<li>产出物的生态圈：如何构建、运行可执行文件以及如何使用库</li>
<li>工具：编辑器，编译器，调试器，代码质量检测器（linter）</li>
</ul>
<p>这四项很容易搞混，不幸的结果是，当你遇到问题最终完全找错了地方。</p>
<p>举个例子，某些完全的编程新手，可能准备开始构建iOS应用。他们可能会试着让自己的应用在朋友的手机上运行，只看得到有关需要开发人员证书或其他信息的消息。这是产出物生态圈的一部分，不过小白可能将此视为编写代码的问题。他们可能会浏览自己写的每行代码来尝试解决问题，尽管问题和代码没有半毛钱关系。</p>
<p>如果我系统地处理这些方面，我会发现学习一种语言会更加容易。我将在其它的博客文章中罗列一些待回答的宽泛问题的列表，应该能帮助你学习任何语言。</p>
<h2 id="明确目的，外部模型，内部型型"><a href="#明确目的，外部模型，内部型型" class="headerlink" title="明确目的，外部模型，内部型型"></a>明确目的，外部模型，内部型型</h2><p>任何时候你学习使用新工具，明确学习的目的，外部模型和内部模型都是十分有用的。</p>
<p>当你了解了工具的目的时，你的大脑会加载有用的上下文细节，使你更容易吸收新知识。这就好比拼图：当你看到完整拼图之后，更容易把各部件拼到一起。这个道理适用于语言本身及及语言库。</p>
<p>工具的外部模型就是它呈现出来的接口以及它想让你思考问题解决的方式。Clojure 的外部模型就是一个Lisp，它想让你把编程当做大部分以数据为中心，不可变的转换过程。Ansible 希望你把服务器的整备工作想成定义最终状态，而不是定义抵达那种状态所要采取的步骤。</p>
<p>工具的内部模型就是如何将输入到其接口转换成一些底层的抽象。Clojure 把 Lisp 转换成 JVM 的字节码。Ansible 把任务定义转换成了 shell 命令。在一个理想国中，你不需要理解工具的内部模型，但事实上，理解内部模型总是有用的，因为在某些看上去迷惑或者矛盾的部分，它可以让你有个统一视图。举个例子，当 DNA 双螺旋模型被发现的时候，它帮助科学家们了解更高层次的现象。从我的角度来讲，当然，这篇博文也是历来所有伟大科学成就之一。</p>
<p>很多教程经常混淆工具的外部和内部模型，使学习者感到困惑。意识到这点可以帮你轻松辨别何时你会感到沮丧。</p>
<h2 id="间隔重复帮助记忆"><a href="#间隔重复帮助记忆" class="headerlink" title="间隔重复帮助记忆"></a>间隔重复帮助记忆</h2><p>间隔重复被证明是长期记忆中新信息编码的最佳方法之一。 这个想法是以不断增加的时间间隔来测验自己，以使用最少重复次数最小化记忆衰减。 卫报写了一篇<a href="https://www.theguardian.com/education/2016/jan/23/spaced-repetition-a-hack-to-make-your-brain-store-information" target="_blank" rel="external">很好的介绍性文章</a>。</p>
<h2 id="睡眠和练习"><a href="#睡眠和练习" class="headerlink" title="睡眠和练习"></a>睡眠和练习</h2><p>保重身体！身体可不仅仅是你脑袋的载体。如果你想保持专注和高效的学习，就要足够的睡眠和练习，而不是（原文：beats the pants off）狂饮咖啡因和能量饮料。</p>
<h2 id="更多小贴士"><a href="#更多小贴士" class="headerlink" title="更多小贴士"></a>更多小贴士</h2><p>如果你还有其它有用的技巧，请不吝评论！如果你想知道更多关于学习编程的优质资源，敬请查看<a href="http://www.communitypicks.com/r/learnprogramming" target="_blank" rel="external">Community Picks: Learn Programming</a>，这是一个社区策划编程学习书籍的收集活动，内容广泛，包含入门编程，工艺以及关于软技能和面试方面的书籍。</p>
<hr>
<p>原文链接<br>[1] <a href="http://www.flyingmachinestudios.com/programming/learn-programming-languages-efficiently/" target="_blank" rel="external">Techniques for Efficiently Learning Programming Languages</a></p>
]]></content>
    
    <summary type="html">
    
      &lt;blockquote&gt;
&lt;p&gt;这是Clojure好书《Clojure for the Brave and True》作者 Daniel Higginbotham 写于2017年4月16日的博文。从作者的丰富的经验来看，本文非常具有指导意义。&lt;/p&gt;
&lt;/blockquote&gt;

    
    </summary>
    
      <category term="Clojure" scheme="https://lameta.com/categories/Clojure/"/>
    
    
      <category term="thinking" scheme="https://lameta.com/tags/thinking/"/>
    
  </entry>
  
  <entry>
    <title>Pipleline as Code</title>
    <link href="https://lameta.com/2017/03/01/Pipleline-as-Code/"/>
    <id>https://lameta.com/2017/03/01/Pipleline-as-Code/</id>
    <published>2017-03-01T02:19:13.000Z</published>
    <updated>2017-03-01T02:52:12.000Z</updated>
    
    <content type="html"><![CDATA[<p>2016年11月份的技术雷达中给出了一个简明的定义：流水线即代码 (Pipeline as Code) 通过编码而非配置持续集成/持续交付 (CI/CD) 运行工具的方式定义部署流水线。<br>其实早在2015年11月份的技术雷达当中就已经有了类似的概念：</p>
<blockquote>
<p>The way to avoid programming in your CI/CD tool is to extract the complexities of the build process from the guts of the tool and into a simple script which can be invoked by a single command. This script can then be executed on any developer workstation and therefore eliminates the privileged/singular status of the build environment<br>大意是将复杂的构建流程纳入一个简单的脚本文件，然后用一条命令调用。这样，任意的开发者都能在自己的工作区中执行脚本重建一套一模一样的构建环境，从而消除 CI/CD 环境由于散乱配置腐化而成的特异性。这么做的原因很好理解，使用 CI/CD 工具是为了暴露产品代码中的问题的，如果它们自身已经复杂到不稳定的地步，我们还使用它就是自找麻烦。</p>
</blockquote>
<p>从某种程度上看，实施流水线即代码是不证自明的。在 CI/CD 的时间过程中，凡是可以被编码的东西都已经被代码化了，比如：构建、测试、数据库迁移、部署和基础设施/环境配置 (Infrastruture as Code) 等。说得烂俗点，流水线已经是 CI/CD 实践过程中的“最后一公里”，让流水线变成软件开发中的“一等公民”（即代码）是大势所趋、民心所向。不过，这种论断毕竟欠缺说服力，我们接着从实践的痛点出发总结当前流水线遇到的问题。</p>
<h2 id="实践中的痛点"><a href="#实践中的痛点" class="headerlink" title="实践中的痛点"></a>实践中的痛点</h2><p>我给客户搭建和配置过不少 CI/CD 流水线（被同事戏谑地称为“CI/CD搭建兽”），最大的痛苦莫过于每次都得从头来过，即便大部分情况下所用的工具和配置都大同小异。其次是手工操作产生的配置漂移 (configuration drift) 。以 Jenkins 为例，先不谈 1.0 版本不支持流水线这一概念的问题，我们为了解决遇到的构建、测试和部署等问题，一般会在多个文本框中粘贴大量 shell/batch 脚本；甚至会通过这些文本框安装各种插件或者依赖包、设置环境变量等等。久而久之（实际上不需要多久），这台 Jenkins 服务器就变得不可替代（特异化）了，因为没人清楚到底对它做了哪些更改以及这些更改对承载它的系统产生哪些影响，这时 Jenkins 服务器俨然腐化成了老马所说的雪花服务器 (snowflake server)。雪花服务器有两点显著的特征：</p>
<ol>
<li>特别难以复现</li>
<li>几乎无法理解</li>
</ol>
<p>第一点是由于以往所做的更改并没有被记录下来，所以做过的操作都是七零八落的，没有办法复现同样的操作，也无法复制一个同样的系统。<br>第二点则是由于绝大部分情况下散乱的配置是没有文档描述的，哪部分是重要的已经无从知晓，改动的风险很大。</p>
<p>这些问题会在流水线的演化过程中恶化得越来越严重。一般来讲，除非不再使用，否则流水线不会保持一成不变。具体实施过程中，考虑到项目，尤其是遗留项目当前的特点和团队成员的“产能”，我们会先将构建和部署自动化；部署节奏稳定后，开始将单元测试和代码分析自动化；接着可以指导测试人员将验收测试自动化；然后尝试将发布自动化。在这之后，就要开始持续优化流水线，包括 CI 的速度和稳定性等。换句话说，流水线的演化其实是和项目的当前进展密切相关的，保证这样的对应关系有时是有必要的，比如：在版本控制下，多发布分支所需流水线和主干分支会存在不同。发布分支是主干分支某个时刻分出去的，它需要在那时的流水线上才能正常工作。由于前面所说雪花服务器的特征，重建这样一条流水线并不是一件容易的事情。</p>
<p><img src="http://upload-images.jianshu.io/upload_images/217988-e8d3fcb49a100197.png?imageMogr2/auto-orient/strip%7CimageView2/2/w/1240" alt="演进式的持续集成"></p>
<h2 id="如何解决"><a href="#如何解决" class="headerlink" title="如何解决"></a>如何解决</h2><p>其实，流水线即代码本身已经回答这个问题了。当前实现了这一概念的工具大体遵循了两种模式：</p>
<ol>
<li>版本控制</li>
<li>DSL（领域特定语言）</li>
</ol>
<p>对于特别难以复现、没有保证对应关系的痛点，我们就把流水线写成代码放到版本控制工具中管理起来。这样一来，每一次更改都能被记录下来，而且它会始终和此时的项目进展保持同步。</p>
<p>对于几乎无法理解、没有文档支持的痛点，我们就选用领域特定语言描述整条流水线。举个 Jenkins 2.0 例子，它允许我们在项目的特定目录下放置一个 Jenkinsfile 的文件，内容大致如下：<br><figure class="highlight stylus"><table><tr><td class="gutter"><pre><div class="line">1</div><div class="line">2</div><div class="line">3</div><div class="line">4</div><div class="line">5</div><div class="line">6</div><div class="line">7</div><div class="line">8</div><div class="line">9</div><div class="line">10</div><div class="line">11</div><div class="line">12</div><div class="line">13</div><div class="line">14</div><div class="line">15</div><div class="line">16</div><div class="line">17</div></pre></td><td class="code"><pre><div class="line"><span class="function"><span class="title">node</span><span class="params">(<span class="string">'master'</span>)</span></span> &#123;</div><div class="line">	stage(<span class="string">'Checkout'</span>) &#123;…&#125;</div><div class="line">	stage(<span class="string">'Code Analysis'</span>) &#123;…&#125;</div><div class="line">	stage(<span class="string">'Unit Test'</span>) &#123;…&#125;</div><div class="line">	stage(<span class="string">'Packing'</span>) &#123;…&#125;</div><div class="line">	stage(<span class="string">'Archive'</span>) &#123;…&#125;</div><div class="line">	stage(<span class="string">'DEV'</span>) &#123;…&#125;</div><div class="line">&#125;</div><div class="line"><span class="function"><span class="title">stage</span><span class="params">(<span class="string">'SIT'</span>)</span></span> &#123;</div><div class="line">	timeout(<span class="selector-tag">time</span>:<span class="number">4</span>, unit:<span class="string">'HOURS'</span>) &#123;</div><div class="line">		<span class="selector-tag">input</span> <span class="string">"Deploy to SIT?"</span></div><div class="line">	&#125;</div><div class="line">	node(<span class="string">'master'</span>) &#123;…&#125;</div><div class="line">&#125;</div><div class="line"><span class="function"><span class="title">stage</span><span class="params">(<span class="string">'Acceptance Test'</span>)</span></span> &#123;</div><div class="line">	node(<span class="string">'slave'</span>) &#123;…&#125;</div><div class="line">&#125;</div></pre></td></tr></table></figure></p>
<p>Jenkins 2.0 使用Groovy实现了一套描述流水线的DSL，即便不了解Groovy语言，只要对流水线稍微熟悉，就能按照<a href="https://github.com/jenkinsci/pipeline-plugin/blob/master/TUTORIAL.md" target="_blank" rel="external">例子和文档</a>编写出符合要求的代码。</p>
<p>类似的工具还有Concourse.ci、λCD (LambdaCD) 等。<br>Concourse.ci 使用了 yaml 实现了DSL，独立抽象出Resource（外部依赖，如：git repo）、Job（函数， get 和 put Resource ）和 Task（纯函数，必须明确定义 Input 和 Output ）模型。</p>
<p><img src="http://upload-images.jianshu.io/upload_images/217988-250bc1dc50e3bdd8.png?imageMogr2/auto-orient/strip%7CimageView2/2/w/1240" alt="Concousre.ci"></p>
<p>而 λCD 则使用 Clojure 语言实现了 DSL，抽象出 Pipeline 和 Step 模型，使用了Lisp特有的宏 (macro) 和普通函数，编写起来简单明了。</p>
<p><img src="http://upload-images.jianshu.io/upload_images/217988-8b29a42d225edebf.png?imageMogr2/auto-orient/strip%7CimageView2/2/w/1240" alt="λCD"></p>
<figure class="highlight clojure"><table><tr><td class="gutter"><pre><div class="line">1</div><div class="line">2</div><div class="line">3</div><div class="line">4</div><div class="line">5</div><div class="line">6</div><div class="line">7</div><div class="line">8</div><div class="line">9</div><div class="line">10</div><div class="line">11</div><div class="line">12</div><div class="line">13</div><div class="line">14</div></pre></td><td class="code"><pre><div class="line">(<span class="name"><span class="builtin-name">def</span></span> pipeline-def</div><div class="line">  `(</div><div class="line">    (<span class="name">either</span></div><div class="line">     manualtrigger/wait-for-manual-trigger</div><div class="line">     wait-for-repo)</div><div class="line"></div><div class="line">    (<span class="name">with-workspace</span></div><div class="line">      clone</div><div class="line">      (<span class="name">in-parallel</span></div><div class="line">       run-some-tests</div><div class="line">       run-smokeing-tests)</div><div class="line"></div><div class="line">      run-package</div><div class="line">      deploy)))</div></pre></td></tr></table></figure>
<p>上述的pipeline-def就是这条流水线的定义，极为优雅得是，它的代码和UI事实上构成了一一映射的关系，简单到极致。</p>
<p>值得一提的是，λCD 有别于其它同类型的工具，它本身就是一份用 Clojure 写就的微服务。换句话说，其它的工具可能需要借助基础设施即代码完成自身的安装，但λCD不用，它完全可以采用其它微服务的部署方式，比如用 λCD 部署它自己，类似于编译器的自举 (bootstraping)。这个时候，我们就需要两套 λCD 服务，一套用于部署自身，另一套部署开发中的工程。</p>
<p><img src="http://upload-images.jianshu.io/upload_images/217988-85057d2c87c41ab1.png?imageMogr2/auto-orient/strip%7CimageView2/2/w/1240" alt="流水线自举"></p>
<h2 id="小结"><a href="#小结" class="headerlink" title="小结"></a>小结</h2><p>流水线即代码是个新概念，也就意味着我们还需要花时间去探索与之相关的实践，比如，调试和测试（既然是代码就需要测试）。一旦有了这些实践，我们就可以把流水线本身作为产品放到流水线上运作起来，那时将会看到一种很好玩的现象——旧的流水线会构建并部署新流水线，完成流水线的自举 (pipeline bootstrap) 。此外，当流水线成为代码，它在最终的交付物中必然占据一席之地，其潜在的价值还等待我们挖掘，至少从精益的角度，流水线能做的事情还有很多。</p>
]]></content>
    
    <summary type="html">
    
      &lt;p&gt;2016年11月份的技术雷达中给出了一个简明的定义：流水线即代码 (Pipeline as Code) 通过编码而非配置持续集成/持续交付 (CI/CD) 运行工具的方式定义部署流水线。&lt;br&gt;其实早在2015年11月份的技术雷达当中就已经有了类似的概念：&lt;/p&gt;
&lt;blo
    
    </summary>
    
      <category term="技术" scheme="https://lameta.com/categories/%E6%8A%80%E6%9C%AF/"/>
    
    
      <category term="thinking" scheme="https://lameta.com/tags/thinking/"/>
    
  </entry>
  
  <entry>
    <title>Emacs configs for a clojurian</title>
    <link href="https://lameta.com/2017/01/01/Emacs-configs-for-a-clojurian/"/>
    <id>https://lameta.com/2017/01/01/Emacs-configs-for-a-clojurian/</id>
    <published>2017-01-01T12:00:24.000Z</published>
    <updated>2017-01-17T06:14:36.000Z</updated>
    
    <content type="html"><![CDATA[<p>俗话说，工欲善其事必先利其器，完善开发工具与我而言是一件快乐的事情，分享也是一件令人愉悦的事情，所以我想把学习过程中的点滴记录下，留作备忘。本文不会介绍太多花式或有深度的emacs配置，更多是摸索学习的过程，其中充满了乐趣。</p>
<h2 id="原因"><a href="#原因" class="headerlink" title="原因"></a>原因</h2><p>网络上的<em>.emacs.d/init.el</em>配置数不胜数，各路lisp大神的<em>dot file</em>都已经放在github上了，而且前有牛人撰文推荐学习emacs配置的详实方法，看似确实没有什么必要自己折腾一份配置。这个说法对，也不对。我在转向emacs之前，是一名忠实的vim党，从大学开始就不断折腾vim的配置，还花过一段时间专门学习了<em>vimscript</em>，曾经惊叹于<em>vimscript</em>的动态函数式风格的优美和强大。类似地，<em>.vimrc</em>配置文件在网络上也多如牛毛，华丽和酷炫的插件极大地提升了vim的操作性。尽管如此，我还是乐于一砖一瓦地打造自己的vim环境，竭力演化它变成我心目中的“编辑器之神”。这个过程一般会充满修改然后重启的重复性机械劳作，偶尔会遭遇无论怎么修改就是不生效、甚至遍寻google也一无所获的挫折，但是我就是无法厌倦它，人天生好奇，探索未知事物本身就充满了乐趣，而且一旦配置奏效，便能获得满满的成就感。新事物对程序员具有极大的吸引力，但是程序员不会止步于使用新事物，而且会在惊奇之余，渴望控制那股背后主导它的力量本身，行使“上帝之力”。</p>
<p>话说回来，为什么我会从vim党摇身一变成为emacs党呢？这就不得不提起Clojure这门lisp方言，出于对lisp和函数式编程的痴迷，我选择了基于JVM的Clojure作为自己的偏好语言，而emacs天生为lisp而生。有了这个充足的理由，我开始收集emacs的cheatsheet并打印出来，天天放在手边翻阅，甚至买了一本英文版的<em>Learning GNU Emacs</em>书籍，只要有机会就打开emacs开始刷4clojure上的编程题。由于emacs对lisp的亲和性，我几乎没花多少时间就掌握住了常用的操作技巧。不过，emacs最负盛名的学习曲线确实让学习者绕过圈子，只要一段时间不用，就会忘记很多基本操作。另外，为了更好地在emacs中编写Clojure，还需要cider-mode和clojure-mode的支持，这时候就不得不编辑<em>init.el</em>文件，本着<em>KISS (keep it simple, stupid)</em>原则，我照着各种插件的说明文档中，把配置项复制粘贴到<em>init.el</em>文件当中，运行起来没有问题就好。随着自定义的内容变多，<em>init.el</em>文件也急剧膨胀起来。膨胀本来算不上问题，但我是个比较有操守的程序员，臃肿的代码是我极力避免的坏味道（bad smell）。所以胸臆之中涌动一股浩然之气，决心学起<em>emacs lisp</em>，把emacs的配置从头来过。</p>
<h2 id="从『头』开始"><a href="#从『头』开始" class="headerlink" title="从『头』开始"></a>从『头』开始</h2><p><em>init.el</em>文件位于<em>~/.emacs.d</em>目录之下，如果没有，自行创建一份即可。<br>首先，我们需要用到emacs的包管理工具<em>package.el</em>，因为emacs 24及其以上的版本都已经内置，所以无需下载到本地，直接通过<code>require</code>加载到emacs的运行时。</p>
<figure class="highlight lisp"><table><tr><td class="gutter"><pre><div class="line">1</div><div class="line">2</div><div class="line">3</div><div class="line">4</div><div class="line">5</div><div class="line">6</div><div class="line">7</div></pre></td><td class="code"><pre><div class="line">(<span class="name">require</span> 'package)</div><div class="line">(<span class="name">setq</span> package-archives '((<span class="string">"melpa"</span> . <span class="string">"http://melpa.org/packages/"</span>)</div><div class="line">						 (<span class="string">"melpa-stable"</span> . <span class="string">"https://stable.melpa.org/packages/"</span>)</div><div class="line">						 (<span class="string">"marmalade"</span> . <span class="string">"http://marmalade-repo.org/packages/"</span>)</div><div class="line">						 (<span class="string">"elpy"</span> . <span class="string">"http://jorgenschaefer.github.io/packages/"</span>)</div><div class="line">						 (<span class="string">"gnu"</span> . <span class="string">"http://elpa.gnu.org/packages/"</span>))</div><div class="line">	  package-enable-at-startup <span class="literal">nil</span>)</div></pre></td></tr></table></figure>
<p>上面的代码涉及到<code>setq</code>（变量赋值）的操作，<code>package-archives</code>，顾名思义，多个包的下载源，我给<code>package-archives</code>设置了5个包源，它们之间服从顺序的优先级，即先从第一个源中下载包，如果没有，到第二个原种寻找，以此类推。此外，这里<code>(&quot;melpa&quot; . &quot;http://melpa.org/packages/&quot;)</code>中的点号（dot）表示法也比较奇怪，其实这是lisp中的<em>Dotted pair</em>表示法，用法和普通的列表类似，但因为是<em>pair</em>的缘故，你可以使用<code>(car )</code>获取<code>&quot;melpa&quot;</code>，<code>(cdr )</code>获取到的却不再是一个列表，而是<code>&quot;http://melpa.org/packages/&quot;</code>这个值本身。</p>
<p>对<em>emacs lisp</em>不熟悉不要紧，先找个教程练习一下它的用法，比如<a href="https://learnxinyminutes.com/docs/elisp/" target="_blank" rel="external">learnxinyminutes</a>就非常不错。完成这个教程，大体不会对<em>elisp</em>犯怵了。接下来，只需要使用<code>c-h v</code>和<code>c-h f</code>查看<em>elisp</em>中定义的变量函数就能很快上手自行配置。<br>来个实际的例子，在大牛的配置文件中，经常能看到如下成对的配置：<br><figure class="highlight lisp"><table><tr><td class="gutter"><pre><div class="line">1</div><div class="line">2</div></pre></td><td class="code"><pre><div class="line">(<span class="name">setq</span> package-enable-at-startup <span class="literal">nil</span>)</div><div class="line">(<span class="name">package-initialize</span>)</div></pre></td></tr></table></figure></p>
<p>开始我觉得这是一对矛盾的配置，<code>package-enable-at-startup</code>设置为nil，暗示emacs启动时不会启用<code>package</code>，而<code>package-initialize</code>明显表明在做<code>package</code>的初始化工作。这种时候，我心中就蹦跶出一句话“世界上本没有矛盾，如果出现了，检查你都有哪些前提条件，就会发现其中一个是错的”。这种非异常的知识点很难通过搜索引擎找到满意的答案，而阅读文档恰恰是最合适的解决方式。emacs对<code>elisp</code>文档的支持非常全面，只需将鼠标移到<code>package-enable-at-startup</code>变量上，按下<code>c-h v</code> (control + h, v) 组合键，就能在其它窗口（window) 看到文档描述：</p>
<blockquote>
<p>Whether to activate installed packages when Emacs starts.<br>If non-nil, packages are activated after reading the init file<br>and before <code>after-init-hook&#39;.  Activation is not done if</code>user-init-file’ is nil (e.g. Emacs was started with “-q”).</p>
</blockquote>
<p>意思是在读入<em>init.el</em>之后，这个变量才会生效。换句话说，在读取<em>init.el</em>的过程中，该变量不论是nil或是non-nil都不会影响<code>package</code>的加载和初始化。所以，这两者之间并没有矛盾。当然，此时你可能会想把<code>package-enable-at-startup</code>设置为nil意欲何为？<a href="https://www.gnu.org/software/emacs/manual/html_node/emacs/Package-Installation.html" target="_blank" rel="external">官方文档</a>中有如下的解释：</p>
<blockquote>
<p>This will automatically set package-enable-at-startup to nil, to avoid loading the packages again after processing the init file.</p>
</blockquote>
<p>简单点说，就是防止在<code>package-initialize</code>之后重复加载包，因为可能会影响性能。</p>
<h2 id="模块化"><a href="#模块化" class="headerlink" title="模块化"></a>模块化</h2><p>如果把什么东西都揉到<em>init.el</em>文件中，这个文件一定会很快变得臃肿不堪。为了解决这个问题，需要引入模块化的思想——把特定功能的配置放到独立的文件中，然后<code>require</code>进来。按照惯例，我在<em>~/.emacs.d</em>目录下建立一个<em>lisp</em>目录用于存放所有自定义的模块文件，随后在<em>init.el</em>中加入下面这句代码，意在把<em>lisp</em>目录加到emacs的加载路径列表里。<br><figure class="highlight lisp"><table><tr><td class="gutter"><pre><div class="line">1</div></pre></td><td class="code"><pre><div class="line">(<span class="name">add-to-list</span> 'load-path (<span class="name">expand-file-name</span> <span class="string">"lisp"</span> user-emacs-directory))</div></pre></td></tr></table></figure></p>
<p>看似，接下来就可以在每个独立的模块文件中编写各种功能的配置。但是由于<code>package.el</code>功能的局限，我们很快就会遇到包重复安装和配置漂移（configuration drift）的麻烦。<code>package.el</code>提供了<code>package-install-p</code>（p是predicate的意思）和<code>package-install</code>两个配套使用的函数，也就是说一般得先判断包在不在，才决定安不安装。幸运的是，有人已经很好地解决了这部分问题，<a href="https://github.com/jwiegley/use-package" target="_blank" rel="external">use-package</a>就是非常好用的包，它将包的配置和包的定义聚合到了一块，并且保证包一定会安装在你的系统当中。<br>在使用<code>use-package</code>之前，我们需要先安装它，如下：<br><figure class="highlight lisp"><table><tr><td class="gutter"><pre><div class="line">1</div><div class="line">2</div><div class="line">3</div><div class="line">4</div><div class="line">5</div><div class="line">6</div></pre></td><td class="code"><pre><div class="line">(<span class="name">unless</span> (<span class="name">package-installed-p</span> 'use-package)</div><div class="line">  (<span class="name">package-refresh-contents</span>)</div><div class="line">  (<span class="name">package-install</span> 'use-package))</div><div class="line"></div><div class="line">(<span class="name">eval-when-compile</span></div><div class="line">  (<span class="name">require</span> 'use-package))</div></pre></td></tr></table></figure></p>
<p>由于<code>use-package</code>本身就是一个包，所以可以使用<code>package-install</code>安装到本地，然后<code>require</code>到emacs的运行时，值得一提的是这个<code>eval-when-compile</code>函数，使用<code>c-h f</code>查看它的定义:</p>
<blockquote>
<p>Like ‘progn’, but evaluates the body at compile time if you’re compiling.<br>Thus, the result of the body appears to the compiler as a quoted constant.<br>In interpreted code, this is entirely equivalent to `progn’.</p>
</blockquote>
<p>初次看到<strong>compile time</strong>，心中难免会有疑问：lisp不是动态语言吗，怎么还需要编译？这种时候，我们就要求助于<em>elisp</em>的文档了。在emacs中按下<code>c-h i</code>获取主话题（topic）的菜单，然后点击<em>Elisp</em>进入它的操作指南。重点查看<em>Evaluation</em>和<em>Byte Compilation</em>两个章节。不难发现lisp的解析器可以读取解析两种类型的lisp代码，一种是适合人类阅读的代码，以<em>el</em>作为后缀；另一种是编译字节码，以<em>elc</em>作为后缀。编译字节码运行速度优于前一种代码，我们可以通过<code>byte-compile-file</code>把前一种代码的文件编译成字节码文件。有趣的是，如果我们使用<em>package</em>来安装包，对应包的目录下都存在配套的<em>el</em>和<em>elc</em>两类文件。<br>在<em>Byte Compilation</em>条目下，有<code>eval-when-compile</code>的完整描述：</p>
<blockquote>
<p>If you’re using another package, but only need macros from it (the byte compiler will expand those), then ‘eval-when-compile’ can be used to load it for compiling, but not executing. For example,<br><figure class="highlight lisp"><table><tr><td class="gutter"><pre><div class="line">1</div><div class="line">2</div></pre></td><td class="code"><pre><div class="line">(<span class="name">eval-when-compile</span></div><div class="line">   (<span class="name">require</span> 'my-macro-package))</div></pre></td></tr></table></figure></p>
</blockquote>
<p>这里头有三个关键字<strong>load</strong>、<strong>compiling</strong>和<strong>executing</strong>值得留意一下。为了弄懂它们的含义，我们需要了解lisp解析器基本的工作原理：<code>code text -[characters]-&gt; load -[lisp object]-&gt; evaluation/compiling -[bytecode]-&gt; lisp interpretor</code>。换句话说，除非你想编译包含上述代码的文件，否则它的作用和<code>progn</code>一模一样，顺序地求值包含其中的表达式。当你正在编译文件的时候，包中宏就会原地展开，然后被<code>eval-when-compile</code>宏加载进内存并被编译成字节码，供后续解析器执行。</p>
<h2 id="Clojure相关"><a href="#Clojure相关" class="headerlink" title="Clojure相关"></a>Clojure相关</h2><p>载入<em>use-package</em>之后，我需要开始配置自己强大的Clojure开发环境了。首先，引入几个包：<br><figure class="highlight lisp"><table><tr><td class="gutter"><pre><div class="line">1</div><div class="line">2</div><div class="line">3</div><div class="line">4</div><div class="line">5</div><div class="line">6</div><div class="line">7</div><div class="line">8</div></pre></td><td class="code"><pre><div class="line">(<span class="name">use-package</span> rainbow-delimiters</div><div class="line">  <span class="symbol">:ensure</span> <span class="literal">t</span>)</div><div class="line">(<span class="name">use-package</span> clj-refactor</div><div class="line">  <span class="symbol">:ensure</span> <span class="literal">t</span>)</div><div class="line">(<span class="name">use-package</span> company</div><div class="line">  <span class="symbol">:ensure</span> <span class="literal">t</span></div><div class="line">  <span class="symbol">:defer</span> <span class="literal">t</span></div><div class="line">  <span class="symbol">:config</span> (<span class="name">global-company-mode</span>))</div></pre></td></tr></table></figure></p>
<p><em>rainbow-delimiters</em>能够让括号变得如同彩虹一样绚丽（主要是易于区分forms），<em>clj-refactor</em>是重构Clojure程序的神器，<em>company</em>提供了强大的命令补全提示功能。</p>
<h3 id="clojure-mode"><a href="#clojure-mode" class="headerlink" title="clojure mode"></a>clojure mode</h3><p>接下来，我们在<em>~/.emacs.d/lisp</em>目录下新建一个<em>init-clojure.el</em>文件，内容如下：<br><figure class="highlight lisp"><table><tr><td class="gutter"><pre><div class="line">1</div><div class="line">2</div><div class="line">3</div><div class="line">4</div><div class="line">5</div><div class="line">6</div><div class="line">7</div><div class="line">8</div><div class="line">9</div><div class="line">10</div><div class="line">11</div><div class="line">12</div><div class="line">13</div><div class="line">14</div><div class="line">15</div><div class="line">16</div><div class="line">17</div><div class="line">18</div><div class="line">19</div><div class="line">20</div><div class="line">21</div></pre></td><td class="code"><pre><div class="line">(<span class="name">require</span> 'clj-refactor)</div><div class="line">(<span class="name">require</span> 'rainbow-delimiters)</div><div class="line"></div><div class="line">(<span class="name">use-package</span> midje-mode</div><div class="line">  <span class="symbol">:ensure</span> <span class="literal">t</span>)</div><div class="line"></div><div class="line">(<span class="name">defun</span> my-clj-refactor-mode-hook ()</div><div class="line">	(<span class="name">clj-refactor-mode</span> <span class="number">1</span>)</div><div class="line">	(<span class="name">yas-minor-mode</span> <span class="number">1</span>) <span class="comment">; for adding require/use/import</span></div><div class="line">	(<span class="name">cljr-add-keybindings-with-prefix</span> <span class="string">"C-c C-m"</span>))</div><div class="line"></div><div class="line">(<span class="name">use-package</span> clojure-mode</div><div class="line">			 <span class="symbol">:ensure</span> <span class="literal">t</span></div><div class="line">			 <span class="symbol">:config</span></div><div class="line">			 (<span class="name">add-hook</span> 'clojure-mode-hook #'rainbow-delimiters-mode)</div><div class="line">			 (<span class="name">add-hook</span> 'clojure-mode-hook #'subword-mode)</div><div class="line">			 (<span class="name">add-hook</span> 'clojure-mode-hook #'midje-mode)</div><div class="line">			 (<span class="name">add-hook</span> 'clojure-mode-hook #'my-clj-refactor-mode-hook)</div><div class="line">			 (<span class="name">add-hook</span> 'clojure-mode-hook #'enable-paredit-mode))</div><div class="line"></div><div class="line">(<span class="name">provide</span> 'init-clojure)</div></pre></td></tr></table></figure></p>
<p>这里就能看出<em>use-package</em>的好处来了，针对<em>clojure-mode</em>的配置项都统一放到<code>:config</code>中管理起来。配置完毕后，使用<code>(provide &#39;init-clojure)</code>将模块以这样的名字暴露给其它客户端调用。</p>
<h3 id="CIDER-mode"><a href="#CIDER-mode" class="headerlink" title="CIDER mode"></a>CIDER mode</h3><p>有了<em>clojure-mode</em>之后，我们还需要一个Clojure可交互式的开发工具，CIDER便是这么一款工具。同样地，我们在<em>lisp</em>目录下新建一个名为<em>init-clojure-cider.el</em>，内容如下：<br><figure class="highlight lisp"><table><tr><td class="gutter"><pre><div class="line">1</div><div class="line">2</div><div class="line">3</div><div class="line">4</div><div class="line">5</div><div class="line">6</div><div class="line">7</div><div class="line">8</div><div class="line">9</div><div class="line">10</div><div class="line">11</div><div class="line">12</div><div class="line">13</div><div class="line">14</div><div class="line">15</div><div class="line">16</div><div class="line">17</div><div class="line">18</div></pre></td><td class="code"><pre><div class="line">(<span class="name">require</span> 'init-clojure)</div><div class="line">(<span class="name">require</span> 'company)</div><div class="line"></div><div class="line">(<span class="name">use-package</span> cider</div><div class="line">			 <span class="symbol">:ensure</span> <span class="literal">t</span></div><div class="line">			 <span class="symbol">:config</span></div><div class="line">			 (<span class="name">setq</span> nrepl-popup-stacktraces <span class="literal">nil</span>)</div><div class="line">			 (<span class="name">add-hook</span> 'cider-mode-hook 'eldoc-mode)</div><div class="line">			 (<span class="name">add-hook</span> 'cider-mode-hook #'rainbow-delimiters-mode)</div><div class="line">			 <span class="comment">;; Replace return key with newline-and-indent when in cider mode.</span></div><div class="line">			 (<span class="name">add-hook</span> 'cider-mode-hook '(lambda () (local-set-key (kbd <span class="string">"RET"</span>) 'newline-and-indent)))</div><div class="line">			 (<span class="name">add-hook</span> 'cider-mode-hook #'company-mode)</div><div class="line">			 (<span class="name">add-hook</span> 'cider-repl-mode-hook 'subword-mode)</div><div class="line">			 (<span class="name">add-hook</span> 'cider-repl-mode-hook 'paredit-mode)</div><div class="line">			 (<span class="name">add-hook</span> 'cider-repl-mode-hook #'company-mode)</div><div class="line">			 (<span class="name">add-hook</span> 'cider-repl-mode-hook #'rainbow-delimiters-mode))</div><div class="line"></div><div class="line">(<span class="name">provide</span> 'init-clojure-cider)</div></pre></td></tr></table></figure></p>
<p>配置的首部，我使用<code>(require &#39;init-clojure)</code>先加载<em>init-clojure</em>，然后对CIDER本身进行一系列的配置。配置的详细信息可以通过CIDER github主页获取到，这里我就不再赘述。</p>
<p>最后，需要在<em>init.el</em>文件中添加入这么一句<code>(require &#39;init-clojure-cider)</code>，重新启动emacs，找到一个Clojure项目，按下<code>C-c M-j</code> (hack-jack-in)，就能获得一个Clojure的交互式开发环境。</p>
<h2 id="小结"><a href="#小结" class="headerlink" title="小结"></a>小结</h2><p>当然，我的emacs配置绝对不止这些，但是其余的过程大体类似。由于emacs速来有伪装成编辑器的操作系统的称号，所以我的探索是无止境的。如果大家对我的配置感兴趣，可以直接去我github上<a href="https://github.com/qianyan/dotfiles" target="_blank" rel="external">dotfiles</a>上查看。</p>
<h2 id="—"><a href="#—" class="headerlink" title="—"></a>—</h2><p>参考链接<br>[1] <a href="[https://sriramkswamy.github.io/dotemacs/">sriramkswamy dotemacs</a><br>[2] <a href="https://github.com/purcell/emacs.d/" target="_blank" rel="external">purcell emacs.d</a></p>
]]></content>
    
    <summary type="html">
    
      &lt;p&gt;俗话说，工欲善其事必先利其器，完善开发工具与我而言是一件快乐的事情，分享也是一件令人愉悦的事情，所以我想把学习过程中的点滴记录下，留作备忘。本文不会介绍太多花式或有深度的emacs配置，更多是摸索学习的过程，其中充满了乐趣。&lt;/p&gt;
&lt;h2 id=&quot;原因&quot;&gt;&lt;a href=
    
    </summary>
    
      <category term="Clojure" scheme="https://lameta.com/categories/Clojure/"/>
    
    
      <category term="emacs" scheme="https://lameta.com/tags/emacs/"/>
    
  </entry>
  
  <entry>
    <title>Generative Testing</title>
    <link href="https://lameta.com/2016/11/04/Generative-Testing/"/>
    <id>https://lameta.com/2016/11/04/Generative-Testing/</id>
    <published>2016-11-03T23:21:34.000Z</published>
    <updated>2016-11-04T01:13:01.000Z</updated>
    
    <content type="html"><![CDATA[<p>我们为什么要写单元测试？</p>
<p>满足需求是所有软件存在的必要条件，单元测试一定是为它服务的。从这一点出发，我们可以总结出写单元测试的两个动机：驱动（如：TDD）和验证功能实现。另外，软件需求易变的特征决定修改代码成为必然，在这种情况下，单元测试能保护已有的功能不被破坏。</p>
<p>基于以上两点共识，我们看看传统的单元测试有什么特征？</p>
<h2 id="基于用例的测试（By-Example）"><a href="#基于用例的测试（By-Example）" class="headerlink" title="基于用例的测试（By Example）"></a>基于用例的测试（By Example）</h2><p>单元测试最常见的套路就是Given、When、Then三部曲。</p>
<ul>
<li>Given：初始状态或前置条件 </li>
<li>When：行为发生</li>
<li>Then：断言结果</li>
</ul>
<p>编写时，我们会精心准备（Given）一组输入数据，然后在调用行为后，断言返回的结果与预期相符。这种基于用例的测试方式在开发（包括TDD）过程中十分好用。因为它清晰地定义了输入输出，而且大部分情况下体量都很小、容易理解。</p>
<p>但这样的测试方式也有坏处。</p>
<p>第一点在于测试的意图。用例太过具体，我们就很容易忽略自己的测试意图。比如我曾经看过有人在写计算器kata程序的时候，将其中的一个测试命名为<em>return 3 when add 1 and 2</em>，这样的命名其实掩盖了测试用例背后的真实意图——传入两个整型参数，调用add方法之后得到的结果应该是参数之和。我们常说测试即文档，既然是文档就应该明确描述待测方法的行为，而不是陈述一个例子。</p>
<p>第二点在于测试完备性。因为省事省心并且回报率高，我们更乐于写<em>happy path</em>的代码。尽管出于职业道德，我们也会找一个明显的异常路径进行测试，不过这还远远不够。</p>
<p>为了辅助单元测试改善这两点。我这里介绍另一种测试方式——生成式测试（Generative Testing，也称<a href="https://github.com/fpinscala/fpinscala/wiki/Chapter-8:-Property-based-testing" target="_blank" rel="external">Property-Based Testing</a>）。这种测试方式会基于输入假设输出，并且生成许多可能的数据验证假设的正确性。</p>
<h2 id="生成式测试"><a href="#生成式测试" class="headerlink" title="生成式测试"></a>生成式测试</h2><p>对于第一个问题，我们换种思路思考一下。假设我们不需要写具体的测试用例，那么掩盖意图的可能性也就没有了。想法很美好，但如何实践Given、When、Then呢？答案是让程序生成并自动验证它们。这也就引出生成式测试的概念——我们先声明传入数据可能的情况，然后使用生成器生成符合入参情况的数据，调用待测方法，最后才进行验证。</p>
<h3 id="Given阶段"><a href="#Given阶段" class="headerlink" title="Given阶段"></a>Given阶段</h3><p>Clojure 1.9（Alpha）新内置的clojure.spec可以很轻松地做到这点：<br><figure class="highlight lisp"><table><tr><td class="gutter"><pre><div class="line">1</div><div class="line">2</div><div class="line">3</div><div class="line">4</div><div class="line">5</div><div class="line">6</div><div class="line">7</div></pre></td><td class="code"><pre><div class="line"><span class="comment">;; 定义输入参数的可能情况：两个整型参数</span></div><div class="line">(<span class="name">s/def</span> :<span class="symbol">:add-operators</span> (<span class="name">s/cat</span> <span class="symbol">:a</span> int? <span class="symbol">:b</span> int?))</div><div class="line"><span class="comment">;; 尝试生成数据</span></div><div class="line">(<span class="name">gen/generate</span> (<span class="name">s/gen</span> :<span class="symbol">:add-operators</span>))</div><div class="line"></div><div class="line"><span class="comment">;; 生成的数据</span></div><div class="line">-&gt; (<span class="number">1</span> <span class="number">-122</span>)</div></pre></td></tr></table></figure></p>
<p>首先，我们尝试声明两个参数可能出现的情况或者称为规格（specification），即参数a和b都是整数。然后调用生成器产生一对整数。整个分析和构造的过程中，都没有涉及具体的数据，这样会强制我们揣摩输入数据可能的模样，而且也能避免测试意图被掩盖掉——正如前面所说，<em>return 3 when add 1 and 2</em>并不代表什么，<em>return the sum of two integers</em>才具有普遍意义。</p>
<h3 id="Then阶段"><a href="#Then阶段" class="headerlink" title="Then阶段"></a>Then阶段</h3><p>数据是生成了，待测方法也可以调用，但是Then这个断言阶段又让人头疼了，因为我们根本没法预知生成的数据，也就无法知道正确的结果，怎么断言？</p>
<p>拿我们定义的加法运算为例：<br><figure class="highlight clojure"><table><tr><td class="gutter"><pre><div class="line">1</div><div class="line">2</div></pre></td><td class="code"><pre><div class="line">(<span class="name"><span class="builtin-name">defn</span></span> add [a b]</div><div class="line">  (<span class="name"><span class="builtin-name">+</span></span> a b))</div></pre></td></tr></table></figure></p>
<p>我们尝试把断言改成一个全称命题的格式：</p>
<blockquote>
<p>任取两个整数a, b，a和b加起来的结果总是a, b之和。</p>
</blockquote>
<p>借助<a href="https://github.com/clojure/test.check" target="_blank" rel="external">test.check</a>，我们在<em>Clojure</em>可以这样表达<br><figure class="highlight clojure"><table><tr><td class="gutter"><pre><div class="line">1</div><div class="line">2</div><div class="line">3</div><div class="line">4</div></pre></td><td class="code"><pre><div class="line">(<span class="name"><span class="builtin-name">def</span></span> test-add</div><div class="line">  (<span class="name">prop/for-all</span> [a (<span class="name">gen/int</span>)</div><div class="line">                 b (<span class="name">gen/int</span>)]</div><div class="line">                (<span class="name"><span class="builtin-name">=</span></span> (<span class="name">add</span> a b) (<span class="name"><span class="builtin-name">+</span></span> a b))))</div></pre></td></tr></table></figure></p>
<p>等等，我们把add方法的实现<code>(+ a b)</code>写到了断言里，这几乎丧失了单元测试的基本意义。换一种断言方式，我们使用加法的逆运算进行描述：</p>
<blockquote>
<p>任取两个整数，把a和b加起来的结果减去a总会得到b。</p>
</blockquote>
<figure class="highlight scheme"><table><tr><td class="gutter"><pre><div class="line">1</div><div class="line">2</div><div class="line">3</div><div class="line">4</div></pre></td><td class="code"><pre><div class="line">(<span class="name">def</span> test-add</div><div class="line">  (<span class="name">prop/for-all</span> [<span class="name">a</span> (<span class="name">gen/int</span>)</div><div class="line">                 b (<span class="name">gen/int</span>)]</div><div class="line">                (<span class="name"><span class="builtin-name">=</span></span> (<span class="name"><span class="builtin-name">-</span></span> (<span class="name">add</span> a b) a) b))))</div></pre></td></tr></table></figure>
<p>我们通过程序陈述了一个已知的真命题。变换以后，就可以使用<code>quick-check</code>对多组生成的整数进行测试。<br><figure class="highlight elixir"><table><tr><td class="gutter"><pre><div class="line">1</div><div class="line">2</div><div class="line">3</div><div class="line">4</div><div class="line">5</div></pre></td><td class="code"><pre><div class="line">;; 随机生成<span class="number">100</span>组数据测试add方法</div><div class="line">(tc/quick-check <span class="number">100</span> test-add)</div><div class="line"></div><div class="line">;; 测试结果</div><div class="line">-&gt; &#123;<span class="symbol">:result</span> <span class="keyword">true</span>, <span class="symbol">:num-tests</span> <span class="number">100</span>, <span class="symbol">:seed</span> <span class="number">1477285296502</span>&#125;</div></pre></td></tr></table></figure></p>
<p>测试结果表明，刚才运行了100组测试，并且都通过了。理论上，程序可以生成无数的测试数据来验证add方法的正确性。即便不能穷尽，我们也获得一组统计上的数字，而不仅仅是几个纯手工挑选的用例。</p>
<p>至于第二个问题，首先得明确测试是无法做到完备的。很多指导方法保证使用较少的用例做到有效覆盖，比如：等价类、边界值、判定表、因果图、pairwise等等。但是在实际使用过程当中，依然存在问题。举个等价类的例子，假如我们有一个接受自然数并直接返回入参的方法<em>identity-nat</em>，那么对于输入参数而言，全体自然数都互为等价类，其中的一个有效等价类可以是自然数1。如果入参被假定在整数的范围，我们很容易找到一个无效等价类，比如-1。</p>
<p>用Clojure测试代码表现出来：<br><figure class="highlight clojure"><table><tr><td class="gutter"><pre><div class="line">1</div><div class="line">2</div><div class="line">3</div><div class="line">4</div><div class="line">5</div></pre></td><td class="code"><pre><div class="line">(<span class="name">deftest</span> test-with-identity-nat</div><div class="line">  (<span class="name">testing</span> <span class="string">"identity of natural integers"</span></div><div class="line">    (<span class="name">is</span> (<span class="name"><span class="builtin-name">=</span></span> <span class="number">1</span> (<span class="name">identity-nat</span> <span class="number">1</span>))))</div><div class="line">  (<span class="name">testing</span> <span class="string">"throw exception for non-natural integers"</span></div><div class="line">    (<span class="name">is</span> (<span class="name">thrown?</span> RuntimeException (<span class="name">identity-nat</span> <span class="number">-1</span>)))))</div></pre></td></tr></table></figure></p>
<p>不过如果有人修改了方法<em>identity-nat</em>的实现，单独处理入参为0的情况，这个测试还是能够照常通过。也就是说，实现发生改变，基于等价类的测试有可能起不到防护作用的。当然你完全可以反驳：规则改变，等价类也得重新定义。道理确实如此，但是反过来想想，我们写测试的目的不正是构建一张安全网吗？我们信任测试能在代码变动时给予警告，但此处它失信了，这就尴尬了。</p>
<p>如果使用生成式测试，我们规定：</p>
<blockquote>
<p>任取一个自然数a，在其上调用f的结果总是a。</p>
</blockquote>
<figure class="highlight elixir"><table><tr><td class="gutter"><pre><div class="line">1</div><div class="line">2</div><div class="line">3</div><div class="line">4</div><div class="line">5</div><div class="line">6</div><div class="line">7</div><div class="line">8</div><div class="line">9</div><div class="line">10</div><div class="line">11</div></pre></td><td class="code"><pre><div class="line">(<span class="function"><span class="keyword">def</span> <span class="title">test</span></span>-identity-nat</div><div class="line"> (prop/<span class="keyword">for</span>-all [a (s/gen nat-int?)]</div><div class="line">               (= a (identity-nat a))))</div><div class="line"></div><div class="line"></div><div class="line">(tc/quick-check <span class="number">100</span> test-identity-nat)</div><div class="line"></div><div class="line">-&gt; &#123;<span class="symbol">:result</span> <span class="keyword">false</span>, <span class="symbol">:seed</span> <span class="number">1477362396044</span>, <span class="symbol">:failing-size</span> <span class="number">0</span>, <span class="symbol">:num-tests</span> <span class="number">1</span>, <span class="symbol">:fail</span> [<span class="number">0</span>], <span class="symbol">:shrunk</span> &#123;<span class="symbol">:total-nodes-visited</span> <span class="number">0</span>, </div><div class="line">         <span class="symbol">:depth</span> <span class="number">0</span>, </div><div class="line">         <span class="symbol">:result</span> <span class="keyword">false</span>, </div><div class="line">         <span class="symbol">:smallest</span> [<span class="number">0</span>]&#125;&#125;</div></pre></td></tr></table></figure>
<p>这个测试尝试对100组生成的自然数（nat-int?）进行测试，但首次运行就发现代码发生过变动。失败的数据是0，而且还给出了最小失败集[0]。拿着这个最小失败集，我们就可以快速地重现失败用例，从而修正。</p>
<p>当然也有可能在一次运行中，我们的测试无法发现失败的用例。但是，如果100个测试用例都通过了，至少表明我们程序对于100个随机的自然数都是正确的。和基于用例的测试相比，这就如同编织出一道更加紧密的安全网。网孔越小，漏掉的情况也越少。</p>
<p>Clojure语言之父Rich Hickey推崇<em><a href="https://www.infoq.com/presentations/Simple-Made-Easy" target="_blank" rel="external">Simple Made Easy</a></em>哲学，所以生成式测试在Clojure.spec中有更为简约的表达。以上述为例：<br><figure class="highlight clojure"><table><tr><td class="gutter"><pre><div class="line">1</div><div class="line">2</div><div class="line">3</div><div class="line">4</div><div class="line">5</div><div class="line">6</div></pre></td><td class="code"><pre><div class="line">(<span class="name">s/fdef</span> identity-nat</div><div class="line">        <span class="symbol">:args</span> (<span class="name">s/cat</span> <span class="symbol">:a</span> nat-int?) <span class="comment">;输入参数的规格</span></div><div class="line">        <span class="symbol">:ret</span> nat-int?             <span class="comment">;返回结果的规格</span></div><div class="line">        <span class="symbol">:fn</span> #(<span class="name"><span class="builtin-name">=</span></span> (<span class="symbol">:ret</span> %) (<span class="name"><span class="builtin-name">-&gt;</span></span> % <span class="symbol">:args</span> <span class="symbol">:a</span>))) <span class="comment">;入参和出参之间的约束</span></div><div class="line"></div><div class="line">(<span class="name">stest/check</span> `identity-nat)</div></pre></td></tr></table></figure></p>
<p><em>fdef</em>宏定义了方法<em>identity-nat</em>的规格，默认情况下会基于参数的规格生成1000组数据进行生成式测试。除了这一好处，它还提供部分类型检查的功能。</p>
<h2 id="再谈TDD"><a href="#再谈TDD" class="headerlink" title="再谈TDD"></a>再谈TDD</h2><p>TDD（测试驱动开发）是一种驱动代码实现和设计的过程。我们说要先有测试，再去实现；保证实现功能的前提下，重构代码以达到较好的设计。整个过程就好比演绎推理，测试就是其中的证明步骤，而最终实现的功能则是证明的结果。</p>
<p>对于开发人员而言，基于用例的测试方式是友好的，因为它能简单直接地表达实现的功能并保证其正确性。一旦进入红、绿、重构的节（guai）奏（quan），开发人员根本停不下来，进入一种心流状态。只不过问题是，基于用例驱动出来的实现可能并不是<strong>恰好</strong>通过的。我们常常会发现，在写完上组测试用例的实现之后，无需任何改动，下组测试照常能运行通过。换句话说，实现代码可能做了多余的事情而我们却浑然不知。在这种情况下，我们可以利用生成式测试准备大量符合规格的数据探测程序，以此检查程序的健壮性，让缺陷无处遁形。</p>
<p>凡是想到的情况都能测试，但是想不到情况也需要测试。这才是生成式测试的价值所在。有人把TDD概念化为“展示你的功能”（Show your work），而把生成式测试概念化为“检查你的功能“（Check your work），我深以为然。</p>
<h2 id="小结"><a href="#小结" class="headerlink" title="小结"></a>小结</h2><p>回到我们写单元测试的动机上：</p>
<ol>
<li>保证或验证实现功能；</li>
<li>保护已经实现的功能不被破坏。<br>基于用例的单元测试和生成式测试在这两点上是相辅相成的。我们可以借助它们尽可能早地找出缺陷，避免缺陷逃逸到生产环境。</li>
</ol>
<p>ThoughtWorks 2016年11月份的技术雷达把Clojure.spec移到了工具象限的评估环中，这表明这个工具值得作一番探究。当然，除了Clojure，<a href="http://hypothesis.works/articles/quickcheck-in-every-language/" target="_blank" rel="external">其它语言</a>都有相应的生成式测试的框架，你不妨在自己的项目中试一试。</p>
]]></content>
    
    <summary type="html">
    
      &lt;p&gt;我们为什么要写单元测试？&lt;/p&gt;
&lt;p&gt;满足需求是所有软件存在的必要条件，单元测试一定是为它服务的。从这一点出发，我们可以总结出写单元测试的两个动机：驱动（如：TDD）和验证功能实现。另外，软件需求易变的特征决定修改代码成为必然，在这种情况下，单元测试能保护已有的功能不被破
    
    </summary>
    
      <category term="Clojure" scheme="https://lameta.com/categories/Clojure/"/>
    
    
      <category term="testing" scheme="https://lameta.com/tags/testing/"/>
    
  </entry>
  
  <entry>
    <title>Clojure集合管道函数练习</title>
    <link href="https://lameta.com/2016/09/12/Collection-Pipelines-By-TDD/"/>
    <id>https://lameta.com/2016/09/12/Collection-Pipelines-By-TDD/</id>
    <published>2016-09-12T01:26:24.000Z</published>
    <updated>2016-10-06T02:30:45.000Z</updated>
    
    <content type="html"><![CDATA[<h2 id="起源"><a href="#起源" class="headerlink" title="起源"></a>起源</h2><p>TDD讨论组里的<a href="http://www.jackyshen.com/" target="_blank" rel="external">申导</a>最近在B站直播了Martin Fowler的经典文章<em><a href="http://www.martinfowler.com/articles/refactoring-pipelines.html" target="_blank" rel="external">Refactoring with Loops and Collection Pipelines</a></em>中谈到的<strong>利用集合管道对循环进行函数式重构</strong>。视频地址在<a href="http://www.bilibili.com/mobile/video/av6146294.html" target="_blank" rel="external">这里</a>，申导的翻译在<a href="http://www.jackyshen.com/2016/07/04/Refactoring-with-Loops-and-Collection-Pipelines/" target="_blank" rel="external">这里</a>。组织者<a href="http://www.seabornlee.cn/" target="_blank" rel="external">小波（Seaborn Lee）</a>趁机出了一道关于集合管道函数<a href="https://codingstyle.cn/topics/205" target="_blank" rel="external">题目</a>。我就想啊，论函数式编程，舍<em>Clojure</em>其谁？而且我在<em>Clojure</em>很少能写出<code>loop... recur</code>这样偏底层的循环代码。话不多说，撸起袖子开工。</p>
<h2 id="题目"><a href="#题目" class="headerlink" title="题目"></a>题目</h2><p>一家澡堂有 m 个房间，每个房间有 n 个时段，现在要给用户推荐「最早的可以预约的时段」。</p>
<p>例子：<br><figure class="highlight less"><table><tr><td class="gutter"><pre><div class="line">1</div><div class="line">2</div><div class="line">3</div><div class="line">4</div><div class="line">5</div><div class="line">6</div><div class="line">7</div><div class="line">8</div><div class="line">9</div><div class="line">10</div><div class="line">11</div><div class="line">12</div><div class="line">13</div><div class="line">14</div><div class="line">15</div><div class="line">16</div><div class="line">17</div><div class="line">18</div><div class="line">19</div><div class="line">20</div><div class="line">21</div><div class="line">22</div><div class="line">23</div><div class="line">24</div><div class="line">25</div><div class="line">26</div><div class="line">27</div></pre></td><td class="code"><pre><div class="line"><span class="attribute">rooms</span>: [</div><div class="line">  &#123;</div><div class="line">     <span class="attribute">room_id</span>: <span class="number">1</span>,</div><div class="line">     <span class="attribute">periods</span>: [</div><div class="line">        &#123;</div><div class="line">           <span class="attribute">time</span>: <span class="string">'17:00-18:00'</span>,</div><div class="line">           <span class="attribute">status</span>: <span class="string">'available'</span></div><div class="line">        &#125;,</div><div class="line">        &#123;</div><div class="line">           <span class="attribute">time</span>: <span class="string">'18:00-19:00'</span>,</div><div class="line">           <span class="attribute">status</span>: <span class="string">'occupied'</span></div><div class="line">        &#125;</div><div class="line">     ]</div><div class="line">  &#125;, &#123;</div><div class="line">     <span class="attribute">room_id</span>: <span class="number">2</span>,</div><div class="line">     <span class="attribute">periods</span>: [</div><div class="line">        &#123;</div><div class="line">           <span class="attribute">time</span>: <span class="string">'17:00-18:00'</span>,</div><div class="line">           <span class="attribute">status</span>: <span class="string">'occupied'</span></div><div class="line">        &#125;,</div><div class="line">        &#123;</div><div class="line">           <span class="attribute">time</span>: <span class="string">'18:00-19:00'</span>,</div><div class="line">           <span class="attribute">status</span>: <span class="string">'available'</span></div><div class="line">        &#125;</div><div class="line">     ]</div><div class="line">  &#125;</div><div class="line">]</div></pre></td></tr></table></figure></p>
<p>期望返回：<br><figure class="highlight css"><table><tr><td class="gutter"><pre><div class="line">1</div><div class="line">2</div><div class="line">3</div><div class="line">4</div></pre></td><td class="code"><pre><div class="line">&#123;</div><div class="line">  <span class="attribute">room_id</span>: <span class="number">1</span>,</div><div class="line">  time: <span class="string">'17:00-18:00'</span></div><div class="line">&#125;</div></pre></td></tr></table></figure></p>
<h2 id="解析"><a href="#解析" class="headerlink" title="解析"></a>解析</h2><p>题目很简单，基本思路：首先过滤出每个房间<code>periods</code>中<code>status</code>为<code>available</code>的时间段，然后取第一个也就是最早的时间段（默认为递增排序的），接着将<code>room_id</code>和这个时间段以<strong>期望返回</strong>的形式合并。再然后对所有合并的结果依据时间段进行一次排序（<code>sort</code>），最后取第一个结果即可。</p>
<h2 id="1-Clojure-解法"><a href="#1-Clojure-解法" class="headerlink" title="1. Clojure 解法"></a>1. Clojure 解法</h2><h3 id="转换数据格式"><a href="#转换数据格式" class="headerlink" title="转换数据格式"></a>转换数据格式</h3><p>原题中给的是json的格式，不适合在<em>Clojure</em>中处理，所以我们手工转换成需要的形式，如下：<br><strong>清单1-1</strong> 数据定义<br><figure class="highlight clojure"><table><tr><td class="gutter"><pre><div class="line">1</div><div class="line">2</div><div class="line">3</div><div class="line">4</div><div class="line">5</div><div class="line">6</div><div class="line">7</div><div class="line">8</div><div class="line">9</div><div class="line">10</div><div class="line">11</div></pre></td><td class="code"><pre><div class="line">(<span class="name"><span class="builtin-name">def</span></span> rooms</div><div class="line">  [&#123;<span class="symbol">:room-id</span> <span class="number">1</span></div><div class="line">    <span class="symbol">:periods</span> [&#123;<span class="symbol">:time</span> <span class="string">"17:00-18:00"</span></div><div class="line">               <span class="symbol">:status</span> <span class="symbol">:available</span>&#125;</div><div class="line">              &#123;<span class="symbol">:time</span> <span class="string">"18:00-19:00"</span></div><div class="line">               <span class="symbol">:status</span> <span class="symbol">:occupied</span>&#125;]&#125;</div><div class="line">   &#123;<span class="symbol">:room-id</span> <span class="number">2</span></div><div class="line">    <span class="symbol">:periods</span> [&#123;<span class="symbol">:time</span> <span class="string">"17:00-18:00"</span></div><div class="line">               <span class="symbol">:status</span> <span class="symbol">:occupied</span>&#125;</div><div class="line">              &#123;<span class="symbol">:time</span> <span class="string">"18:00-19:00"</span></div><div class="line">               <span class="symbol">:status</span> <span class="symbol">:available</span>&#125;]&#125;])</div></pre></td></tr></table></figure></p>
<h3 id="代码"><a href="#代码" class="headerlink" title="代码"></a>代码</h3><p><strong>清单1-2</strong> 房间最早可预约时间段<br><figure class="highlight"><table><tr><td class="gutter"><pre><div class="line">1</div><div class="line">2</div><div class="line">3</div><div class="line">4</div><div class="line">5</div><div class="line">6</div><div class="line">7</div><div class="line">8</div><div class="line">9</div><div class="line">10</div><div class="line">11</div><div class="line">12</div><div class="line">13</div></pre></td><td class="code"><pre><div class="line">(defn the-earliest-available-room [rooms]</div><div class="line">  (-&gt;&gt; rooms</div><div class="line">       (map</div><div class="line">        (juxt first (fn [&#123;:keys [periods]&#125;]</div><div class="line">                      (-&gt;&gt; periods</div><div class="line">                           (filter #(= (:status %) :available))</div><div class="line">                           (ffirst)))))</div><div class="line">       (map #(into &#123;&#125; %))</div><div class="line">       (sort-by :time)</div><div class="line">       (first)))</div><div class="line">       </div><div class="line">(the-earliest-available-room rooms)</div><div class="line">-&gt; &#123;:room-id 1, :time "17:00-18:00"&#125;</div></pre></td></tr></table></figure></p>
<p>这段代码和上面的解析是一一对应的关系。为了让程序清晰，符合管道的用法，这里使用了thread last宏（<code>-&gt;&gt;</code>），它的作用是把前面一个<code>form</code>作为后一个<code>form</code>的最后一个参数。与之呼应的是thread first宏（<code>-&gt;</code>），它的作用类似，不过会传成第一个参数。</p>
<p>我们先看<code>(map (juxt ...) ...)</code>这一段代码。<code>juxt</code>是一个非常有意思的函数，而且超级实用。它的文档描述如下：</p>
<figure class="highlight"><table><tr><td class="gutter"><pre><div class="line">1</div><div class="line">2</div><div class="line">3</div><div class="line">4</div><div class="line">5</div><div class="line">6</div><div class="line">7</div><div class="line">8</div><div class="line">9</div><div class="line">10</div><div class="line">11</div><div class="line">12</div><div class="line">13</div></pre></td><td class="code"><pre><div class="line">(doc juxt)</div><div class="line">-&gt;</div><div class="line">clojure.core/juxt</div><div class="line"> [f]</div><div class="line"> [f g]</div><div class="line"> [f g h]</div><div class="line"> [f g h &amp; fs]</div><div class="line">Added in 1.1</div><div class="line">  Takes a set of functions and returns a fn that is the juxtaposition</div><div class="line">  of those fns.  The returned fn takes a variable number of args, and</div><div class="line">  returns a vector containing the result of applying each fn to the</div><div class="line">  args (left-to-right).</div><div class="line">  ((juxt a b c) x) =&gt; [(a x) (b x) (c x)]</div></pre></td></tr></table></figure>
<p>它的神奇之处在于可以对同一个参数应用不同的函数，而且还能将应用的结果全部收集起来。想想题目的解析中提及的以<strong>期望返回</strong>的形式合并，如果我们应用<code>juxt</code>函数，就能得到<code>[(:room-id 1) (:time &quot;17:00-18:00&quot;)]</code>这样的中间结果。</p>
<p><code>(juxt first (fn ...))</code>中<code>first</code>用于提取<code>:room-id</code>，而后面的<code>lambda</code>表达式则用于提取<code>:time</code>。解法很直观，筛选出<code>:status</code>是<code>:available</code>的时间段，然后使用<code>(ffirst)</code>取第一个map的首个entry。如：<code>{:time &quot;17:00-18:00&quot; :status :available}</code>，那么应用<code>(ffirst)</code>的结果就是<code>[:time &quot;17:00-18:00&quot;]</code>。</p>
<p>接下来，又进行了一次map操作，这次的目的是把元组的entries，转换为map。举个例子：<code>[[:room-id 1] [:time &quot;17:00-18:00&quot;]]</code> =&gt; <code>{:room-id 1 :time &quot;17:00-18:00&quot;}</code>。转换成map之后，方便以<code>:time</code>对结果进行排序<code>(sort-by :time)</code>，最后取出第一个元素<code>(first)</code>，即我们期望的返回。</p>
<p>写完之后，我很想再写个TDD版本的。话不多说，继续撸袖子。</p>
<h2 id="2-Clojure-TDD-解法"><a href="#2-Clojure-TDD-解法" class="headerlink" title="2. Clojure TDD 解法"></a>2. Clojure TDD 解法</h2><h3 id="环境准备"><a href="#环境准备" class="headerlink" title="环境准备"></a>环境准备</h3><ul>
<li>生成工程</li>
</ul>
<p>进入命令行，输入<code>lein new midje the-earliest-available-period-of-bathroom</code>，leiningen会生成基于<code>midje</code>这个测试框架的工程。</p>
<ul>
<li>Git<figure class="highlight dockerfile"><table><tr><td class="gutter"><pre><div class="line">1</div><div class="line">2</div><div class="line">3</div><div class="line">4</div><div class="line">5</div><div class="line">6</div><div class="line">7</div><div class="line">8</div></pre></td><td class="code"><pre><div class="line">git init</div><div class="line">&gt; .gitignore</div><div class="line">.lein*</div><div class="line">.nrep*</div><div class="line">target/</div><div class="line">这里ctrl-c退出</div><div class="line">git <span class="keyword">add</span><span class="bash"> .</span></div><div class="line">git commit --message <span class="string">"init commit"</span></div></pre></td></tr></table></figure>
</li>
</ul>
<p>我使用了<code>zsh</code>和<code>oh-my-zsh</code>，自带了很多git操作的alias，可以通过<code>alias |grep git</code>查看。后续的git操作都会使用alias。</p>
<ul>
<li>自动测试</li>
</ul>
<p>输入<code>lein repl</code>，然后<code>(use &#39;midje.repl)</code>，最后输入<code>(autotest)</code>。这样一旦文件修改保存，测试就会自动触发。</p>
<ul>
<li>Emacs</li>
</ul>
<p>用来写代码的。</p>
<h3 id="Tasking（任务拆分）"><a href="#Tasking（任务拆分）" class="headerlink" title="Tasking（任务拆分）"></a>Tasking（任务拆分）</h3><p>先不急着敲代码，我们先从测试的角度看看完成这个需求需要哪几步？</p>
<ul>
<li>[ ] 单间澡堂有一个可用时间段</li>
<li>[ ] 单间澡堂有多个可用时间段</li>
<li>[ ] 所有澡堂（包含输入为空）没有可用时间段</li>
<li>[ ] 多间澡堂都有可用时间段</li>
<li>[ ] 多间澡堂中有的有可用时间段，有的没有可用时间段</li>
</ul>
<h3 id="第1个任务"><a href="#第1个任务" class="headerlink" title="第1个任务"></a>第1个任务</h3><ul>
<li>[ ] 单间澡堂有一个可用时间段</li>
</ul>
<h4 id="1-写测试"><a href="#1-写测试" class="headerlink" title="1. 写测试"></a>1. 写测试</h4><figure class="highlight"><table><tr><td class="gutter"><pre><div class="line">1</div><div class="line">2</div><div class="line">3</div><div class="line">4</div><div class="line">5</div><div class="line">6</div><div class="line">7</div><div class="line">8</div><div class="line">9</div><div class="line">10</div><div class="line">11</div><div class="line">12</div><div class="line">13</div><div class="line">14</div><div class="line">15</div><div class="line">16</div><div class="line">17</div><div class="line">18</div></pre></td><td class="code"><pre><div class="line">(def room-1 &#123;:room-id 1</div><div class="line">    :periods [&#123;:time "17:00-18:00"</div><div class="line">               :status :available&#125;</div><div class="line">              &#123;:time "18:00-19:00"</div><div class="line">               :status :occupied&#125;]&#125;)</div><div class="line"></div><div class="line">(def room-2 &#123;:room-id 2</div><div class="line">             :periods [&#123;:time "17:00-18:00"</div><div class="line">                        :status :occupied&#125;</div><div class="line">                       &#123;:time "18:00-19:00"</div><div class="line">                        :status :available&#125;]&#125;)</div><div class="line">                        </div><div class="line">(facts "about `the-earliest-avaible-period-of-bathroom`"</div><div class="line">  (fact "should recommand if there is only one room with available period"</div><div class="line">    ;; 1号</div><div class="line">    (the-earliest-available-recommand [room-1]) =&gt; &#123;:room-id 1 :time "17:00-18:00"&#125;)</div><div class="line">    ;; 2号</div><div class="line">    (the-earliest-available-recommand [room-2]) =&gt; &#123;:room-id 2 :time "18:00-19:00"&#125;))</div></pre></td></tr></table></figure>
<h4 id="2-写实现"><a href="#2-写实现" class="headerlink" title="2. 写实现"></a>2. 写实现</h4><figure class="highlight clojure"><table><tr><td class="gutter"><pre><div class="line">1</div><div class="line">2</div></pre></td><td class="code"><pre><div class="line">(<span class="name"><span class="builtin-name">defn</span></span> the-earliest-available-recommand [rooms]</div><div class="line">  &#123;<span class="symbol">:room-id</span> <span class="number">1</span> <span class="symbol">:time</span> <span class="string">"17:00-18:00"</span>&#125;)</div></pre></td></tr></table></figure>
<p>针对1号测试，这个实现有点“荒诞”，术语<code>hard code</code>说的就是这个，但是眼下足够了。不过此时，应该再写一个类似的测试来去除<code>hard code</code>，即2号测试。<br>相应地，我们要修改实现。<br><figure class="highlight"><table><tr><td class="gutter"><pre><div class="line">1</div><div class="line">2</div><div class="line">3</div><div class="line">4</div><div class="line">5</div></pre></td><td class="code"><pre><div class="line">defn the-earliest-available-recommand [rooms]</div><div class="line">  (let [&#123;:keys [room-id periods]&#125; (first rooms)</div><div class="line">        available-periods (filter #(#&#123;:available&#125; (:status %)) periods)]</div><div class="line">    (merge &#123;:room-id room-id&#125;</div><div class="line">           (select-keys (first available-periods) [:time]))))</div></pre></td></tr></table></figure></p>
<h4 id="3-关闭并提交"><a href="#3-关闭并提交" class="headerlink" title="3. 关闭并提交"></a>3. 关闭并提交</h4><ul>
<li>[x] 单间澡堂有一个可用时间段</li>
</ul>
<figure class="highlight erlang"><table><tr><td class="gutter"><pre><div class="line">1</div><div class="line">2</div></pre></td><td class="code"><pre><div class="line">ga .</div><div class="line">gcmsg <span class="string">"one available room"</span></div></pre></td></tr></table></figure>
<h3 id="第2个任务"><a href="#第2个任务" class="headerlink" title="第2个任务"></a>第2个任务</h3><ul>
<li>[ ] 单间澡堂有多个可用时间段</li>
</ul>
<h4 id="1-写测试-1"><a href="#1-写测试-1" class="headerlink" title="1. 写测试"></a>1. 写测试</h4><figure class="highlight"><table><tr><td class="gutter"><pre><div class="line">1</div><div class="line">2</div><div class="line">3</div><div class="line">4</div><div class="line">5</div><div class="line">6</div><div class="line">7</div><div class="line">8</div><div class="line">9</div><div class="line">10</div></pre></td><td class="code"><pre><div class="line">(def room-3 &#123;:room-id 3</div><div class="line">             :periods [&#123;:time "17:00-18:00"</div><div class="line">                        :status :occupied&#125;</div><div class="line">                       &#123;:time "18:00-19:00"</div><div class="line">                        :status :available&#125;</div><div class="line">                       &#123;:time "19:00-20:00"</div><div class="line">                        :status :available&#125;]&#125;)</div><div class="line">...                        </div><div class="line">(fact "should recommand the earliest one if there is only one room with multiple available periods"</div><div class="line">    (the-earliest-available-recommand [room-3]) =&gt; &#123;:room-id 3 :time "18:00-19:00"&#125;)</div></pre></td></tr></table></figure>
<p>保存，发现测试还是跑过了。原因在于我们默认了<code>period</code>是递增排序的。我们看看有没有重构点？实现太简单了暂时找不到，那就欢欢喜喜地跳过实现步骤。</p>
<h4 id="2-关闭并提交"><a href="#2-关闭并提交" class="headerlink" title="2. 关闭并提交"></a>2. 关闭并提交</h4><ul>
<li>[x] 单间澡堂有多个可用时间段<figure class="highlight erlang"><table><tr><td class="gutter"><pre><div class="line">1</div><div class="line">2</div></pre></td><td class="code"><pre><div class="line">ga .</div><div class="line">gcmsg <span class="string">"one room with multiple available periods"</span></div></pre></td></tr></table></figure>
</li>
</ul>
<h3 id="第3个任务"><a href="#第3个任务" class="headerlink" title="第3个任务"></a>第3个任务</h3><ul>
<li>[ ] 所有澡堂（包含输入为空）没有可用时间段<h4 id="1-写测试-2"><a href="#1-写测试-2" class="headerlink" title="1. 写测试"></a>1. 写测试</h4></li>
</ul>
<figure class="highlight"><table><tr><td class="gutter"><pre><div class="line">1</div><div class="line">2</div><div class="line">3</div><div class="line">4</div><div class="line">5</div><div class="line">6</div><div class="line">7</div><div class="line">8</div><div class="line">9</div><div class="line">10</div><div class="line">11</div></pre></td><td class="code"><pre><div class="line">(def non-available-room &#123;:room-id 4</div><div class="line">             :periods [&#123;:time "17:00-18:00"</div><div class="line">                        :status :occupied&#125;</div><div class="line">                       &#123;:time "18:00-19:00"</div><div class="line">                        :status :occupied&#125;</div><div class="line">                       &#123;:time "19:00-20:00"</div><div class="line">                        :status :occupied&#125;]&#125;)</div><div class="line">                        </div><div class="line">(fact "should show `:no-available-room` if there is no available room"</div><div class="line">    (the-earliest-available-recommand []) =&gt; :no-available-room</div><div class="line">    (the-earliest-available-recommand [non-available-room]) =&gt; :no-available-room))</div></pre></td></tr></table></figure>
<p>这回肯定挂掉。</p>
<h4 id="2-写实现-1"><a href="#2-写实现-1" class="headerlink" title="2. 写实现"></a>2. 写实现</h4><figure class="highlight clojure"><table><tr><td class="gutter"><pre><div class="line">1</div><div class="line">2</div><div class="line">3</div><div class="line">4</div><div class="line">5</div><div class="line">6</div><div class="line">7</div></pre></td><td class="code"><pre><div class="line">(<span class="name"><span class="builtin-name">defn</span></span> the-earliest-available-recommand [rooms]</div><div class="line">  (<span class="name"><span class="builtin-name">let</span></span> [&#123;<span class="symbol">:keys</span> [room-id periods]&#125; (<span class="name"><span class="builtin-name">first</span></span> rooms)</div><div class="line">        available-periods (<span class="name"><span class="builtin-name">filter</span></span> #(<span class="name">#</span>&#123;<span class="symbol">:available</span>&#125; (<span class="symbol">:status</span> %)) periods)]</div><div class="line">    (<span class="name"><span class="builtin-name">if</span></span> (<span class="name"><span class="builtin-name">seq</span></span> available-periods)</div><div class="line">      (<span class="name"><span class="builtin-name">merge</span></span> &#123;<span class="symbol">:room-id</span> room-id&#125;</div><div class="line">             (<span class="name"><span class="builtin-name">select-keys</span></span> (<span class="name"><span class="builtin-name">first</span></span> available-periods) [<span class="symbol">:time</span>]))</div><div class="line">      <span class="symbol">:no-available-room</span>)))</div></pre></td></tr></table></figure>
<p>这里使用了<em>Clojure</em>中判断集合是否为空较为常用的手法<code>(seq )</code>，如果集合非空，那么返回集合本身；反之，返回nil，nil在逻辑上是false。测试通过。</p>
<h4 id="3-关闭并提交-1"><a href="#3-关闭并提交-1" class="headerlink" title="3. 关闭并提交"></a>3. 关闭并提交</h4><ul>
<li>[x] 所有澡堂（包含输入为空）没有可用时间段</li>
</ul>
<figure class="highlight erlang"><table><tr><td class="gutter"><pre><div class="line">1</div><div class="line">2</div></pre></td><td class="code"><pre><div class="line">ga .</div><div class="line">gcmsg <span class="string">"no available room"</span></div></pre></td></tr></table></figure>
<h3 id="第4个任务"><a href="#第4个任务" class="headerlink" title="第4个任务"></a>第4个任务</h3><ul>
<li>[ ] 多间澡堂都有可用时间段</li>
</ul>
<h4 id="1-写测试-3"><a href="#1-写测试-3" class="headerlink" title="1. 写测试"></a>1. 写测试</h4><figure class="highlight clojure"><table><tr><td class="gutter"><pre><div class="line">1</div><div class="line">2</div><div class="line">3</div><div class="line">4</div><div class="line">5</div></pre></td><td class="code"><pre><div class="line">(<span class="name">fact</span> <span class="string">"should recommand the earliest if there has more than one room and each has available periods"</span></div><div class="line">   (<span class="name">the-earliest-available-recommand</span> [room-1 room-2]) =&gt; &#123;<span class="symbol">:room-id</span> <span class="number">1</span> <span class="symbol">:time</span> <span class="string">"17:00-18:00"</span>&#125;</div><div class="line">   (<span class="name">the-earliest-available-recommand</span> [room-2 room-1]) =&gt; &#123;<span class="symbol">:room-id</span> <span class="number">1</span> <span class="symbol">:time</span> <span class="string">"17:00-18:00"</span>&#125;</div><div class="line">   (<span class="name">the-earliest-available-recommand</span> [room-2 room-3]) =&gt; &#123;<span class="symbol">:room-id</span> <span class="number">2</span> <span class="symbol">:time</span> <span class="string">"18:00-19:00"</span>&#125;</div><div class="line">   (<span class="name">the-earliest-available-recommand</span> [room-1 room-2 room-3]) =&gt; &#123;<span class="symbol">:room-id</span> <span class="number">1</span> <span class="symbol">:time</span> <span class="string">"17:00-18:00"</span>&#125;)</div></pre></td></tr></table></figure>
<h4 id="2-写实现-2"><a href="#2-写实现-2" class="headerlink" title="2. 写实现"></a>2. 写实现</h4><figure class="highlight clojure"><table><tr><td class="gutter"><pre><div class="line">1</div><div class="line">2</div><div class="line">3</div><div class="line">4</div><div class="line">5</div><div class="line">6</div><div class="line">7</div><div class="line">8</div><div class="line">9</div><div class="line">10</div><div class="line">11</div><div class="line">12</div></pre></td><td class="code"><pre><div class="line">(<span class="name"><span class="builtin-name">defn</span></span> the-earliest-available-recommand [rooms]</div><div class="line">  (<span class="name"><span class="builtin-name">if</span></span> (<span class="name"><span class="builtin-name">seq</span></span> rooms)</div><div class="line">    (<span class="name"><span class="builtin-name">first</span></span> (<span class="name"><span class="builtin-name">sort-by</span></span> <span class="symbol">:time</span></div><div class="line">                    (<span class="name"><span class="builtin-name">map</span></span> (<span class="name"><span class="builtin-name">fn</span></span> [room]</div><div class="line">                           (<span class="name"><span class="builtin-name">let</span></span> [&#123;<span class="symbol">:keys</span> [room-id periods]&#125; room</div><div class="line">                                 available-periods (<span class="name"><span class="builtin-name">filter</span></span> #(<span class="name">#</span>&#123;<span class="symbol">:available</span>&#125; (<span class="symbol">:status</span> %)) periods)]</div><div class="line">                             (<span class="name"><span class="builtin-name">if</span></span> (<span class="name"><span class="builtin-name">seq</span></span> available-periods)</div><div class="line">                               (<span class="name"><span class="builtin-name">merge</span></span> &#123;<span class="symbol">:room-id</span> room-id&#125;</div><div class="line">                                      (<span class="name"><span class="builtin-name">select-keys</span></span> (<span class="name"><span class="builtin-name">first</span></span> available-periods) [<span class="symbol">:time</span>]))</div><div class="line">                               <span class="symbol">:no-available-room</span>)))</div><div class="line">                         rooms)))</div><div class="line">    <span class="symbol">:no-available-room</span>))</div></pre></td></tr></table></figure>
<p>到这里，我们开始使用<code>(map )</code>函数处理多个房间的内容。注意，当输入房间是空集合的时候，这里需要相应地做<code>(seq rooms)</code>判空处理，否则会返回nil，而不是我们想要的<code>:no-available-room</code>。</p>
<h4 id="3-关闭并提交-2"><a href="#3-关闭并提交-2" class="headerlink" title="3. 关闭并提交"></a>3. 关闭并提交</h4><ul>
<li>[x] 多间澡堂都有可用时间段</li>
</ul>
<figure class="highlight erlang"><table><tr><td class="gutter"><pre><div class="line">1</div><div class="line">2</div></pre></td><td class="code"><pre><div class="line">ga .</div><div class="line">gcmsg <span class="string">"more than one room"</span></div></pre></td></tr></table></figure>
<h4 id="4-重构"><a href="#4-重构" class="headerlink" title="4. 重构"></a>4. 重构</h4><p>代码写到这里，再不重构就说不过去了。另外，管道没看到，倒是看到一堆括号。<br>我们使用thread last<code>(-&gt;&gt; )</code>做一次重构：<br><figure class="highlight clojure"><table><tr><td class="gutter"><pre><div class="line">1</div><div class="line">2</div><div class="line">3</div><div class="line">4</div><div class="line">5</div><div class="line">6</div><div class="line">7</div><div class="line">8</div><div class="line">9</div><div class="line">10</div><div class="line">11</div><div class="line">12</div><div class="line">13</div></pre></td><td class="code"><pre><div class="line">(<span class="name"><span class="builtin-name">defn</span></span> the-earliest-available-recommand [rooms]</div><div class="line">  (<span class="name"><span class="builtin-name">if</span></span> (<span class="name"><span class="builtin-name">seq</span></span> rooms)</div><div class="line">    (<span class="name"><span class="builtin-name">-&gt;&gt;</span></span> rooms</div><div class="line">         (<span class="name"><span class="builtin-name">map</span></span> (<span class="name"><span class="builtin-name">fn</span></span> [room]</div><div class="line">                (<span class="name"><span class="builtin-name">let</span></span> [&#123;<span class="symbol">:keys</span> [room-id periods]&#125; room</div><div class="line">                      available-periods (<span class="name"><span class="builtin-name">filter</span></span> #(<span class="name">#</span>&#123;<span class="symbol">:available</span>&#125; (<span class="symbol">:status</span> %)) periods)]</div><div class="line">                  (<span class="name"><span class="builtin-name">if</span></span> (<span class="name"><span class="builtin-name">seq</span></span> available-periods)</div><div class="line">                    (<span class="name"><span class="builtin-name">merge</span></span> &#123;<span class="symbol">:room-id</span> room-id&#125;</div><div class="line">                           (<span class="name"><span class="builtin-name">select-keys</span></span> (<span class="name"><span class="builtin-name">first</span></span> available-periods) [<span class="symbol">:time</span>]))</div><div class="line">                    <span class="symbol">:no-available-room</span>))))</div><div class="line">         (<span class="name"><span class="builtin-name">sort-by</span></span> <span class="symbol">:time</span>)</div><div class="line">         first)</div><div class="line">    <span class="symbol">:no-available-room</span>))</div></pre></td></tr></table></figure></p>
<p>还行，至少没那么多嵌套了。提交一次。<br><figure class="highlight sql"><table><tr><td class="gutter"><pre><div class="line">1</div><div class="line">2</div></pre></td><td class="code"><pre><div class="line">ga .</div><div class="line">gcmsg "[refactor] <span class="keyword">use</span> macro <span class="keyword">thread</span>-<span class="keyword">last</span> -&gt;&gt; <span class="keyword">to</span> <span class="keyword">pipe</span><span class="string">"</span></div></pre></td></tr></table></figure></p>
<p>继续重构，使用我们的<code>juxt</code>函数。<br><figure class="highlight clojure"><table><tr><td class="gutter"><pre><div class="line">1</div><div class="line">2</div><div class="line">3</div><div class="line">4</div><div class="line">5</div><div class="line">6</div><div class="line">7</div><div class="line">8</div><div class="line">9</div><div class="line">10</div><div class="line">11</div><div class="line">12</div><div class="line">13</div></pre></td><td class="code"><pre><div class="line">(<span class="name"><span class="builtin-name">defn</span></span> the-earliest-available-recommand [rooms]</div><div class="line">  (<span class="name"><span class="builtin-name">letfn</span></span> [(<span class="name">period</span> [&#123;<span class="symbol">:keys</span> [periods]&#125;]</div><div class="line">            (<span class="name"><span class="builtin-name">-&gt;&gt;</span></span> periods</div><div class="line">                 (<span class="name"><span class="builtin-name">filter</span></span> #(<span class="name">#</span>&#123;<span class="symbol">:available</span>&#125; (<span class="symbol">:status</span> %)))</div><div class="line">                 ffirst</div><div class="line">                 (<span class="name">#</span>(<span class="name"><span class="builtin-name">or</span></span> % [<span class="symbol">:time</span> <span class="symbol">::non-available</span>]))))]</div><div class="line">    (<span class="name"><span class="builtin-name">-&gt;&gt;</span></span> rooms</div><div class="line">         (<span class="name"><span class="builtin-name">map</span></span> (<span class="name"><span class="builtin-name">fn</span></span> [room]</div><div class="line">                (<span class="name"><span class="builtin-name">apply</span></span> conj &#123;&#125; ((<span class="name"><span class="builtin-name">juxt</span></span> first period) room))))</div><div class="line">         (<span class="name"><span class="builtin-name">remove</span></span> #(<span class="name">#</span>&#123;<span class="symbol">::non-available</span>&#125; (<span class="symbol">:time</span> %)))</div><div class="line">         (<span class="name"><span class="builtin-name">sort-by</span></span> <span class="symbol">:time</span>)</div><div class="line">         first</div><div class="line">         (<span class="name">#</span>(<span class="name"><span class="builtin-name">or</span></span> % <span class="symbol">:no-available-room</span>)))))</div></pre></td></tr></table></figure></p>
<p>看上去还行，不过不爽的是<code>(#(or % [:time ::non-available]))</code>。为了迎合<code>(-&gt;&gt; )</code>宏，我们给<code>(or )</code>包了一层。原因是<code>(-&gt;&gt; )</code>会让前面的结果出现在最后一个参数的位置，而我们需要将结果放到<code>(or )</code>的第一个参数的位置。有没有什么好看的解决方法呢？当然有！我们可以使用<code>(-&gt; )</code>来做到这点。<br><figure class="highlight clojure"><table><tr><td class="gutter"><pre><div class="line">1</div><div class="line">2</div><div class="line">3</div><div class="line">4</div><div class="line">5</div><div class="line">6</div><div class="line">7</div><div class="line">8</div><div class="line">9</div><div class="line">10</div><div class="line">11</div><div class="line">12</div><div class="line">13</div></pre></td><td class="code"><pre><div class="line">(<span class="name"><span class="builtin-name">defn</span></span> the-earliest-available-recommand [rooms]</div><div class="line">  (<span class="name"><span class="builtin-name">letfn</span></span> [(<span class="name">period</span> [&#123;<span class="symbol">:keys</span> [periods]&#125;]</div><div class="line">            (<span class="name"><span class="builtin-name">-&gt;</span></span> periods</div><div class="line">                (<span class="name"><span class="builtin-name">-&gt;&gt;</span></span> (<span class="name"><span class="builtin-name">filter</span></span> #(<span class="name">#</span>&#123;<span class="symbol">:available</span>&#125; (<span class="symbol">:status</span> %)))</div><div class="line">                     ffirst)</div><div class="line">                (<span class="name"><span class="builtin-name">or</span></span> [<span class="symbol">:time</span> <span class="symbol">::non-available</span>])))]</div><div class="line">    (<span class="name"><span class="builtin-name">-&gt;</span></span> rooms</div><div class="line">        (<span class="name"><span class="builtin-name">-&gt;&gt;</span></span> (<span class="name"><span class="builtin-name">map</span></span> (<span class="name"><span class="builtin-name">fn</span></span> [room]</div><div class="line">                    (<span class="name"><span class="builtin-name">apply</span></span> conj &#123;&#125; ((<span class="name"><span class="builtin-name">juxt</span></span> first period) room))))</div><div class="line">             (<span class="name"><span class="builtin-name">remove</span></span> #(<span class="name">#</span>&#123;<span class="symbol">::non-available</span>&#125; (<span class="symbol">:time</span> %)))</div><div class="line">             (<span class="name"><span class="builtin-name">sort-by</span></span> <span class="symbol">:time</span>)</div><div class="line">             first)</div><div class="line">        (<span class="name"><span class="builtin-name">or</span></span> <span class="symbol">:no-available-room</span>))))</div></pre></td></tr></table></figure></p>
<p>顿时觉得世界干净了不少。再提交一次。<br><figure class="highlight sql"><table><tr><td class="gutter"><pre><div class="line">1</div><div class="line">2</div></pre></td><td class="code"><pre><div class="line">ga .</div><div class="line">gcmsg "[refactor] <span class="keyword">use</span> juxt <span class="keyword">to</span> <span class="keyword">extract</span> needed <span class="keyword">fields</span><span class="string">"</span></div></pre></td></tr></table></figure></p>
<h3 id="第5个任务"><a href="#第5个任务" class="headerlink" title="第5个任务"></a>第5个任务</h3><ul>
<li>[ ] 多间澡堂中有的有可用时间段，有的没有可用时间段</li>
</ul>
<h4 id="1-写测试-4"><a href="#1-写测试-4" class="headerlink" title="1. 写测试"></a>1. 写测试</h4><figure class="highlight clojure"><table><tr><td class="gutter"><pre><div class="line">1</div><div class="line">2</div><div class="line">3</div></pre></td><td class="code"><pre><div class="line">(<span class="name">fact</span> <span class="string">"should recommand the earliest available room even if there has non available room"</span></div><div class="line">    (<span class="name">the-earliest-available-recommand</span> [room-1 non-available-room]) =&gt; &#123;<span class="symbol">:room-id</span> <span class="number">1</span> <span class="symbol">:time</span> <span class="string">"17:00-18:00"</span>&#125;</div><div class="line">    (<span class="name">the-earliest-available-recommand</span> [room-2 non-available-room]) =&gt; &#123;<span class="symbol">:room-id</span> <span class="number">2</span> <span class="symbol">:time</span> <span class="string">"18:00-19:00"</span>&#125;)</div></pre></td></tr></table></figure>
<p>测试直接通过，又可以跳过实现代码了。不过，这也预示着我们的测试是有覆盖的，也需要花时间整理这些测试用例。在那之前，先提交一下。</p>
<h4 id="2-关闭并提交-1"><a href="#2-关闭并提交-1" class="headerlink" title="2. 关闭并提交"></a>2. 关闭并提交</h4><ul>
<li>[x] 多间澡堂中有的有可用时间段，有的没有可用时间段</li>
</ul>
<figure class="highlight erlang"><table><tr><td class="gutter"><pre><div class="line">1</div><div class="line">2</div></pre></td><td class="code"><pre><div class="line">ga .</div><div class="line">gcmsg <span class="string">"mixed non-available and available rooms"</span></div></pre></td></tr></table></figure>
<h3 id="为第3个任务补上测试用例"><a href="#为第3个任务补上测试用例" class="headerlink" title="为第3个任务补上测试用例"></a>为第3个任务补上测试用例</h3><ul>
<li>[x] 所有（包含多个）澡堂（包含输入为空）没有可用时间段</li>
</ul>
<figure class="highlight"><table><tr><td class="gutter"><pre><div class="line">1</div><div class="line">2</div><div class="line">3</div><div class="line">4</div></pre></td><td class="code"><pre><div class="line">(fact "should show `:no-available-room` if there is no available room"</div><div class="line">   (the-earliest-available-recommand []) =&gt; :no-available-room</div><div class="line">   (the-earliest-available-recommand [non-available-room]) =&gt; :no-available-room</div><div class="line">   (the-earliest-available-recommand [non-available-room non-available-room]) =&gt; :no-available-room))</div></pre></td></tr></table></figure>
<p>这里的第3个用例包含第2个用例，我们待会整理掉。不过现在先提交一下。<br><figure class="highlight erlang"><table><tr><td class="gutter"><pre><div class="line">1</div><div class="line">2</div></pre></td><td class="code"><pre><div class="line">ga .</div><div class="line">gcmsg <span class="string">"multiple non-available rooms"</span></div></pre></td></tr></table></figure></p>
<h3 id="整理测试"><a href="#整理测试" class="headerlink" title="整理测试"></a>整理测试</h3><p>在前面进行的任务当中，我们发现有两次没有写实现测试就通过的情况。这说明测试用例是有覆盖的。</p>
<ul>
<li>第2个任务的测试用例其实覆盖了第1个任务的测试用例，所以可以直接删去后者；</li>
<li>第5个任务的测试用例覆盖了第4个任务的部分测试用例，所以可以合并到一起。</li>
</ul>
<p>整理下来，最终的测试变成下面这样：<br><figure class="highlight clojure"><table><tr><td class="gutter"><pre><div class="line">1</div><div class="line">2</div><div class="line">3</div><div class="line">4</div><div class="line">5</div><div class="line">6</div><div class="line">7</div><div class="line">8</div><div class="line">9</div><div class="line">10</div><div class="line">11</div><div class="line">12</div><div class="line">13</div><div class="line">14</div></pre></td><td class="code"><pre><div class="line">(<span class="name">facts</span> <span class="string">"about `the-earliest-avaible-period-of-bathroom`"</span></div><div class="line">  (<span class="name">fact</span> <span class="string">"should recommand the earliest one if there is only one room with multiple available periods"</span></div><div class="line">    (<span class="name">the-earliest-available-recommand</span> [room-3]) =&gt; &#123;<span class="symbol">:room-id</span> <span class="number">3</span> <span class="symbol">:time</span> <span class="string">"18:00-19:00"</span>&#125;)</div><div class="line">  </div><div class="line">  (<span class="name">fact</span> <span class="string">"should show `:no-available-room` if there is no available room"</span></div><div class="line">    (<span class="name">the-earliest-available-recommand</span> []) =&gt; <span class="symbol">:no-available-room</span></div><div class="line">    (<span class="name">the-earliest-available-recommand</span> [non-available-room non-available-room]) =&gt; <span class="symbol">:no-available-room</span>)</div><div class="line"> </div><div class="line">  (<span class="name">fact</span> <span class="string">"should recommand the earliest if there has more than one room and each may have available periods"</span></div><div class="line">    (<span class="name">the-earliest-available-recommand</span> [room-1 room-2]) =&gt; &#123;<span class="symbol">:room-id</span> <span class="number">1</span> <span class="symbol">:time</span> <span class="string">"17:00-18:00"</span>&#125;</div><div class="line">    (<span class="name">the-earliest-available-recommand</span> [room-2 room-1]) =&gt; &#123;<span class="symbol">:room-id</span> <span class="number">1</span> <span class="symbol">:time</span> <span class="string">"17:00-18:00"</span>&#125;</div><div class="line">    (<span class="name">the-earliest-available-recommand</span> [room-2 room-3]) =&gt; &#123;<span class="symbol">:room-id</span> <span class="number">2</span> <span class="symbol">:time</span> <span class="string">"18:00-19:00"</span>&#125;</div><div class="line">    (<span class="name">the-earliest-available-recommand</span> [room-1 room-2 room-3]) =&gt; &#123;<span class="symbol">:room-id</span> <span class="number">1</span> <span class="symbol">:time</span> <span class="string">"17:00-18:00"</span>&#125;</div><div class="line">    (<span class="name">the-earliest-available-recommand</span> [room-1 non-available-room]) =&gt; &#123;<span class="symbol">:room-id</span> <span class="number">1</span> <span class="symbol">:time</span> <span class="string">"17:00-18:00"</span>&#125;))</div></pre></td></tr></table></figure></p>
<h3 id="文档"><a href="#文档" class="headerlink" title="文档"></a>文档</h3><blockquote>
<p>The final goal of any engineering activity is some type of documentation.</p>
</blockquote>
<p>更新README.md文件，其中描述程序解决的问题以及运行步骤，当然包含设计思路那更好了。提交一下。<br><figure class="highlight erlang"><table><tr><td class="gutter"><pre><div class="line">1</div><div class="line">2</div></pre></td><td class="code"><pre><div class="line">ga .</div><div class="line">gcmsg <span class="string">"update readme"</span></div></pre></td></tr></table></figure></p>
<h3 id="美化代码"><a href="#美化代码" class="headerlink" title="美化代码"></a>美化代码</h3><blockquote>
<p>代码是诗行 - by lambeta</p>
</blockquote>
<p>什么是好看的代码？除了清晰明了，格式也必须产生美感。<br><figure class="highlight clojure"><table><tr><td class="gutter"><pre><div class="line">1</div><div class="line">2</div><div class="line">3</div><div class="line">4</div><div class="line">5</div><div class="line">6</div><div class="line">7</div><div class="line">8</div><div class="line">9</div><div class="line">10</div><div class="line">11</div><div class="line">12</div><div class="line">13</div></pre></td><td class="code"><pre><div class="line">(<span class="name"><span class="builtin-name">defn</span></span> the-earliest-available-recommand [rooms]</div><div class="line">  (<span class="name"><span class="builtin-name">letfn</span></span> [(<span class="name">period</span> [&#123;<span class="symbol">:keys</span> [periods]&#125;]</div><div class="line">            (<span class="name"><span class="builtin-name">-&gt;</span></span> periods</div><div class="line">                (<span class="name"><span class="builtin-name">-&gt;&gt;</span></span> (<span class="name"><span class="builtin-name">filter</span></span> #(<span class="name">#</span>&#123;<span class="symbol">:available</span>&#125; (<span class="symbol">:status</span> %))))</div><div class="line">                (<span class="name"><span class="builtin-name">ffirst</span></span>) <span class="comment">; 统一套上括号</span></div><div class="line">                (<span class="name"><span class="builtin-name">or</span></span> [<span class="symbol">:time</span> <span class="symbol">::non-available</span>])))]</div><div class="line">    (<span class="name"><span class="builtin-name">-&gt;</span></span> rooms</div><div class="line">        (<span class="name"><span class="builtin-name">-&gt;&gt;</span></span> (<span class="name"><span class="builtin-name">map</span></span> (<span class="name"><span class="builtin-name">juxt</span></span> first period))</div><div class="line">             (<span class="name"><span class="builtin-name">map</span></span> #(<span class="name"><span class="builtin-name">into</span></span> &#123;&#125; %)) <span class="comment">; 合并单独提出来</span></div><div class="line">             (<span class="name"><span class="builtin-name">remove</span></span> #(<span class="name">#</span>&#123;<span class="symbol">::non-available</span>&#125; (<span class="symbol">:time</span> %)))</div><div class="line">             (<span class="name"><span class="builtin-name">sort-by</span></span> <span class="symbol">:time</span>)</div><div class="line">             (<span class="name"><span class="builtin-name">first</span></span>)) <span class="comment">; 统一套上括号</span></div><div class="line">        (<span class="name"><span class="builtin-name">or</span></span> <span class="symbol">:no-available-room</span>))))</div></pre></td></tr></table></figure></p>
<p>顺眼不少，最后提交一下。<br><figure class="highlight erlang"><table><tr><td class="gutter"><pre><div class="line">1</div><div class="line">2</div></pre></td><td class="code"><pre><div class="line">ga .</div><div class="line">gcmsg <span class="string">"[refactor] beautify pipe format"</span></div></pre></td></tr></table></figure></p>
<hr>
<p>这篇文章发出来一天，TDD讨论群的一位麦姓朋友@我道：</p>
<blockquote>
<p>core=&gt; (first {:a 1 :b 2})<br>[:a 1]<br>core=&gt; (first {:b 2 :a 1})<br>[:b 2]<br>@lambeta map的元素应该是无序的，用first来获得key value pair是不可靠的。</p>
</blockquote>
<p>看到这个建议的时候，我心里一阵欣喜——又有一员<em>Clojurians</em>，可以切磋技艺了！冷静下来，发现自己确实忽略了map中的entries可能是无序的。所以我做了如下的验证：<br><figure class="highlight"><table><tr><td class="gutter"><pre><div class="line">1</div><div class="line">2</div></pre></td><td class="code"><pre><div class="line">(type &#123;&#125;)</div><div class="line">-&gt; clojure.lang.PersistentArrayMap</div></pre></td></tr></table></figure></p>
<p>看到<code>PersistentArrayMap</code>的时候，我明白这些entries是保持插入顺序的，也就是说，<code>(first {:a 1 :b 2})</code>的求值结果一定是<code>[:a 1]</code>。照这个思路，在我的程序当中使用<code>(first )</code>取map的第一个元素并不会出错。不过，本着谨慎的心态，我查了一下clojure的<a href="https://clojuredocs.org/clojure.core/array-map" target="_blank" rel="external">array-map</a>，发现一个有趣的例子：<br><figure class="highlight clojure"><table><tr><td class="gutter"><pre><div class="line">1</div><div class="line">2</div><div class="line">3</div><div class="line">4</div><div class="line">5</div></pre></td><td class="code"><pre><div class="line">(<span class="name"><span class="builtin-name">defn</span></span> make-map [count] (<span class="name"><span class="builtin-name">zipmap</span></span> (<span class="name"><span class="builtin-name">range</span></span> count) (<span class="name"><span class="builtin-name">range</span></span> count)))</div><div class="line">(<span class="name"><span class="builtin-name">type</span></span> (<span class="name">make-map</span> <span class="number">9</span>))</div><div class="line"><span class="comment">;; =&gt; clojure.lang.PersistentArrayMap</span></div><div class="line">(<span class="name"><span class="builtin-name">type</span></span> (<span class="name">make-map</span> <span class="number">10</span>))</div><div class="line"><span class="comment">;; =&gt; clojure.lang.PersistentHashMap</span></div></pre></td></tr></table></figure></p>
<p>这表明当map中的entries数量超过一定数量（不一定是9，例外见：<a href="http://dev.clojure.org/jira/browse/CLJ-1587" target="_blank" rel="external">PersistentArrayMap’s assoc doesn’t respect HASHTABLE_THRESHOLD</a>）时，<code>PersistentArrayMap</code>就变成了<code>PersistentHashMap</code>，那也就意味着，<code>(first )</code>取出来的值可能是<em>随机的</em>。举个例子：<br><figure class="highlight"><table><tr><td class="gutter"><pre><div class="line">1</div><div class="line">2</div></pre></td><td class="code"><pre><div class="line">(first &#123;7 7, 1 1, 4 4, 6 6, 3 3, 2 2, 9 9, 0 0, 8 8, 5 5&#125;)</div><div class="line">-&gt; [0 0]</div></pre></td></tr></table></figure></p>
<p>返回的结果并不是相当然的<code>[7 7]</code>，而是<code>[0 0]</code>。那么<code>(first )</code>到底干了些什么呢？Cognitect公司的<a href="https://github.com/puredanger" target="_blank" rel="external">alexmiller</a>回答我说：<code>(first )</code>会把它的参数强制转换（coerce）成了一个序列，然后取第一个值。我们试着用<code>(seq )</code>转换一下：<br><figure class="highlight"><table><tr><td class="gutter"><pre><div class="line">1</div><div class="line">2</div><div class="line">3</div><div class="line">4</div></pre></td><td class="code"><pre><div class="line">(type &#123; 7 7, 1 1, 4 4, 6 6, 3 3, 2 2, 9 9, 0 0, 8 8, 5 5&#125;)</div><div class="line">-&gt; clojure.lang.PersistentHashMap</div><div class="line">(seq &#123; 7 7, 1 1, 4 4, 6 6, 3 3, 2 2, 9 9, 0 0, 8 8, 5 5&#125;)</div><div class="line">-&gt; ([0 0] [7 7] [1 1] [4 4] [6 6] [3 3] [2 2] [9 9] [8 8])</div></pre></td></tr></table></figure></p>
<p>果然，<code>[0 0]</code>出现在序列的首位。至于为什么是这样的顺序，需要深入<em>Clojure</em>的hash算法和数据结构当中，有时间另起一篇博客解释。我们再试试<code>PersistentArrayMap</code>的情况：<br><figure class="highlight"><table><tr><td class="gutter"><pre><div class="line">1</div><div class="line">2</div><div class="line">3</div><div class="line">4</div></pre></td><td class="code"><pre><div class="line">(type &#123; 7 7, 1 1, 4 4, 6 6, 3 3, 2 2, 9 9, 0 0&#125;)</div><div class="line">-&gt; clojure.lang.PersistentArrayMap</div><div class="line">(seq &#123; 7 7, 1 1, 4 4, 6 6, 3 3, 2 2, 9 9, 0 0&#125;)</div><div class="line">-&gt; ([7 7] [1 1] [4 4] [6 6] [3 3] [2 2] [9 9] [0 0])</div></pre></td></tr></table></figure></p>
<p>顺序确实和原来的一致。</p>
<p>我们的程序当中是不应该假设map是有序的，所以需要修改实现代码。</p>
<figure class="highlight clojure"><table><tr><td class="gutter"><pre><div class="line">1</div><div class="line">2</div><div class="line">3</div><div class="line">4</div><div class="line">5</div><div class="line">6</div><div class="line">7</div><div class="line">8</div><div class="line">9</div><div class="line">10</div><div class="line">11</div><div class="line">12</div><div class="line">13</div><div class="line">14</div></pre></td><td class="code"><pre><div class="line">(<span class="name"><span class="builtin-name">defn</span></span> the-earliest-available-recommand [rooms]</div><div class="line">  (<span class="name"><span class="builtin-name">letfn</span></span> [(<span class="name">period</span> [&#123;<span class="symbol">:keys</span> [periods]&#125;]</div><div class="line">            (<span class="name"><span class="builtin-name">-&gt;</span></span> periods</div><div class="line">                (<span class="name"><span class="builtin-name">-&gt;&gt;</span></span> (<span class="name"><span class="builtin-name">filter</span></span> (<span class="name"><span class="builtin-name">comp</span></span> #&#123;<span class="symbol">:available</span>&#125; <span class="symbol">:status</span>)))</div><div class="line">                (<span class="name"><span class="builtin-name">first</span></span>)</div><div class="line">                (<span class="name"><span class="builtin-name">find</span></span> <span class="symbol">:time</span>)))</div><div class="line">          (<span class="name">room-id</span> [room]</div><div class="line">            (<span class="name"><span class="builtin-name">find</span></span> room <span class="symbol">:room-id</span>))]</div><div class="line">    (<span class="name"><span class="builtin-name">-&gt;</span></span> rooms</div><div class="line">        (<span class="name"><span class="builtin-name">-&gt;&gt;</span></span> (<span class="name"><span class="builtin-name">map</span></span> (<span class="name"><span class="builtin-name">comp</span></span> (<span class="name"><span class="builtin-name">partial</span></span> into &#123;&#125;) (<span class="name"><span class="builtin-name">juxt</span></span> room-id period)))</div><div class="line">             (<span class="name"><span class="builtin-name">filter</span></span> <span class="symbol">:time</span>)</div><div class="line">             (<span class="name"><span class="builtin-name">sort-by</span></span> <span class="symbol">:time</span>)</div><div class="line">             (<span class="name"><span class="builtin-name">first</span></span>))</div><div class="line">        (<span class="name"><span class="builtin-name">or</span></span> <span class="symbol">:no-available-room</span>))))</div></pre></td></tr></table></figure>
<p><code>(find )</code>函数，用于从map中获取包含该键值的entry，如果找不到，返回nil。这样就避免了潜在无序的entries对程序的干扰。另外，<code>(partial into {})</code>和<a href="https://en.wikipedia.org/wiki/Currying" target="_blank" rel="external">Currying</a>很像，它通过接收<code>into</code>函数及其首个参数，构造出一个接收后续参数的函数。当然也可以直接使用<code>#(into {} %)</code>这样的形式。</p>
<p>下面是麦姓朋友的另一种解法，和我的解法思路不完全一样，值得学习借鉴。<br><figure class="highlight clojure"><table><tr><td class="gutter"><pre><div class="line">1</div><div class="line">2</div><div class="line">3</div><div class="line">4</div><div class="line">5</div><div class="line">6</div><div class="line">7</div><div class="line">8</div><div class="line">9</div><div class="line">10</div><div class="line">11</div><div class="line">12</div><div class="line">13</div><div class="line">14</div></pre></td><td class="code"><pre><div class="line">(<span class="name"><span class="builtin-name">defn</span></span> the-earliest-available-recommand [rooms]</div><div class="line">  (<span class="name"><span class="builtin-name">letfn</span></span> [(<span class="name">earliest-available-time</span> [periods]</div><div class="line">            (<span class="name"><span class="builtin-name">-&gt;&gt;</span></span> periods</div><div class="line">                 (<span class="name"><span class="builtin-name">filter</span></span> (<span class="name"><span class="builtin-name">comp</span></span> #&#123;<span class="symbol">:available</span>&#125; <span class="symbol">:status</span>))</div><div class="line">                 (<span class="name"><span class="builtin-name">map</span></span> <span class="symbol">:time</span>)</div><div class="line">                 (<span class="name"><span class="builtin-name">sort</span></span>)</div><div class="line">                 (<span class="name"><span class="builtin-name">first</span></span>)))]</div><div class="line">    (<span class="name">rename-keys</span></div><div class="line">     (<span class="name"><span class="builtin-name">-&gt;&gt;</span></span> rooms</div><div class="line">          (<span class="name"><span class="builtin-name">map</span></span> #(<span class="name"><span class="builtin-name">update-in</span></span> % [<span class="symbol">:periods</span>] earliest-available-time))</div><div class="line">          (<span class="name"><span class="builtin-name">filter</span></span> <span class="symbol">:periods</span>)</div><div class="line">          (<span class="name"><span class="builtin-name">sort-by</span></span> <span class="symbol">:periods</span>)</div><div class="line">          (<span class="name"><span class="builtin-name">first</span></span>))</div><div class="line">     &#123;<span class="symbol">:periods</span> <span class="symbol">:time</span>&#125;)))</div></pre></td></tr></table></figure></p>
<p>真诚欢迎大家继续点评。</p>
<hr>
<p><a href="https://github.com/qianyan/kata-bathroom/" target="_blank" rel="external">本文样例</a></p>
]]></content>
    
    <summary type="html">
    
      &lt;h2 id=&quot;起源&quot;&gt;&lt;a href=&quot;#起源&quot; class=&quot;headerlink&quot; title=&quot;起源&quot;&gt;&lt;/a&gt;起源&lt;/h2&gt;&lt;p&gt;TDD讨论组里的&lt;a href=&quot;http://www.jackyshen.com/&quot; target=&quot;_blank&quot; rel=&quot;exter
    
    </summary>
    
      <category term="Clojure" scheme="https://lameta.com/categories/Clojure/"/>
    
    
      <category term="kata" scheme="https://lameta.com/tags/kata/"/>
    
  </entry>
  
  <entry>
    <title>Reading and Writing Files</title>
    <link href="https://lameta.com/2016/09/04/Reading-and-Writing-Files/"/>
    <id>https://lameta.com/2016/09/04/Reading-and-Writing-Files/</id>
    <published>2016-09-04T02:10:44.000Z</published>
    <updated>2016-09-07T08:49:21.000Z</updated>
    
    <content type="html"><![CDATA[<h2 id="读取和写入文件"><a href="#读取和写入文件" class="headerlink" title="读取和写入文件"></a>读取和写入文件</h2><p>数据一般都是存储在纯文本文件当中，存储的形式多种多样。本文，我会介绍如何在Clojure中读取和写入这些数据。</p>
<h3 id="1-打开文件"><a href="#1-打开文件" class="headerlink" title="1. 打开文件"></a>1. 打开文件</h3><p>新建文件<em>hello.txt</em>，放到<em>resources</em>目录，内容如下：<br><figure class="highlight erlang-repl"><table><tr><td class="gutter"><pre><div class="line">1</div><div class="line">2</div><div class="line">3</div></pre></td><td class="code"><pre><div class="line">hello world!</div><div class="line">hello lambeta!</div><div class="line">hello life!</div></pre></td></tr></table></figure></p>
<p>新建<em>4io.clj</em>，输入程序：<br><figure class="highlight clojure"><table><tr><td class="gutter"><pre><div class="line">1</div><div class="line">2</div><div class="line">3</div><div class="line">4</div><div class="line">5</div><div class="line">6</div></pre></td><td class="code"><pre><div class="line">(<span class="name"><span class="builtin-name">ns</span></span> the-way-to-clojure.4io</div><div class="line">  (<span class="symbol">:require</span> [clojure.java.io <span class="symbol">:as</span> io]</div><div class="line">            [clojure.string <span class="symbol">:as</span> str]))</div><div class="line"></div><div class="line">(<span class="name"><span class="builtin-name">def</span></span> data-file (<span class="name">io/resource</span> <span class="string">"hello.txt"</span>))</div><div class="line">(<span class="name"><span class="builtin-name">slurp</span></span> data-file)</div></pre></td></tr></table></figure></p>
<p>运行程序，输出如下：<br><figure class="highlight lsl"><table><tr><td class="gutter"><pre><div class="line">1</div></pre></td><td class="code"><pre><div class="line"><span class="string">"hello world! <span class="subst">\n</span>hello lambeta!<span class="subst">\n</span>hello life!<span class="subst">\n</span>"</span></div></pre></td></tr></table></figure></p>
<h4 id="读取所有行"><a href="#读取所有行" class="headerlink" title="读取所有行"></a>读取所有行</h4><figure class="highlight clojure"><table><tr><td class="gutter"><pre><div class="line">1</div><div class="line">2</div></pre></td><td class="code"><pre><div class="line">(<span class="name"><span class="builtin-name">line-seq</span></span> (<span class="name">io/reader</span> data-file))</div><div class="line"><span class="comment">;;=&gt; ("hello world!" "hello lambeta!" "hello life!")</span></div></pre></td></tr></table></figure>
<h4 id="with-open宏"><a href="#with-open宏" class="headerlink" title="with-open宏"></a>with-open宏</h4><p>with-open宏用于自动关闭打开的文件。</p>
<p>1.1 读取一行，如下：<br><figure class="highlight clojure"><table><tr><td class="gutter"><pre><div class="line">1</div><div class="line">2</div><div class="line">3</div></pre></td><td class="code"><pre><div class="line">(<span class="name"><span class="builtin-name">with-open</span></span> [rdr (<span class="name">io/reader</span> data-file)]</div><div class="line">  (<span class="name"><span class="builtin-name">when-let</span></span> [line (<span class="name">.readLine</span> rdr)]</div><div class="line">    (<span class="name">println</span> line)))</div></pre></td></tr></table></figure></p>
<p>1.2 读取多行，如下：<br><figure class="highlight clojure"><table><tr><td class="gutter"><pre><div class="line">1</div><div class="line">2</div><div class="line">3</div><div class="line">4</div><div class="line">5</div><div class="line">6</div></pre></td><td class="code"><pre><div class="line"><span class="comment">;;; read multiple lines</span></div><div class="line">(<span class="name"><span class="builtin-name">with-open</span></span> [rdr (<span class="name">io/reader</span> data-file)]</div><div class="line">  (<span class="name"><span class="builtin-name">loop</span></span> [line (<span class="name">.readLine</span> rdr)]</div><div class="line">    (<span class="name"><span class="builtin-name">when</span></span> line</div><div class="line">      (<span class="name">println</span> line)</div><div class="line">      (<span class="name"><span class="builtin-name">recur</span></span> (<span class="name">.readLine</span> rdr)))))</div></pre></td></tr></table></figure></p>
<h3 id="2-读取文件的技巧"><a href="#2-读取文件的技巧" class="headerlink" title="2. 读取文件的技巧"></a>2. 读取文件的技巧</h3><p>想想读取文件可能有哪些场景？</p>
<ul>
<li>读取整个文本</li>
</ul>
<figure class="highlight clojure"><table><tr><td class="gutter"><pre><div class="line">1</div></pre></td><td class="code"><pre><div class="line">(<span class="name"><span class="builtin-name">slurp</span></span> data-file)</div></pre></td></tr></table></figure>
<ul>
<li>读取一行</li>
</ul>
<figure class="highlight"><table><tr><td class="gutter"><pre><div class="line">1</div><div class="line">2</div><div class="line">3</div><div class="line">4</div><div class="line">5</div><div class="line">6</div></pre></td><td class="code"><pre><div class="line">(with-open [rdr (io/reader data-file)]</div><div class="line">  (first (line-seq rdr)))</div><div class="line">;; 或者</div><div class="line">(with-open [rdr (io/reader data-file)]</div><div class="line">  (take 1 (line-seq rdr)))</div><div class="line">-&gt; "hello world!"</div></pre></td></tr></table></figure>
<ul>
<li>读取前n行</li>
</ul>
<figure class="highlight"><table><tr><td class="gutter"><pre><div class="line">1</div><div class="line">2</div><div class="line">3</div></pre></td><td class="code"><pre><div class="line">(with-open [rdr (io/reader data-file)]</div><div class="line">  (doall (take 2 (line-seq rdr))))</div><div class="line">-&gt; ("hello world!" "hello lambeta!")</div></pre></td></tr></table></figure>
<p>这里使用了<code>(doall )</code>方法，如果不用这个方法，在repl中求值的时候会表达式导致抛出<code>Unhandled java.io.IOException  Stream closed</code>异常。究其缘由是<code>(take 2 )</code>返回了一个惰性序列，详细解释参见文末备注。</p>
<ul>
<li>读取前n个字符</li>
</ul>
<figure class="highlight"><table><tr><td class="gutter"><pre><div class="line">1</div><div class="line">2</div><div class="line">3</div><div class="line">4</div><div class="line">5</div><div class="line">6</div><div class="line">7</div><div class="line">8</div><div class="line">9</div></pre></td><td class="code"><pre><div class="line">with-open [rdr (io/reader data-file)]</div><div class="line">  (loop [ch (.read rdr) len 20]</div><div class="line">    (when-not (or (= -1 ch) (zero? len))</div><div class="line">      (println (char ch))</div><div class="line">      (recur (.read rdr) (dec len)))))</div><div class="line">| h</div><div class="line">| e</div><div class="line">| ...</div><div class="line">-&gt; nil</div></pre></td></tr></table></figure>
<ul>
<li>跳过特定的行</li>
</ul>
<p>在<em>resources</em>目录下，新建<em>records.txt</em>，内容即代码注释所示：</p>
<figure class="highlight"><table><tr><td class="gutter"><pre><div class="line">1</div><div class="line">2</div><div class="line">3</div><div class="line">4</div><div class="line">5</div><div class="line">6</div><div class="line">7</div><div class="line">8</div><div class="line">9</div><div class="line">10</div><div class="line">11</div><div class="line">12</div><div class="line">13</div><div class="line">14</div><div class="line">15</div><div class="line">16</div><div class="line">17</div><div class="line">18</div><div class="line">19</div><div class="line">20</div><div class="line">21</div><div class="line">22</div><div class="line">23</div><div class="line">24</div></pre></td><td class="code"><pre><div class="line">(defn read-records [input-file]</div><div class="line">  "Coloured fox fur production, HOPEDALE, Labrador, 1834-1842</div><div class="line">  #Source: C. Elton (1942) \"Voles, Mice and Lemmings\", Oxford Univ. Press</div><div class="line">  #Table 17, p.265--266</div><div class="line">  22</div><div class="line">  29</div><div class="line">  2</div><div class="line">  16</div><div class="line">  12</div><div class="line">  35</div><div class="line">  8</div><div class="line">  83</div><div class="line">  166"</div><div class="line">  (letfn [(skip [lines]</div><div class="line">            (next lines))]</div><div class="line">         (with-open [rdr ((comp io/reader io/resource) input-file)]</div><div class="line">           (-&gt;&gt;</div><div class="line">            (for [line (skip (line-seq rdr))</div><div class="line">                  :when (not (.startsWith line "#"))]</div><div class="line">              (read-string line))</div><div class="line">            (apply +)))))</div><div class="line"></div><div class="line">(read-records "records.txt")</div><div class="line">-&gt; 373</div></pre></td></tr></table></figure>
<p>我们在<code>read-records</code>内部新建一个<code>skip</code>方法，顾名思义，跳过第一个元素，然后返回后面的列表。这里旨在跳过文本的声明头。<code>:when (not ...)</code>过滤了文本的注释部分（以#开头的行），并使用<code>read-string</code>转换字符串到数字类型，<code>(for )</code>求值完成后返回只包含数字的列表。最后，我们对列表做了一次累加操作。</p>
<p>我们试试非过滤而是跳过（删除）以”#”开头行的方式获取数字列表，这样更符合要求。重写<em>with-open</em>部分，如下：<br><figure class="highlight clojure"><table><tr><td class="gutter"><pre><div class="line">1</div><div class="line">2</div><div class="line">3</div><div class="line">4</div><div class="line">5</div><div class="line">6</div></pre></td><td class="code"><pre><div class="line">(<span class="name"><span class="builtin-name">with-open</span></span> [rdr ((<span class="name"><span class="builtin-name">comp</span></span> io/reader io/resource) input-file)]</div><div class="line">      (<span class="name"><span class="builtin-name">apply</span></span> +</div><div class="line">             (<span class="name"><span class="builtin-name">let</span></span> [lines (<span class="name">skip</span> (<span class="name"><span class="builtin-name">line-seq</span></span> rdr))]</div><div class="line">               (<span class="name"><span class="builtin-name">-&gt;&gt;</span></span> lines</div><div class="line">                    (<span class="name"><span class="builtin-name">remove</span></span> (<span class="name">set</span> (<span class="name"><span class="builtin-name">for</span></span> [line lines <span class="symbol">:while</span> (<span class="name">.startsWith</span> line <span class="string">"#"</span>)] line)))</div><div class="line">                    (<span class="name"><span class="builtin-name">map</span></span> read-string)))))</div></pre></td></tr></table></figure></p>
<p>或者<br><figure class="highlight clojure"><table><tr><td class="gutter"><pre><div class="line">1</div><div class="line">2</div><div class="line">3</div><div class="line">4</div><div class="line">5</div><div class="line">6</div></pre></td><td class="code"><pre><div class="line">(<span class="name"><span class="builtin-name">with-open</span></span> [rdr ((<span class="name"><span class="builtin-name">comp</span></span> io/reader io/resource) input-file)]</div><div class="line">      (<span class="name"><span class="builtin-name">apply</span></span> +</div><div class="line">             (<span class="name"><span class="builtin-name">let</span></span> [lines (<span class="name">skip</span> (<span class="name"><span class="builtin-name">line-seq</span></span> rdr))]</div><div class="line">               (<span class="name"><span class="builtin-name">-&gt;&gt;</span></span> lines</div><div class="line">                    (<span class="name"><span class="builtin-name">drop</span></span> (<span class="name"><span class="builtin-name">count</span></span> (<span class="name"><span class="builtin-name">for</span></span> [line lines <span class="symbol">:while</span> (<span class="name">.startsWith</span> line <span class="string">"#"</span>)] line)))</div><div class="line">                    (<span class="name"><span class="builtin-name">map</span></span> read-string)))))</div></pre></td></tr></table></figure></p>
<h3 id="3-读取网络文件"><a href="#3-读取网络文件" class="headerlink" title="3. 读取网络文件"></a>3. 读取网络文件</h3><p>通过slurp读取字符串<br><figure class="highlight livescript"><table><tr><td class="gutter"><pre><div class="line">1</div><div class="line">2</div><div class="line">3</div></pre></td><td class="code"><pre><div class="line"><span class="function"><span class="params">(slurp <span class="string">"http://robjhyndman.com/tsdldata/ecology1/hopedale.dat"</span> :encoding <span class="string">"utf-8"</span>)</span></span></div><div class="line"></div><div class="line">-&gt; <span class="string">"Coloured fox fur production, HOPEDALE, Labrador,, 1834-1925\n<span class="subst">#Source</span>: C. Elton (1942) \"Voles, Mice and Lemmings\", Oxford Univ. Press\n<span class="subst">#Table</span> 17, p.265--266\n      22   \n...</span></div></pre></td></tr></table></figure></p>
<p>注意，这个网页上的数据是用UTF-8编码的，所以解码读取时，也应该使用UTF-8。</p>
<h3 id="4-写入文件"><a href="#4-写入文件" class="headerlink" title="4. 写入文件"></a>4. 写入文件</h3><ul>
<li>使用spit方法</li>
</ul>
<figure class="highlight clojure"><table><tr><td class="gutter"><pre><div class="line">1</div></pre></td><td class="code"><pre><div class="line">(<span class="name">spit</span> <span class="string">"world.txt"</span> <span class="string">"Hello, lambeta!"</span> <span class="symbol">:append</span> <span class="literal">true</span>)</div></pre></td></tr></table></figure>
<p>运行程序之后，项目的根目录下会生成<em>world.txt</em>文件，内容是<em>Hello, lambeta</em>。spit方法其实就是向Java的BufferedWriter中写入内容。</p>
<ul>
<li>使用clojure.java.io/writer</li>
</ul>
<p>我们在项目的根目录新建<em>numbers.txt</em>，内容是多行的数字对，如下：<br><figure class="highlight lsl"><table><tr><td class="gutter"><pre><div class="line">1</div><div class="line">2</div><div class="line">3</div></pre></td><td class="code"><pre><div class="line"><span class="number">1.3</span> <span class="number">2.7</span></div><div class="line"><span class="number">10000</span> <span class="number">1</span></div><div class="line"><span class="number">-1</span> <span class="number">1</span></div></pre></td></tr></table></figure></p>
<p>我们需要把每行两个数字，和它们相加的结果写入到<em>sum-of_numbers.txt</em>文件中。也就是注释中的描述。</p>
<figure class="highlight clojure"><table><tr><td class="gutter"><pre><div class="line">1</div><div class="line">2</div><div class="line">3</div><div class="line">4</div><div class="line">5</div><div class="line">6</div><div class="line">7</div><div class="line">8</div><div class="line">9</div><div class="line">10</div><div class="line">11</div><div class="line">12</div><div class="line">13</div><div class="line">14</div><div class="line">15</div><div class="line">16</div><div class="line">17</div><div class="line">18</div></pre></td><td class="code"><pre><div class="line">(<span class="name"><span class="builtin-name">defn</span></span> sum-number-pairs [input-file output-file]</div><div class="line">  <span class="string">"Read the data from input-file, which contains two floats per line</span></div><div class="line">   separated by a space.  Open file named output-file and, for each line in</div><div class="line">   input-file, write a line to the output file that contains the two floats</div><div class="line">   from the corresponding line of input-file plus a space and the sum of the</div><div class="line">   two floats."</div><div class="line">(<span class="name"><span class="builtin-name">with-open</span></span> [rdr (<span class="name">io/reader</span> input-file) wtr (<span class="name">io/writer</span> output-file <span class="symbol">:append</span> <span class="literal">true</span>)]</div><div class="line">    (<span class="name"><span class="builtin-name">loop</span></span> [line (<span class="name">.readLine</span> rdr)]</div><div class="line">      (<span class="name"><span class="builtin-name">when</span></span> line</div><div class="line">        (<span class="name"><span class="builtin-name">let</span></span> [pair (<span class="name"><span class="builtin-name">map</span></span> read-string</div><div class="line">                        (<span class="name">str/split</span> line #<span class="string">"\s"</span>))</div><div class="line">              first (<span class="name"><span class="builtin-name">first</span></span> pair)</div><div class="line">              second (<span class="name"><span class="builtin-name">second</span></span> pair)</div><div class="line">              sum (<span class="name"><span class="builtin-name">+</span></span> first second)]</div><div class="line">          (<span class="name">.write</span> wtr (<span class="name"><span class="builtin-name">str</span></span> first <span class="string">" "</span> second <span class="string">" "</span> sum <span class="string">"\n"</span>)))</div><div class="line">        (<span class="name"><span class="builtin-name">recur</span></span> (<span class="name">.readLine</span> rdr))))))</div><div class="line"></div><div class="line">(<span class="name">sum-number-pairs</span> <span class="string">"numbers.txt"</span> <span class="string">"sum-of-numbers.txt"</span>)</div></pre></td></tr></table></figure>
<p>with-open同时打开了一个用于读取、名为<em>input-file</em>的文件以及一个用于写入、名为<em>output-file</em>的文件，写入方式是追加<code>:append true</code>。随后循环读取<em>input-file</em>中的每行内容。若<em>line</em>不是nil（即存在），那么用空格分隔这行内容，得到一个数组，如：”1.3 2.7” -&gt; [“1.3” “2.7”]。此时数组的元素类型还不是数字（Number），我们使用<code>(map read-string )</code>将元素转换为对应的数字类型，如：[“1.3” “2.7”] -&gt; [1.3 2.7]。之后，分别提取数组的第一、二个元素以及两者的和。最后，写入到wtr中。</p>
<hr>
<p><strong>注意</strong>：程序中的<em>str/split</em>是通过<code>(:require [clojure.string :as str])</code>方式引入<em>str</em>命名空间的。</p>
<hr>
<p>运行程序之后，<em>sum-of-numbers.txt</em>中的内容如下：<br><figure class="highlight lsl"><table><tr><td class="gutter"><pre><div class="line">1</div><div class="line">2</div><div class="line">3</div></pre></td><td class="code"><pre><div class="line"><span class="number">1.3</span> <span class="number">2.7</span> <span class="number">4.0</span></div><div class="line"><span class="number">10000</span> <span class="number">1</span> <span class="number">10001</span></div><div class="line"><span class="number">-1</span> <span class="number">1</span> <span class="number">0</span></div></pre></td></tr></table></figure></p>
<h3 id="5-多行记录"><a href="#5-多行记录" class="headerlink" title="5. 多行记录"></a>5. 多行记录</h3><h4 id="5-1-有结束标识"><a href="#5-1-有结束标识" class="headerlink" title="5.1 有结束标识"></a>5.1 有结束标识</h4><p>有时候，记录并不是以一行一行的方式存储在文件当中的，而是以多行数据描述一条记录。比如下面的蛋白质数据：<br><strong>清单 5.1</strong> <em>multimol.pdb</em><br><figure class="highlight lsl"><table><tr><td class="gutter"><pre><div class="line">1</div><div class="line">2</div><div class="line">3</div><div class="line">4</div><div class="line">5</div><div class="line">6</div><div class="line">7</div><div class="line">8</div><div class="line">9</div><div class="line">10</div><div class="line">11</div><div class="line">12</div><div class="line">13</div><div class="line">14</div></pre></td><td class="code"><pre><div class="line">COMPND      AMMONIA</div><div class="line">ATOM      <span class="number">1</span>  N  <span class="number">0.257</span>  <span class="number">-0.363</span>   <span class="number">0.000</span></div><div class="line">ATOM      <span class="number">2</span>  H  <span class="number">0.257</span>   <span class="number">0.727</span>   <span class="number">0.000</span></div><div class="line">ATOM      <span class="number">3</span>  H  <span class="number">0.771</span>  <span class="number">-0.727</span>   <span class="number">0.890</span></div><div class="line">ATOM      <span class="number">4</span>  H  <span class="number">0.771</span>  <span class="number">-0.727</span>  <span class="number">-0.890</span></div><div class="line">END</div><div class="line">COMPND      METHANOL</div><div class="line">ATOM      <span class="number">1</span>  C  <span class="number">-0.748</span>  <span class="number">-0.015</span>   <span class="number">0.024</span></div><div class="line">ATOM      <span class="number">2</span>  O  <span class="number">0.558</span>   <span class="number">0.420</span>  <span class="number">-0.278</span></div><div class="line">ATOM      <span class="number">3</span>  H  <span class="number">-1.293</span>  <span class="number">-0.202</span>  <span class="number">-0.901</span></div><div class="line">ATOM      <span class="number">4</span>  H  <span class="number">-1.263</span>   <span class="number">0.754</span>   <span class="number">0.600</span></div><div class="line">ATOM      <span class="number">5</span>  H  <span class="number">-0.699</span>  <span class="number">-0.934</span>   <span class="number">0.609</span></div><div class="line">ATOM      <span class="number">6</span>  H  <span class="number">0.716</span>   <span class="number">1.404</span>   <span class="number">0.137</span></div><div class="line">END</div></pre></td></tr></table></figure></p>
<p>第一行描述的是分子的名字，接下来到END为止的每行代表原子的ID、类型以及在分子中分布的[x y z]坐标。<br>我们需要一个函数，将数据读取出来并且以规定的格式输出，格式如下：<br><figure class="highlight clojure"><table><tr><td class="gutter"><pre><div class="line">1</div><div class="line">2</div><div class="line">3</div><div class="line">4</div><div class="line">5</div><div class="line">6</div><div class="line">7</div><div class="line">8</div><div class="line">9</div><div class="line">10</div><div class="line">11</div><div class="line">12</div></pre></td><td class="code"><pre><div class="line">((<span class="string">"AMMONIA"</span> </div><div class="line">    (<span class="string">"N"</span> <span class="string">"0.257"</span> <span class="string">"-0.363"</span> <span class="string">"0.000"</span>)</div><div class="line">    (<span class="string">"H"</span> <span class="string">"0.257"</span> <span class="string">"0.727"</span> <span class="string">"0.000"</span>)</div><div class="line">    (<span class="string">"H"</span> <span class="string">"0.771"</span> <span class="string">"-0.727"</span> <span class="string">"0.890"</span>)</div><div class="line">    (<span class="string">"H"</span> <span class="string">"0.771"</span> <span class="string">"-0.727"</span> <span class="string">"-0.890"</span>)) </div><div class="line"> (<span class="string">"METHANOL"</span> </div><div class="line">    (<span class="string">"C"</span> <span class="string">"-0.748"</span> <span class="string">"-0.015"</span> <span class="string">"0.024"</span>)</div><div class="line">    (<span class="string">"O"</span> <span class="string">"0.558"</span> <span class="string">"0.420"</span> <span class="string">"-0.278"</span>)</div><div class="line">    (<span class="string">"H"</span> <span class="string">"-1.293"</span> <span class="string">"-0.202"</span> <span class="string">"-0.901"</span>)</div><div class="line">    (<span class="string">"H"</span> <span class="string">"-1.263"</span> <span class="string">"0.754"</span> <span class="string">"0.600"</span>)</div><div class="line">    (<span class="string">"H"</span> <span class="string">"-0.699"</span> <span class="string">"-0.934"</span> <span class="string">"0.609"</span>)</div><div class="line">    (<span class="string">"H"</span> <span class="string">"0.716"</span> <span class="string">"1.404"</span> <span class="string">"0.137"</span>)))</div></pre></td></tr></table></figure></p>
<p>也就是说，我们需要把每条记录读入单个列表中，每个列表由分子的名称和多个<code>(Type X Y Z)</code>的原子列表组成。<br><figure class="highlight clojure"><table><tr><td class="gutter"><pre><div class="line">1</div><div class="line">2</div><div class="line">3</div><div class="line">4</div><div class="line">5</div><div class="line">6</div><div class="line">7</div><div class="line">8</div><div class="line">9</div><div class="line">10</div><div class="line">11</div></pre></td><td class="code"><pre><div class="line">(<span class="name"><span class="builtin-name">defn</span></span> read-all-molecules [input-file]</div><div class="line">  (<span class="name"><span class="builtin-name">map</span></span> (<span class="name"><span class="builtin-name">fn</span></span> [molecules]</div><div class="line">         (<span class="name"><span class="builtin-name">let</span></span> [[_ name] (<span class="name">str/split</span> (<span class="name"><span class="builtin-name">first</span></span> molecules) #<span class="string">"\s+"</span>)</div><div class="line">               atoms (<span class="name"><span class="builtin-name">map</span></span> (<span class="name"><span class="builtin-name">comp</span></span> #(<span class="name"><span class="builtin-name">drop</span></span> <span class="number">2</span> %) #(<span class="name">str/split</span> % #<span class="string">"\s+"</span>))</div><div class="line">                          (<span class="name"><span class="builtin-name">rest</span></span> molecules))]</div><div class="line">           (<span class="name"><span class="builtin-name">concat</span></span> [name] atoms)))</div><div class="line">       <span class="comment">;; 分割成多条记录</span></div><div class="line">       (<span class="name"><span class="builtin-name">remove</span></span> #(<span class="name"><span class="builtin-name">=</span></span> % [<span class="string">"END"</span>]) </div><div class="line">               (<span class="name">partition-by</span> #(<span class="name"><span class="builtin-name">=</span></span> % <span class="string">"END"</span>) (<span class="name"><span class="builtin-name">line-seq</span></span> (<span class="name">io/reader</span> input-file))))))</div><div class="line"></div><div class="line">(<span class="name">read-all-molecules</span> <span class="string">"multimol.pdb"</span>)</div></pre></td></tr></table></figure></p>
<p><code>(remove #(= % [&quot;END&quot;]) (partition-by #(= % &quot;END&quot;) (line-seq (io/reader input-file))))</code>这行代码做的事情就是把文件读取出来变成一个<code>lazy-seq</code>，然后使用<code>parttition-by</code>以<em>END</em>进行分组，最后使用<code>remove</code>方法剔除掉<em>[“END”]</em>这样的分组，得到如下中间结果：<br><figure class="highlight css"><table><tr><td class="gutter"><pre><div class="line">1</div><div class="line">2</div><div class="line">3</div><div class="line">4</div><div class="line">5</div><div class="line">6</div><div class="line">7</div><div class="line">8</div><div class="line">9</div><div class="line">10</div><div class="line">11</div><div class="line">12</div></pre></td><td class="code"><pre><div class="line">(("<span class="selector-tag">COMPND</span>      <span class="selector-tag">AMMONIA</span>" </div><div class="line">  "<span class="selector-tag">ATOM</span>      1  <span class="selector-tag">N</span>  0<span class="selector-class">.257</span>  <span class="selector-tag">-0</span><span class="selector-class">.363</span>   0<span class="selector-class">.000</span>"</div><div class="line">  "<span class="selector-tag">ATOM</span>      2  <span class="selector-tag">H</span>  0<span class="selector-class">.257</span>   0<span class="selector-class">.727</span>   0<span class="selector-class">.000</span>"</div><div class="line">  "<span class="selector-tag">ATOM</span>      3  <span class="selector-tag">H</span>  0<span class="selector-class">.771</span>  <span class="selector-tag">-0</span><span class="selector-class">.727</span>   0<span class="selector-class">.890</span>"</div><div class="line">  "<span class="selector-tag">ATOM</span>      4  <span class="selector-tag">H</span>  0<span class="selector-class">.771</span>  <span class="selector-tag">-0</span><span class="selector-class">.727</span>  <span class="selector-tag">-0</span><span class="selector-class">.890</span>") </div><div class="line"> ("<span class="selector-tag">COMPND</span>      <span class="selector-tag">METHANOL</span>" </div><div class="line">  "<span class="selector-tag">ATOM</span>      1  <span class="selector-tag">C</span>  <span class="selector-tag">-0</span><span class="selector-class">.748</span>  <span class="selector-tag">-0</span><span class="selector-class">.015</span>   0<span class="selector-class">.024</span>"</div><div class="line">  "<span class="selector-tag">ATOM</span>      2  <span class="selector-tag">O</span>  0<span class="selector-class">.558</span>   0<span class="selector-class">.420</span>  <span class="selector-tag">-0</span><span class="selector-class">.278</span>"</div><div class="line">  "<span class="selector-tag">ATOM</span>      3  <span class="selector-tag">H</span>  <span class="selector-tag">-1</span><span class="selector-class">.293</span>  <span class="selector-tag">-0</span><span class="selector-class">.202</span>  <span class="selector-tag">-0</span><span class="selector-class">.901</span>" </div><div class="line">  "<span class="selector-tag">ATOM</span>      4  <span class="selector-tag">H</span>  <span class="selector-tag">-1</span><span class="selector-class">.263</span>   0<span class="selector-class">.754</span>   0<span class="selector-class">.600</span>"</div><div class="line">  "<span class="selector-tag">ATOM</span>      5  <span class="selector-tag">H</span>  <span class="selector-tag">-0</span><span class="selector-class">.699</span>  <span class="selector-tag">-0</span><span class="selector-class">.934</span>   0<span class="selector-class">.609</span>" </div><div class="line">  "<span class="selector-tag">ATOM</span>      6  <span class="selector-tag">H</span>  0<span class="selector-class">.716</span>   1<span class="selector-class">.404</span>   0<span class="selector-class">.137</span>"))</div></pre></td></tr></table></figure></p>
<p>这样离我们的目标已经很近了。观察上述结果，不难发现分子的名称处于列表的第一个<code>(first )</code>，而原子列表可以使用<code>(rest )</code>获取。然后，借助<code>(map )</code>函数遍历所有的记录。</p>
<p><code>(let )</code>中的第一个binding是<code>[_ name] (str/split (first molecules) #&quot;\s+&quot;)</code>，首先用<code>(split )</code>函数分割，再使用了解构提取出分子的名称；第二个binding是原子列表的提取，我们在<code>(split )</code>的基础之上，使用<code>(drop 2 )</code>函数剔除了不用的字段，如：ATOM和1。最后使用<code>(concat )</code>函数将名称和原子列表的列表拼接到一起。</p>
<h4 id="5-2-无结束标识"><a href="#5-2-无结束标识" class="headerlink" title="5.2 无结束标识"></a>5.2 无结束标识</h4><p>5.1中的记录项通过<em>END</em>标识分隔，但是事实上这是一个多余的字段，记录项可以更简练，如下：<br><strong>清单 5.2</strong> <em>multimol-without-end-marker.pdb</em><br><figure class="highlight"><table><tr><td class="gutter"><pre><div class="line">1</div><div class="line">2</div><div class="line">3</div><div class="line">4</div><div class="line">5</div><div class="line">6</div><div class="line">7</div><div class="line">8</div><div class="line">9</div><div class="line">10</div><div class="line">11</div><div class="line">12</div></pre></td><td class="code"><pre><div class="line">COMPND      AMMONIA</div><div class="line">ATOM      1  N  0.257  -0.363   0.000</div><div class="line">ATOM      2  H  0.257   0.727   0.000</div><div class="line">ATOM      3  H  0.771  -0.727   0.890</div><div class="line">ATOM      4  H  0.771  -0.727  -0.890</div><div class="line">COMPND      METHANOL</div><div class="line">ATOM      1  C  -0.748  -0.015   0.024</div><div class="line">ATOM      2  O  0.558   0.420  -0.278</div><div class="line">ATOM      3  H  -1.293  -0.202  -0.901</div><div class="line">ATOM      4  H  -1.263   0.754   0.600</div><div class="line">ATOM      5  H  -0.699  -0.934   0.609</div><div class="line">ATOM      6  H  0.716   1.404   0.137</div></pre></td></tr></table></figure></p>
<p>现在的问题变成了没有<em>END</em>标识符，如何进行分组？观察不难发现以<em>COMPND</em>开头的数据行可以作为记录的分隔符。<br>使用<code>(partition-by #(.startsWith % &quot;COMPND&quot;) (line-seq (io/reader input-file)))</code>进行分组，得到的结果如下：<br><figure class="highlight clojure"><table><tr><td class="gutter"><pre><div class="line">1</div><div class="line">2</div><div class="line">3</div><div class="line">4</div><div class="line">5</div><div class="line">6</div><div class="line">7</div><div class="line">8</div><div class="line">9</div><div class="line">10</div><div class="line">11</div><div class="line">12</div></pre></td><td class="code"><pre><div class="line">((<span class="string">"COMPND      AMMONIA"</span>) </div><div class="line">  (<span class="string">"ATOM      1  N  0.257  -0.363   0.000"</span> </div><div class="line">   <span class="string">"ATOM      2  H  0.257   0.727   0.000"</span> </div><div class="line">   <span class="string">"ATOM      3  H  0.771  -0.727   0.890"</span></div><div class="line">   <span class="string">"ATOM      4  H  0.771  -0.727  -0.890"</span>) </div><div class="line"> (<span class="string">"COMPND      METHANOL"</span>) </div><div class="line">  (<span class="string">"ATOM      1  C  -0.748  -0.015   0.024"</span> </div><div class="line">   <span class="string">"ATOM      2  O  0.558   0.420  -0.278"</span> </div><div class="line">   <span class="string">"ATOM      3  H  -1.293  -0.202  -0.901"</span></div><div class="line">   <span class="string">"ATOM      4  H  -1.263   0.754   0.600"</span></div><div class="line">   <span class="string">"ATOM      5  H  -0.699  -0.934   0.609"</span></div><div class="line">   <span class="string">"ATOM      6  H  0.716   1.404   0.137"</span>))</div></pre></td></tr></table></figure></p>
<p>此时，我们对比5.1中中间结果，会发现它们极为相似。也就是说，我们稍加转换就能让两者一致，而一致的好处就是可以复用原来<code>(map )</code>中的逻辑。</p>
<p>稍稍修改原来的分组逻辑，如下：<br><figure class="highlight clojure"><table><tr><td class="gutter"><pre><div class="line">1</div><div class="line">2</div><div class="line">3</div><div class="line">4</div><div class="line">5</div></pre></td><td class="code"><pre><div class="line">(<span class="name"><span class="builtin-name">map</span></span> (<span class="name"><span class="builtin-name">fn</span></span> [[name atoms]] (<span class="name"><span class="builtin-name">concat</span></span> name atoms))</div><div class="line">       (<span class="name"><span class="builtin-name">partition</span></span> <span class="number">2</span></div><div class="line">                  (<span class="name">partition-by</span></div><div class="line">                   #(<span class="name">.startsWith</span> % <span class="string">"COMPND"</span>)</div><div class="line">                   (<span class="name"><span class="builtin-name">line-seq</span></span> (<span class="name">io/reader</span> input-file)))))</div></pre></td></tr></table></figure></p>
<p>我们先使用<code>(partition 2 )</code>将第一步得到的列表每隔两个元素划为一组，如下：<br><figure class="highlight css"><table><tr><td class="gutter"><pre><div class="line">1</div><div class="line">2</div><div class="line">3</div><div class="line">4</div><div class="line">5</div><div class="line">6</div><div class="line">7</div><div class="line">8</div><div class="line">9</div><div class="line">10</div><div class="line">11</div><div class="line">12</div></pre></td><td class="code"><pre><div class="line">((("<span class="selector-tag">COMPND</span>      <span class="selector-tag">AMMONIA</span>") </div><div class="line">  ("<span class="selector-tag">ATOM</span>      1  <span class="selector-tag">N</span>  0<span class="selector-class">.257</span>  <span class="selector-tag">-0</span><span class="selector-class">.363</span>   0<span class="selector-class">.000</span>" </div><div class="line">   "<span class="selector-tag">ATOM</span>      2  <span class="selector-tag">H</span>  0<span class="selector-class">.257</span>   0<span class="selector-class">.727</span>   0<span class="selector-class">.000</span>" </div><div class="line">   "<span class="selector-tag">ATOM</span>      3  <span class="selector-tag">H</span>  0<span class="selector-class">.771</span>  <span class="selector-tag">-0</span><span class="selector-class">.727</span>   0<span class="selector-class">.890</span>"</div><div class="line">   "<span class="selector-tag">ATOM</span>      4  <span class="selector-tag">H</span>  0<span class="selector-class">.771</span>  <span class="selector-tag">-0</span><span class="selector-class">.727</span>  <span class="selector-tag">-0</span><span class="selector-class">.890</span>")) ; 多出一对括号</div><div class="line"> (("<span class="selector-tag">COMPND</span>      <span class="selector-tag">METHANOL</span>") </div><div class="line">  ("<span class="selector-tag">ATOM</span>      1  <span class="selector-tag">C</span>  <span class="selector-tag">-0</span><span class="selector-class">.748</span>  <span class="selector-tag">-0</span><span class="selector-class">.015</span>   0<span class="selector-class">.024</span>" </div><div class="line">   "<span class="selector-tag">ATOM</span>      2  <span class="selector-tag">O</span>  0<span class="selector-class">.558</span>   0<span class="selector-class">.420</span>  <span class="selector-tag">-0</span><span class="selector-class">.278</span>" </div><div class="line">   "<span class="selector-tag">ATOM</span>      3  <span class="selector-tag">H</span>  <span class="selector-tag">-1</span><span class="selector-class">.293</span>  <span class="selector-tag">-0</span><span class="selector-class">.202</span>  <span class="selector-tag">-0</span><span class="selector-class">.901</span>"</div><div class="line">   "<span class="selector-tag">ATOM</span>      4  <span class="selector-tag">H</span>  <span class="selector-tag">-1</span><span class="selector-class">.263</span>   0<span class="selector-class">.754</span>   0<span class="selector-class">.600</span>"</div><div class="line">   "<span class="selector-tag">ATOM</span>      5  <span class="selector-tag">H</span>  <span class="selector-tag">-0</span><span class="selector-class">.699</span>  <span class="selector-tag">-0</span><span class="selector-class">.934</span>   0<span class="selector-class">.609</span>"</div><div class="line">   "<span class="selector-tag">ATOM</span>      6  <span class="selector-tag">H</span>  0<span class="selector-class">.716</span>   1<span class="selector-class">.404</span>   0<span class="selector-class">.137</span>")))</div></pre></td></tr></table></figure></p>
<p>然后使用<code>(map (fn [[name atoms]] ...)</code>将每组里面的两个列表合成为一个列表，这样就得到和原来5.1一模一样的中间结果。</p>
<p>接下来，我们把转换的逻辑从<code>(read-all-molecules )</code>中提取出来，以便复用。改造如下：<br><figure class="highlight clojure"><table><tr><td class="gutter"><pre><div class="line">1</div><div class="line">2</div><div class="line">3</div><div class="line">4</div><div class="line">5</div><div class="line">6</div><div class="line">7</div></pre></td><td class="code"><pre><div class="line">(<span class="name"><span class="builtin-name">defn</span></span> read-all-molecules [f input-file]</div><div class="line">  (<span class="name"><span class="builtin-name">let</span></span> [data (<span class="name">f</span> input-file)]</div><div class="line">    (<span class="name"><span class="builtin-name">map</span></span> (<span class="name"><span class="builtin-name">fn</span></span> [molecules]</div><div class="line">           (<span class="name"><span class="builtin-name">let</span></span> [[_ name] (<span class="name">str/split</span> (<span class="name"><span class="builtin-name">first</span></span> molecules) #<span class="string">"\s+"</span>)</div><div class="line">                 atoms (<span class="name"><span class="builtin-name">map</span></span> (<span class="name"><span class="builtin-name">comp</span></span> #(<span class="name"><span class="builtin-name">drop</span></span> <span class="number">2</span> %) #(<span class="name">str/split</span> % #<span class="string">"\s+"</span>))</div><div class="line">                            (<span class="name"><span class="builtin-name">rest</span></span> molecules))]</div><div class="line">             (<span class="name"><span class="builtin-name">concat</span></span> [name] atoms))) data)))</div></pre></td></tr></table></figure></p>
<p>定义转换逻辑，如下：<br><figure class="highlight clojure"><table><tr><td class="gutter"><pre><div class="line">1</div><div class="line">2</div><div class="line">3</div><div class="line">4</div><div class="line">5</div><div class="line">6</div></pre></td><td class="code"><pre><div class="line">(<span class="name"><span class="builtin-name">defn</span></span> file-without-markers-&gt;multi-records [input-file]</div><div class="line">  (<span class="name"><span class="builtin-name">map</span></span> (<span class="name"><span class="builtin-name">fn</span></span> [[name atoms]] (<span class="name"><span class="builtin-name">concat</span></span> name atoms))</div><div class="line">       (<span class="name"><span class="builtin-name">partition</span></span> <span class="number">2</span></div><div class="line">                  (<span class="name">partition-by</span></div><div class="line">                   #(<span class="name">.startsWith</span> % <span class="string">"COMPND"</span>)</div><div class="line">                   (<span class="name"><span class="builtin-name">line-seq</span></span> (<span class="name">io/reader</span> input-file))))))</div></pre></td></tr></table></figure></p>
<p>最后，我们来调用的改造之后的方法：<br><figure class="highlight clojure"><table><tr><td class="gutter"><pre><div class="line">1</div><div class="line">2</div></pre></td><td class="code"><pre><div class="line">(<span class="name">read-all-molecules</span> </div><div class="line">        file-without-markers-&gt;multi-records <span class="string">"multimol-without-end-marker.pdb"</span>)</div></pre></td></tr></table></figure></p>
<p>此时，5.1中的转换逻辑也可以提取出一个函数：<br><figure class="highlight clojure"><table><tr><td class="gutter"><pre><div class="line">1</div><div class="line">2</div><div class="line">3</div></pre></td><td class="code"><pre><div class="line">(<span class="name"><span class="builtin-name">defn</span></span> file-&gt;multi-records</div><div class="line">  (<span class="name"><span class="builtin-name">remove</span></span> #(<span class="name"><span class="builtin-name">=</span></span> % [<span class="string">"END"</span>]) </div><div class="line">               (<span class="name">partition-by</span> #(<span class="name"><span class="builtin-name">=</span></span> % <span class="string">"END"</span>) (<span class="name"><span class="builtin-name">line-seq</span></span> (<span class="name">io/reader</span> input-file)))))</div></pre></td></tr></table></figure></p>
<p>原来的程序就重构成了如下的模样：<br><figure class="highlight clojure"><table><tr><td class="gutter"><pre><div class="line">1</div></pre></td><td class="code"><pre><div class="line">(<span class="name">read-all-molecules</span> file-&gt;multi-records <span class="string">"multimol.pdb"</span>)</div></pre></td></tr></table></figure></p>
<hr>
<p><strong>备注</strong></p>
<p>为了清楚定位这个问题，我们需要提前了解两个知识点</p>
<ol>
<li>什么是惰性序列？</li>
<li>惰性序列在repl中什么时候变现（realizes）？</li>
</ol>
<p>惰性序列是用<code>(lazy-seq [&amp; body] )</code>宏创建出来的。<code>lazy-seq</code>仅在需要的时候才会去调用它的body。<br>当repl尝试<code>pretty-print</code>惰性序列的结果时，才会进行变现操作。</p>
<p>有了上面的知识点，我们来考察<code>with-open</code>和<code>(take 2 (line-seq ))</code>的关系。<code>with-open</code>是宏，我们使用<code>clojure.walk/macroexpand-all</code>展开下：<br><figure class="highlight"><table><tr><td class="gutter"><pre><div class="line">1</div><div class="line">2</div><div class="line">3</div><div class="line">4</div><div class="line">5</div><div class="line">6</div><div class="line">7</div><div class="line">8</div></pre></td><td class="code"><pre><div class="line">(clojure.walk/macroexpand-all </div><div class="line">    '(with-open [rdr (io/reader data-file)]</div><div class="line">                      (take 2 (line-seq rdr))))</div><div class="line">                      </div><div class="line">-&gt; (let* [rdr (io/reader data-file)] </div><div class="line">        (try (do </div><div class="line">                (take 2 (line-seq rdr))) </div><div class="line">        (finally (. rdr clojure.core/close))))</div></pre></td></tr></table></figure></p>
<p>使用<code>(doc line-seq)</code>查看文档，得到<br><figure class="highlight livecodeserver"><table><tr><td class="gutter"><pre><div class="line">1</div><div class="line">2</div><div class="line">3</div><div class="line">4</div><div class="line">5</div></pre></td><td class="code"><pre><div class="line">clojure.core/<span class="built_in">line</span>-seq</div><div class="line"> [rdr]</div><div class="line">Added <span class="keyword">in</span> <span class="number">1.0</span></div><div class="line">  Returns <span class="keyword">the</span> <span class="keyword">lines</span> <span class="keyword">of</span> <span class="keyword">text</span> <span class="built_in">from</span> rdr <span class="keyword">as</span> <span class="keyword">a</span> lazy sequence <span class="keyword">of</span> strings.</div><div class="line">  rdr must implement java.io.BufferedReader.</div></pre></td></tr></table></figure></p>
<p>可以确认<code>line-seq</code>返回一个惰性的字符串序列。<br>再看看<code>(doc take)</code>的文档，得到<br><figure class="highlight livecodeserver"><table><tr><td class="gutter"><pre><div class="line">1</div><div class="line">2</div><div class="line">3</div><div class="line">4</div><div class="line">5</div><div class="line">6</div><div class="line">7</div></pre></td><td class="code"><pre><div class="line">clojure.core/take</div><div class="line"> [n]</div><div class="line"> [n coll]</div><div class="line">Added <span class="keyword">in</span> <span class="number">1.0</span></div><div class="line">  Returns <span class="keyword">a</span> lazy sequence <span class="keyword">of</span> <span class="keyword">the</span> <span class="keyword">first</span> n <span class="keyword">items</span> <span class="keyword">in</span> coll, <span class="keyword">or</span> all <span class="keyword">items</span> <span class="keyword">if</span></div><div class="line">  there are fewer than n.  Returns <span class="keyword">a</span> stateful transducer when</div><div class="line">  no collection is provided.</div></pre></td></tr></table></figure></p>
<p>所以<code>take</code>返回的也是一个惰性序列，那么<code>(do (take 2 (line-seq rdr)))</code>（等价于<code>(take 2 (line-seq rdr))</code>）整个返回的就是一个惰性序列。</p>
<p>当我们通过repl求值<code>with-open</code>时，它并没有真的变现<code>(take 2 (line-seq rdr))</code>，而是在运行完<code>try...finally</code>之后，直接返回这个惰性序列作为结果。此时，repl开始尝试pretty-print <code>(take 2 (line-seq rdr))</code>，变现发生，但是rdr已经被关闭了，所以抛出<em>Stream closed</em>异常。</p>
<p>到这里，解决了一大半问题，但是还有一个逻辑上解释不过去的点，就是<br><figure class="highlight clojure"><table><tr><td class="gutter"><pre><div class="line">1</div><div class="line">2</div></pre></td><td class="code"><pre><div class="line">(<span class="name"><span class="builtin-name">with-open</span></span> [rdr (<span class="name">io/reader</span> data-file)]</div><div class="line">  (<span class="name"><span class="builtin-name">take</span></span> <span class="number">1</span> (<span class="name"><span class="builtin-name">line-seq</span></span> rdr)))</div></pre></td></tr></table></figure></p>
<p>当我们尝试<code>(take 1 )</code>时并不会抛出异常！也就是说<code>(take 1 )</code>和<code>(take 2 )</code>的行为不同，但是<code>(take )</code>明明都是返回惰性序列啊？</p>
<p>带着这个疑惑，看看<code>line-seq</code>的源代码<br><figure class="highlight scheme"><table><tr><td class="gutter"><pre><div class="line">1</div><div class="line">2</div></pre></td><td class="code"><pre><div class="line">(<span class="name">when-let</span> [<span class="name">line</span> (<span class="name">.readLine</span> rdr)]</div><div class="line">    (<span class="name"><span class="builtin-name">cons</span></span> line (<span class="name">lazy-seq</span> (<span class="name">line-seq</span> rdr)))))</div></pre></td></tr></table></figure></p>
<p>是不是有种豁然开朗的感觉？没有也没关系，我来解释一下。<br><code>line-seq</code>的<code>when-let</code>语句并没有包在<code>(lazy-seq )</code>（这点可以和<code>take</code>的源码比较）中，这说明<code>[line (.readline rdr)]</code>是需要立即求值的。也就是说，我们在求值<code>with-open</code>时，rdr中第一行的内容会被<code>(line-seq )</code>给抓住了。那么当<code>try...finally</code>运行结束之后，pretty-print变现惰性序列时，发现第一行根本不需要从rdr中读，当然就不会抛出异常了。</p>
<p>明确这几点之后，我们看看<code>(doall )</code>为何能解决惰性序列延迟求值的问题？<code>(doall )</code>其实强制变现了整个惰性序列（不断调用序列的<code>next</code>方法），所以并不会等到<code>with-open</code>求值完成之后才求值。</p>
<p>换个角度，我们知道之所以抛出异常，是因为repl对返回的惰性序列求值了。那么如果我们不在repl中求值，程序还会抛出异常吗？</p>
<figure class="highlight clojure"><table><tr><td class="gutter"><pre><div class="line">1</div><div class="line">2</div><div class="line">3</div><div class="line">4</div><div class="line">5</div><div class="line">6</div><div class="line">7</div></pre></td><td class="code"><pre><div class="line">(<span class="name"><span class="builtin-name">ns</span></span> the-way-to-clojure.core</div><div class="line">  (<span class="symbol">:require</span> [clojure.java.io <span class="symbol">:as</span> io])</div><div class="line">  (<span class="symbol">:gen-class</span>))</div><div class="line"></div><div class="line">(<span class="name"><span class="builtin-name">defn</span></span> -main [&amp; args]</div><div class="line">  (<span class="name"><span class="builtin-name">with-open</span></span> [rdr (<span class="name">io/reader</span> <span class="string">"hello.txt"</span>)]</div><div class="line">    (<span class="name"><span class="builtin-name">take</span></span> <span class="number">100</span> (<span class="name"><span class="builtin-name">line-seq</span></span> rdr))))</div></pre></td></tr></table></figure>
<p>接着，我们使用<code>lein run</code>来运行<code>main</code>方法。程序运行良好，因为根本没有人用到返回的惰性序列。</p>
<p>如果我们加一句打印语句如下：<br><figure class="highlight clojure"><table><tr><td class="gutter"><pre><div class="line">1</div><div class="line">2</div><div class="line">3</div><div class="line">4</div></pre></td><td class="code"><pre><div class="line">(<span class="name"><span class="builtin-name">defn</span></span> -main [&amp; args]</div><div class="line">  (<span class="name">println</span>          <span class="comment">; 变现</span></div><div class="line">      (<span class="name"><span class="builtin-name">with-open</span></span> [rdr (<span class="name">io/reader</span> <span class="string">"hello.txt"</span>)]</div><div class="line">        (<span class="name"><span class="builtin-name">take</span></span> <span class="number">100</span> (<span class="name"><span class="builtin-name">line-seq</span></span> rdr)))))</div></pre></td></tr></table></figure></p>
<p>再用<code>lein run</code>跑一个<code>main</code>方法，异常又不期而遇了。因为此处的<code>println</code>等价于<code>repl</code>的<code>pretty print</code>。</p>
]]></content>
    
    <summary type="html">
    
      &lt;h2 id=&quot;读取和写入文件&quot;&gt;&lt;a href=&quot;#读取和写入文件&quot; class=&quot;headerlink&quot; title=&quot;读取和写入文件&quot;&gt;&lt;/a&gt;读取和写入文件&lt;/h2&gt;&lt;p&gt;数据一般都是存储在纯文本文件当中，存储的形式多种多样。本文，我会介绍如何在Clojure中读取和写入
    
    </summary>
    
      <category term="Clojure" scheme="https://lameta.com/categories/Clojure/"/>
    
    
      <category term="file" scheme="https://lameta.com/tags/file/"/>
    
  </entry>
  
  <entry>
    <title>SonarQube Plugin Step by Step</title>
    <link href="https://lameta.com/2016/06/24/SonarQube-Plugin-Step-by-Step/"/>
    <id>https://lameta.com/2016/06/24/SonarQube-Plugin-Step-by-Step/</id>
    <published>2016-06-23T16:40:23.000Z</published>
    <updated>2016-09-06T14:59:18.000Z</updated>
    
    <content type="html"><![CDATA[<h2 id="插件不好写？！"><a href="#插件不好写？！" class="headerlink" title="插件不好写？！"></a>插件不好写？！</h2><p>插件确实不好写，因为插件是插入庞大的系统当中工作的，那也就意味着写插件需要具备一定的领域知识，包括系统架构、扩展点、业务共性及差异、API及其业务模型对应、安装和测试。而对于开发者而言，学习这些知识的代价绝对是昂贵的。<br>在《函数式编程思想》一书中，作者Neal Ford提到开发过程当中的两种抽象方式——composable and contextual abstract. 谈及contextual抽象的时候，他把插件系统列为这一抽象中最经典的例子。</p>
<blockquote>
<p>Plugin-based architectures are excellent examples of the contextual abstraction. The plug-in API provides a plethora of data structures and other useful context that developers inherit from or summon via already existing methods. But to use the API, a developer must understand what that context provides, and that understanding is sometimes expensive. </p>
</blockquote>
<p>大意是开发者能够借助已存在的方法来使用Plugin API中提供的大量数据结构和有用的上下文信息。但是，理解起这些上下文信息有时是很昂贵的。</p>
<p>基于一个共识：开发者的时间都是宝贵的。知道插件难写之后，我的这篇文章才有价值。</p>
<h2 id="理解领域模型"><a href="#理解领域模型" class="headerlink" title="理解领域模型"></a>理解领域模型</h2><p>一说写插件，估计大家都会上官网寻找开发指南或者google大量博客来快速完成开发任务。这里不是说这种方式不好，其实一开始我也是这么做的，但是着手开发以后，很快就遭遇处处掣肘。比如：开发sonar plugin，会用到<em>Profile、Rule、Language</em>和<em>Repository</em>等概念。单从代码层面上看，我们很难理清这些概念所代表的模型和它们之间的<strong>关系</strong>。所以需要从用户的视角来<strong>感受</strong>这些领域知识。</p>
<p>而用户视角大部分情况下就是UI界面。</p>
<h3 id="规则（Rules）"><a href="#规则（Rules）" class="headerlink" title="规则（Rules）"></a>规则（Rules）</h3><p>我们先看看<em>Rules</em>导航栏，左边的单选框是这些规则的过滤条件。<br>说明规则包含或者被包含这些属性之下：</p>
<p><img src="http://upload-images.jianshu.io/upload_images/217988-a6ad47ad581e7fc4.png?imageMogr2/auto-orient/strip%7CimageView2/2/w/1240" alt="Rules"></p>
<ul>
<li>Language：规则对应的某种编程语言。</li>
<li>Type：规则的类型，比如：缺陷（Bug）、代码坏味道（Code Smell）、易受攻击（Vulnerability）。</li>
<li>Tag：规则设置的标签，易于检索。</li>
<li>Repository：承载特定语言下各种规则的容器；通过它可以通过规则的键值（ruleKey）检索。</li>
<li>Default Severity：触犯规则的严重程度。<ul>
<li>Blocker：最高等级，阻碍的</li>
<li>Critical：高等级，极为严重的</li>
<li>Major：较高等级，主要的；默认级别。</li>
<li>Minor：较低等级</li>
<li>Info：低等级</li>
</ul>
</li>
<li>Status：规则现在的状态，可用、废弃还是实验版（Beta）。</li>
<li>Avaiable Since：什么时候开始可用。</li>
<li>Template：规则模板：比如某些参数可以运行时传入。</li>
<li>Quality Profile：挑选特定语言下各种规则组成的配置；其中可以启用或禁用一部分规则。</li>
</ul>
<h3 id="质量Profile（Quality-Profile）"><a href="#质量Profile（Quality-Profile）" class="headerlink" title="质量Profile（Quality Profile）"></a>质量Profile（Quality Profile）</h3><p>再看看<em>Quality Profiles</em>导航栏，左侧栏显示的是某种语言包含的所有Profiles.</p>
<p><img src="http://upload-images.jianshu.io/upload_images/217988-7b8bbbf54b9462dd.png?imageMogr2/auto-orient/strip%7CimageView2/2/w/1240" alt="Profiles"></p>
<p>从关系型数据库的角度，Language和Profile是1对多（one-to-many）关系，但是从领域建模的角度，Profile其实和Language是1对1的关系。所以可以是Profile包含Language属性。利用领域建模的思考方式，可以联想到Repository和Rules是1对多的关系，所以Repository包含一个Rules的集合。Repository和Language是1对1的关系，Repository包含Language属性。那么Rules和Profiles的对应关系呢？多对多。但是我们更关心Profile到Rules这一层的关系，所以选择Profile包含一个Rules的集合。</p>
<p>我整理出这样一份对应关系图：<br><figure class="highlight plain"><table><tr><td class="gutter"><pre><div class="line">1</div><div class="line">2</div><div class="line">3</div><div class="line">4</div><div class="line">5</div><div class="line">6</div></pre></td><td class="code"><pre><div class="line">profile</div><div class="line">    - language</div><div class="line">    - [rules]</div><div class="line">respository</div><div class="line">    - lanuage</div><div class="line">    - [rules]</div></pre></td></tr></table></figure></p>
<p>现在，缺少Profile和Repository的关系。不过既然有了Rule这一层联系，那么就可以这样考虑，Rule和Repository是1对1的关系（为什么呢？因为每个Rule显然只能存在于一个特定的Repository当中）。所以原图可以修改为：<br><figure class="highlight plain"><table><tr><td class="gutter"><pre><div class="line">1</div><div class="line">2</div><div class="line">3</div><div class="line">4</div><div class="line">5</div><div class="line">6</div><div class="line">7</div><div class="line">8</div></pre></td><td class="code"><pre><div class="line">profile</div><div class="line">    - language</div><div class="line">    - [rules]</div><div class="line">      - rule</div><div class="line">        - respository</div><div class="line">respository</div><div class="line">    - language</div><div class="line">    - [rules]</div></pre></td></tr></table></figure></p>
<p>好了。梳理完这些领域知识，我们可以开始依照官方的教程<a href="http://docs.sonarqube.org/display/DEV/Developing+a+Plugin" target="_blank" rel="external">Developing a Plugin</a>.</p>
<h2 id="扫描特定领域语言（DSL）的SonarQube插件"><a href="#扫描特定领域语言（DSL）的SonarQube插件" class="headerlink" title="扫描特定领域语言（DSL）的SonarQube插件"></a>扫描特定领域语言（DSL）的SonarQube插件</h2><p>SonarQube 5.6现在只支持Java 8、Maven 3.1以上。当然也支持Gradle。</p>
<h3 id="第一步-创建一个Maven工程"><a href="#第一步-创建一个Maven工程" class="headerlink" title="第一步 创建一个Maven工程"></a>第一步 创建一个Maven工程</h3><p>这里有两种方式。第一种方式就是从头开始写起，包括创建工程；另一种就是拷贝官方的<a href="https://github.com/SonarSource/sonar-examples" target="_blank" rel="external">样例程序</a>。我自然是推荐第二种做法，不过这里我从零开始开发。</p>
<figure class="highlight plain"><table><tr><td class="gutter"><pre><div class="line">1</div></pre></td><td class="code"><pre><div class="line">$ mvn archetype:create -DgroupId=com.lambeta -DartifactId=sonar-lambeta -DarchetypeArtifactId=maven-archetype-quickstart</div></pre></td></tr></table></figure>
<p>依照官方文档将pom.xml修改如下：<br><figure class="highlight xml"><table><tr><td class="gutter"><pre><div class="line">1</div><div class="line">2</div><div class="line">3</div><div class="line">4</div><div class="line">5</div><div class="line">6</div><div class="line">7</div><div class="line">8</div><div class="line">9</div><div class="line">10</div><div class="line">11</div><div class="line">12</div><div class="line">13</div><div class="line">14</div><div class="line">15</div><div class="line">16</div><div class="line">17</div><div class="line">18</div><div class="line">19</div><div class="line">20</div><div class="line">21</div><div class="line">22</div><div class="line">23</div><div class="line">24</div><div class="line">25</div><div class="line">26</div><div class="line">27</div><div class="line">28</div><div class="line">29</div><div class="line">30</div><div class="line">31</div><div class="line">32</div><div class="line">33</div><div class="line">34</div><div class="line">35</div><div class="line">36</div><div class="line">37</div><div class="line">38</div><div class="line">39</div><div class="line">40</div><div class="line">41</div><div class="line">42</div><div class="line">43</div><div class="line">44</div><div class="line">45</div><div class="line">46</div><div class="line">47</div><div class="line">48</div><div class="line">49</div><div class="line">50</div><div class="line">51</div><div class="line">52</div><div class="line">53</div><div class="line">54</div><div class="line">55</div><div class="line">56</div><div class="line">57</div><div class="line">58</div></pre></td><td class="code"><pre><div class="line"><span class="tag">&lt;<span class="name">project</span> <span class="attr">xmlns</span>=<span class="string">"http://maven.apache.org/POM/4.0.0"</span> <span class="attr">xmlns:xsi</span>=<span class="string">"http://www.w3.org/2001/XMLSchema-instance"</span></span></div><div class="line">    <span class="attr">xsi:schemaLocation</span>=<span class="string">"http://maven.apache.org/POM/4.0.0 http://maven.apache.org/xsd/maven-4.0.0.xsd"</span>&gt;</div><div class="line">    <span class="tag">&lt;<span class="name">modelVersion</span>&gt;</span>4.0.0<span class="tag">&lt;/<span class="name">modelVersion</span>&gt;</span></div><div class="line"></div><div class="line">    <span class="tag">&lt;<span class="name">groupId</span>&gt;</span>com.lambeta<span class="tag">&lt;/<span class="name">groupId</span>&gt;</span></div><div class="line">    <span class="tag">&lt;<span class="name">artifactId</span>&gt;</span>sonar-custom<span class="tag">&lt;/<span class="name">artifactId</span>&gt;</span></div><div class="line">    <span class="tag">&lt;<span class="name">version</span>&gt;</span>1.0-SNAPSHOT<span class="tag">&lt;/<span class="name">version</span>&gt;</span></div><div class="line">    <span class="tag">&lt;<span class="name">packaging</span>&gt;</span>sonar-plugin<span class="tag">&lt;/<span class="name">packaging</span>&gt;</span></div><div class="line"></div><div class="line">    <span class="tag">&lt;<span class="name">name</span>&gt;</span>sonar-custom<span class="tag">&lt;/<span class="name">name</span>&gt;</span></div><div class="line">    <span class="tag">&lt;<span class="name">url</span>&gt;</span>https://www.lambeta.com<span class="tag">&lt;/<span class="name">url</span>&gt;</span></div><div class="line"></div><div class="line">    <span class="tag">&lt;<span class="name">properties</span>&gt;</span></div><div class="line">        <span class="tag">&lt;<span class="name">project.build.sourceEncoding</span>&gt;</span>UTF-8<span class="tag">&lt;/<span class="name">project.build.sourceEncoding</span>&gt;</span></div><div class="line">    <span class="tag">&lt;/<span class="name">properties</span>&gt;</span></div><div class="line"></div><div class="line">    <span class="tag">&lt;<span class="name">dependencies</span>&gt;</span></div><div class="line">        <span class="tag">&lt;<span class="name">dependency</span>&gt;</span></div><div class="line">            <span class="tag">&lt;<span class="name">groupId</span>&gt;</span>org.sonarsource.sonarqube<span class="tag">&lt;/<span class="name">groupId</span>&gt;</span></div><div class="line">            <span class="tag">&lt;<span class="name">artifactId</span>&gt;</span>sonar-plugin-api<span class="tag">&lt;/<span class="name">artifactId</span>&gt;</span></div><div class="line">            <span class="comment">&lt;!-- minimal version of SonarQube to support. Note that the groupId was "org.codehaus.sonar" before version 5.2 --&gt;</span></div><div class="line">            <span class="tag">&lt;<span class="name">version</span>&gt;</span>5.6<span class="tag">&lt;/<span class="name">version</span>&gt;</span></div><div class="line">            <span class="comment">&lt;!-- mandatory scope --&gt;</span></div><div class="line">            <span class="tag">&lt;<span class="name">scope</span>&gt;</span>provided<span class="tag">&lt;/<span class="name">scope</span>&gt;</span></div><div class="line">        <span class="tag">&lt;/<span class="name">dependency</span>&gt;</span></div><div class="line">        <span class="tag">&lt;<span class="name">dependency</span>&gt;</span></div><div class="line">            <span class="tag">&lt;<span class="name">groupId</span>&gt;</span>net.sourceforge.pmd<span class="tag">&lt;/<span class="name">groupId</span>&gt;</span></div><div class="line">            <span class="tag">&lt;<span class="name">artifactId</span>&gt;</span>pmd-xml<span class="tag">&lt;/<span class="name">artifactId</span>&gt;</span></div><div class="line">            <span class="tag">&lt;<span class="name">version</span>&gt;</span>5.4.2<span class="tag">&lt;/<span class="name">version</span>&gt;</span></div><div class="line">        <span class="tag">&lt;/<span class="name">dependency</span>&gt;</span></div><div class="line">        <span class="tag">&lt;<span class="name">dependency</span>&gt;</span></div><div class="line">            <span class="tag">&lt;<span class="name">groupId</span>&gt;</span>dom4j<span class="tag">&lt;/<span class="name">groupId</span>&gt;</span></div><div class="line">            <span class="tag">&lt;<span class="name">artifactId</span>&gt;</span>dom4j<span class="tag">&lt;/<span class="name">artifactId</span>&gt;</span></div><div class="line">            <span class="tag">&lt;<span class="name">version</span>&gt;</span>1.6.1<span class="tag">&lt;/<span class="name">version</span>&gt;</span></div><div class="line">        <span class="tag">&lt;/<span class="name">dependency</span>&gt;</span></div><div class="line">        <span class="tag">&lt;<span class="name">dependency</span>&gt;</span></div><div class="line">            <span class="tag">&lt;<span class="name">groupId</span>&gt;</span>junit<span class="tag">&lt;/<span class="name">groupId</span>&gt;</span></div><div class="line">            <span class="tag">&lt;<span class="name">artifactId</span>&gt;</span>junit<span class="tag">&lt;/<span class="name">artifactId</span>&gt;</span></div><div class="line">            <span class="tag">&lt;<span class="name">version</span>&gt;</span>3.8.1<span class="tag">&lt;/<span class="name">version</span>&gt;</span></div><div class="line">            <span class="tag">&lt;<span class="name">scope</span>&gt;</span>test<span class="tag">&lt;/<span class="name">scope</span>&gt;</span></div><div class="line">        <span class="tag">&lt;/<span class="name">dependency</span>&gt;</span></div><div class="line">    <span class="tag">&lt;/<span class="name">dependencies</span>&gt;</span></div><div class="line"></div><div class="line">    <span class="tag">&lt;<span class="name">build</span>&gt;</span></div><div class="line">        <span class="tag">&lt;<span class="name">plugins</span>&gt;</span></div><div class="line">            <span class="tag">&lt;<span class="name">plugin</span>&gt;</span></div><div class="line">                <span class="tag">&lt;<span class="name">groupId</span>&gt;</span>org.sonarsource.sonar-packaging-maven-plugin<span class="tag">&lt;/<span class="name">groupId</span>&gt;</span></div><div class="line">                <span class="tag">&lt;<span class="name">artifactId</span>&gt;</span>sonar-packaging-maven-plugin<span class="tag">&lt;/<span class="name">artifactId</span>&gt;</span></div><div class="line">                <span class="tag">&lt;<span class="name">version</span>&gt;</span>1.16<span class="tag">&lt;/<span class="name">version</span>&gt;</span></div><div class="line">                <span class="tag">&lt;<span class="name">extensions</span>&gt;</span>true<span class="tag">&lt;/<span class="name">extensions</span>&gt;</span></div><div class="line">                <span class="tag">&lt;<span class="name">configuration</span>&gt;</span></div><div class="line">                    <span class="tag">&lt;<span class="name">pluginClass</span>&gt;</span>com.lambeta.CustomPlugin<span class="tag">&lt;/<span class="name">pluginClass</span>&gt;</span></div><div class="line">                    <span class="tag">&lt;<span class="name">pluginDescription</span>&gt;</span>how to write sonar plugin<span class="tag">&lt;/<span class="name">pluginDescription</span>&gt;</span></div><div class="line">                <span class="tag">&lt;/<span class="name">configuration</span>&gt;</span></div><div class="line">            <span class="tag">&lt;/<span class="name">plugin</span>&gt;</span></div><div class="line">        <span class="tag">&lt;/<span class="name">plugins</span>&gt;</span></div><div class="line">    <span class="tag">&lt;/<span class="name">build</span>&gt;</span></div><div class="line"><span class="tag">&lt;/<span class="name">project</span>&gt;</span></div></pre></td></tr></table></figure></p>
<blockquote>
<p><strong>注意：</strong> pmd-xml、dom4j会在后面的编程当中使用到。</p>
</blockquote>
<p>依据标准的代码结构，新建<em>CustomPlugin.java</em>文件。<br><figure class="highlight plain"><table><tr><td class="gutter"><pre><div class="line">1</div><div class="line">2</div><div class="line">3</div><div class="line">4</div><div class="line">5</div><div class="line">6</div></pre></td><td class="code"><pre><div class="line">├── src</div><div class="line">│   ├── main</div><div class="line">│   │   ├── java</div><div class="line">│   │   │   └── com</div><div class="line">│   │   │       └── lambeta</div><div class="line">│   │   │           ├── CustomPlugin.java</div></pre></td></tr></table></figure></p>
<h3 id="第二步-识别扩展点"><a href="#第二步-识别扩展点" class="headerlink" title="第二步 识别扩展点"></a>第二步 识别扩展点</h3><p>此时，该去查看<a href="http://docs.sonarqube.org/display/DEV/API+Basics" target="_blank" rel="external">API Basics</a>了。不过在写代码之前，还得先了解所谓的扩展点（Extension Points）。</p>
<blockquote>
<p>Scanner, which runs the source code analysis<br>Compute Engine, which consolidates the output of scanners, for example by<br>computing 2nd-level measures such as ratings<br>aggregating measures (for example number of lines of code of project = sum of lines of code of all files)<br>assigning new issues to developers<br>persisting everything in data stores<br>Web application</p>
</blockquote>
<p>翻译如下</p>
<ul>
<li>扫描器：分析源代码</li>
<li>计算引擎：聚合扫描器的输出。举例：计算第二轮measures，如打分；聚合measures（举例：工程中所有代码的行数 = 所有文件的代码行的综合）；给开发者安排新的问题；持久化。</li>
<li>Web应用程序。<br>翻译还不如不翻译！一言不合，去看例子程序…的<a href="https://github.com/SonarSource/sonar-examples/blob/master/plugins/sonar-example-plugin/src/main/java/org/sonarsource/plugins/example/ExamplePlugin.java" target="_blank" rel="external">注释</a></li>
</ul>
<p>这三个扩展点，其实对应于API中的三个接口。<br><figure class="highlight clean"><table><tr><td class="gutter"><pre><div class="line">1</div><div class="line">2</div><div class="line">3</div></pre></td><td class="code"><pre><div class="line">扫描器 -&gt; Sensor</div><div class="line">计算引擎 -&gt; MeasureComputer</div><div class="line">Web应用程序 -&gt; Widget</div></pre></td></tr></table></figure></p>
<h3 id="第三步-定义Sensor（Scanner"><a href="#第三步-定义Sensor（Scanner" class="headerlink" title="第三步 定义Sensor（Scanner)"></a>第三步 定义Sensor（Scanner)</h3><p>基于扫描DSL源码的需求，我们需要扩展Sensor这个接口。新建<em>CustomSensor.java</em>如下：<br><figure class="highlight java"><table><tr><td class="gutter"><pre><div class="line">1</div><div class="line">2</div><div class="line">3</div><div class="line">4</div><div class="line">5</div><div class="line">6</div></pre></td><td class="code"><pre><div class="line"><span class="function"><span class="keyword">public</span> class CustomSensor implements Sensor</span></div><div class="line"></div><div class="line">    <span class="keyword">public</span> <span class="keyword">void</span> <span class="title">describe</span><span class="params">(SensorDescriptor descriptor)</span> </div><div class="line">    ...</div><div class="line">    <span class="keyword">public</span> <span class="keyword">void</span> <span class="title">execute</span><span class="params">(SensorContext context)</span></div><div class="line">    ...</div></pre></td></tr></table></figure></p>
<p>接下来，我们需要定义这门DSL语言的某些属性，以便于识别以及扫描时过滤相关的源文件（通过文件的后缀）。</p>
<h3 id="第四步-定义语言（Language）"><a href="#第四步-定义语言（Language）" class="headerlink" title="第四步 定义语言（Language）"></a>第四步 定义语言（Language）</h3><p>新建<em>CustomLanguage</em>如下：<br><figure class="highlight java"><table><tr><td class="gutter"><pre><div class="line">1</div><div class="line">2</div><div class="line">3</div><div class="line">4</div><div class="line">5</div><div class="line">6</div><div class="line">7</div><div class="line">8</div><div class="line">9</div><div class="line">10</div><div class="line">11</div><div class="line">12</div><div class="line">13</div><div class="line">14</div><div class="line">15</div></pre></td><td class="code"><pre><div class="line"><span class="keyword">package</span> com.lambeta;</div><div class="line"><span class="keyword">import</span> org.sonar.api.resources.AbstractLanguage;</div><div class="line"></div><div class="line"><span class="keyword">public</span> <span class="class"><span class="keyword">class</span> <span class="title">CustomLanguage</span> <span class="keyword">extends</span> <span class="title">AbstractLanguage</span> </span>&#123;</div><div class="line">    <span class="keyword">public</span> <span class="keyword">static</span> <span class="keyword">final</span> String KEY = <span class="string">"custom-key"</span>;</div><div class="line">    <span class="keyword">public</span> <span class="keyword">static</span> <span class="keyword">final</span> String NAME = <span class="string">"custom-name"</span>;</div><div class="line"></div><div class="line">    <span class="function"><span class="keyword">public</span> <span class="title">CustomLanguage</span><span class="params">()</span> </span>&#123;</div><div class="line">        <span class="keyword">super</span>(KEY, NAME);</div><div class="line">    &#125;</div><div class="line"></div><div class="line">    <span class="keyword">public</span> String[] getFileSuffixes() &#123;</div><div class="line">        <span class="keyword">return</span> <span class="keyword">new</span> String[] &#123;<span class="string">"csm.xml"</span>&#125;; <span class="comment">//custom这门基于xml的内部DSL的文件后缀</span></div><div class="line">    &#125;</div><div class="line">&#125;</div></pre></td></tr></table></figure></p>
<p>我定义了一门基于xml语法的内部DSL，其文件的后缀是<em>csm.xml</em>。比如：<em>right-syntax.csm.xml</em></p>
<p>Language定义出来了，我们还得定义rule、profile和repository. 回到上文提及的language、rule、profile以及repository的关系图：<br><figure class="highlight plain"><table><tr><td class="gutter"><pre><div class="line">1</div><div class="line">2</div><div class="line">3</div><div class="line">4</div><div class="line">5</div><div class="line">6</div><div class="line">7</div><div class="line">8</div></pre></td><td class="code"><pre><div class="line">profile</div><div class="line">    - language</div><div class="line">    - [rules]</div><div class="line">      - rule</div><div class="line">        - respository</div><div class="line">respository</div><div class="line">    - language</div><div class="line">    - [rules]</div></pre></td></tr></table></figure></p>
<h3 id="第五步-定义规则（Rule）"><a href="#第五步-定义规则（Rule）" class="headerlink" title="第五步 定义规则（Rule）"></a>第五步 定义规则（Rule）</h3><figure class="highlight plain"><table><tr><td class="gutter"><pre><div class="line">1</div><div class="line">2</div><div class="line">3</div></pre></td><td class="code"><pre><div class="line">respository</div><div class="line">    - language</div><div class="line">    - [rules]</div></pre></td></tr></table></figure>
<p>我们需要实现接口<em>RulesDefinition</em><br><figure class="highlight java"><table><tr><td class="gutter"><pre><div class="line">1</div><div class="line">2</div><div class="line">3</div><div class="line">4</div><div class="line">5</div><div class="line">6</div><div class="line">7</div><div class="line">8</div><div class="line">9</div><div class="line">10</div><div class="line">11</div><div class="line">12</div><div class="line">13</div><div class="line">14</div><div class="line">15</div><div class="line">16</div><div class="line">17</div><div class="line">18</div><div class="line">19</div><div class="line">20</div><div class="line">21</div><div class="line">22</div><div class="line">23</div><div class="line">24</div><div class="line">25</div><div class="line">26</div><div class="line">27</div><div class="line">28</div><div class="line">29</div><div class="line">30</div><div class="line">31</div><div class="line">32</div></pre></td><td class="code"><pre><div class="line"><span class="keyword">package</span> com.lambeta;</div><div class="line"><span class="keyword">import</span> org.apache.commons.io.Charsets;</div><div class="line"><span class="keyword">import</span> org.apache.commons.io.IOUtils;</div><div class="line"><span class="keyword">import</span> org.sonar.api.server.rule.RulesDefinition;</div><div class="line"><span class="keyword">import</span> org.sonar.api.server.rule.RulesDefinitionXmlLoader;</div><div class="line"></div><div class="line"><span class="keyword">import</span> java.io.InputStream;</div><div class="line"></div><div class="line"><span class="keyword">public</span> <span class="class"><span class="keyword">class</span> <span class="title">CustomRulesDefinition</span> <span class="keyword">implements</span> <span class="title">RulesDefinition</span> </span>&#123;</div><div class="line"></div><div class="line">    <span class="keyword">public</span> <span class="keyword">static</span> <span class="keyword">final</span> String REPOSITORY_KEY = <span class="string">"custom-repo"</span>;</div><div class="line">    <span class="keyword">private</span> <span class="keyword">final</span> RulesDefinitionXmlLoader xmlLoader;</div><div class="line"></div><div class="line">    <span class="function"><span class="keyword">public</span> <span class="title">CustomRulesDefinition</span><span class="params">(RulesDefinitionXmlLoader xmlLoader)</span> </span>&#123;</div><div class="line">        <span class="keyword">this</span>.xmlLoader = xmlLoader;</div><div class="line">    &#125;</div><div class="line"></div><div class="line">    <span class="function"><span class="keyword">public</span> <span class="keyword">void</span> <span class="title">define</span><span class="params">(Context context)</span> </span>&#123;</div><div class="line"></div><div class="line">        <span class="keyword">final</span> InputStream stream = getClass().getResourceAsStream(<span class="string">"/rules.xml"</span>);</div><div class="line">        <span class="keyword">final</span> NewRepository repository = context.createRepository(REPOSITORY_KEY, CustomLanguage.KEY);</div><div class="line"></div><div class="line">        <span class="keyword">try</span> &#123;</div><div class="line">            <span class="keyword">if</span> (stream != <span class="keyword">null</span>) &#123;</div><div class="line">                xmlLoader.load(repository, stream, Charsets.UTF_8);</div><div class="line">            &#125;</div><div class="line">            repository.done();</div><div class="line">        &#125; <span class="keyword">finally</span> &#123;</div><div class="line">            IOUtils.closeQuietly(stream);</div><div class="line">        &#125;</div><div class="line">    &#125;</div><div class="line">&#125;</div></pre></td></tr></table></figure></p>
<p>我们通过context新建出一个repository。respository需要一个唯一key作为其标识（可以通过setName方法设置名称）以及一个language key来关联（从UI上可以看出来）。然后，通过DI进来的<em>RulesDefinitionXmlLoader</em>将<em>rules.xml</em>中定义的rules加载进repository中。最后，调用<em>reposiotory.done()</em>宣告加载完成。</p>
<p>定义的<em>rules.xml</em>内容如下：<br><figure class="highlight xml"><table><tr><td class="gutter"><pre><div class="line">1</div><div class="line">2</div><div class="line">3</div><div class="line">4</div><div class="line">5</div><div class="line">6</div><div class="line">7</div><div class="line">8</div><div class="line">9</div><div class="line">10</div><div class="line">11</div><div class="line">12</div><div class="line">13</div><div class="line">14</div><div class="line">15</div><div class="line">16</div><div class="line">17</div><div class="line">18</div><div class="line">19</div><div class="line">20</div><div class="line">21</div><div class="line">22</div><div class="line">23</div><div class="line">24</div><div class="line">25</div></pre></td><td class="code"><pre><div class="line"><span class="php"><span class="meta">&lt;?</span>xml version=<span class="string">"1.0"</span> encoding=<span class="string">"UTF-8"</span> <span class="meta">?&gt;</span></span></div><div class="line"><span class="tag">&lt;<span class="name">rules</span>&gt;</span></div><div class="line">    <span class="tag">&lt;<span class="name">rule</span>&gt;</span></div><div class="line">        <span class="tag">&lt;<span class="name">key</span>&gt;</span>ComponentsMustNotBeFollowedByComponentsRule<span class="tag">&lt;/<span class="name">key</span>&gt;</span></div><div class="line"></div><div class="line">        <span class="tag">&lt;<span class="name">name</span>&gt;</span>Components标签后不能跟随Components标签规则<span class="tag">&lt;/<span class="name">name</span>&gt;</span></div><div class="line">        <span class="tag">&lt;<span class="name">description</span>&gt;</span></div><div class="line">            &lt;![CDATA[</div><div class="line">                Components标签后不能跟随Components标签</div><div class="line">            ]]&gt;</div><div class="line">        <span class="tag">&lt;/<span class="name">description</span>&gt;</span></div><div class="line">        <span class="tag">&lt;<span class="name">severity</span>&gt;</span>MINOR<span class="tag">&lt;/<span class="name">severity</span>&gt;</span></div><div class="line">        <span class="tag">&lt;<span class="name">cardinality</span>&gt;</span>SINGLE<span class="tag">&lt;/<span class="name">cardinality</span>&gt;</span></div><div class="line">        <span class="tag">&lt;<span class="name">status</span>&gt;</span>READY<span class="tag">&lt;/<span class="name">status</span>&gt;</span></div><div class="line">        <span class="tag">&lt;<span class="name">tag</span>&gt;</span>custom<span class="tag">&lt;/<span class="name">tag</span>&gt;</span></div><div class="line">        <span class="tag">&lt;<span class="name">example</span>&gt;</span></div><div class="line">            &lt;![CDATA[</div><div class="line">                &lt;components&gt;</div><div class="line">                &lt;!-- Error, components must be here! --&gt;</div><div class="line">                    &lt;components/&gt;</div><div class="line">                &lt;/components&gt;</div><div class="line">            ]]&gt;</div><div class="line">        <span class="tag">&lt;/<span class="name">example</span>&gt;</span></div><div class="line">    <span class="tag">&lt;/<span class="name">rule</span>&gt;</span></div><div class="line"><span class="tag">&lt;/<span class="name">rules</span>&gt;</span></div></pre></td></tr></table></figure></p>
<p>包含了rule的key和其他相关的属性。它们最终显示在UI上，会是这样：</p>
<p><img src="http://upload-images.jianshu.io/upload_images/217988-91243613d40090e7.png?imageMogr2/auto-orient/strip%7CimageView2/2/w/1240" alt="Rule"></p>
<h3 id="第六步-定义Profile"><a href="#第六步-定义Profile" class="headerlink" title="第六步 定义Profile"></a>第六步 定义Profile</h3><figure class="highlight plain"><table><tr><td class="gutter"><pre><div class="line">1</div><div class="line">2</div><div class="line">3</div><div class="line">4</div><div class="line">5</div></pre></td><td class="code"><pre><div class="line">profile</div><div class="line">    - language</div><div class="line">    - [rules]</div><div class="line">      - rule</div><div class="line">        - respository</div></pre></td></tr></table></figure>
<p>我们需要实现接口<em>ProfileDefinition</em>.<br><figure class="highlight java"><table><tr><td class="gutter"><pre><div class="line">1</div><div class="line">2</div><div class="line">3</div><div class="line">4</div><div class="line">5</div><div class="line">6</div><div class="line">7</div><div class="line">8</div><div class="line">9</div><div class="line">10</div><div class="line">11</div><div class="line">12</div><div class="line">13</div><div class="line">14</div><div class="line">15</div><div class="line">16</div><div class="line">17</div><div class="line">18</div><div class="line">19</div><div class="line">20</div><div class="line">21</div><div class="line">22</div><div class="line">23</div><div class="line">24</div><div class="line">25</div><div class="line">26</div><div class="line">27</div></pre></td><td class="code"><pre><div class="line"><span class="keyword">package</span> com.lambeta;</div><div class="line"><span class="keyword">import</span> org.apache.commons.io.IOUtils;</div><div class="line"><span class="keyword">import</span> org.sonar.api.profiles.ProfileDefinition;</div><div class="line"><span class="keyword">import</span> org.sonar.api.profiles.RulesProfile;</div><div class="line"><span class="keyword">import</span> org.sonar.api.profiles.XMLProfileParser;</div><div class="line"><span class="keyword">import</span> org.sonar.api.utils.ValidationMessages;</div><div class="line"></div><div class="line"><span class="keyword">import</span> java.io.InputStreamReader;</div><div class="line"></div><div class="line"><span class="keyword">public</span> <span class="class"><span class="keyword">class</span> <span class="title">CustomProfileDefinition</span> <span class="keyword">extends</span> <span class="title">ProfileDefinition</span> </span>&#123;</div><div class="line">    <span class="keyword">private</span> <span class="keyword">final</span> XMLProfileParser xmlProfileParser;</div><div class="line"></div><div class="line">    <span class="function"><span class="keyword">public</span> <span class="title">CustomProfileDefinition</span><span class="params">(XMLProfileParser xmlProfileParser)</span> </span>&#123;</div><div class="line">        <span class="keyword">this</span>.xmlProfileParser = xmlProfileParser;</div><div class="line">    &#125;</div><div class="line"></div><div class="line">    <span class="meta">@Override</span></div><div class="line">    <span class="function"><span class="keyword">public</span> RulesProfile <span class="title">createProfile</span><span class="params">(ValidationMessages validation)</span> </span>&#123;</div><div class="line">        <span class="keyword">final</span> InputStreamReader reader = <span class="keyword">new</span> InputStreamReader(getClass().getResourceAsStream(<span class="string">"/profile.xml"</span>));</div><div class="line"></div><div class="line">        <span class="keyword">try</span> &#123;</div><div class="line">            <span class="keyword">return</span> xmlProfileParser.parse(reader, validation);</div><div class="line">        &#125; <span class="keyword">finally</span> &#123;</div><div class="line">            IOUtils.closeQuietly(reader);</div><div class="line">        &#125;</div><div class="line">    &#125;</div><div class="line">&#125;</div></pre></td></tr></table></figure></p>
<p>使用DI注入的XMLProfileParser解析<em>profile.xml</em>文件，并生成RulesProfile对象。我们来看看<em>profile.xml</em>的内容：<br><figure class="highlight xml"><table><tr><td class="gutter"><pre><div class="line">1</div><div class="line">2</div><div class="line">3</div><div class="line">4</div><div class="line">5</div><div class="line">6</div><div class="line">7</div><div class="line">8</div><div class="line">9</div><div class="line">10</div><div class="line">11</div><div class="line">12</div></pre></td><td class="code"><pre><div class="line"><span class="php"><span class="meta">&lt;?</span>xml version=<span class="string">"1.0"</span> encoding=<span class="string">"utf-8"</span> <span class="meta">?&gt;</span></span></div><div class="line"><span class="tag">&lt;<span class="name">profile</span>&gt;</span></div><div class="line">    <span class="tag">&lt;<span class="name">language</span>&gt;</span>custom-key<span class="tag">&lt;/<span class="name">language</span>&gt;</span></div><div class="line">    <span class="tag">&lt;<span class="name">name</span>&gt;</span>Custom Quality<span class="tag">&lt;/<span class="name">name</span>&gt;</span></div><div class="line">    <span class="tag">&lt;<span class="name">rules</span>&gt;</span></div><div class="line">        <span class="tag">&lt;<span class="name">rule</span>&gt;</span></div><div class="line">            <span class="tag">&lt;<span class="name">repositoryKey</span>&gt;</span>custom-repo<span class="tag">&lt;/<span class="name">repositoryKey</span>&gt;</span></div><div class="line">            <span class="tag">&lt;<span class="name">key</span>&gt;</span>ComponentsMustNotBeFollowedByComponentsRule<span class="tag">&lt;/<span class="name">key</span>&gt;</span></div><div class="line">            <span class="tag">&lt;<span class="name">priority</span>&gt;</span>MAJOR<span class="tag">&lt;/<span class="name">priority</span>&gt;</span></div><div class="line">        <span class="tag">&lt;/<span class="name">rule</span>&gt;</span></div><div class="line">    <span class="tag">&lt;/<span class="name">rules</span>&gt;</span></div><div class="line"><span class="tag">&lt;/<span class="name">profile</span>&gt;</span></div></pre></td></tr></table></figure></p>
<p>这里定义一个名为<em>Custom Quality</em>的profile，它关联CustomLanguage的键值：custom-key. 同时包含了多条rules，每条rule拥有自己的标识key以及其所在的repository（事实上，profile会在repository中通过ruleKey来查找rule）。</p>
<p>写到这里，一个DSL的SonarQube Plugin已经几近完善。但是，我们还缺少至关重要的一环——<strong>规则的执行！</strong></p>
<h3 id="第七步-运行PMD扫描代码"><a href="#第七步-运行PMD扫描代码" class="headerlink" title="第七步 运行PMD扫描代码"></a>第七步 运行PMD扫描代码</h3><h4 id="PMD简介"><a href="#PMD简介" class="headerlink" title="PMD简介"></a>PMD简介</h4><p>我们需要一个静态扫描工具来扫描源代码，发现这些代码存在的缺陷和坏味道。PMD就是这么一款好用的工具。</p>
<blockquote>
<p>PMD is a source code analyzer. It finds common programming flaws like unused variables, empty catch blocks, unnecessary object creation, and so forth. It supports Java, JavaScript, PLSQL, Apache Velocity, XML, XSL.</p>
</blockquote>
<p>翻译：<br>PMD是一款源码分析工具。它会发现编程中的普遍缺陷，如未使用的变量、空的catch块、不必要的对象创建等等。它支持分析Java、Javascript、PLSQL、Apache Velocity、XML、XSL语言。</p>
<p>前面提到我定义的是一门基于XML的DSL，那么理所当然，可以借助PMD，扩展XML的扫描规则来满足自己的需求。</p>
<p>PMD在命令行中执行的方式如下：<br><figure class="highlight plain"><table><tr><td class="gutter"><pre><div class="line">1</div></pre></td><td class="code"><pre><div class="line">pmd -d src/ -f xml -R myrule.xml -r dest/report.xml</div></pre></td></tr></table></figure></p>
<ul>
<li>-d 代表要扫描的源码目录</li>
<li>-f 代表报告输出的格式</li>
<li>-R 代表采用哪些规则来扫描源代码</li>
<li>-r 代表报告的输出路径</li>
</ul>
<blockquote>
<p><strong>注意：</strong>这里PMD的规则和SonarQube中的规则其实没有太大关系，属于两种事物。不过，为方便后续提取PMD输出的报告，需要将PMD规则的名字和Sonar规则的键值保持一致。</p>
</blockquote>
<p>我们定义PMD需要使用到的规则集<em>custom-pmd-rules.xml</em>：<br><figure class="highlight xml"><table><tr><td class="gutter"><pre><div class="line">1</div><div class="line">2</div><div class="line">3</div><div class="line">4</div><div class="line">5</div><div class="line">6</div><div class="line">7</div><div class="line">8</div><div class="line">9</div><div class="line">10</div><div class="line">11</div><div class="line">12</div><div class="line">13</div><div class="line">14</div><div class="line">15</div><div class="line">16</div><div class="line">17</div><div class="line">18</div><div class="line">19</div><div class="line">20</div><div class="line">21</div><div class="line">22</div><div class="line">23</div><div class="line">24</div><div class="line">25</div><div class="line">26</div><div class="line">27</div><div class="line">28</div><div class="line">29</div><div class="line">30</div><div class="line">31</div><div class="line">32</div><div class="line">33</div><div class="line">34</div><div class="line">35</div><div class="line">36</div></pre></td><td class="code"><pre><div class="line"><span class="php"><span class="meta">&lt;?</span>xml version=<span class="string">"1.0"</span><span class="meta">?&gt;</span></span></div><div class="line"><span class="tag">&lt;<span class="name">ruleset</span> <span class="attr">name</span>=<span class="string">"ExamplePmdRuleset"</span></span></div><div class="line">         <span class="attr">xmlns</span>=<span class="string">"http://pmd.sourceforge.net/ruleset/2.0.0"</span></div><div class="line">         <span class="attr">xmlns:xsi</span>=<span class="string">"http://www.w3.org/2001/XMLSchema-instance"</span></div><div class="line">         <span class="attr">xsi:schemaLocation</span>=<span class="string">"http://pmd.sourceforge.net/ruleset/2.0.0 http://pmd.sourceforge.net/ruleset_2_0_0.xsd"</span>&gt;</div><div class="line"></div><div class="line">    <span class="tag">&lt;<span class="name">description</span>&gt;</span></div><div class="line">        Example set of configured PMD rules</div><div class="line">    <span class="tag">&lt;/<span class="name">description</span>&gt;</span></div><div class="line"></div><div class="line">    <span class="tag">&lt;<span class="name">rule</span> <span class="attr">name</span>=<span class="string">"ComponentsMustNotBeFollowedByComponentsRule"</span></span></div><div class="line">          <span class="attr">message</span>=<span class="string">"Components tags followed by components tag found!"</span></div><div class="line">          <span class="attr">language</span>=<span class="string">"xml"</span></div><div class="line">          <span class="attr">class</span>=<span class="string">"net.sourceforge.pmd.lang.rule.XPathRule"</span>&gt;</div><div class="line"></div><div class="line">        <span class="tag">&lt;<span class="name">description</span>&gt;</span></div><div class="line">            Tag components must not be followed by components tag.</div><div class="line">        <span class="tag">&lt;/<span class="name">description</span>&gt;</span></div><div class="line"></div><div class="line">        <span class="tag">&lt;<span class="name">priority</span>&gt;</span>1<span class="tag">&lt;/<span class="name">priority</span>&gt;</span></div><div class="line"></div><div class="line">        <span class="tag">&lt;<span class="name">properties</span>&gt;</span></div><div class="line">            <span class="tag">&lt;<span class="name">property</span> <span class="attr">name</span>=<span class="string">"xpath"</span>&gt;</span></div><div class="line">                <span class="tag">&lt;<span class="name">value</span>&gt;</span>//components/components<span class="tag">&lt;/<span class="name">value</span>&gt;</span></div><div class="line">            <span class="tag">&lt;/<span class="name">property</span>&gt;</span></div><div class="line">        <span class="tag">&lt;/<span class="name">properties</span>&gt;</span></div><div class="line"></div><div class="line">        <span class="tag">&lt;<span class="name">example</span>&gt;</span></div><div class="line">            &lt;![CDATA[</div><div class="line">                &lt;components&gt;</div><div class="line">                    &lt;components&gt;</div><div class="line">                &lt;/components&gt;</div><div class="line">            ]]&gt;</div><div class="line">        <span class="tag">&lt;/<span class="name">example</span>&gt;</span></div><div class="line">    <span class="tag">&lt;/<span class="name">rule</span>&gt;</span></div><div class="line"><span class="tag">&lt;/<span class="name">ruleset</span>&gt;</span></div></pre></td></tr></table></figure></p>
<p>这里的类<em>net.sourceforge.pmd.lang.rule.XPathRule</em>来自于我们先前在pom.xml中声明的pmd-xml这个依赖包。它可以让我们通过设置<em>xpath</em>这一属性的值来构建各种不同规则。扫描中XML文件一旦匹配这些xpath规则，就会输出错误报告。</p>
<p>以<em>ComponentsMustNotBeFollowedByComponentsRule</em>这个自定义的规则为例。顾名思义，Components元素下不能再跟着Components元素。它在PMD扫描过程中如果被匹配上，会输出这样的报告：<br><figure class="highlight xml"><table><tr><td class="gutter"><pre><div class="line">1</div><div class="line">2</div><div class="line">3</div><div class="line">4</div><div class="line">5</div><div class="line">6</div><div class="line">7</div><div class="line">8</div><div class="line">9</div><div class="line">10</div><div class="line">11</div><div class="line">12</div><div class="line">13</div><div class="line">14</div><div class="line">15</div><div class="line">16</div><div class="line">17</div><div class="line">18</div><div class="line">19</div><div class="line">20</div><div class="line">21</div><div class="line">22</div><div class="line">23</div><div class="line">24</div><div class="line">25</div></pre></td><td class="code"><pre><div class="line"><span class="php"><span class="meta">&lt;?</span>xml version=<span class="string">"1.0"</span> encoding=<span class="string">"UTF-8"</span><span class="meta">?&gt;</span></span></div><div class="line"><span class="tag">&lt;<span class="name">pmd</span> <span class="attr">version</span>=<span class="string">"5.4.2"</span> <span class="attr">timestamp</span>=<span class="string">"2016-06-23T23:06:04.120"</span>&gt;</span></div><div class="line">    <span class="tag">&lt;<span class="name">file</span> <span class="attr">name</span>=<span class="string">"/Users/qianyan/github/sonar/sonar-custom/src/test/resources/wrong-syntax-but-not-csm.xml"</span>&gt;</span></div><div class="line">        <span class="tag">&lt;<span class="name">violation</span> <span class="attr">beginline</span>=<span class="string">"4"</span></span></div><div class="line">                   <span class="attr">endline</span>=<span class="string">"4"</span></div><div class="line">                   <span class="attr">begincolumn</span>=<span class="string">"5"</span></div><div class="line">                   <span class="attr">endcolumn</span>=<span class="string">"17"</span></div><div class="line">                   <span class="attr">rule</span>=<span class="string">"ComponentsMustNotBeFollowedByComponentsRule"</span></div><div class="line">                   <span class="attr">ruleset</span>=<span class="string">"ExamplePmdRuleset"</span></div><div class="line">                   <span class="attr">priority</span>=<span class="string">"1"</span>&gt;</div><div class="line">            Components tags followed by components tag found!</div><div class="line">        <span class="tag">&lt;/<span class="name">violation</span>&gt;</span></div><div class="line">    <span class="tag">&lt;/<span class="name">file</span>&gt;</span></div><div class="line">    <span class="tag">&lt;<span class="name">file</span> <span class="attr">name</span>=<span class="string">"/Users/qianyan/github/sonar/sonar-custom/src/test/resources/wrong-syntax.csm.xml"</span>&gt;</span></div><div class="line">        <span class="tag">&lt;<span class="name">violation</span> <span class="attr">beginline</span>=<span class="string">"4"</span></span></div><div class="line">                   <span class="attr">endline</span>=<span class="string">"4"</span></div><div class="line">                   <span class="attr">begincolumn</span>=<span class="string">"5"</span></div><div class="line">                   <span class="attr">endcolumn</span>=<span class="string">"17"</span></div><div class="line">                   <span class="attr">rule</span>=<span class="string">"ComponentsMustNotBeFollowedByComponentsRule"</span></div><div class="line">                   <span class="attr">ruleset</span>=<span class="string">"ExamplePmdRuleset"</span></div><div class="line">                   <span class="attr">priority</span>=<span class="string">"1"</span>&gt;</div><div class="line">            Components tags followed by components tag found!</div><div class="line">        <span class="tag">&lt;/<span class="name">violation</span>&gt;</span></div><div class="line">    <span class="tag">&lt;/<span class="name">file</span>&gt;</span></div><div class="line"><span class="tag">&lt;/<span class="name">pmd</span>&gt;</span></div></pre></td></tr></table></figure></p>
<h4 id="PMD报告转化为Sonar的Issue"><a href="#PMD报告转化为Sonar的Issue" class="headerlink" title="PMD报告转化为Sonar的Issue"></a>PMD报告转化为Sonar的Issue</h4><p>由于PMD是由Java编写的，所以我们可以在代码中调用PMD这个类<em>net.sourceforge.pmd.PMD</em>根据我们写好的PMD规则，来扫描Sonar指定的目录及其文件。最后，将PMD输出的XML格式的报告转化成Sonar能够理解的Issue。</p>
<p>代码如下：<br><figure class="highlight java"><table><tr><td class="gutter"><pre><div class="line">1</div><div class="line">2</div><div class="line">3</div><div class="line">4</div><div class="line">5</div></pre></td><td class="code"><pre><div class="line"><span class="function"><span class="keyword">public</span> <span class="keyword">void</span> <span class="title">execute</span><span class="params">(SensorContext context)</span> </span>&#123;</div><div class="line">        File reportFile = <span class="keyword">new</span> File(context.fileSystem().workDir(), <span class="string">"report.xml"</span>); <span class="comment">// 1</span></div><div class="line">        runPMD(context, reportFile); <span class="comment">// 2</span></div><div class="line">        convertToIssues(context, doc(reportFile)); <span class="comment">// 3</span></div><div class="line">&#125;</div></pre></td></tr></table></figure></p>
<ol>
<li>指定PMD输出文件的路径；</li>
<li>运行PMD，输出XML格式的报告到1指定的文件当中；</li>
<li>解析报告，并转化为Issue。</li>
</ol>
<p>下面我们一步步来解释对应的代码：</p>
<ul>
<li>runPMD<figure class="highlight java"><table><tr><td class="gutter"><pre><div class="line">1</div><div class="line">2</div><div class="line">3</div><div class="line">4</div><div class="line">5</div><div class="line">6</div><div class="line">7</div><div class="line">8</div><div class="line">9</div><div class="line">10</div><div class="line">11</div><div class="line">12</div><div class="line">13</div><div class="line">14</div><div class="line">15</div><div class="line">16</div><div class="line">17</div><div class="line">18</div><div class="line">19</div><div class="line">20</div></pre></td><td class="code"><pre><div class="line"><span class="function"><span class="keyword">private</span> <span class="keyword">void</span> <span class="title">runPMD</span><span class="params">(SensorContext context, File reportFile)</span> </span>&#123;</div><div class="line">    <span class="keyword">final</span> String dir = context.settings().getString(<span class="string">"sonar.sources"</span>);</div><div class="line">    <span class="keyword">final</span> File file = <span class="keyword">new</span> File(dir);</div><div class="line">    String[] pmdArgs = &#123;</div><div class="line">            <span class="string">"-f"</span>, <span class="string">"xml"</span>,</div><div class="line">            <span class="string">"-R"</span>, <span class="string">"custom-pmd-rules.xml"</span>,</div><div class="line">            <span class="string">"-d"</span>, dir,</div><div class="line">            <span class="string">"-r"</span>, reportFile.getAbsolutePath(),</div><div class="line">            <span class="string">"-e"</span>, context.settings().getString(<span class="string">"sonar.sourceEncoding"</span>),</div><div class="line">            <span class="string">"-language"</span>, <span class="string">"xml"</span>,</div><div class="line">            <span class="string">"-version"</span>, <span class="string">"1.0"</span></div><div class="line">    &#125;;</div><div class="line">    <span class="keyword">final</span> ClassLoader loader = Thread.currentThread().getContextClassLoader();</div><div class="line">    <span class="keyword">try</span> &#123;</div><div class="line">        Thread.currentThread().setContextClassLoader(getClass().getClassLoader());</div><div class="line">        PMD.run(pmdArgs);</div><div class="line">    &#125; <span class="keyword">finally</span> &#123;</div><div class="line">        Thread.currentThread().setContextClassLoader(loader);</div><div class="line">    &#125;</div><div class="line">&#125;</div></pre></td></tr></table></figure>
</li>
</ul>
<p>我们通过PMD这个类运行pmdArgs。这里值得注意的是自SonarQube 5.6之后，我们可以通过context.settings()来获取工程的配置了，而不像以前那样依赖注入Settings对象了。</p>
<p>至于<code>Thread.currentThread().setContextClassLoader(getClass().getClassLoader());</code>这步操作和Sonar使用独立的classLoader加载自己的类有关。</p>
<ul>
<li>convertToIssues<figure class="highlight java"><table><tr><td class="gutter"><pre><div class="line">1</div><div class="line">2</div><div class="line">3</div><div class="line">4</div><div class="line">5</div><div class="line">6</div><div class="line">7</div><div class="line">8</div><div class="line">9</div><div class="line">10</div><div class="line">11</div><div class="line">12</div><div class="line">13</div><div class="line">14</div><div class="line">15</div><div class="line">16</div><div class="line">17</div><div class="line">18</div><div class="line">19</div><div class="line">20</div><div class="line">21</div><div class="line">22</div><div class="line">23</div><div class="line">24</div><div class="line">25</div><div class="line">26</div><div class="line">27</div><div class="line">28</div><div class="line">29</div><div class="line">30</div></pre></td><td class="code"><pre><div class="line"><span class="function"><span class="keyword">private</span> <span class="keyword">void</span> <span class="title">convertToIssues</span><span class="params">(SensorContext context, Document doc)</span> </span>&#123;</div><div class="line">    <span class="keyword">final</span> Element root = doc.getRootElement();</div><div class="line">    <span class="keyword">final</span> List&lt;Element&gt; files = root.elements(<span class="string">"file"</span>);</div><div class="line">    <span class="keyword">for</span> (Element file : files) &#123;</div><div class="line"></div><div class="line">        <span class="keyword">final</span> List&lt;Element&gt; violations = file.elements(<span class="string">"violation"</span>);</div><div class="line">        <span class="keyword">final</span> String filePath = file.attributeValue(<span class="string">"name"</span>);</div><div class="line">        <span class="keyword">final</span> FileSystem fs = context.fileSystem();</div><div class="line">        <span class="keyword">final</span> InputFile inputFile = fs.inputFile(fs.predicates().hasAbsolutePath(filePath));</div><div class="line">        <span class="keyword">if</span> (inputFile == <span class="keyword">null</span>) &#123;</div><div class="line">            LOG.info(<span class="string">"fs predicates that there is no &#123;&#125;"</span>, filePath);</div><div class="line">            <span class="keyword">continue</span>;</div><div class="line">        &#125;</div><div class="line">        <span class="keyword">for</span> (Element violation : violations) &#123;</div><div class="line">            <span class="keyword">final</span> String rule = violation.attributeValue(<span class="string">"rule"</span>);</div><div class="line">            <span class="keyword">final</span> <span class="keyword">int</span> beginLine = Integer.parseInt(violation.attributeValue(<span class="string">"beginline"</span>));</div><div class="line">            <span class="keyword">final</span> <span class="keyword">int</span> endLine = Integer.parseInt(violation.attributeValue(<span class="string">"endline"</span>));</div><div class="line">            <span class="keyword">final</span> <span class="keyword">int</span> beginColumn = Integer.parseInt(violation.attributeValue(<span class="string">"begincolumn"</span>));</div><div class="line">            <span class="keyword">final</span> <span class="keyword">int</span> endColumn = Integer.parseInt(violation.attributeValue(<span class="string">"endcolumn"</span>));</div><div class="line">            <span class="keyword">final</span> NewIssue newIssue = context.newIssue()</div><div class="line">                    .forRule(RuleKey.of(CustomRulesDefinition.REPOSITORY_KEY, rule));</div><div class="line">            <span class="keyword">final</span> NewIssueLocation newIssueLocation = newIssue</div><div class="line">                    .newLocation()</div><div class="line">                    .on(inputFile)</div><div class="line">                    .at(inputFile.newRange(beginLine, beginColumn, endLine, endColumn))</div><div class="line">                    .message(violation.getText());</div><div class="line">            newIssue.at(newIssueLocation).save();</div><div class="line">        &#125;</div><div class="line">    &#125;</div><div class="line">&#125;</div></pre></td></tr></table></figure>
</li>
</ul>
<p>这里主要是对PMD生成XML报告的解析和转换。比较需要关注是这块代码：<br><figure class="highlight java"><table><tr><td class="gutter"><pre><div class="line">1</div><div class="line">2</div><div class="line">3</div><div class="line">4</div><div class="line">5</div></pre></td><td class="code"><pre><div class="line"><span class="keyword">final</span> InputFile inputFile = fs.inputFile(fs.predicates().hasAbsolutePath(filePath));</div><div class="line"><span class="keyword">if</span> (inputFile == <span class="keyword">null</span>) &#123;</div><div class="line">    LOG.info(<span class="string">"fs predicates that there is no &#123;&#125;"</span>, filePath);</div><div class="line">    <span class="keyword">continue</span>;</div><div class="line">&#125;</div></pre></td></tr></table></figure></p>
<p>InputFile这是Sonar定义的合法的待扫描文件。举个例子：我们定义了一门基于XML的DSL，其文件的后缀是<em>csm.xml</em>，那么合法的待扫描文件就只能是这个后缀的文件了。像上述PMD输出的那份报告中出现的<br><figure class="highlight xml"><table><tr><td class="gutter"><pre><div class="line">1</div></pre></td><td class="code"><pre><div class="line"><span class="tag">&lt;<span class="name">file</span> <span class="attr">name</span>=<span class="string">"/Users/qianyan/github/sonar/sonar-custom/src/test/resources/wrong-syntax-but-not-csm.xml"</span>&gt;</span></div></pre></td></tr></table></figure></p>
<p>就是不合法的。这个文件是以xml作为后缀的，PMD肯定可以扫描它，但是对于Sonar而言，它并不是InputFile（如果不作处理，就会返回null），所以我们需要在转换为Issue之前剔除掉。</p>
<p>最后，不要忘记保存，<code>newIssue.at(newIssueLocation).save();</code>。</p>
<p>Issue呈现在UI上，是这样的：</p>
<p><img src="http://upload-images.jianshu.io/upload_images/217988-87d84e5cc8a49c37.png?imageMogr2/auto-orient/strip%7CimageView2/2/w/1240" alt="Issue"></p>
<h3 id="第八步-注册所有组件"><a href="#第八步-注册所有组件" class="headerlink" title="第八步 注册所有组件"></a>第八步 注册所有组件</h3><p>现在所有的组件已经就绪，是时候将这些组件注册进插件当中了。还记得第一步我们创建的<em>CustomPlugin.java</em>? 所有上述组件，包括Language、Rules、Profiles以及Sensor都得在这个类中进行注册。代码如下：<br><figure class="highlight java"><table><tr><td class="gutter"><pre><div class="line">1</div><div class="line">2</div><div class="line">3</div><div class="line">4</div><div class="line">5</div><div class="line">6</div><div class="line">7</div><div class="line">8</div><div class="line">9</div><div class="line">10</div><div class="line">11</div><div class="line">12</div></pre></td><td class="code"><pre><div class="line"><span class="keyword">package</span> com.lambeta;</div><div class="line"></div><div class="line"><span class="keyword">import</span> org.sonar.api.Plugin;</div><div class="line"></div><div class="line"><span class="keyword">public</span> <span class="class"><span class="keyword">class</span> <span class="title">CustomPlugin</span> <span class="keyword">implements</span> <span class="title">Plugin</span> </span>&#123;</div><div class="line">    <span class="function"><span class="keyword">public</span> <span class="keyword">void</span> <span class="title">define</span><span class="params">(Context context)</span> </span>&#123;</div><div class="line">        context.addExtension(CustomLanguage.class)</div><div class="line">                .addExtension(CustomRulesDefinition.class)</div><div class="line">                .addExtension(CustomProfileDefinition.class)</div><div class="line">                .addExtension(CustomSensor.class);</div><div class="line">    &#125;</div><div class="line">&#125;</div></pre></td></tr></table></figure></p>
<p>到此，这个插件算是写完了。那么接下来的问题就是如何运行它？</p>
<h2 id="使用插件扫描工程"><a href="#使用插件扫描工程" class="headerlink" title="使用插件扫描工程"></a>使用插件扫描工程</h2><h3 id="下载sonarqube-docker镜像"><a href="#下载sonarqube-docker镜像" class="headerlink" title="下载sonarqube docker镜像"></a>下载sonarqube docker镜像</h3><p>最易于调试的地方莫过于本地了。如果机器是Mac，建议使用Kitematic这个Docker的客户端下载sonarqube的官方镜像，同时将映射的Port定在9000端口上，启动该镜像的容器实例。</p>
<p><img src="http://upload-images.jianshu.io/upload_images/217988-2e1f14fff13e1fd7.png?imageMogr2/auto-orient/strip%7CimageView2/2/w/1240" alt="sonarqube docker"></p>
<h3 id="构建和Copy插件包"><a href="#构建和Copy插件包" class="headerlink" title="构建和Copy插件包"></a>构建和Copy插件包</h3><p>在插件的工程根目录下，运行<br><figure class="highlight plain"><table><tr><td class="gutter"><pre><div class="line">1</div></pre></td><td class="code"><pre><div class="line">mvn clean package</div></pre></td></tr></table></figure></p>
<p>然后执行<br><figure class="highlight plain"><table><tr><td class="gutter"><pre><div class="line">1</div></pre></td><td class="code"><pre><div class="line">cp target/sonar-custom-1.0-SNAPSHOT.jar /Users/your-name/Documents/Kitematic/sonarqube/opt/sonarqube/extensions/plugins</div></pre></td></tr></table></figure></p>
<p>如果plugins目录不存在，可以手动创建。执行完命令之后，重启容器。</p>
<h3 id="安装Maven的sonar插件"><a href="#安装Maven的sonar插件" class="headerlink" title="安装Maven的sonar插件"></a>安装Maven的sonar插件</h3><figure class="highlight xml"><table><tr><td class="gutter"><pre><div class="line">1</div><div class="line">2</div><div class="line">3</div><div class="line">4</div><div class="line">5</div><div class="line">6</div><div class="line">7</div><div class="line">8</div><div class="line">9</div><div class="line">10</div><div class="line">11</div><div class="line">12</div><div class="line">13</div><div class="line">14</div><div class="line">15</div><div class="line">16</div><div class="line">17</div><div class="line">18</div><div class="line">19</div><div class="line">20</div><div class="line">21</div><div class="line">22</div><div class="line">23</div><div class="line">24</div><div class="line">25</div><div class="line">26</div><div class="line">27</div><div class="line">28</div><div class="line">29</div><div class="line">30</div><div class="line">31</div></pre></td><td class="code"><pre><div class="line"><span class="comment">&lt;!-- settings.xml --&gt;</span></div><div class="line"><span class="tag">&lt;<span class="name">settings</span> <span class="attr">xmlns</span>=<span class="string">"http://maven.apache.org/SETTINGS/1.0.0"</span></span></div><div class="line">    <span class="attr">xmlns:xsi</span>=<span class="string">"http://www.w3.org/2001/XMLSchema-instance"</span></div><div class="line">    <span class="attr">xsi:schemaLocation</span>=<span class="string">"http://maven.apache.org/SETTINGS/1.0.0</span></div><div class="line">    https://maven.apache.org/xsd/settings-1.0.0.xsd"&gt;</div><div class="line">    <span class="tag">&lt;<span class="name">localRepository</span>/&gt;</span></div><div class="line">    <span class="tag">&lt;<span class="name">interactiveMode</span>/&gt;</span></div><div class="line">    <span class="tag">&lt;<span class="name">usePluginRegistry</span>/&gt;</span></div><div class="line">    <span class="tag">&lt;<span class="name">offline</span>/&gt;</span></div><div class="line">    <span class="tag">&lt;<span class="name">pluginGroups</span>&gt;</span></div><div class="line">        <span class="tag">&lt;<span class="name">pluginGroup</span>&gt;</span>org.sonarsource.scanner.maven<span class="tag">&lt;/<span class="name">pluginGroup</span>&gt;</span></div><div class="line">    <span class="tag">&lt;/<span class="name">pluginGroups</span>&gt;</span></div><div class="line">    <span class="tag">&lt;<span class="name">profiles</span>&gt;</span></div><div class="line">        <span class="tag">&lt;<span class="name">profile</span>&gt;</span></div><div class="line">            <span class="tag">&lt;<span class="name">id</span>&gt;</span>sonar<span class="tag">&lt;/<span class="name">id</span>&gt;</span></div><div class="line">            <span class="tag">&lt;<span class="name">activation</span>&gt;</span></div><div class="line">                <span class="tag">&lt;<span class="name">activeByDefault</span>&gt;</span>true<span class="tag">&lt;/<span class="name">activeByDefault</span>&gt;</span></div><div class="line">            <span class="tag">&lt;/<span class="name">activation</span>&gt;</span></div><div class="line">            <span class="tag">&lt;<span class="name">properties</span>&gt;</span></div><div class="line">                <span class="comment">&lt;!-- Optional URL to server. Default value is http://localhost:9000 --&gt;</span></div><div class="line">                <span class="tag">&lt;<span class="name">sonar.host.url</span>&gt;</span></div><div class="line">                  http://192.168.99.100:9000</div><div class="line">                <span class="tag">&lt;/<span class="name">sonar.host.url</span>&gt;</span></div><div class="line">            <span class="tag">&lt;/<span class="name">properties</span>&gt;</span></div><div class="line">        <span class="tag">&lt;/<span class="name">profile</span>&gt;</span></div><div class="line">     <span class="tag">&lt;/<span class="name">profiles</span>&gt;</span></div><div class="line">    <span class="tag">&lt;<span class="name">servers</span>/&gt;</span></div><div class="line">    <span class="tag">&lt;<span class="name">mirrors</span>/&gt;</span></div><div class="line">    <span class="tag">&lt;<span class="name">proxies</span>/&gt;</span></div><div class="line">    <span class="tag">&lt;<span class="name">activeProfiles</span>/&gt;</span></div><div class="line"><span class="tag">&lt;/<span class="name">settings</span>&gt;</span></div></pre></td></tr></table></figure>
<p>将这个settings.xml的文件放到~/.m2下。</p>
<h3 id="运行Maven-sonar-sonar"><a href="#运行Maven-sonar-sonar" class="headerlink" title="运行Maven sonar:sonar"></a>运行Maven sonar:sonar</h3><figure class="highlight plain"><table><tr><td class="gutter"><pre><div class="line">1</div></pre></td><td class="code"><pre><div class="line">mvn sonar:sonar -Dsonar.sources=src/test/resources/ -Dsonar.language=custom-key -X</div></pre></td></tr></table></figure>
<p><em>src/test/resources</em>目录展开如下：<br><figure class="highlight plain"><table><tr><td class="gutter"><pre><div class="line">1</div><div class="line">2</div><div class="line">3</div><div class="line">4</div></pre></td><td class="code"><pre><div class="line">src/test/resources</div><div class="line">├── right-syntax.csm.xml</div><div class="line">├── wrong-syntax-but-not-csm.xml</div><div class="line">└── wrong-syntax.csm.xml</div></pre></td></tr></table></figure></p>
<p>然后，根据输出提示，访问<br><a href="http://192.168.99.100:9000/dashboard/index/com.lambeta:sonar-custom" target="_blank" rel="external">http://192.168.99.100:9000/dashboard/index/com.lambeta:sonar-custom</a></p>
<h2 id="总结"><a href="#总结" class="headerlink" title="总结"></a>总结</h2><p><img src="http://upload-images.jianshu.io/upload_images/217988-136f034d165b9c38.png?imageMogr2/auto-orient/strip%7CimageView2/2/w/1240" alt="Sonar Plugin"></p>
<p><img src="http://upload-images.jianshu.io/upload_images/217988-fd2a3f30fc23d31d.png?imageMogr2/auto-orient/strip%7CimageView2/2/w/1240" alt="Plugin implements details"></p>
<p>[1] <a href="http://docs.sonarqube.org/display/DEV/Developing+a+Plugin" target="_blank" rel="external">官方教程</a><br>[2] <a href="https://deors.wordpress.com/2014/03/20/sonarqube-plugins-1/" target="_blank" rel="external">博客</a><br>[3] <a href="https://github.com/SonarSource/sonar-examples/tree/master/plugins/sonar-example-plugin" target="_blank" rel="external">官方样例</a><br>[4] <a href="https://github.com/qianyan/sonar-custom-plugin" target="_blank" rel="external">本文样例</a></p>
]]></content>
    
    <summary type="html">
    
      &lt;h2 id=&quot;插件不好写？！&quot;&gt;&lt;a href=&quot;#插件不好写？！&quot; class=&quot;headerlink&quot; title=&quot;插件不好写？！&quot;&gt;&lt;/a&gt;插件不好写？！&lt;/h2&gt;&lt;p&gt;插件确实不好写，因为插件是插入庞大的系统当中工作的，那也就意味着写插件需要具备一定的领域知识，包括系
    
    </summary>
    
      <category term="技术" scheme="https://lameta.com/categories/%E6%8A%80%E6%9C%AF/"/>
    
    
      <category term="java" scheme="https://lameta.com/tags/java/"/>
    
  </entry>
  
  <entry>
    <title>Leiningen Profile</title>
    <link href="https://lameta.com/2016/05/30/Leiningen-Profile/"/>
    <id>https://lameta.com/2016/05/30/Leiningen-Profile/</id>
    <published>2016-05-30T13:21:26.000Z</published>
    <updated>2016-09-06T22:42:23.000Z</updated>
    
    <content type="html"><![CDATA[<h2 id="记忆用的Mindmap"><a href="#记忆用的Mindmap" class="headerlink" title="记忆用的Mindmap"></a>记忆用的Mindmap</h2><p><img src="/images/Leiningen-Profiles.png" alt="leingingen profiles"></p>
<h2 id="标准的leiningen的工程目录结构"><a href="#标准的leiningen的工程目录结构" class="headerlink" title="标准的leiningen的工程目录结构"></a>标准的leiningen的工程目录结构</h2><p>当我们使用<em>lein new your-project-name</em>之后，工程目录结构如下：<br><a id="more"></a><br><figure class="highlight plain"><table><tr><td class="gutter"><pre><div class="line">1</div><div class="line">2</div><div class="line">3</div><div class="line">4</div><div class="line">5</div><div class="line">6</div><div class="line">7</div><div class="line">8</div><div class="line">9</div><div class="line">10</div><div class="line">11</div><div class="line">12</div><div class="line">13</div><div class="line">14</div></pre></td><td class="code"><pre><div class="line">.</div><div class="line">├── CHANGELOG.md</div><div class="line">├── LICENSE</div><div class="line">├── README.md</div><div class="line">├── doc</div><div class="line">│   └── intro.md</div><div class="line">├── project.clj</div><div class="line">├── resources</div><div class="line">├── src</div><div class="line">│   └── playground</div><div class="line">│       └── core.clj</div><div class="line">└── test</div><div class="line">    └── playground</div><div class="line">        └── core_test.clj</div></pre></td></tr></table></figure></p>
<p>此处的project.clj就是这个工程的描述文件，不过内容是使用clojure编写的，展开如下：<br><figure class="highlight clojure"><table><tr><td class="gutter"><pre><div class="line">1</div><div class="line">2</div><div class="line">3</div><div class="line">4</div><div class="line">5</div><div class="line">6</div></pre></td><td class="code"><pre><div class="line">(<span class="name">defproject</span> playground <span class="string">"0.1.0-SNAPSHOT"</span></div><div class="line">  <span class="symbol">:description</span> <span class="string">"FIXME: write description"</span></div><div class="line">  <span class="symbol">:url</span> <span class="string">"http://example.com/FIXME"</span></div><div class="line">  <span class="symbol">:license</span> &#123;<span class="symbol">:name</span> <span class="string">"Eclipse Public License"</span></div><div class="line">            <span class="symbol">:url</span> <span class="string">"http://www.eclipse.org/legal/epl-v10.html"</span>&#125;</div><div class="line">  <span class="symbol">:dependencies</span> [[org.clojure/clojure <span class="string">"1.8.0"</span>]])</div></pre></td></tr></table></figure></p>
<p>此时还看不到profiles的影子，不过试想一下这样的场景：某些工具只是开发时需要，打包artifact却不应包含——写过Node.js的应该对package.json中devDependencies不陌生，其实是一个道理。只不过在project.clj中是这样定义的：<br><figure class="highlight clojure"><table><tr><td class="gutter"><pre><div class="line">1</div><div class="line">2</div><div class="line">3</div><div class="line">4</div><div class="line">5</div></pre></td><td class="code"><pre><div class="line">(<span class="name">defproject</span> playground <span class="string">"0.1.0-SNAPSHOT"</span></div><div class="line">...</div><div class="line"><span class="symbol">:profiles</span> &#123;<span class="symbol">:dev</span> &#123;<span class="symbol">:plugins</span> [[refactor-nrepl <span class="string">"2.0.0-SNAPSHOT"</span>]</div><div class="line">                           [cider/cider-nrepl <span class="string">"0.10.0"</span>]]&#125;</div><div class="line">           <span class="symbol">:provided</span> &#123;<span class="symbol">:dependencies</span> [[javax.servlet/servlet-api <span class="string">"2.5"</span>]]&#125;)</div></pre></td></tr></table></figure></p>
<p>因为我使用emacs编程，除了亲和编辑源码的<a href="https://github.com/clojure-emacs/clojure-mode" target="_blank" rel="external">clojure-mode</a>，还使用了<a href="https://github.com/clojure-emacs/cider" target="_blank" rel="external">cider</a>这个与Clojure进程交互的工具，而<a href="https://github.com/clojure-emacs/clj-refactor.el" target="_blank" rel="external">clj-refactor.el</a>则是Clojure中重构的神器，所以在开发的时候，需要显式地依赖进来（不过自<a href="https://github.com/clojure-emacs/cider/releases/tag/v0.11.0" target="_blank" rel="external">CIDER 0.11.0 (Bulgaria)</a>之后两者都自动引入了）。另外，写过servlet程序的人大概对于这个servlet-api很是熟悉，看到:provided想必也会会心一笑。</p>
<p>其实这里:dev和:provided是leiningen内置的两种profile，它们类型不同，功能也迥异。在继续深究之前，我们先想想profile应该具有的功能。</p>
<h2 id="Profile的功能"><a href="#Profile的功能" class="headerlink" title="Profile的功能"></a>Profile的功能</h2><p>Profile的主要功能和pom.xml以及package.json一致，描述当前工程的配置、为构建工具（leiningen）提供一套执行流程。具体来讲，包括项目的不同scope（compile/test/provided）的依赖、开发环境依赖、各种plugin的依赖，以及范围性质的依赖，比如:user，表明当前用户下所有的leinigen工程都会使用到该profile。</p>
<p>每个project（项目）当前<strong>可用</strong>的profiles，可以通过下面这条命令查看：<br><figure class="highlight plain"><table><tr><td class="gutter"><pre><div class="line">1</div><div class="line">2</div><div class="line">3</div><div class="line">4</div><div class="line">5</div><div class="line">6</div><div class="line">7</div><div class="line">8</div><div class="line">9</div><div class="line">10</div><div class="line">11</div><div class="line">12</div><div class="line">13</div><div class="line">14</div></pre></td><td class="code"><pre><div class="line">//under root of project playground</div><div class="line">$ lein show-profiles</div><div class="line">...</div><div class="line">base</div><div class="line">debug</div><div class="line">default</div><div class="line">dev</div><div class="line">leiningen/default</div><div class="line">leiningen/test</div><div class="line">offline</div><div class="line">repl</div><div class="line">uberjar</div><div class="line">update</div><div class="line">user</div></pre></td></tr></table></figure></p>
<p>可能其他人的机器上看到的结果和我的不一致，这是正常的，原因是我在~/.lein/profiles.clj中配置过了。</p>
<h3 id="默认Profiles"><a href="#默认Profiles" class="headerlink" title="默认Profiles"></a>默认Profiles</h3><p>默认启用的profiles定义在:default当中，我们来查看default profile中包含哪些profiles？<br><figure class="highlight plain"><table><tr><td class="gutter"><pre><div class="line">1</div><div class="line">2</div><div class="line">3</div><div class="line">4</div></pre></td><td class="code"><pre><div class="line">$  lein show-profiles default</div><div class="line">[:leiningen/default] ;;指向leiningen/default</div><div class="line">$ lein show-profiles leiningen/default</div><div class="line">[:base :system :user :provided :dev] ;;五个默认启用的profiles</div></pre></td></tr></table></figure></p>
<p>这五个profiles代表的含义，在官方文档中有详细的描述，这里我只简略地描述一下:</p>
<ul>
<li>base: leiningen使用到的最基本的配置项，包括设置jvm-opts参数、往resource-paths中添加dev-resources等。它是leinigen的一部分，不应该被修改；</li>
<li>system: 和user类似，不过针对的是所有的用户；</li>
<li>user: 对当前用户下的所有projects有效。所以不应该在project.clj中设置user profile.</li>
<li>provided: 这个和pom.xml中scope是provided类似，只在生成jar的时候才有效，即生成pom.xml时，会把这个profile中定义的依赖以provided的scope添加进入pom.xml。</li>
<li>dev: 指定project专属的开发工具时会用到。</li>
</ul>
<h3 id="特定任务-task-specific-Profile"><a href="#特定任务-task-specific-Profile" class="headerlink" title="特定任务(task-specific) Profile"></a>特定任务(task-specific) Profile</h3><p>除了上面的default profile，还有像:repl和:uberjar这样以任务名称命名的profile。顾名思义，当你运行repl或者uberjar任务时，这些profiles的配置项会被合并到当前工程的profile当中。举个例子，我们定义一个uberjar profile，如下：<br><figure class="highlight clojure"><table><tr><td class="gutter"><pre><div class="line">1</div></pre></td><td class="code"><pre><div class="line"><span class="symbol">:uberjar</span> &#123;<span class="symbol">:aot</span> <span class="symbol">:all</span>&#125;</div></pre></td></tr></table></figure></p>
<p>这里有个概念需要澄清，什么是:aot?<br>Clojure的代码总是会以两种方式被编译，aot和runtime。<br>aot是指Ahead of Time Compilation，与之相对的是Runtime Compilation。 两者的区别就是aot会将生成的bytecode保存到磁盘上，也就是我们所熟知的class文件；而Runtime Compilation时的bytecode则在JVM终止后消失。</p>
<p>此时，当你运行<em>lein uberjar</em>时，就会首先在target/classes目录下生成clj源文件对应的class文件，打包包含clj和class文件；反之，如果不指定:aot，最终打出的jar包只会包含clj文件。</p>
<p>对于repl profile，这里也给出一个例子：<br><figure class="highlight plain"><table><tr><td class="gutter"><pre><div class="line">1</div></pre></td><td class="code"><pre><div class="line">:repl &#123;:dependencies [^:displace [org.clojure/clojure &quot;1.9.0-alpha3&quot;]] &#125;&#125;</div></pre></td></tr></table></figure></p>
<p>现在当你在任意工程（project）之外运行<em>lein repl</em>的时候，默认的Clojure版本就是1.9.0-alpha3了，如下：<br><figure class="highlight plain"><table><tr><td class="gutter"><pre><div class="line">1</div><div class="line">2</div><div class="line">3</div><div class="line">4</div><div class="line">5</div><div class="line">6</div><div class="line">7</div><div class="line">8</div><div class="line">9</div><div class="line">10</div></pre></td><td class="code"><pre><div class="line">nREPL server started on port 54003 on host 127.0.0.1 - nrepl://127.0.0.1:54003</div><div class="line">REPL-y 0.3.7, nREPL 0.2.12</div><div class="line">Clojure 1.9.0-alpha3 //这里的版本号</div><div class="line">Java HotSpot(TM) 64-Bit Server VM 1.8.0_45-b14</div><div class="line">    Docs: (doc function-name-here)</div><div class="line">          (find-doc &quot;part-of-name-here&quot;)</div><div class="line">  Source: (source function-name-here)</div><div class="line"> Javadoc: (javadoc java-object-or-class-here)</div><div class="line">    Exit: Control+D or (exit) or (quit)</div><div class="line"> Results: Stored in vars *1, *2, *3, an exception in *e</div></pre></td></tr></table></figure></p>
<p>这里说到在任意工程之外运行会有不一样的结果，隐含的一层意思是项目根目录下的project.clj描述文件的优先级比较高。</p>
<h2 id="Profile的优先级"><a href="#Profile的优先级" class="headerlink" title="Profile的优先级"></a>Profile的优先级</h2><p>leiningen中有3类profiles定义：</p>
<ol>
<li>项目范围：<em>your-project/profiles.clj</em></li>
<li>用户范围：<em>(or ~/.lein/profiles.clj ~/.lein/profiles.d/xxx.clj)</em></li>
<li>系统范围：<em>/etc/leiningen/profiles.clj</em></li>
</ol>
<h3 id="优先级次序"><a href="#优先级次序" class="headerlink" title="优先级次序"></a>优先级次序</h3><p>这些profiles的优先级是这样的：项目范围 &gt; project.clj &gt; 用户范围 &gt; 系统范围。其中项目范围的profile有一种典型应用：对当前项目进行个性化的配置（通过覆写project.clj中的配置），而不提交到git仓库。</p>
<h3 id="定义profiles-clj"><a href="#定义profiles-clj" class="headerlink" title="定义profiles.clj"></a>定义profiles.clj</h3><p>知道优先级之后，接下来的问题是我们该如何定义这样的profiles.clj呢？先看一下我的~/.lein/profiles.clj<br><figure class="highlight clojure"><table><tr><td class="gutter"><pre><div class="line">1</div><div class="line">2</div><div class="line">3</div><div class="line">4</div><div class="line">5</div><div class="line">6</div><div class="line">7</div><div class="line">8</div><div class="line">9</div><div class="line">10</div><div class="line">11</div><div class="line">12</div><div class="line">13</div><div class="line">14</div><div class="line">15</div><div class="line">16</div><div class="line">17</div><div class="line">18</div><div class="line">19</div><div class="line">20</div><div class="line">21</div><div class="line">22</div><div class="line">23</div></pre></td><td class="code"><pre><div class="line">&#123;<span class="symbol">:user</span>  &#123;<span class="symbol">:plugins</span> [[lein-codox <span class="string">"0.9.4"</span>]</div><div class="line">                   [lein-pprint <span class="string">"1.1.1"</span>]</div><div class="line">                   [lein-midje <span class="string">"3.1.3"</span>]</div><div class="line">                   [lein-try <span class="string">"0.4.3"</span>]</div><div class="line">                   [lein-midje-doc <span class="string">"0.0.22"</span>] ]</div><div class="line">         <span class="symbol">:injections</span>  [(<span class="name">require</span> 'spyscope.core)</div><div class="line">                       (<span class="name">require</span> 'io.aviso.repl </div><div class="line">                       'clojure.repl </div><div class="line">                       'clojure.main)</div><div class="line">              (<span class="name">alter-var-root</span> #'clojure.main/repl-caught</div><div class="line">                (<span class="name"><span class="builtin-name">constantly</span></span> @#'io.aviso.repl/pretty-pst))</div><div class="line">              (<span class="name">alter-var-root</span> #'clojure.repl/pst</div><div class="line">                (<span class="name"><span class="builtin-name">constantly</span></span> @#'io.aviso.repl/pretty-pst))]</div><div class="line"></div><div class="line">         <span class="symbol">:dependencies</span> [[org.clojure/tools.nrepl <span class="string">"0.2.12"</span>]</div><div class="line">                        <span class="comment">;; exception</span></div><div class="line">                        [io.aviso/pretty <span class="string">"0.1.26"</span>]</div><div class="line">                        <span class="comment">;; debug</span></div><div class="line">                        [spyscope <span class="string">"0.1.5"</span>] ]</div><div class="line">         <span class="symbol">:repositories</span> [[<span class="string">"releases"</span> &#123;<span class="symbol">:url</span> <span class="string">"http://blueant.com/archiva/internal"</span></div><div class="line">                           <span class="symbol">:creds</span> <span class="symbol">:gpg</span>&#125;]]&#125;</div><div class="line"> <span class="symbol">:dev</span>  &#123;<span class="symbol">:dependencies</span> [[org.clojure/tools.namespace <span class="string">"0.2.11"</span>]]&#125; </div><div class="line"> <span class="symbol">:repl</span> &#123;<span class="symbol">:dependencies</span> [^<span class="symbol">:displace</span> [org.clojure/clojure <span class="string">"1.9.0-alpha3"</span>]] &#125;&#125;</div></pre></td></tr></table></figure></p>
<p>这个文件定义了3种profiles，分别是:user, :dev, :repl. 语法结构是一个map的maps，通过keyword命名来区分。:user定义了插件、依赖和启动时自动绑定命名空间，:dev定义了依赖，:repl也定义了依赖，不过这个依赖前面用到了^:displace (Metadata)，标识在具体的项目之外才会依赖此版本的Clojure.</p>
<h2 id="启用Profile"><a href="#启用Profile" class="headerlink" title="启用Profile"></a>启用Profile</h2><p>前面提到leiningen默认会启用包含[:base :system :user :provided :dev]的leiningen/profile，default profile指向了leiningen/profile. 这里头其实包含了几层意义：</p>
<ul>
<li>profiles是可以自由组合的；</li>
<li>default profile是可以敷写的；</li>
<li>完全可以显式地启用某些profiles.</li>
</ul>
<p>如何组合这些profiles，官方文档中定义的很清楚，无须赘述。敷写default profile也比较简单，直接这么操作{:default [:profile-you-want]}. 那么只剩下如何显式地启用某些profiles了？答案是：with-profile.<br><figure class="highlight plain"><table><tr><td class="gutter"><pre><div class="line">1</div></pre></td><td class="code"><pre><div class="line">lein with-profile user repl</div></pre></td></tr></table></figure></p>
<p>这就是只启用user profile来运行repl命令了。</p>
<h2 id="使用Metadata"><a href="#使用Metadata" class="headerlink" title="使用Metadata"></a>使用Metadata</h2><p>这里的Metadata是用于描述profile自身，那么它们到底有什么作用呢？<br>文档中列举了3个Metadata: ^:leaky, ^{:pom-scope :test}, ^{:pom-scope :provided}. 并告诉我们在生成pom.xml时会根据Metadata的类型将profile中定义的依赖写入其中，但是并未讲述如何使用它们。其实很简单，只要像下面那样在profile的名字之后写上这些Metadata即可。</p>
<figure class="highlight clojure"><table><tr><td class="gutter"><pre><div class="line">1</div></pre></td><td class="code"><pre><div class="line"><span class="symbol">:repl</span> <span class="comment">^&#123;:pom-scope :test&#125;</span> &#123;<span class="symbol">:dependencies</span> [[clj-http <span class="string">"3.1.0"</span>]] &#125;</div></pre></td></tr></table></figure>
<p>leiningen会自动把该profile的dependencies的scope设置成test，如下：<br><figure class="highlight clojure"><table><tr><td class="gutter"><pre><div class="line">1</div></pre></td><td class="code"><pre><div class="line"><span class="symbol">:repl</span> &#123;<span class="symbol">:dependencies</span> [ [clj-http <span class="string">"3.1.0"</span> <span class="symbol">:scope</span> <span class="string">"test"</span>] ]&#125;</div></pre></td></tr></table></figure></p>
<p>这段逻辑可以从leiningen的<a href="https://github.com/technomancy/leiningen/blob/master/leiningen-core/src/leiningen/core/project.clj#L784" target="_blank" rel="external">源码</a>当中获得：<br><figure class="highlight"><table><tr><td class="gutter"><pre><div class="line">1</div><div class="line">2</div><div class="line">3</div></pre></td><td class="code"><pre><div class="line">(apply-profile-meta [default-meta profile]</div><div class="line">    ...</div><div class="line">    (-&gt; dep (conj :scope) (conj (name scope)))).</div></pre></td></tr></table></figure></p>
<p>所以如果你指定:repl profile生成pom.xml时，clj-http就会出现在test scope的dependency定义中。<br><figure class="highlight plain"><table><tr><td class="gutter"><pre><div class="line">1</div></pre></td><td class="code"><pre><div class="line">lein with-profile repl pom</div></pre></td></tr></table></figure></p>
<p>生成的pom文件，如下：<br><figure class="highlight xml"><table><tr><td class="gutter"><pre><div class="line">1</div><div class="line">2</div><div class="line">3</div><div class="line">4</div><div class="line">5</div><div class="line">6</div></pre></td><td class="code"><pre><div class="line"><span class="tag">&lt;<span class="name">dependency</span>&gt;</span></div><div class="line">  <span class="tag">&lt;<span class="name">groupId</span>&gt;</span>clj-http<span class="tag">&lt;/<span class="name">groupId</span>&gt;</span></div><div class="line">  <span class="tag">&lt;<span class="name">artifactId</span>&gt;</span>clj-http<span class="tag">&lt;/<span class="name">artifactId</span>&gt;</span></div><div class="line">  <span class="tag">&lt;<span class="name">version</span>&gt;</span>3.1.0<span class="tag">&lt;/<span class="name">version</span>&gt;</span></div><div class="line">  <span class="tag">&lt;<span class="name">scope</span>&gt;</span>test<span class="tag">&lt;/<span class="name">scope</span>&gt;</span></div><div class="line"><span class="tag">&lt;/<span class="name">dependency</span>&gt;</span></div></pre></td></tr></table></figure></p>
<p>其余主题Dynamic Eval和Debugging，文档中叙述的比较完整且简单。</p>
<hr>
<p>参考链接<br>[1] <a href="https://github.com/technomancy/leiningen/blob/master/doc/PROFILES.md" target="_blank" rel="external">Leiningen Profile</a></p>
]]></content>
    
    <summary type="html">
    
      &lt;h2 id=&quot;记忆用的Mindmap&quot;&gt;&lt;a href=&quot;#记忆用的Mindmap&quot; class=&quot;headerlink&quot; title=&quot;记忆用的Mindmap&quot;&gt;&lt;/a&gt;记忆用的Mindmap&lt;/h2&gt;&lt;p&gt;&lt;img src=&quot;/images/Leiningen-Profiles.png&quot; alt=&quot;leingingen profiles&quot;&gt;&lt;/p&gt;
&lt;h2 id=&quot;标准的leiningen的工程目录结构&quot;&gt;&lt;a href=&quot;#标准的leiningen的工程目录结构&quot; class=&quot;headerlink&quot; title=&quot;标准的leiningen的工程目录结构&quot;&gt;&lt;/a&gt;标准的leiningen的工程目录结构&lt;/h2&gt;&lt;p&gt;当我们使用&lt;em&gt;lein new your-project-name&lt;/em&gt;之后，工程目录结构如下：&lt;br&gt;
    
    </summary>
    
      <category term="Clojure" scheme="https://lameta.com/categories/Clojure/"/>
    
    
      <category term="leiningen" scheme="https://lameta.com/tags/leiningen/"/>
    
  </entry>
  
  <entry>
    <title>高编译低运行错误(ConcurrentHashMap.keySet)</title>
    <link href="https://lameta.com/2016/05/24/Using-Java1.8-to-Target-Older-JVMs/"/>
    <id>https://lameta.com/2016/05/24/Using-Java1.8-to-Target-Older-JVMs/</id>
    <published>2016-05-24T15:26:09.000Z</published>
    <updated>2016-09-07T08:01:19.000Z</updated>
    
    <content type="html"><![CDATA[<h2 id="问题"><a href="#问题" class="headerlink" title="问题"></a>问题</h2><p>本地使用maven编译和运行时一切都正常，但是通过ci的方式，编译、打包、发布到部署环境，运行时抛出了一条一眼便知是关于JDK版本的错误。</p>
<p>错误是这个样子：<br><figure class="highlight java"><table><tr><td class="gutter"><pre><div class="line">1</div><div class="line">2</div></pre></td><td class="code"><pre><div class="line">java.lang.NoSuchMethodError: java.util.concurrent.ConcurrentHashMap.keySet() </div><div class="line">Ljava/util/concurrent/ConcurrentHashMap$KeySetView;</div></pre></td></tr></table></figure></p>
<p>报的是的NoSuchMethodError的错误，且是关于java.util.concurrent.ConcurrentHashMap的。所以不难排查出原因是ci使用了JDK 8来进行编译，导致生成的字节码包含了JDK 8更改的新方法keySet——ConcurrentHashMap$KeySetView这个新增内部类作为其返回值的类型。</p>
<p>为了进一步验证部署服务器上的class文件都是JDK 8编译的，我使用javap这个JDK自带的工具做了如下的验证：<br><figure class="highlight java"><table><tr><td class="gutter"><pre><div class="line">1</div></pre></td><td class="code"><pre><div class="line">javap -v a.class |grep major</div></pre></td></tr></table></figure></p>
<p>返回的结果是<br><figure class="highlight plain"><table><tr><td class="gutter"><pre><div class="line">1</div></pre></td><td class="code"><pre><div class="line">major version: 51</div></pre></td></tr></table></figure></p>
<p>问题初露端倪，51对应的JDK版本号应该是1.7（或者7），52才是JDK 8的major版本。这里出现了两个疑惑：</p>
<ul>
<li>为什么ci使用JDK 8编译的class会是JDK 7的编译结果？</li>
<li>既然是JDK 7编译的class文件，那为何会出现JDK 8才有的内部类？</li>
</ul>
<p>先看第一个疑惑。之前说到ci也是通过maven compiler plugin进行编译的，pom.xml中可以配置language level如下：<br><figure class="highlight xml"><table><tr><td class="gutter"><pre><div class="line">1</div><div class="line">2</div><div class="line">3</div><div class="line">4</div><div class="line">5</div><div class="line">6</div><div class="line">7</div><div class="line">8</div><div class="line">9</div></pre></td><td class="code"><pre><div class="line"><span class="tag">&lt;<span class="name">plugin</span>&gt;</span></div><div class="line">    <span class="tag">&lt;<span class="name">groupId</span>&gt;</span>org.apache.maven.plugins<span class="tag">&lt;/<span class="name">groupId</span>&gt;</span></div><div class="line">    <span class="tag">&lt;<span class="name">artifactId</span>&gt;</span>maven-compiler-plugin<span class="tag">&lt;/<span class="name">artifactId</span>&gt;</span></div><div class="line">    <span class="tag">&lt;<span class="name">version</span>&gt;</span>3.5.1<span class="tag">&lt;/<span class="name">version</span>&gt;</span></div><div class="line">    <span class="tag">&lt;<span class="name">configuration</span>&gt;</span></div><div class="line">      <span class="tag">&lt;<span class="name">source</span>&gt;</span>1.7<span class="tag">&lt;/<span class="name">source</span>&gt;</span></div><div class="line">      <span class="tag">&lt;<span class="name">target</span>&gt;</span>1.7<span class="tag">&lt;/<span class="name">target</span>&gt;</span></div><div class="line">    <span class="tag">&lt;/<span class="name">configuration</span>&gt;</span></div><div class="line"><span class="tag">&lt;/<span class="name">plugin</span>&gt;</span></div></pre></td></tr></table></figure></p>
<p>这实际对应于javac的-source和-target参数，那么这两个参数具体代表什么呢？<br><figure class="highlight plain"><table><tr><td class="gutter"><pre><div class="line">1</div><div class="line">2</div><div class="line">3</div></pre></td><td class="code"><pre><div class="line">$ javac -help</div><div class="line">-source &lt;release&gt;          Provide source compatibility with specified release</div><div class="line">-target &lt;release&gt;          Generate class files for specific VM version</div></pre></td></tr></table></figure></p>
<p>source参数指的是源代码级别的语法兼容，而target参数指的是生成release版本的兼容性的class文件，不过<strong>只确保目标VM能够加载class文件，却无法保证运行时的正确性</strong>。接下来，我们尝试使用javac加上这些参数来编译源码。</p>
<p>首先我们写一段程序，如下：<br><figure class="highlight java"><table><tr><td class="gutter"><pre><div class="line">1</div><div class="line">2</div><div class="line">3</div><div class="line">4</div><div class="line">5</div><div class="line">6</div><div class="line">7</div><div class="line">8</div><div class="line">9</div><div class="line">10</div></pre></td><td class="code"><pre><div class="line"><span class="comment">// App.java</span></div><div class="line"><span class="keyword">package</span> com.lambeta;</div><div class="line"><span class="keyword">import</span> java.util.concurrent.ConcurrentHashMap;</div><div class="line"></div><div class="line"><span class="keyword">public</span> <span class="class"><span class="keyword">class</span> <span class="title">App</span> </span>&#123;</div><div class="line">    <span class="function"><span class="keyword">public</span> <span class="keyword">static</span> <span class="keyword">void</span> <span class="title">main</span><span class="params">(String[] args)</span> </span>&#123;</div><div class="line">        ConcurrentHashMap map = <span class="keyword">new</span> ConcurrentHashMap();</div><div class="line">        map.keySet();</div><div class="line">    &#125;</div><div class="line">&#125;</div></pre></td></tr></table></figure></p>
<p>我本机的java版本是1.8，直接使用javac来编译App.java，结果如下<br><figure class="highlight plain"><table><tr><td class="gutter"><pre><div class="line">1</div><div class="line">2</div><div class="line">3</div></pre></td><td class="code"><pre><div class="line">$ javac App.java</div><div class="line">$ javap -v App.class |grep major</div><div class="line"> major version: 52</div></pre></td></tr></table></figure></p>
<p>如果指定source和target参数，再用javac编译App.java<br><figure class="highlight plain"><table><tr><td class="gutter"><pre><div class="line">1</div><div class="line">2</div><div class="line">3</div><div class="line">4</div><div class="line">5</div><div class="line">6</div><div class="line">7</div><div class="line">8</div></pre></td><td class="code"><pre><div class="line">$ java -version</div><div class="line">java version &quot;1.8.0_45&quot;</div><div class="line">...</div><div class="line">$ javac -source 7 -target 7 App.java</div><div class="line">warning: [options] bootstrap class path not set in conjunction with -source 1.7</div><div class="line">1 warning</div><div class="line">$ ls</div><div class="line">App.class App.java</div></pre></td></tr></table></figure></p>
<p>这里有个警告，我们暂时不看。先使用javap反编译App.class，观察major version以及keySet()这个方法的返回值。<br><figure class="highlight plain"><table><tr><td class="gutter"><pre><div class="line">1</div><div class="line">2</div><div class="line">3</div><div class="line">4</div><div class="line">5</div><div class="line">6</div><div class="line">7</div><div class="line">8</div></pre></td><td class="code"><pre><div class="line">$ javap -v App.class</div><div class="line">...</div><div class="line">major version: 51</div><div class="line">...</div><div class="line">9: invokevirtual #4                  </div><div class="line">// Method java/util/concurrent/ConcurrentHashMap.keySet:()</div><div class="line">Ljava/util/concurrent/ConcurrentHashMap$KeySetView;</div><div class="line">...</div></pre></td></tr></table></figure></p>
<p>这样，第二个疑惑也解开了。可以初步得出一个结论。</p>
<h3 id="小结"><a href="#小结" class="headerlink" title="小结"></a>小结</h3><p>在javac指定了这些参数，降低版本号来编译，会导致生成class文件被标识为较低版本以供指定的JVM加载。<strong>但是</strong>，基于JDK 8的bootstrap class编译而成的keySet()方法，其返回值依旧是JDK 8中ConcurrentHashMap\$KeySetView这个新增内部类。运行时，1.7的JVM尝试加载这个class文件，一定找不到KeySetView作为返回值的keySet()方法，出错。</p>
<h2 id="解决方式"><a href="#解决方式" class="headerlink" title="解决方式"></a>解决方式</h2><p>既然知道错在那里，就比较容易寻找到解决方案了。</p>
<ul>
<li>编译期间，替换掉bootstrap class</li>
<li>使用父类/接口替换子类，即ConcurrentMap替换ConcurrentHashMap声明</li>
</ul>
<h3 id="编译期间，替换掉bootstrap-clas"><a href="#编译期间，替换掉bootstrap-clas" class="headerlink" title="编译期间，替换掉bootstrap clas"></a>编译期间，替换掉bootstrap clas</h3><p>javac编译时，可以指定bootclasspath，来替换默认的加载路径，如下：<br><figure class="highlight plain"><table><tr><td class="gutter"><pre><div class="line">1</div><div class="line">2</div><div class="line">3</div><div class="line">4</div><div class="line">5</div></pre></td><td class="code"><pre><div class="line">javac -bootclasspath /Library/Java/JavaVirtualMachines/jdk1.7.0_60.jdk/Contents/Home/jre/lib/rt.jar \</div><div class="line">-source 7 -target 7 App.java</div><div class="line">// or</div><div class="line">javac -Xbootclasspath:/Library/Java/JavaVirtualMachines/jdk1.7.0_60.jdk/Contents/Home/jre/lib/rt.jar \</div><div class="line">-source 7 -target 7 App.java</div></pre></td></tr></table></figure></p>
<p>这时候，再去看看反编译的结果，就会是这样：<br><figure class="highlight plain"><table><tr><td class="gutter"><pre><div class="line">1</div><div class="line">2</div><div class="line">3</div><div class="line">4</div><div class="line">5</div></pre></td><td class="code"><pre><div class="line">...</div><div class="line">major version: 51</div><div class="line">...</div><div class="line">9: invokevirtual #4                  </div><div class="line">// Method java/util/concurrent/ConcurrentHashMap.keySet:()Ljava/util/Set;</div></pre></td></tr></table></figure></p>
<p>此时major是51(JDK 7），而keySet()的返回值也是JDK 7中的java.util.Set类型了。</p>
<h3 id="使用父类-接口替换子类，即ConcurrentMap替换ConcurrentHashMap声明"><a href="#使用父类-接口替换子类，即ConcurrentMap替换ConcurrentHashMap声明" class="headerlink" title="使用父类/接口替换子类，即ConcurrentMap替换ConcurrentHashMap声明"></a>使用父类/接口替换子类，即ConcurrentMap替换ConcurrentHashMap声明</h3><p>上一种方案虽然可行，但是却不实用——因为不能要求ci服务器上有两个不同版本的JDK，也不能要求在maven构建时传递与安装路径如此紧耦合的值作为bootclasspath的参数值。所以可以采取将具体实现类的声明替换成为其接口的方式，如下：<br><figure class="highlight java"><table><tr><td class="gutter"><pre><div class="line">1</div><div class="line">2</div><div class="line">3</div><div class="line">4</div><div class="line">5</div><div class="line">6</div><div class="line">7</div><div class="line">8</div><div class="line">9</div><div class="line">10</div></pre></td><td class="code"><pre><div class="line"><span class="keyword">package</span> com.lambeta;</div><div class="line"><span class="keyword">import</span> java.util.concurrent.ConcurrentHashMap;</div><div class="line"><span class="keyword">import</span> java.util.concurrent.ConcurrentMap;</div><div class="line"></div><div class="line"><span class="keyword">public</span> <span class="class"><span class="keyword">class</span> <span class="title">App</span> </span>&#123;</div><div class="line">    <span class="function"><span class="keyword">public</span> <span class="keyword">static</span> <span class="keyword">void</span> <span class="title">main</span><span class="params">(String[] args)</span> </span>&#123;</div><div class="line">        ConcurrentMap map = <span class="keyword">new</span> ConcurrentHashMap();</div><div class="line">        map.keySet();</div><div class="line">    &#125;</div><div class="line">&#125;</div></pre></td></tr></table></figure></p>
<p>这样编译好的字节码中就不会有ConcurrentHashMap$KeySetView这样的返回值类型了。在JDK 7上运行时，JVM动态调用的一定是ConcurrentHashMap的keySet():java.util.Set方法了。</p>
<hr>
<h2 id="结论"><a href="#结论" class="headerlink" title="结论"></a>结论</h2><ul>
<li>保证编译、打包环境和最终部署环境JDK版本的一致性</li>
<li>如果无法保证，就尽量面向接口编程，尤其是JDK中提供的类。原因是接口不易改变，而实现类遵循“宽收严发”原则，方法的入参和出参都是易变的。</li>
</ul>
<hr>
<p>参考链接<br>[1] <a href="http://vanillajava.blogspot.jp/2012/02/using-java-7-to-target-much-older-jvms.html" target="_blank" rel="external">Using Java 7 to target much older JVMs</a></p>
]]></content>
    
    <summary type="html">
    
      &lt;h2 id=&quot;问题&quot;&gt;&lt;a href=&quot;#问题&quot; class=&quot;headerlink&quot; title=&quot;问题&quot;&gt;&lt;/a&gt;问题&lt;/h2&gt;&lt;p&gt;本地使用maven编译和运行时一切都正常，但是通过ci的方式，编译、打包、发布到部署环境，运行时抛出了一条一眼便知是关于JDK版本的错误。&lt;
    
    </summary>
    
      <category term="技术" scheme="https://lameta.com/categories/%E6%8A%80%E6%9C%AF/"/>
    
    
      <category term="java" scheme="https://lameta.com/tags/java/"/>
    
      <category term="Mistakes I Hav Made" scheme="https://lameta.com/tags/Mistakes-I-Hav-Made/"/>
    
  </entry>
  
  <entry>
    <title>内部类的异常处理</title>
    <link href="https://lameta.com/2016/04/26/Java-Inner-Class-Exception-Handler/"/>
    <id>https://lameta.com/2016/04/26/Java-Inner-Class-Exception-Handler/</id>
    <published>2016-04-25T23:29:50.000Z</published>
    <updated>2016-09-06T14:41:53.000Z</updated>
    
    <content type="html"><![CDATA[<h2 id="问题"><a href="#问题" class="headerlink" title="问题"></a>问题</h2><p>最近遇到一个问题，使用Java写某个DSL标记语言X的<em>parser（解析器）Maven</em>插件的时候，对外暴露一个名为<code>Callback</code>的接口和一个待实现的方法<em>getHTML()</em>——基于调用处传入的文件名<code>srcX</code>构造出HTML文件的输出路径（其实此处的<code>Callback</code>就是一个闭包，文件名是一个自由变量）。大致代码如下：</p>
<figure class="highlight java"><table><tr><td class="gutter"><pre><div class="line">1</div><div class="line">2</div><div class="line">3</div><div class="line">4</div><div class="line">5</div><div class="line">6</div><div class="line">7</div><div class="line">8</div><div class="line">9</div><div class="line">10</div><div class="line">11</div><div class="line">12</div><div class="line">13</div><div class="line">14</div><div class="line">15</div><div class="line">16</div><div class="line">17</div></pre></td><td class="code"><pre><div class="line">parser.parse(srcX, <span class="keyword">new</span> Callback() &#123;</div><div class="line">    <span class="meta">@Override</span></div><div class="line">    <span class="function"><span class="keyword">public</span> FileWriter <span class="title">getHTML</span><span class="params">()</span> </span>&#123;</div><div class="line">        <span class="keyword">return</span> <span class="keyword">new</span> FileWriter(outputPath(suffix(srcX, <span class="string">"html"</span>)));</div><div class="line">    &#125;</div><div class="line">&#125;);</div><div class="line"></div><div class="line"><span class="function"><span class="keyword">private</span> String <span class="title">suffix</span><span class="params">(String filename, String suffix)</span> </span>&#123;</div><div class="line">	<span class="keyword">return</span> Joiner.on(<span class="string">"."</span>).join(filename, suffix);</div><div class="line">&#125;</div><div class="line"></div><div class="line"><span class="comment">//这里假设输入和输出根路径地址已知</span></div><div class="line"><span class="function"><span class="keyword">private</span> File <span class="title">outputPath</span><span class="params">(String file)</span> </span>&#123;</div><div class="line">	<span class="keyword">return</span> <span class="keyword">new</span> File(</div><div class="line">	           file.replace(srcDir.getAbsolutePath(), <span class="comment">//srcDir: File</span></div><div class="line">	             outputDir.getAbsolutePath())); <span class="comment">//outputDir: File</span></div><div class="line">&#125;</div></pre></td></tr></table></figure>
<p>目前为止还没有任何问题。但若是运行时，这段程序很可能抛出异常<em>java.io.FileNotFoundException: your-file-name (No such file or directory)</em>。原因在于<em>file</em>的路径当中可能存在多级父级目录，例如：<em>outputDir/p1/p2/srcX.html</em>，那么当<em>FileWriter</em>尝试创建<em>srcX.html</em>就会失败。此时最简单的方法就是提前创建好所有的父级目录，于是<em>outputPath()</em>方法会变成下面这样：<br><figure class="highlight java"><table><tr><td class="gutter"><pre><div class="line">1</div><div class="line">2</div><div class="line">3</div><div class="line">4</div><div class="line">5</div><div class="line">6</div><div class="line">7</div><div class="line">8</div></pre></td><td class="code"><pre><div class="line"><span class="function"><span class="keyword">private</span> File <span class="title">outputPath</span><span class="params">(String file)</span> </span>&#123;</div><div class="line">    File outputFile = <span class="keyword">new</span> File(</div><div class="line">            file.replace(srcDir.getAbsolutePath(),</div><div class="line">                    outputDir.getAbsolutePath()));</div><div class="line">    outputFile.getParentFile().mkdirs(); <span class="comment">//创建可能不存在的父级目录</span></div><div class="line"></div><div class="line">    <span class="keyword">return</span> outputFile;</div><div class="line">&#125;</div></pre></td></tr></table></figure></p>
<p>似乎这段程序可以正常工作了，但是创建文件夹这样的操作是可能失败的。所以我们需要关注是否创建成功，若失败，则写入Log文件当中。修改程序如下：<br><figure class="highlight java"><table><tr><td class="gutter"><pre><div class="line">1</div><div class="line">2</div><div class="line">3</div><div class="line">4</div><div class="line">5</div><div class="line">6</div><div class="line">7</div><div class="line">8</div><div class="line">9</div><div class="line">10</div><div class="line">11</div><div class="line">12</div></pre></td><td class="code"><pre><div class="line"><span class="function"><span class="keyword">private</span> File <span class="title">outputPath</span><span class="params">(String file)</span> </span>&#123;</div><div class="line">    File outputFile = <span class="keyword">new</span> File(</div><div class="line">            file.replace(srcDir.getAbsolutePath(),</div><div class="line">                    outputDir.getAbsolutePath()));</div><div class="line">    <span class="keyword">final</span> File parentDirs = outputFile.getParentFile();</div><div class="line">    <span class="keyword">if</span> (!parentDirs.exists()) &#123;</div><div class="line">        <span class="keyword">if</span> (!parentDirs.mkdirs()) &#123;<span class="comment">//创建可能不存在的父级目录</span></div><div class="line">            getLog().error(<span class="string">"Cannot create parent dirs for &#123;&#125;"</span>, outputFile);</div><div class="line">        &#125;</div><div class="line">    &#125;</div><div class="line">    <span class="keyword">return</span> outputFile;</div><div class="line">&#125;</div></pre></td></tr></table></figure></p>
<p><strong>注意</strong><br>这里我们需要先判断父级目录是否存在，即<em>parentDirs.exists()？</em>可是<em>parentDirs.mkdirs()</em>不是直接返回boolean值来表示是否创建成功吗？是这样么？这儿有<em>mkdirs()</em>方法的说明：</p>
<blockquote>
<p>public boolean mkdirs()<br>Creates the directory named by this abstract pathname, including any necessary but nonexistent parent directories. Note that if this operation fails it may have succeeded in creating some of the necessary parent directories.<br>Returns:<br>true if and only if the directory was created, along with all necessary parent directories; false otherwise</p>
</blockquote>
<p>也就是说只有当这个目录及其所有的父级目录都被创建时，才返回true，反之返回false。照这个推论，如果所有目录事先已经存在了，这个方法应该也会返回true，毕竟都被创建过了嘛。但是只要稍微看一眼源码，你就会发现事实并非如此：<br><figure class="highlight java"><table><tr><td class="gutter"><pre><div class="line">1</div><div class="line">2</div><div class="line">3</div><div class="line">4</div></pre></td><td class="code"><pre><div class="line"><span class="comment">//mkdirs源码</span></div><div class="line"><span class="keyword">if</span> (exists()) &#123;</div><div class="line">    <span class="keyword">return</span> <span class="keyword">false</span>;</div><div class="line">&#125;</div></pre></td></tr></table></figure></p>
<p>所以这里需要特别强调<strong>was created</strong>是一种操作，如果没有进行这个操作，那就不能算这个方法成功。</p>
<p>前面已经提到过，我需要写一个maven的插件，所以最好在这种导致程序崩溃的地方抛出一个maven中通用的异常<em>MojoExecutionException</em>。这样，更改代码如下：<br><figure class="highlight java"><table><tr><td class="gutter"><pre><div class="line">1</div><div class="line">2</div><div class="line">3</div><div class="line">4</div><div class="line">5</div><div class="line">6</div><div class="line">7</div><div class="line">8</div><div class="line">9</div><div class="line">10</div><div class="line">11</div><div class="line">12</div><div class="line">13</div></pre></td><td class="code"><pre><div class="line"><span class="function"><span class="keyword">private</span> File <span class="title">outputPath</span><span class="params">(String file)</span> </span>&#123;</div><div class="line">    File outputFile = <span class="keyword">new</span> File(</div><div class="line">            file.replace(srcDir.getAbsolutePath(),</div><div class="line">                    outputDir.getAbsolutePath()));</div><div class="line">    <span class="keyword">final</span> File parentDirs = outputFile.getParentFile();</div><div class="line">    <span class="keyword">if</span> (!parentDirs.exists()) &#123;</div><div class="line">        <span class="keyword">if</span> (!parentDirs.mkdirs()) &#123;<span class="comment">//创建可能不存在的父级目录</span></div><div class="line">            getLog().error(<span class="string">"Cannot create parent dirs for &#123;&#125;"</span>, outputFile);</div><div class="line">            <span class="keyword">throw</span> <span class="keyword">new</span> MojoExecutionException(<span class="string">"Cannot create parent dirs"</span>);</div><div class="line">        &#125;</div><div class="line">    &#125;</div><div class="line">    <span class="keyword">return</span> outputFile;</div><div class="line">&#125;</div></pre></td></tr></table></figure></p>
<p>此时，问题才显出端倪——异常<em>MojoExecutionException</em>是一个受检的异常(checked Exception)，它间接继承自<em>java.lang.Exception</em>。可是我们的<em>getHTML()</em>方法并没有在签名中抛出任何异常，编译无法通过。那唯一的办法就是<em>try…catch</em>了，但是我不应该捕获自己<strong>刚刚</strong>抛出来的异常，否则抛出受检异常的意义何在？</p>
<p>这时，自然而然会想到，将方法签名改成<em>getHTML() throws MojoExecutionException</em>。确实可行，但是并不合适，因为<em>MojoExecutionException</em>只是Maven插件规定的异常，而getHTML()则是一个对外暴露的API，不应该依赖于某个具体的异常。所以我将异常扩大化：<em>getHTML() throws Exception</em>，这样做的好处很明显，坏处也很显眼。</p>
<h3 id="好处"><a href="#好处" class="headerlink" title="好处"></a>好处</h3><ol>
<li>牢记《Unix编程艺术》中的“宽收严发”原则。即子类实现父类、接口的方法，入参可以扩大，出参可以缩小。举个例子：父类、接口有个方法</li>
</ol>
<figure class="highlight java"><table><tr><td class="gutter"><pre><div class="line">1</div></pre></td><td class="code"><pre><div class="line"><span class="function"><span class="keyword">public</span> Object <span class="title">something</span><span class="params">(HashMap map)</span> <span class="keyword">throws</span> Exception</span></div></pre></td></tr></table></figure>
<p>那么子类实现这个方法可以这样写</p>
<figure class="highlight java"><table><tr><td class="gutter"><pre><div class="line">1</div><div class="line">2</div></pre></td><td class="code"><pre><div class="line"><span class="function"><span class="keyword">public</span> String <span class="title">something</span><span class="params">(Map map)</span></span></div><div class="line">                  <span class="keyword">throws</span> ExecutionException, NoSuchMethodException</div></pre></td></tr></table></figure>
<p>这里，入参是HashMap，出参是Object和Exception。入参扩大，所以子类出现了Map；出参缩小，所以子类出现了String和ExecutionException和NoSuchMethodException。同理，此处<em>getHTML() throws Exception</em>由子类实现的形式可以是<em>getHTML() throws MojoExecutionException</em>。</p>
<h3 id="坏处"><a href="#坏处" class="headerlink" title="坏处"></a>坏处</h3><ol>
<li>不管getHTML()是否需要抛出异常，你都得在实现代码中抛出异常；</li>
<li>由于对外表现的是抛出较宽泛的Exception，所以丧失了对于具体受检 (checked exception)异常进行检查的好处。</li>
</ol>
<p>这里有个JDK中比较类似的例子，就是关于<code>Runnable</code>和<code>Callable</code>接口的设计问题：</p>
<figure class="highlight java"><table><tr><td class="gutter"><pre><div class="line">1</div><div class="line">2</div><div class="line">3</div><div class="line">4</div><div class="line">5</div><div class="line">6</div><div class="line">7</div></pre></td><td class="code"><pre><div class="line"><span class="keyword">public</span> <span class="class"><span class="keyword">interface</span> <span class="title">Runnable</span> </span>&#123;</div><div class="line">    <span class="function"><span class="keyword">public</span> <span class="keyword">void</span> <span class="title">run</span><span class="params">()</span></span>;</div><div class="line">&#125;</div><div class="line"></div><div class="line"><span class="keyword">public</span> <span class="class"><span class="keyword">interface</span> <span class="title">Callable</span>&lt;<span class="title">V</span>&gt; </span>&#123;</div><div class="line">    <span class="function">V <span class="title">call</span><span class="params">()</span> <span class="keyword">throws</span> Exception</span>;</div><div class="line">&#125;</div></pre></td></tr></table></figure>
<p>它们就是两个极端，<code>Runnable</code>必须将受检的异常转换成非受检(unchecked exception)或者发明一种方式来将异常暴露给调用者；<code>Callable</code>就是无论如何都得抛出异常，而且迫使用户去捕获一个较宽泛的异常。</p>
<h2 id="解决方式"><a href="#解决方式" class="headerlink" title="解决方式"></a>解决方式</h2><p>这个时候，泛型就派上用场了。<br><figure class="highlight java"><table><tr><td class="gutter"><pre><div class="line">1</div><div class="line">2</div><div class="line">3</div><div class="line">4</div><div class="line">5</div><div class="line">6</div></pre></td><td class="code"><pre><div class="line"><span class="class"><span class="keyword">interface</span> <span class="title">Callback</span>&lt;<span class="title">E</span> <span class="keyword">extends</span> <span class="title">Exception</span>&gt; </span>&#123;</div><div class="line">    <span class="function">FileWriter <span class="title">getHTML</span><span class="params">()</span> <span class="keyword">throws</span> E</span>;</div><div class="line">&#125;</div><div class="line"></div><div class="line"><span class="comment">//interface parser</span></div><div class="line"><span class="keyword">public</span> &lt;E extends Exception&gt; <span class="function"><span class="keyword">void</span> <span class="title">parse</span><span class="params">(String srcX, Callback&lt;E&gt; cb)</span> <span class="keyword">throws</span> E</span>;</div></pre></td></tr></table></figure></p>
<p>通过这种方式，我们可以捕获具体的异常：<br><figure class="highlight java"><table><tr><td class="gutter"><pre><div class="line">1</div><div class="line">2</div><div class="line">3</div><div class="line">4</div><div class="line">5</div><div class="line">6</div><div class="line">7</div><div class="line">8</div><div class="line">9</div><div class="line">10</div></pre></td><td class="code"><pre><div class="line"><span class="keyword">try</span> &#123;</div><div class="line">    parser.parse(srcX, <span class="keyword">new</span> Callback&lt;MojoExecutionException&gt;() &#123;</div><div class="line">        <span class="meta">@Override</span></div><div class="line">        <span class="function"><span class="keyword">public</span> FileWriter <span class="title">getHTML</span><span class="params">()</span> <span class="keyword">throws</span> MojoExecutionException </span>&#123;</div><div class="line">            <span class="keyword">return</span> <span class="keyword">new</span> FileWriter(outputPath(suffix(srcX, <span class="string">"html"</span>)));</div><div class="line">        &#125;</div><div class="line">    &#125;);</div><div class="line">&#125; <span class="keyword">catch</span> (MojoExecutionException e) &#123;</div><div class="line">    getLog().error(<span class="string">"Failed to execute. &#123;&#125;"</span>, e);</div><div class="line">&#125;</div></pre></td></tr></table></figure></p>
<p>使用lambda表达式可以简化成下面的模样：<br><figure class="highlight java"><table><tr><td class="gutter"><pre><div class="line">1</div><div class="line">2</div><div class="line">3</div><div class="line">4</div><div class="line">5</div></pre></td><td class="code"><pre><div class="line"><span class="keyword">try</span> &#123;</div><div class="line">    parser.parse(srcX, (Callback&lt;MojoExecutionException&gt;) () -&gt; <span class="keyword">new</span> FileWriter(outputPath(suffix(srcX, <span class="string">"html"</span>))));</div><div class="line">&#125; <span class="keyword">catch</span> (MojoExecutionException e) &#123;</div><div class="line">    getLog().error(<span class="string">"Failed to execute. &#123;&#125;"</span>, e);</div><div class="line">&#125;</div></pre></td></tr></table></figure></p>
<p>我们解决了<em>迫使用户去捕获一个较宽泛的异常</em>的问题，但是<em>无论如何都得抛出异常这个问题</em>还是没有得到解决。或许我们需要一个像是<em>throws Nothing</em>一样的语法，表示什么也没有抛出来。我们知道RuntimeException是非受检的异常(unchecked exception)，所以<em>throws RuntimeException</em>就表明这个异常跟没有抛出异常一样，不需要捕获。如下：<br><figure class="highlight java"><table><tr><td class="gutter"><pre><div class="line">1</div><div class="line">2</div><div class="line">3</div><div class="line">4</div><div class="line">5</div><div class="line">6</div><div class="line">7</div><div class="line">8</div></pre></td><td class="code"><pre><div class="line">parser.parse(srcX, <span class="keyword">new</span> Callback&lt;Nothing&gt;() &#123;</div><div class="line">    <span class="meta">@Override</span></div><div class="line">    <span class="function"><span class="keyword">public</span> FileWriter <span class="title">getHTML</span><span class="params">()</span> <span class="keyword">throws</span> Nothing </span>&#123;</div><div class="line">        <span class="keyword">return</span> <span class="keyword">new</span> FileWriter(outputPath(suffix(srcX, <span class="string">"html"</span>)));</div><div class="line">    &#125;</div><div class="line">&#125;);</div><div class="line"></div><div class="line"><span class="keyword">public</span> <span class="keyword">abstract</span> <span class="class"><span class="keyword">class</span> <span class="title">Nothing</span> <span class="keyword">extends</span> <span class="title">RuntimeException</span> </span>&#123;&#125;</div></pre></td></tr></table></figure></p>
<p>走到这一步，我们算是较为完全地解决了匿名内部类的异常处理问题。</p>
<h2 id="异常透明化"><a href="#异常透明化" class="headerlink" title="异常透明化"></a>异常透明化</h2><blockquote>
<p>With the throws type parameter on the Block interface, we can now accurately generify over the set of exceptions thrown by the Block; with the generic forEach method, we can mirror the exception behavior of the block in forEach(). This is called exception transparency because now the exception behavior of forEach can match the exception behavior of its block argument. Exception transparency simplifies the construction of library classes that implement idioms like internal iteration of data structures, because it is common that methods that accept function-valued arguments will invoke those functions, meaning that the library method will throw a superset of the exceptions thrown by its function-valued arguments.</p>
</blockquote>
<figure class="highlight java"><table><tr><td class="gutter"><pre><div class="line">1</div><div class="line">2</div><div class="line">3</div><div class="line">4</div><div class="line">5</div><div class="line">6</div><div class="line">7</div></pre></td><td class="code"><pre><div class="line"><span class="class"><span class="keyword">interface</span> <span class="title">Block</span>&lt;<span class="title">T</span>, <span class="title">throws</span> <span class="title">E</span>&gt; </span>&#123;</div><div class="line">    <span class="function"><span class="keyword">public</span> <span class="keyword">void</span> <span class="title">invoke</span><span class="params">(T element)</span> <span class="keyword">throws</span> E</span>;</div><div class="line">&#125;</div><div class="line"></div><div class="line"><span class="class"><span class="keyword">interface</span> <span class="title">NewCollection</span>&lt;<span class="title">T</span>&gt; </span>&#123;</div><div class="line">    <span class="keyword">public</span>&lt;<span class="keyword">throws</span> E&gt; forEach(Block&lt;T, <span class="keyword">throws</span> E&gt; block) <span class="keyword">throws</span> E;</div><div class="line">&#125;</div></pre></td></tr></table></figure>
<p>异常透明化，简单来讲，就是调用者的签名中的异常完全由它的函数值(function-valued)的参数决定，所有这些调用者最终的异常都会是该函数值所注异常的超集。</p>
<p>异常透明化就是用来解决我们常用的通过内部类模拟闭包调用时异常处理的手法了。</p>
<hr>
<p><strong>闭包的定义</strong><br>一个包含了自由变量的开发表达式，和该自由变量的约束环境组合之后，产生了一种封闭的状态。</p>
<hr>
<p>参考链接<br>[1] <a href="https://blogs.oracle.com/briangoetz/entry/exception_transparency_in_java" target="_blank" rel="external">Exception Transparency</a><br>[2] <a href="http://stackoverflow.com/questions/8101575/throwing-checked-exceptions-from-anonymous-inner-classes" target="_blank" rel="external">Throwing Checked Exceptions from Anonymous Inner Classes</a></p>
]]></content>
    
    <summary type="html">
    
      &lt;h2 id=&quot;问题&quot;&gt;&lt;a href=&quot;#问题&quot; class=&quot;headerlink&quot; title=&quot;问题&quot;&gt;&lt;/a&gt;问题&lt;/h2&gt;&lt;p&gt;最近遇到一个问题，使用Java写某个DSL标记语言X的&lt;em&gt;parser（解析器）Maven&lt;/em&gt;插件的时候，对外暴露一个名为&lt;cod
    
    </summary>
    
      <category term="技术" scheme="https://lameta.com/categories/%E6%8A%80%E6%9C%AF/"/>
    
    
      <category term="java" scheme="https://lameta.com/tags/java/"/>
    
      <category term="Mistakes I Hav Made" scheme="https://lameta.com/tags/Mistakes-I-Hav-Made/"/>
    
  </entry>
  
  <entry>
    <title>Node.js on Windows</title>
    <link href="https://lameta.com/2016/03/22/Node-js-On-Windows/"/>
    <id>https://lameta.com/2016/03/22/Node-js-On-Windows/</id>
    <published>2016-03-22T14:39:24.000Z</published>
    <updated>2016-09-06T14:56:31.000Z</updated>
    
    <content type="html"><![CDATA[<h2 id="搭建-Node-js-开发环境"><a href="#搭建-Node-js-开发环境" class="headerlink" title="搭建 Node.js 开发环境"></a>搭建 Node.js 开发环境</h2><h3 id="NVM安装"><a href="#NVM安装" class="headerlink" title="NVM安装"></a>NVM安装</h3><p>1. <a href="https://github.com/coreybutler/nvm/releases" target="_blank" rel="external">Download nvm-noinstall.zip</a><br>2. Update the system environment variables:<br><em>NVM_HOME</em>, <em>NVM_SYMLINK</em> (C:\Users\Program Files\nodejs <strong>This directory should not exist in previously.</strong>)<br>3. Create <em>settings.txt</em> file</p>
<blockquote>
<p>root: C:\Users\qinayan\bin\nvm<br>path: C:\Program Files\nodejs<br>arch: 64<br>proxy: none</p>
</blockquote>
<p>详情请参考 <a href="https://github.com/coreybutler/nvm-windows/wiki#manual-installation" target="_blank" rel="external">如何安装nvm-windows</a><br>另外别忘了在<em>NVM_HOME</em>目录中运行<em>install</em>命令</p>
<h3 id="node-js安装"><a href="#node-js安装" class="headerlink" title="node.js安装"></a>node.js安装</h3><figure class="highlight lsl"><table><tr><td class="gutter"><pre><div class="line">1</div><div class="line">2</div><div class="line">3</div><div class="line">4</div><div class="line">5</div><div class="line">6</div><div class="line">7</div><div class="line">8</div><div class="line">9</div><div class="line">10</div><div class="line">11</div><div class="line">12</div><div class="line">13</div><div class="line">14</div></pre></td><td class="code"><pre><div class="line">安装特定版本的nodejs</div><div class="line">&gt; nvm install <span class="number">0.12</span><span class="number">.10</span></div><div class="line">安装最新版本的nodejs</div><div class="line">&gt; nvm install latest</div><div class="line">查看本地安装了哪些nodejs</div><div class="line">&gt; nvm ls</div><div class="line"> * <span class="number">5.7</span><span class="number">.1</span> (Currently using <span class="number">64</span>-bit executable)</div><div class="line">   <span class="number">0.12</span><span class="number">.10</span></div><div class="line">验证安装完成   </div><div class="line">&gt; node -v</div><div class="line">  v5<span class="number">.7</span><span class="number">.1</span></div><div class="line">&gt; node</div><div class="line">...&gt; console.log(<span class="string">"hello world"</span>);</div><div class="line">  hello world</div></pre></td></tr></table></figure>
<h2 id="模块"><a href="#模块" class="headerlink" title="模块"></a>模块</h2><p>每个文件就是一个模块，文件的路径名就是模块的名字</p>
<h3 id="require"><a href="#require" class="headerlink" title="require"></a>require</h3><p>类似于Java中的<code>import</code>关键字，导入不同的包。<br><figure class="highlight ebnf"><table><tr><td class="gutter"><pre><div class="line">1</div></pre></td><td class="code"><pre><div class="line"><span class="attribute">var express</span> = require(<span class="string">'express'</span>);</div></pre></td></tr></table></figure></p>
<h3 id="exports"><a href="#exports" class="headerlink" title="exports"></a>exports</h3><p>导出模块的公有方法和属性。可以理解为Java中的<code>public</code>方法和属性。<br><figure class="highlight javascript"><table><tr><td class="gutter"><pre><div class="line">1</div><div class="line">2</div><div class="line">3</div><div class="line">4</div><div class="line">5</div><div class="line">6</div><div class="line">7</div><div class="line">8</div></pre></td><td class="code"><pre><div class="line"><span class="comment">// util.js</span></div><div class="line">exports.greeting = <span class="function"><span class="keyword">function</span>(<span class="params">name</span>) </span>&#123;</div><div class="line">   <span class="keyword">return</span> <span class="string">"hello, "</span> + name;   </div><div class="line">&#125;</div><div class="line"><span class="comment">// index.js</span></div><div class="line"><span class="keyword">var</span> greeting = <span class="built_in">require</span>(<span class="string">'./util'</span>).greeting;</div><div class="line"><span class="built_in">console</span>.log(greeting(<span class="string">"lambeta"</span>))</div><div class="line">&gt;  hello, lambeta</div></pre></td></tr></table></figure></p>
<h3 id="module"><a href="#module" class="headerlink" title="module"></a>module</h3><p>包含当前模块的一些信息，常用的做法是替换当前模块的导出对象。<br><figure class="highlight taggerscript"><table><tr><td class="gutter"><pre><div class="line">1</div><div class="line">2</div><div class="line">3</div><div class="line">4</div><div class="line">5</div><div class="line">6</div><div class="line">7</div><div class="line">8</div><div class="line">9</div><div class="line">10</div><div class="line">11</div><div class="line">12</div><div class="line">13</div><div class="line">14</div><div class="line">15</div><div class="line">16</div><div class="line">17</div><div class="line">18</div><div class="line">19</div><div class="line">20</div><div class="line">21</div><div class="line">22</div><div class="line">23</div><div class="line">24</div><div class="line">25</div><div class="line">26</div><div class="line">27</div><div class="line">28</div><div class="line">29</div><div class="line">30</div><div class="line">31</div><div class="line">32</div><div class="line">33</div></pre></td><td class="code"><pre><div class="line">// util.js</div><div class="line">console.log(module)</div><div class="line">&gt;&gt;</div><div class="line">Module &#123;</div><div class="line">  id: '.',</div><div class="line">  exports: &#123; greeting: [Function] &#125;,</div><div class="line">  parent: null,</div><div class="line">  filename: 'C:<span class="symbol">\\</span>Users<span class="symbol">\\</span>qianyan<span class="symbol">\\</span>Projects<span class="symbol">\\</span>lesson2<span class="symbol">\\</span>util.js',</div><div class="line">  loaded: false,</div><div class="line">  children: [],</div><div class="line">  paths:</div><div class="line">   [ 'C:<span class="symbol">\\</span>Users<span class="symbol">\\</span>qianyan<span class="symbol">\\</span>Projects<span class="symbol">\\</span>lesson2<span class="symbol">\\</span>node_modules',</div><div class="line">     'C:<span class="symbol">\\</span>Users<span class="symbol">\\</span>qianyan<span class="symbol">\\</span>Projects<span class="symbol">\\</span>node_modules',</div><div class="line">     'C:<span class="symbol">\\</span>Users<span class="symbol">\\</span>qianyan<span class="symbol">\\</span>node_modules',</div><div class="line">     'C:<span class="symbol">\\</span>Users<span class="symbol">\\</span>node_modules',</div><div class="line">     'C:<span class="symbol">\\</span>node_modules' ] &#125;</div><div class="line"></div><div class="line">// replace with obj</div><div class="line">module.exports = &#123;greeting: &#123;&#125;&#125;;</div><div class="line">&gt;&gt;</div><div class="line">Module &#123;</div><div class="line">  id: '.',</div><div class="line">  exports: &#123; greeting: &#123;&#125; &#125;,</div><div class="line">  parent: null,</div><div class="line">  filename: 'C:<span class="symbol">\\</span>Users<span class="symbol">\\</span>qianyan<span class="symbol">\\</span>Projects<span class="symbol">\\</span>lesson2<span class="symbol">\\</span>util.js',</div><div class="line">  loaded: false,</div><div class="line">  children: [],</div><div class="line">  paths:</div><div class="line">   [ 'C:<span class="symbol">\\</span>Users<span class="symbol">\\</span>qianyan<span class="symbol">\\</span>Projects<span class="symbol">\\</span>lesson2<span class="symbol">\\</span>node_modules',</div><div class="line">     'C:<span class="symbol">\\</span>Users<span class="symbol">\\</span>qianyan<span class="symbol">\\</span>Projects<span class="symbol">\\</span>node_modules',</div><div class="line">     'C:<span class="symbol">\\</span>Users<span class="symbol">\\</span>qianyan<span class="symbol">\\</span>node_modules',</div><div class="line">     'C:<span class="symbol">\\</span>Users<span class="symbol">\\</span>node_modules',</div><div class="line">     'C:<span class="symbol">\\</span>node_modules' ] &#125;</div></pre></td></tr></table></figure></p>
<h3 id="module-initialize发生的时机"><a href="#module-initialize发生的时机" class="headerlink" title="module initialize发生的时机"></a>module initialize发生的时机</h3><p>模块中的代码只会在首次被使用的时候才会执行一次，同时初始化该模块的导出对象，之后导出对象会被缓存到内存当中，供任意使用。</p>
<h2 id="小结"><a href="#小结" class="headerlink" title="小结"></a>小结</h2><ul>
<li>NVM是Node Version Manager，管理node的版本的工具。使用NVM，可以保证同一个操作系统下，多个不同版本的node得以共存。</li>
<li>node作为javascript的解析器，可以在终端下进入交互式模式(repl read-eval-print-loop)，很方便快速地反馈我们程序的结果。</li>
<li>nodeJS的模块系统实现了CMD标准，即<a href="http://wiki.commonjs.org/" target="_blank" rel="external">CommonJS Module Definition</a>标准；而对于运行在浏览器上的javascript的模块化，因为需要异步加载js文件，所以由require.js实现了AMD (Asynchronous Module Definition)标准</li>
</ul>
<figure class="highlight javascript"><table><tr><td class="gutter"><pre><div class="line">1</div><div class="line">2</div><div class="line">3</div><div class="line">4</div><div class="line">5</div><div class="line">6</div><div class="line">7</div><div class="line">8</div><div class="line">9</div><div class="line">10</div><div class="line">11</div><div class="line">12</div></pre></td><td class="code"><pre><div class="line">CMD</div><div class="line"><span class="built_in">require</span>(<span class="string">'express'</span>)</div><div class="line"></div><div class="line">AMD</div><div class="line">define(<span class="string">"alpha"</span>, [<span class="string">"require"</span>, <span class="string">"exports"</span>, <span class="string">"beta"</span>],</div><div class="line"><span class="function"><span class="keyword">function</span> (<span class="params">require, exports, beta</span>) </span>&#123;</div><div class="line">       exports.verb = <span class="function"><span class="keyword">function</span>(<span class="params"></span>) </span>&#123;</div><div class="line">           <span class="keyword">return</span> beta.verb();</div><div class="line">           <span class="comment">//Or:</span></div><div class="line">           <span class="keyword">return</span> <span class="built_in">require</span>(<span class="string">"beta"</span>).verb();</div><div class="line">       &#125;</div><div class="line">&#125;);</div></pre></td></tr></table></figure>
<h2 id="问题"><a href="#问题" class="headerlink" title="问题"></a>问题</h2><ul>
<li>是否可以使用<code>require(&#39;./data.json&#39;</code>)将json文件引入到我们的程序当中呢？</li>
<li>有两个js文件同时引入了<code>data.json</code>，先执行a.js，后执行b.js。下面的程序会输出什么？<figure class="highlight lasso"><table><tr><td class="gutter"><pre><div class="line">1</div><div class="line">2</div><div class="line">3</div><div class="line">4</div><div class="line">5</div><div class="line">6</div><div class="line">7</div><div class="line">8</div><div class="line">9</div><div class="line">10</div></pre></td><td class="code"><pre><div class="line"><span class="comment">//data.json</span></div><div class="line">&#123;<span class="string">"hello"</span>: <span class="string">"world"</span>&#125;</div><div class="line"></div><div class="line"><span class="comment">//a.js</span></div><div class="line"><span class="built_in">var</span> <span class="built_in">data</span> = <span class="keyword">require</span>(<span class="string">'./data.json'</span>);</div><div class="line"><span class="built_in">data</span> = &#123;&#125;;</div><div class="line"></div><div class="line"><span class="comment">//b.js</span></div><div class="line"><span class="built_in">var</span> <span class="built_in">data</span> = <span class="keyword">require</span>(<span class="string">'./data.json'</span>);</div><div class="line">console.<span class="keyword">log</span>(<span class="built_in">data</span>)</div></pre></td></tr></table></figure>
</li>
</ul>
<hr>
<h2 id="代码组织"><a href="#代码组织" class="headerlink" title="代码组织"></a>代码组织</h2><h3 id="模块解析路径"><a href="#模块解析路径" class="headerlink" title="模块解析路径"></a>模块解析路径</h3><h4 id="node-modules"><a href="#node-modules" class="headerlink" title="node_modules"></a>node_modules</h4><p>不想直接require文件路径名，因为这样一旦所依赖的文件路径发生变化，牵扯的文件会很多。所以我们需要一个约定的根目录。这个根目录就是<code>node_modules</code><br>以这个文件的路径为例：<code>C:\\Users\\qianyan\\Projects\\lesson2\\util.js</code>，node搜索的路径如下。<br><figure class="highlight taggerscript"><table><tr><td class="gutter"><pre><div class="line">1</div><div class="line">2</div><div class="line">3</div><div class="line">4</div><div class="line">5</div><div class="line">6</div></pre></td><td class="code"><pre><div class="line">paths:</div><div class="line">  [ 'C:<span class="symbol">\\</span>Users<span class="symbol">\\</span>qianyan<span class="symbol">\\</span>Projects<span class="symbol">\\</span>lesson2<span class="symbol">\\</span>node_modules',</div><div class="line">    'C:<span class="symbol">\\</span>Users<span class="symbol">\\</span>qianyan<span class="symbol">\\</span>Projects<span class="symbol">\\</span>node_modules',</div><div class="line">    'C:<span class="symbol">\\</span>Users<span class="symbol">\\</span>qianyan<span class="symbol">\\</span>node_modules',</div><div class="line">    'C:<span class="symbol">\\</span>Users<span class="symbol">\\</span>node_modules',</div><div class="line">    'C:<span class="symbol">\\</span>node_modules' ]</div></pre></td></tr></table></figure></p>
<h4 id="NODE-PATH"><a href="#NODE-PATH" class="headerlink" title="NODE_PATH"></a>NODE_PATH</h4><p>我们知道java中依赖包的搜索路径是通过classpath这个JVM的参数控制的。其实node也有这样的变量提供支持。这个变量就是<code>NODE_PATH</code><br>windows下<br><figure class="highlight dockerfile"><table><tr><td class="gutter"><pre><div class="line">1</div><div class="line">2</div><div class="line">3</div><div class="line">4</div></pre></td><td class="code"><pre><div class="line"><span class="keyword">cmd</span><span class="bash"></span></div><div class="line"><span class="built_in">set</span> NODE_PATH=<span class="string">"your_path"</span></div><div class="line">powershell</div><div class="line"><span class="keyword">env</span>:NODE_PATH=<span class="string">"your_path"</span></div></pre></td></tr></table></figure></p>
<p>NODE_PATH中的路径被遍历是发生在从项目的根位置递归搜寻 node_modules 目录，直到文件系统根目录的node_modules，如果还没有查找到指定模块的话，就会去 NODE_PATH中注册的路径中查找。</p>
<h4 id="内置模块"><a href="#内置模块" class="headerlink" title="内置模块"></a>内置模块</h4><p>如<code>fs</code>, <code>http</code>等，不做路径解析就直接使用其导出对象<code>require(&#39;fs&#39;)</code>, <code>require(&#39;http&#39;)</code></p>
<h3 id="Package（包）"><a href="#Package（包）" class="headerlink" title="Package（包）"></a>Package（包）</h3><p>包就是封装多个子模块，同时提供入口的大模块。这个大模块的功能是内聚的。举个例子：<br><figure class="highlight stylus"><table><tr><td class="gutter"><pre><div class="line">1</div><div class="line">2</div><div class="line">3</div><div class="line">4</div><div class="line">5</div><div class="line">6</div></pre></td><td class="code"><pre><div class="line">C:\USERS\QIANYAN\PROJECTS\LESSON2</div><div class="line">└───plane</div><div class="line">        <span class="selector-tag">body</span><span class="selector-class">.js</span></div><div class="line">        engine<span class="selector-class">.js</span></div><div class="line">        main<span class="selector-class">.js</span></div><div class="line">        wing.js</div></pre></td></tr></table></figure></p>
<p>其中plane目录定义了一个包，其中包含了4个子模块。<code>main.js</code>作为入口模块，如下：<br><figure class="highlight javascript"><figcaption><span>main.js</span></figcaption><table><tr><td class="gutter"><pre><div class="line">1</div><div class="line">2</div><div class="line">3</div><div class="line">4</div><div class="line">5</div><div class="line">6</div><div class="line">7</div><div class="line">8</div><div class="line">9</div><div class="line">10</div></pre></td><td class="code"><pre><div class="line"><span class="keyword">var</span> engine = <span class="built_in">require</span>(<span class="string">'./engine'</span>);</div><div class="line"><span class="keyword">var</span> wing = <span class="built_in">require</span>(<span class="string">'./wing'</span>);</div><div class="line"><span class="keyword">var</span> body = <span class="built_in">require</span>(<span class="string">'./body'</span>);</div><div class="line"></div><div class="line">exports.plane = &#123;</div><div class="line">    <span class="attr">type</span>: <span class="string">"747"</span>,</div><div class="line">    <span class="attr">engine</span>: engine,</div><div class="line">    <span class="attr">wing</span>: wing,</div><div class="line">    <span class="attr">body</span>: body</div><div class="line">&#125;</div></pre></td></tr></table></figure></p>
<p>其他模块需要使用plane这个包时，得使用<code>require(&#39;./plane/main&#39;)</code>才行。不过，这里有两种方法可以省去写文件名<code>main</code>。</p>
<h4 id="1-index-js"><a href="#1-index-js" class="headerlink" title="1. index.js"></a>1. index.js</h4><p>这里有个约定，如果将main.js重命名成index.js，那么就不需要写出文件名字，直接<code>require(&#39;./plane&#39;)</code>就可以了。</p>
<p>这样模块显得更内聚，和Clojure中的<code>(use namespace)</code>的用法类似。以下两条语句等价。<br><figure class="highlight javascript"><table><tr><td class="gutter"><pre><div class="line">1</div><div class="line">2</div></pre></td><td class="code"><pre><div class="line"><span class="built_in">require</span>(<span class="string">'./plane/main'</span>)</div><div class="line"><span class="built_in">require</span>(<span class="string">'./plane'</span>)</div></pre></td></tr></table></figure></p>
<h4 id="2-package-json"><a href="#2-package-json" class="headerlink" title="2. package.json"></a>2. package.json</h4><p>如果想自定义入口模块的文件名和存放位置，就需要在包目录下包含一个package.json文件，并在其中指定入口模块的路径。上例中的模块可以重构如下。<br><figure class="highlight stylus"><table><tr><td class="gutter"><pre><div class="line">1</div><div class="line">2</div><div class="line">3</div><div class="line">4</div><div class="line">5</div><div class="line">6</div><div class="line">7</div><div class="line">8</div><div class="line">9</div><div class="line">10</div><div class="line">11</div></pre></td><td class="code"><pre><div class="line">C:\USERS\QIANYAN\PROJECTS\LESSON2\PLANE</div><div class="line">│   package<span class="selector-class">.json</span> <span class="comment">//包描述文件</span></div><div class="line">│</div><div class="line">├───doc <span class="comment">//文档</span></div><div class="line">├───lib <span class="comment">//API文件</span></div><div class="line">│       <span class="selector-tag">body</span><span class="selector-class">.js</span></div><div class="line">│       engine<span class="selector-class">.js</span></div><div class="line">│       main<span class="selector-class">.js</span></div><div class="line">│       wing<span class="selector-class">.js</span></div><div class="line">│</div><div class="line">└───tests <span class="comment">//测试文件</span></div></pre></td></tr></table></figure></p>
<p>其中package.json内容如下。<br><figure class="highlight javascript"><table><tr><td class="gutter"><pre><div class="line">1</div><div class="line">2</div><div class="line">3</div><div class="line">4</div></pre></td><td class="code"><pre><div class="line">&#123;</div><div class="line">    <span class="string">"name"</span>: <span class="string">"plane"</span>,</div><div class="line">    <span class="string">"main"</span>: <span class="string">"./lib/main.js"</span> <span class="comment">//这里入口文件的名字可以按自己的喜好更改</span></div><div class="line">&#125;</div></pre></td></tr></table></figure></p>
<p>如此一来，就同样可以使用require(‘./lib/plane’)的方式加载模块。NodeJS会根据包目录下的package.json找到入口模块所在位置。</p>
<hr>
<h3 id="命令行程序"><a href="#命令行程序" class="headerlink" title="命令行程序"></a>命令行程序</h3><p>node.js的程序是跑在命令行之中的，命令行程序长得类似<code>cmd --name=value</code>这样的形式。</p>
<h4 id="创建目录"><a href="#创建目录" class="headerlink" title="创建目录"></a>创建目录</h4><p>在windows下创建一个<code>greeting</code>程序的目录，如下：<br><figure class="highlight crystal"><table><tr><td class="gutter"><pre><div class="line">1</div><div class="line">2</div><div class="line">3</div><div class="line">4</div><div class="line">5</div><div class="line">6</div><div class="line">7</div><div class="line">8</div><div class="line">9</div><div class="line">10</div><div class="line">11</div><div class="line">12</div><div class="line">13</div></pre></td><td class="code"><pre><div class="line"><span class="symbol">C:</span>\USERS\QIANYAN\PROJECTS\LESSON2\GREETING</div><div class="line">│   package.json</div><div class="line">│   README.md</div><div class="line">│</div><div class="line">├───bin</div><div class="line">│       greeting.cmd</div><div class="line">│</div><div class="line">├───doc</div><div class="line">├───<span class="class"><span class="keyword">lib</span></span></div><div class="line">│       index.js</div><div class="line">│</div><div class="line">├───node_modules</div><div class="line">└───tests</div></pre></td></tr></table></figure></p>
<p>package.json的内容如下：<br><figure class="highlight json"><table><tr><td class="gutter"><pre><div class="line">1</div><div class="line">2</div><div class="line">3</div><div class="line">4</div><div class="line">5</div><div class="line">6</div><div class="line">7</div><div class="line">8</div><div class="line">9</div><div class="line">10</div><div class="line">11</div><div class="line">12</div><div class="line">13</div><div class="line">14</div><div class="line">15</div><div class="line">16</div><div class="line">17</div><div class="line">18</div></pre></td><td class="code"><pre><div class="line">&#123;</div><div class="line">  <span class="attr">"name"</span>: <span class="string">"greeting"</span>,</div><div class="line">  <span class="attr">"version"</span>: <span class="string">"1.0.0"</span>,</div><div class="line">  <span class="attr">"description"</span>: <span class="string">"say hi to everyone"</span>,</div><div class="line">  <span class="attr">"main"</span>: <span class="string">"lib/index.js"</span>,</div><div class="line">  <span class="attr">"directories"</span>: &#123;</div><div class="line">    <span class="attr">"doc"</span>: <span class="string">"doc"</span>,</div><div class="line">    <span class="attr">"test"</span>: <span class="string">"tests"</span></div><div class="line">  &#125;,</div><div class="line">  <span class="attr">"bin"</span>: &#123;</div><div class="line">    <span class="attr">"greeting"</span>: <span class="string">"bin/greeting.cmd"</span>  </div><div class="line">  &#125;,</div><div class="line">  <span class="attr">"scripts"</span>: &#123;</div><div class="line">    <span class="attr">"test"</span>: <span class="string">"node test"</span></div><div class="line">  &#125;,</div><div class="line">  <span class="attr">"author"</span>: <span class="string">"lambeta"</span>,</div><div class="line">  <span class="attr">"license"</span>: <span class="string">"MIT"</span></div><div class="line">&#125;</div></pre></td></tr></table></figure></p>
<p>在windows下，如果我们想要实现<em>cmd –name=value</em>的效果，就必须使用cmd后缀的文件，如下：<br><figure class="highlight plain"><table><tr><td class="gutter"><pre><div class="line">1</div><div class="line">2</div></pre></td><td class="code"><pre><div class="line">//greeting.cmd</div><div class="line">@node &quot;lib/index.js&quot; %*</div></pre></td></tr></table></figure></p>
<p>我们实现一个接受人的名字作为参数的命令行程序<em>lib/index.js</em>，如下：<br><figure class="highlight javascript"><table><tr><td class="gutter"><pre><div class="line">1</div></pre></td><td class="code"><pre><div class="line"><span class="built_in">console</span>.log(<span class="string">'hello,'</span>, process.argv[<span class="number">2</span>]);</div></pre></td></tr></table></figure></p>
<p>到这里，可以直接这样运行<code>./bin/greeting.cmd lambeta</code>，输出<code>hello, lambeta</code>。不过，还是没有预期的样子。</p>
<h4 id="npm-link"><a href="#npm-link" class="headerlink" title="npm link"></a>npm link</h4><p>我们再运行一条npm的命令<br><figure class="highlight taggerscript"><table><tr><td class="gutter"><pre><div class="line">1</div><div class="line">2</div><div class="line">3</div></pre></td><td class="code"><pre><div class="line">&gt; npm link</div><div class="line">C:<span class="symbol">\P</span>rogram Files<span class="symbol">\n</span>odejs<span class="symbol">\g</span>reeting -&gt; C:<span class="symbol">\P</span>rogram Files<span class="symbol">\n</span>odejs<span class="symbol">\n</span>ode_modules<span class="symbol">\g</span>reeting<span class="symbol">\b</span>in<span class="symbol">\g</span>reeting.cmd</div><div class="line">C:<span class="symbol">\P</span>rogram Files<span class="symbol">\n</span>odejs<span class="symbol">\n</span>ode_modules<span class="symbol">\g</span>reeting -&gt; C:<span class="symbol">\U</span>sers<span class="symbol">\q</span>ianyan<span class="symbol">\P</span>rojects<span class="symbol">\l</span>esson2<span class="symbol">\g</span>reeting</div></pre></td></tr></table></figure></p>
<p>这条命令帮助我们设置两个软链接。第一个链接使得我们可以直接运行<em>greeting lambeta</em>；第二个则在全局范围内，其他的模块得以引入greeting这个包。<br>此时，我们可以直接使用<em>greeting lambeta</em>来运行程序了</p>
<h4 id="依赖第三方库"><a href="#依赖第三方库" class="headerlink" title="依赖第三方库"></a>依赖第三方库</h4><p>为了实现真正的<em>cmd –name=value</em>，我们使用一个第三方库<code>yargs</code>。</p>
<ol>
<li>安装yargs: <em>npm install yargs –save</em></li>
<li><p>修改index.js文件如下</p>
<figure class="highlight javascript"><table><tr><td class="gutter"><pre><div class="line">1</div><div class="line">2</div></pre></td><td class="code"><pre><div class="line"><span class="keyword">var</span> argv = <span class="built_in">require</span>(<span class="string">'yargs'</span>).argv;</div><div class="line"><span class="built_in">console</span>.log(<span class="string">'hello,'</span>, argv.name);</div></pre></td></tr></table></figure>
</li>
<li><p>运行</p>
<figure class="highlight ada"><table><tr><td class="gutter"><pre><div class="line">1</div><div class="line">2</div></pre></td><td class="code"><pre><div class="line">&gt; greeting <span class="comment">--name=lambeta</span></div><div class="line">  hello, lambeta</div></pre></td></tr></table></figure>
</li>
</ol>
<p>最后再来看看一个完整的node.js的整体结构<br><figure class="highlight stylus"><table><tr><td class="gutter"><pre><div class="line">1</div><div class="line">2</div><div class="line">3</div><div class="line">4</div><div class="line">5</div><div class="line">6</div><div class="line">7</div><div class="line">8</div><div class="line">9</div><div class="line">10</div><div class="line">11</div><div class="line">12</div><div class="line">13</div></pre></td><td class="code"><pre><div class="line">C:\USERS\QIANYAN\PROJECTS\LESSON2\GREETING</div><div class="line">│   package<span class="selector-class">.json</span> <span class="comment">//包描述文件</span></div><div class="line">│   README<span class="selector-class">.md</span>    <span class="comment">//说明文件</span></div><div class="line">│</div><div class="line">├───bin</div><div class="line">│       greeting<span class="selector-class">.cmd</span> <span class="comment">//命令行相关代码</span></div><div class="line">│</div><div class="line">├───doc   <span class="comment">//文档</span></div><div class="line">├───lib   <span class="comment">//API代码</span></div><div class="line">│       index<span class="selector-class">.js</span></div><div class="line">│</div><div class="line">├───node_modules <span class="comment">//第三方依赖</span></div><div class="line">└───tests <span class="comment">//测试</span></div></pre></td></tr></table></figure></p>
<h2 id="小结-1"><a href="#小结-1" class="headerlink" title="小结"></a>小结</h2><ul>
<li>按照标准目录结构</li>
<li>分模块管理项目</li>
<li>使用NPM管理第三方模块和命令行程序</li>
<li>使用package.json描述项目信息和依赖</li>
</ul>
<h2 id="问题-1"><a href="#问题-1" class="headerlink" title="问题"></a>问题</h2><ul>
<li>下载一个第三方命令行程序到本地<code>npm install es-checker</code>，不要使用<code>-g</code>参数，如何运行起来这个程序？</li>
<li>了解一下<a href="https://docs.npmjs.com/misc/scripts" target="_blank" rel="external">npm scripts</a>，在上题的基础上，添加包含下面的内容的package.json，运行<code>npm test</code>。思考这样做是否可行？<figure class="highlight xquery"><table><tr><td class="gutter"><pre><div class="line">1</div><div class="line">2</div><div class="line">3</div><div class="line">4</div><div class="line">5</div><div class="line">6</div></pre></td><td class="code"><pre><div class="line">//package.json</div><div class="line">&#123;</div><div class="line">    <span class="string">"scripts"</span>: &#123;</div><div class="line">        <span class="string">"test"</span>: <span class="string">"es-checker"</span></div><div class="line">    &#125;</div><div class="line">&#125;</div></pre></td></tr></table></figure>
</li>
</ul>
<hr>
<h2 id="文件操作"><a href="#文件操作" class="headerlink" title="文件操作"></a>文件操作</h2><p>前置条件：<a href="https://zealdocs.org/" target="_blank" rel="external">安装Windows上的离线文档工具</a></p>
<h3 id="文件操作相关的API"><a href="#文件操作相关的API" class="headerlink" title="文件操作相关的API"></a>文件操作相关的API</h3><h4 id="buffer对象（数据块）"><a href="#buffer对象（数据块）" class="headerlink" title="buffer对象（数据块）"></a>buffer对象（数据块）</h4><p>Javascript语言本身只支持字符串操作，没有提供针对二进制数据流的操作。NodeJS提供了一个与<code>String</code>对等的全局对象<code>Buffer</code>. Buffer和整数的数组很类似，但是它是固定长度，一旦创建就不能修改。</p>
<figure class="highlight haxe"><table><tr><td class="gutter"><pre><div class="line">1</div><div class="line">2</div><div class="line">3</div><div class="line">4</div><div class="line">5</div><div class="line">6</div><div class="line">7</div><div class="line">8</div></pre></td><td class="code"><pre><div class="line"><span class="keyword">var</span> bin = <span class="keyword">new</span> <span class="type">Buffer</span>(<span class="string">'hello'</span>, <span class="string">'utf8'</span>);<span class="comment">// &lt;Buffer 68 65 6c 6c 6f&gt;</span></div><div class="line">bin.toString(); <span class="comment">//'hello'</span></div><div class="line"><span class="keyword">var</span> bin = <span class="keyword">new</span> <span class="type">Buffer</span>([ <span class="number">0x68</span>, <span class="number">0x65</span>, <span class="number">0x6c</span>, <span class="number">0x6c</span>, <span class="number">0x6f</span> ]);</div><div class="line">bin.toString(); <span class="comment">//'hello'</span></div><div class="line"></div><div class="line"><span class="keyword">var</span> bin = <span class="keyword">new</span> <span class="type">Buffer</span>([ <span class="number">0x68</span>, <span class="number">0x65</span>, <span class="number">0x6c</span>, <span class="number">0x6c</span>, <span class="number">0x6f</span> ]);</div><div class="line"><span class="keyword">var</span> dump = <span class="keyword">new</span> <span class="type">Buffer</span>(bin.length);</div><div class="line">bin.copy(dump);</div></pre></td></tr></table></figure>
<h4 id="stream模块（数据流）"><a href="#stream模块（数据流）" class="headerlink" title="stream模块（数据流）"></a>stream模块（数据流）</h4><p>Stream是一个抽象的接口，所有的stream都是EventEmitter的实例。<br>当内存中无法一次装下需要处理的数据时，或者一边读取一边处理更加高效时，我们就需要用到数据流。NodeJS中通过各种Stream来提供对数据流的操作。<br><figure class="highlight javascript"><table><tr><td class="gutter"><pre><div class="line">1</div><div class="line">2</div><div class="line">3</div><div class="line">4</div><div class="line">5</div><div class="line">6</div><div class="line">7</div><div class="line">8</div><div class="line">9</div><div class="line">10</div></pre></td><td class="code"><pre><div class="line"><span class="keyword">var</span> fs = <span class="built_in">require</span>(<span class="string">'fs'</span>);</div><div class="line"><span class="keyword">var</span> readStream = fs.createReadStream(<span class="string">'README.md'</span>); <span class="comment">//readStream是EventEmitter的实例。</span></div><div class="line"></div><div class="line">readStream.on(<span class="string">'data'</span>, <span class="function"><span class="keyword">function</span>(<span class="params">chunk</span>) </span>&#123;</div><div class="line">   <span class="built_in">console</span>.log(chunk.toString());</div><div class="line">&#125;);</div><div class="line"></div><div class="line">readStream.on(<span class="string">'end'</span>, <span class="function"><span class="keyword">function</span>(<span class="params"></span>) </span>&#123;</div><div class="line">   <span class="built_in">console</span>.log(<span class="string">'end.'</span>);</div><div class="line">&#125;);</div></pre></td></tr></table></figure></p>
<h4 id="fs模块"><a href="#fs模块" class="headerlink" title="fs模块"></a>fs模块</h4><p>NodeJS通过<code>fs</code>内置模块提供对文件的操作。<code>fs</code>模块提供的API基本可以分为以下三类：</p>
<ul>
<li><p>文件属性读写。<br>其中常用的有fs.stat、fs.chmod、fs.chown等等。</p>
</li>
<li><p>文件内容读写。<br>其中常用的有fs.readFile、fs.readdir、fs.writeFile、fs.mkdir等等。</p>
</li>
<li><p>底层文件操作。<br>其中常用的有fs.open、fs.read、fs.write、fs.close等等。</p>
</li>
</ul>
<p>我们可以通过<code>require(&#39;fs&#39;)</code>来引用这个模块，而且该模块下的每个方法都有同步和异步的形式。<br><figure class="highlight javascript"><table><tr><td class="gutter"><pre><div class="line">1</div><div class="line">2</div><div class="line">3</div><div class="line">4</div><div class="line">5</div><div class="line">6</div><div class="line">7</div><div class="line">8</div><div class="line">9</div><div class="line">10</div><div class="line">11</div><div class="line">12</div></pre></td><td class="code"><pre><div class="line"><span class="comment">// read sync</span></div><div class="line"><span class="keyword">var</span> file = fs.readFileSync(process.cwd() + <span class="string">'/README.md'</span>, <span class="string">'utf8'</span>);</div><div class="line"><span class="built_in">console</span>.log(file);</div><div class="line"></div><div class="line"><span class="comment">// read async</span></div><div class="line">fs.readFile(process.cwd() + <span class="string">'/README.md'</span>, <span class="string">'utf8'</span>, <span class="function"><span class="keyword">function</span> (<span class="params">err, data</span>) </span>&#123;</div><div class="line">    <span class="keyword">if</span>(err) &#123;</div><div class="line">        <span class="built_in">console</span>.log(err);</div><div class="line">        <span class="keyword">return</span>;</div><div class="line">    &#125;</div><div class="line">    <span class="built_in">console</span>.log(data)</div><div class="line">&#125;);</div></pre></td></tr></table></figure></p>
<p>一段遍历当前目录的程序<br><figure class="highlight javascript"><table><tr><td class="gutter"><pre><div class="line">1</div><div class="line">2</div><div class="line">3</div><div class="line">4</div><div class="line">5</div><div class="line">6</div><div class="line">7</div><div class="line">8</div><div class="line">9</div><div class="line">10</div><div class="line">11</div><div class="line">12</div><div class="line">13</div><div class="line">14</div><div class="line">15</div><div class="line">16</div><div class="line">17</div><div class="line">18</div><div class="line">19</div><div class="line">20</div><div class="line">21</div><div class="line">22</div><div class="line">23</div><div class="line">24</div><div class="line">25</div><div class="line">26</div></pre></td><td class="code"><pre><div class="line"><span class="keyword">var</span> fs = <span class="built_in">require</span>(<span class="string">'fs'</span>);</div><div class="line"><span class="keyword">var</span> p = <span class="built_in">require</span>(<span class="string">'path'</span>);</div><div class="line"></div><div class="line"><span class="function"><span class="keyword">function</span> <span class="title">recursiveRead</span>(<span class="params">path</span>) </span>&#123;</div><div class="line">    <span class="keyword">if</span>(fs.statSync(path).isFile()) &#123;</div><div class="line">        <span class="built_in">console</span>.log(<span class="string">"File:"</span>, path);</div><div class="line">    &#125; <span class="keyword">else</span> &#123;</div><div class="line">        fs.readdir(path, <span class="function"><span class="keyword">function</span>(<span class="params">err, files</span>) </span>&#123;</div><div class="line">            <span class="keyword">if</span>(err) &#123;</div><div class="line">                <span class="built_in">console</span>.log(err);</div><div class="line">                <span class="keyword">return</span>;</div><div class="line">            &#125;</div><div class="line"></div><div class="line">            files.forEach(<span class="function"><span class="keyword">function</span>(<span class="params">file</span>) </span>&#123;</div><div class="line">                <span class="keyword">var</span> absPath = p.join(path, file);</div><div class="line">                <span class="keyword">if</span>(fs.statSync(absPath).isFile()) &#123;</div><div class="line">                    <span class="built_in">console</span>.log(<span class="string">"File:"</span>, absPath);</div><div class="line">                &#125; <span class="keyword">else</span> &#123;</div><div class="line">                    <span class="built_in">console</span>.log(<span class="string">"Directory:"</span>, absPath);</div><div class="line">                    recursiveRead(absPath);</div><div class="line">                &#125;</div><div class="line">            &#125;);</div><div class="line">        &#125;);  </div><div class="line">    &#125;</div><div class="line">&#125;</div><div class="line">recursiveRead(__dirname); <span class="comment">//全局的对象__dirname，当前脚本执行的目录。</span></div></pre></td></tr></table></figure></p>
<h4 id="path模块"><a href="#path模块" class="headerlink" title="path模块"></a>path模块</h4><p>和<code>java</code>类似，NodeJS提供path来简化对文件路径的操作。</p>
<ul>
<li><p>path.normalize</p>
<figure class="highlight ruby"><table><tr><td class="gutter"><pre><div class="line">1</div><div class="line">2</div></pre></td><td class="code"><pre><div class="line">var path = <span class="keyword">require</span>(<span class="string">'path'</span>);</div><div class="line">path.normalize(<span class="string">'foo/bar/..'</span>); <span class="regexp">//</span> <span class="string">'foo'</span></div></pre></td></tr></table></figure>
</li>
<li><p>path.join &amp; path.sep</p>
<figure class="highlight awk"><table><tr><td class="gutter"><pre><div class="line">1</div><div class="line">2</div></pre></td><td class="code"><pre><div class="line">path.join(<span class="string">'foo'</span>, <span class="string">'/bar/'</span>, <span class="string">'/baz'</span>, <span class="string">'par/'</span>) <span class="regexp">//</span> <span class="string">'foo\\bar\\baz\\par\\'</span></div><div class="line">path.sep <span class="regexp">//</span> <span class="string">'\\'</span></div></pre></td></tr></table></figure>
</li>
<li><p>path.extname</p>
<figure class="highlight crmsh"><table><tr><td class="gutter"><pre><div class="line">1</div></pre></td><td class="code"><pre><div class="line">path.extname('<span class="keyword">node</span>.<span class="title">js</span>') //'.js'</div></pre></td></tr></table></figure>
</li>
</ul>
<h2 id="小结-2"><a href="#小结-2" class="headerlink" title="小结"></a>小结</h2><ul>
<li><code>Buffer</code>提供了NodeJS操作二进制的机制；</li>
<li><code>Stream</code>是一个抽象的接口，每种stream都是EventEmitter的实例。当我们在读取大文件时，可以使用数据流一边读取，一边处理；</li>
<li><code>fs</code>提供了文件属性读写，内容读写以及底层文件操作。</li>
<li>不要使用字符串拼接，使用<code>path</code>简化操作</li>
</ul>
<h2 id="问题-2"><a href="#问题-2" class="headerlink" title="问题"></a>问题</h2><ul>
<li>使用<code>fs</code>的API创建一个copy的函数；</li>
<li>NodeJS对文本编码的处理；</li>
<li>使用第三方包<code>findit</code>重写遍历当前目录的程序。</li>
</ul>
<hr>
<h2 id="网络操作"><a href="#网络操作" class="headerlink" title="网络操作"></a>网络操作</h2><h3 id="简单的HTTP服务器"><a href="#简单的HTTP服务器" class="headerlink" title="简单的HTTP服务器"></a>简单的HTTP服务器</h3><p>使用<code>http</code>实现一个简单的HTTP服务器。<br><figure class="highlight javascript"><table><tr><td class="gutter"><pre><div class="line">1</div><div class="line">2</div><div class="line">3</div><div class="line">4</div><div class="line">5</div></pre></td><td class="code"><pre><div class="line"><span class="keyword">var</span> http = <span class="built_in">require</span>(<span class="string">'http'</span>);</div><div class="line">http.createServer(<span class="function"><span class="keyword">function</span>(<span class="params">req, res</span>) </span>&#123;</div><div class="line">    res.writeHead(<span class="number">200</span>, &#123;<span class="string">'Content-type'</span>: <span class="string">'text/plain'</span>&#125;);</div><div class="line">    res.end(<span class="string">'Hello Node.js'</span>);</div><div class="line">&#125;).listen(<span class="number">12306</span>);</div></pre></td></tr></table></figure></p>
<h3 id="http模块"><a href="#http模块" class="headerlink" title="http模块"></a>http模块</h3><p>http模块提供两种使用方式：</p>
<ul>
<li>作为服务端使用时，创建一个HTTP服务器，监听HTTP客户端请求并返回响应；</li>
<li>作为客户端使用时，发起一个HTTP客户端请求，获取服务端响应。</li>
</ul>
<p>先创建一个HTTP服务器<br><figure class="highlight javascript"><table><tr><td class="gutter"><pre><div class="line">1</div><div class="line">2</div><div class="line">3</div><div class="line">4</div><div class="line">5</div><div class="line">6</div><div class="line">7</div><div class="line">8</div><div class="line">9</div><div class="line">10</div><div class="line">11</div><div class="line">12</div><div class="line">13</div><div class="line">14</div><div class="line">15</div><div class="line">16</div><div class="line">17</div><div class="line">18</div></pre></td><td class="code"><pre><div class="line"><span class="comment">//http-server.js</span></div><div class="line"><span class="keyword">var</span> http = <span class="built_in">require</span>(<span class="string">'http'</span>);</div><div class="line"></div><div class="line">http.createServer(<span class="function"><span class="keyword">function</span>(<span class="params">req, res</span>) </span>&#123;</div><div class="line">   <span class="keyword">var</span> body = [];</div><div class="line">   res.writeHead(<span class="number">200</span>, &#123;<span class="string">'Content-type'</span>: <span class="string">'text/plain'</span>&#125;);</div><div class="line"></div><div class="line">   req.on(<span class="string">'data'</span>, <span class="function"><span class="keyword">function</span>(<span class="params">chunk</span>) </span>&#123;</div><div class="line">      res.write(chunk);</div><div class="line">      body.push(chunk);</div><div class="line">   &#125;);</div><div class="line"></div><div class="line">   req.on(<span class="string">'end'</span>, <span class="function"><span class="keyword">function</span>(<span class="params"></span>) </span>&#123;</div><div class="line">      body = Buffer.concat(body);</div><div class="line">      <span class="built_in">console</span>.log(body.toString());</div><div class="line">      res.end();</div><div class="line">   &#125;)</div><div class="line">&#125;).listen(<span class="number">12306</span>);</div></pre></td></tr></table></figure></p>
<p>再创建一个HTTP客户端<br><figure class="highlight javascript"><table><tr><td class="gutter"><pre><div class="line">1</div><div class="line">2</div><div class="line">3</div><div class="line">4</div><div class="line">5</div><div class="line">6</div><div class="line">7</div><div class="line">8</div><div class="line">9</div><div class="line">10</div><div class="line">11</div><div class="line">12</div><div class="line">13</div><div class="line">14</div><div class="line">15</div><div class="line">16</div></pre></td><td class="code"><pre><div class="line"><span class="comment">//http-client.js</span></div><div class="line"><span class="keyword">var</span> http = <span class="built_in">require</span>(<span class="string">'http'</span>);</div><div class="line"><span class="keyword">var</span> options = &#123;<span class="attr">hostname</span>: <span class="string">'localhost'</span>,</div><div class="line">    <span class="attr">port</span>: <span class="number">12306</span>,</div><div class="line">    <span class="attr">method</span>: <span class="string">'POST'</span>,</div><div class="line">    <span class="attr">headers</span>: &#123;</div><div class="line">        <span class="string">'Content-type'</span>: <span class="string">'text/plain'</span></div><div class="line">    &#125;&#125;;</div><div class="line"><span class="keyword">var</span> req = http.request(options, <span class="function"><span class="keyword">function</span>(<span class="params">res</span>) </span>&#123;</div><div class="line">    res.on(<span class="string">'data'</span>, <span class="function"><span class="keyword">function</span>(<span class="params">chunk</span>) </span>&#123;</div><div class="line">       <span class="built_in">console</span>.log(<span class="string">'res:'</span>, chunk.toString());</div><div class="line">    &#125;);</div><div class="line">&#125;);</div><div class="line"></div><div class="line">req.write(<span class="string">'hello world'</span>);</div><div class="line">req.end();</div></pre></td></tr></table></figure></p>
<h3 id="url模块"><a href="#url模块" class="headerlink" title="url模块"></a>url模块</h3><ul>
<li><p>parse<br>使用url解析成URL对象</p>
<figure class="highlight less"><table><tr><td class="gutter"><pre><div class="line">1</div><div class="line">2</div><div class="line">3</div><div class="line">4</div><div class="line">5</div><div class="line">6</div><div class="line">7</div><div class="line">8</div><div class="line">9</div><div class="line">10</div><div class="line">11</div><div class="line">12</div><div class="line">13</div><div class="line">14</div></pre></td><td class="code"><pre><div class="line">&gt; <span class="selector-tag">url</span><span class="selector-class">.parse</span>('<span class="attribute">http</span>:<span class="comment">//user:pass@host.com:8080/p/a/t/h?query=string#hash</span></div><div class="line">Url &#123;</div><div class="line">  <span class="attribute">protocol</span>: <span class="string">'http:'</span>,</div><div class="line">  <span class="attribute">slashes</span>: true,</div><div class="line">  <span class="attribute">auth</span>: <span class="string">'user:pass'</span>,</div><div class="line">  <span class="attribute">host</span>: <span class="string">'host.com:8080'</span>,</div><div class="line">  <span class="attribute">port</span>: <span class="string">'8080'</span>,</div><div class="line">  <span class="attribute">hostname</span>: <span class="string">'host.com'</span>,</div><div class="line">  <span class="attribute">hash</span>: <span class="string">'#hash'</span>,</div><div class="line">  <span class="attribute">search</span>: <span class="string">'?query=string'</span>,</div><div class="line">  <span class="attribute">query</span>: <span class="string">'query=string'</span>,</div><div class="line">  <span class="attribute">pathname</span>: <span class="string">'/p/a/t/h'</span>,</div><div class="line">  <span class="attribute">path</span>: <span class="string">'/p/a/t/h?query=string'</span>,</div><div class="line">  <span class="attribute">href</span>: <span class="string">'http://user:pass@host.com:8080/p/a/t/h?query=string#hash'</span> &#125;</div></pre></td></tr></table></figure>
</li>
<li><p>format<br>format方法允许将一个URL对象转换为URL字符串</p>
<figure class="highlight less"><table><tr><td class="gutter"><pre><div class="line">1</div><div class="line">2</div><div class="line">3</div><div class="line">4</div><div class="line">5</div><div class="line">6</div><div class="line">7</div></pre></td><td class="code"><pre><div class="line">&gt; <span class="selector-tag">url</span><span class="selector-class">.format</span>(&#123;</div><div class="line">     <span class="attribute">protocol</span>: <span class="string">'http:'</span>,</div><div class="line">     <span class="attribute">host</span>: <span class="string">'www.example.com'</span>,</div><div class="line">     <span class="attribute">pathname</span>: <span class="string">'/p/a/t/h'</span>,</div><div class="line">     <span class="attribute">search</span>: <span class="string">'query=string'</span></div><div class="line"> &#125;);</div><div class="line"><span class="comment">//'http://www.example.com/p/a/t/h?query=string'</span></div></pre></td></tr></table></figure>
</li>
<li><p>resolve<br>resolve方法拼接两个URL</p>
<figure class="highlight livecodeserver"><table><tr><td class="gutter"><pre><div class="line">1</div><div class="line">2</div><div class="line">3</div><div class="line">4</div></pre></td><td class="code"><pre><div class="line">&gt; url.<span class="built_in">resolve</span>(<span class="string">'http://www.baid.com/path'</span>, <span class="string">'../www.google.com'</span>)</div><div class="line"><span class="string">'http://www.baid.com/www.google.com'</span></div><div class="line">&gt; url.<span class="built_in">resolve</span>(<span class="string">'http://example.com/one'</span>, <span class="string">'/two'</span>)</div><div class="line"><span class="string">'http://example.com/two'</span></div></pre></td></tr></table></figure>
</li>
</ul>
<h3 id="querystring"><a href="#querystring" class="headerlink" title="querystring"></a>querystring</h3><figure class="highlight less"><table><tr><td class="gutter"><pre><div class="line">1</div><div class="line">2</div></pre></td><td class="code"><pre><div class="line">&gt; <span class="selector-tag">querystring</span><span class="selector-class">.parse</span>(<span class="string">'foo=bar&amp;baz=qux&amp;baz=quux&amp;corge'</span>);</div><div class="line">&#123; <span class="attribute">foo</span>: <span class="string">'bar'</span>, <span class="attribute">baz</span>: [ <span class="string">'qux'</span>, <span class="string">'quux'</span> ], <span class="attribute">corge</span>: <span class="string">''</span> &#125;</div></pre></td></tr></table></figure>
<h2 id="小结-3"><a href="#小结-3" class="headerlink" title="小结"></a>小结</h2><ul>
<li>http模块支持服务端模式和客户端模式两种使用方式；</li>
<li>request和response对象除了用于读写头数据外，可以当作数据流来操作；</li>
<li>url.parse方法加上request.url属性是处理HTTP请求时的固定搭配。</li>
</ul>
<h2 id="问题-3"><a href="#问题-3" class="headerlink" title="问题"></a>问题</h2><ul>
<li>http模块和https模块的区别？</li>
<li>如何创建一个https服务器？</li>
</ul>
<hr>
<h2 id="进程操作"><a href="#进程操作" class="headerlink" title="进程操作"></a>进程操作</h2><h3 id="API一览"><a href="#API一览" class="headerlink" title="API一览"></a>API一览</h3><h4 id="Process"><a href="#Process" class="headerlink" title="Process"></a>Process</h4><p><code>process</code>是一个全局的对象，可以在node环境中随处访问。并且它是EventEmitter的实例。<br>一个进程对象里头到底包含些什么属性？<br><figure class="highlight dockerfile"><table><tr><td class="gutter"><pre><div class="line">1</div><div class="line">2</div><div class="line">3</div><div class="line">4</div></pre></td><td class="code"><pre><div class="line">pid</div><div class="line">stdio</div><div class="line">argv</div><div class="line"><span class="keyword">env</span></div></pre></td></tr></table></figure></p>
<p>只在POSIX平台支持的函数<br><figure class="highlight ebnf"><table><tr><td class="gutter"><pre><div class="line">1</div><div class="line">2</div><div class="line">3</div><div class="line">4</div></pre></td><td class="code"><pre><div class="line"><span class="attribute">getuid</span></div><div class="line">getgid</div><div class="line">geteuid</div><div class="line">getegid</div></pre></td></tr></table></figure></p>
<p>进程ID、标准输入输出以及错误流、启动进程的参数、运行环境、运行时权限。</p>
<h5 id="应用场景"><a href="#应用场景" class="headerlink" title="应用场景"></a>应用场景</h5><ul>
<li>获取命令行参数<figure class="highlight crmsh"><table><tr><td class="gutter"><pre><div class="line">1</div><div class="line">2</div><div class="line">3</div><div class="line">4</div><div class="line">5</div><div class="line">6</div><div class="line">7</div></pre></td><td class="code"><pre><div class="line">// index.js</div><div class="line">console.log(process.argv);</div><div class="line"></div><div class="line">&gt; <span class="keyword">node</span> <span class="title">index</span>.js hello</div><div class="line">[ 'C:\\Program Files\\nodejs\\<span class="keyword">node</span>.<span class="title">exe</span>', //<span class="keyword">node</span><span class="title">的执行路径</span></div><div class="line">  'C:\\Users\\qianyan\\Projects\\lesson5\\index.js', //文件路径</div><div class="line">  'hello' ] //参数</div></pre></td></tr></table></figure>
</li>
</ul>
<p>一般获取参数的写法<br><figure class="highlight stylus"><table><tr><td class="gutter"><pre><div class="line">1</div></pre></td><td class="code"><pre><div class="line">process<span class="selector-class">.argv</span><span class="selector-class">.splice</span>(<span class="number">2</span>)</div></pre></td></tr></table></figure></p>
<ul>
<li><p>退出程序<br>类似Java中的<code>System.exit(1)</code>，当我们捕获一个异常，同时觉得程序需要立即停止时，就执行<code>process.exit(1)</code>来表示非正常退出。</p>
</li>
<li><p>控制输入和输出<br><code>stdin</code>是只读流，而<code>stdout</code>和<code>stderr</code>都是只写流。<code>console.log</code>等价于</p>
<figure class="highlight coffeescript"><table><tr><td class="gutter"><pre><div class="line">1</div><div class="line">2</div><div class="line">3</div></pre></td><td class="code"><pre><div class="line"><span class="built_in">console</span>.log = <span class="function"><span class="params">(msg)</span> =&gt;</span> &#123;</div><div class="line">  process.stdout.write(`<span class="javascript">$&#123;msg&#125;\n</span>`);</div><div class="line">&#125;;</div></pre></td></tr></table></figure>
</li>
</ul>
<h4 id="Child-Process"><a href="#Child-Process" class="headerlink" title="Child Process"></a>Child Process</h4><p><code>child_process</code>是一个内置模块，可以创建和控制子进程。该模块的主要功能都是<code>child_process.spawn()</code>函数提供的。其余诸如<code>exec</code>, <code>fork</code>, <code>execFile</code>等都是对<code>spawn()</code>进行的封装。</p>
<h5 id="应用场景-1"><a href="#应用场景-1" class="headerlink" title="应用场景"></a>应用场景</h5><ul>
<li>创建子进程</li>
</ul>
<figure class="highlight javascript"><table><tr><td class="gutter"><pre><div class="line">1</div><div class="line">2</div><div class="line">3</div><div class="line">4</div><div class="line">5</div><div class="line">6</div><div class="line">7</div></pre></td><td class="code"><pre><div class="line"><span class="comment">//(command[, args][, options])</span></div><div class="line"><span class="keyword">const</span> spawn = <span class="built_in">require</span>(<span class="string">'child_process'</span>).spawn;</div><div class="line"><span class="keyword">const</span> echo = spawn(<span class="string">'cmd'</span>, [<span class="string">'/c'</span>, <span class="string">'env'</span>], &#123;<span class="attr">env</span>: process.env&#125;);<span class="comment">//尝试设置&#123;env: &#123;&#125;&#125;，观察结果。</span></div><div class="line"></div><div class="line">echo.stdout.on(<span class="string">'data'</span>, (data) =&gt; &#123;</div><div class="line">    <span class="built_in">console</span>.log(<span class="string">`stdout: <span class="subst">$&#123;data&#125;</span>`</span>);</div><div class="line">&#125;)</div></pre></td></tr></table></figure>
<p>第一参数是可执行文件的路径，第二参数是数组对应可执行文件接收的参数，第三参数用于配置子进程运行的环境和行为。</p>
<ul>
<li>进程间通信<br>如果父子进程都是Node.js的进程，那么就可以通过IPC通道通信。</li>
</ul>
<figure class="highlight javascript"><table><tr><td class="gutter"><pre><div class="line">1</div><div class="line">2</div><div class="line">3</div><div class="line">4</div><div class="line">5</div><div class="line">6</div><div class="line">7</div><div class="line">8</div><div class="line">9</div><div class="line">10</div><div class="line">11</div><div class="line">12</div><div class="line">13</div><div class="line">14</div><div class="line">15</div><div class="line">16</div><div class="line">17</div><div class="line">18</div><div class="line">19</div><div class="line">20</div><div class="line">21</div></pre></td><td class="code"><pre><div class="line"><span class="comment">//parent.js</span></div><div class="line"><span class="keyword">const</span> spawn = <span class="built_in">require</span>(<span class="string">'child_process'</span>).spawn;</div><div class="line"><span class="keyword">const</span> child = spawn(<span class="string">'node'</span>, [<span class="string">'child.js'</span>], &#123;</div><div class="line">    <span class="attr">stdio</span>: [process.stdin, process.stdout, process.stderr, <span class="string">'ipc'</span>]</div><div class="line">&#125;);</div><div class="line"></div><div class="line">child.on(<span class="string">'message'</span>, (msg) =&gt; &#123;</div><div class="line">    <span class="built_in">console</span>.log(<span class="string">'parent:'</span>, msg);</div><div class="line">&#125;);</div><div class="line"></div><div class="line">child.send(&#123;<span class="attr">hello</span>: <span class="string">'world'</span>&#125;);</div><div class="line"></div><div class="line"><span class="comment">//child.js</span></div><div class="line">process.on(<span class="string">'message'</span>, (msg) =&gt; &#123;</div><div class="line">    <span class="built_in">console</span>.log(<span class="string">'child:'</span>, msg);</div><div class="line">    msg.hello = msg.hello.toUpperCase();</div><div class="line">    process.send(msg);</div><div class="line">&#125;)</div><div class="line">=&gt;</div><div class="line">child: &#123; <span class="attr">hello</span>: <span class="string">'world'</span> &#125;</div><div class="line">parent: &#123; <span class="attr">hello</span>: <span class="string">'WORLD'</span> &#125;</div></pre></td></tr></table></figure>
<p>父进程在创建子进程的时候，使用了<code>options.stdio</code>的<code>ipc</code>额外开辟了一条通道，之后开始监听子进程的<code>message</code>事件来接收子进程的消息，同时通过<code>send</code>方法给子进程发送消息。子进程则通过<code>process</code>对象监听来自父进程的消息，并通过<code>process.send</code>方法向父进程发送消息。</p>
<h4 id="Cluster"><a href="#Cluster" class="headerlink" title="Cluster"></a>Cluster</h4><p>单个实例的Node.js运行在单独的进程当中。但是我们有时候可能需要利用多核处理器的优势，在每个单独的核上跑一个Node.js的进程。<br><code>Cluster</code>就是创造出来简化多进程服务程序开发的，让每一个核上面运行一个工作进程，并统一通过主进程监听端口和分发请求。</p>
<h5 id="应用场景-2"><a href="#应用场景-2" class="headerlink" title="应用场景"></a>应用场景</h5><figure class="highlight javascript"><table><tr><td class="gutter"><pre><div class="line">1</div><div class="line">2</div><div class="line">3</div><div class="line">4</div><div class="line">5</div><div class="line">6</div><div class="line">7</div><div class="line">8</div><div class="line">9</div><div class="line">10</div><div class="line">11</div><div class="line">12</div><div class="line">13</div><div class="line">14</div><div class="line">15</div><div class="line">16</div><div class="line">17</div><div class="line">18</div><div class="line">19</div><div class="line">20</div><div class="line">21</div><div class="line">22</div><div class="line">23</div><div class="line">24</div><div class="line">25</div><div class="line">26</div><div class="line">27</div><div class="line">28</div><div class="line">29</div><div class="line">30</div><div class="line">31</div><div class="line">32</div></pre></td><td class="code"><pre><div class="line"><span class="keyword">const</span> cluster = <span class="built_in">require</span>(<span class="string">'cluster'</span>);</div><div class="line"><span class="keyword">const</span> http = <span class="built_in">require</span>(<span class="string">'http'</span>);</div><div class="line"><span class="keyword">const</span> numCPUs = <span class="built_in">require</span>(<span class="string">'os'</span>).cpus().length;</div><div class="line"></div><div class="line"><span class="keyword">if</span> (cluster.isMaster) &#123;</div><div class="line">  <span class="comment">// Fork workers.</span></div><div class="line">  <span class="keyword">for</span> (<span class="keyword">var</span> i = <span class="number">0</span>; i &lt; numCPUs; i++) &#123;</div><div class="line">    cluster.fork();</div><div class="line">  &#125;</div><div class="line"></div><div class="line">  cluster.on(<span class="string">'exit'</span>, (worker, code, signal) =&gt; &#123;</div><div class="line">    <span class="built_in">console</span>.log(<span class="string">`worker <span class="subst">$&#123;worker.process.pid&#125;</span> died`</span>);</div><div class="line">  &#125;);</div><div class="line">&#125; <span class="keyword">else</span> &#123;</div><div class="line">  <span class="comment">// Workers can share any TCP connection</span></div><div class="line">  <span class="comment">// In this case it is an HTTP server</span></div><div class="line">  http.createServer(<span class="function">(<span class="params">req, res</span>) =&gt;</span> &#123;</div><div class="line">    res.writeHead(<span class="number">200</span>);</div><div class="line">    res.end(<span class="string">'hello world\n'</span>);</div><div class="line">  &#125;).listen(<span class="number">8000</span>);</div><div class="line">&#125;</div><div class="line">=&gt;</div><div class="line">NODE_DEBUG=cluster node server.js</div><div class="line">isMaster</div><div class="line">worker</div><div class="line">worker</div><div class="line">worker</div><div class="line">worker</div><div class="line">worker</div><div class="line">worker</div><div class="line">worker</div><div class="line">worker</div></pre></td></tr></table></figure>
<p>该模块很简单地创建多个共享一个服务端口的子进程，而这些子进程是通过IPC和Master，也即父进程进行信息交互的，可应用于负载均衡。</p>
<h2 id="小结-4"><a href="#小结-4" class="headerlink" title="小结"></a>小结</h2><ul>
<li>使用<code>process</code>对象管理进程</li>
<li>使用<code>child_process</code>对象管理子进程，其最主要的方法就是<code>spawn</code><h2 id="问题-4"><a href="#问题-4" class="headerlink" title="问题"></a>问题</h2></li>
</ul>
<hr>
<h2 id="异步编程"><a href="#异步编程" class="headerlink" title="异步编程"></a>异步编程</h2><p>NodeJS最大的卖点——事件机制和异步IO，开发者需要按照异步的方式去组织代码。</p>
<h3 id="回调"><a href="#回调" class="headerlink" title="回调"></a>回调</h3><p>异步编程的直接体现就是回调函数，但是不是有回调函数，就是异步编程呢？<br><figure class="highlight javascript"><table><tr><td class="gutter"><pre><div class="line">1</div><div class="line">2</div><div class="line">3</div><div class="line">4</div><div class="line">5</div><div class="line">6</div><div class="line">7</div><div class="line">8</div><div class="line">9</div><div class="line">10</div><div class="line">11</div><div class="line">12</div></pre></td><td class="code"><pre><div class="line"><span class="function"><span class="keyword">function</span> <span class="title">sum</span>(<span class="params">arr, callback</span>) </span>&#123;</div><div class="line">    <span class="keyword">var</span> sum = <span class="number">0</span>;</div><div class="line">    <span class="keyword">for</span>(<span class="keyword">var</span> i=<span class="number">0</span>; i&lt;arr.length; i++) &#123;</div><div class="line">        sum = sum + arr[i];</div><div class="line">    &#125;</div><div class="line"></div><div class="line">    callback(sum);</div><div class="line">&#125;</div><div class="line"></div><div class="line">sum([<span class="number">1</span>,<span class="number">2</span>,<span class="number">3</span>,<span class="number">4</span>,<span class="number">5</span>], <span class="built_in">console</span>.log);</div><div class="line">=&gt;</div><div class="line"><span class="number">15</span></div></pre></td></tr></table></figure></p>
<p>显然，这个<code>callback</code>还是顺序（同步）执行的。我们知道，JS本身是单线程的，所以不具备多线程并发执行的特点，那么异步从何体现呢？<br>我们再看一段程序：<br><figure class="highlight javascript"><table><tr><td class="gutter"><pre><div class="line">1</div><div class="line">2</div><div class="line">3</div><div class="line">4</div><div class="line">5</div><div class="line">6</div><div class="line">7</div><div class="line">8</div></pre></td><td class="code"><pre><div class="line">setTimeout(<span class="function"><span class="keyword">function</span>(<span class="params"></span>) </span>&#123;</div><div class="line">    <span class="built_in">console</span>.log(<span class="string">"world"</span>)</div><div class="line">&#125;, <span class="number">1000</span>);</div><div class="line"></div><div class="line"><span class="built_in">console</span>.log(<span class="string">"hello"</span>);</div><div class="line">=&gt;</div><div class="line">hello</div><div class="line">world</div></pre></td></tr></table></figure></p>
<p>上面的例子先打印出“hello”，然后打印出“world”。看上去好像是<code>setTimeout()</code>另外启动了一个“平行线程”，等待了1秒钟之后，调用回调函数打印“world”。<br>JS中提供了两大类异步函数，一种是计时函数，如：<code>setTimeout</code>和<code>setInterval</code>。另外一类是I/O异步函数，如：<code>fs.readFile</code>。</p>
<p>但是JS是单线程的。也就是说如果“主”线程一直处于忙碌状态，即使“平行”线程完成工作，通知“主”线程调用它的回调函数，也会等到“主”线程空闲了才能真正去调用。<br><figure class="highlight javascript"><table><tr><td class="gutter"><pre><div class="line">1</div><div class="line">2</div><div class="line">3</div><div class="line">4</div><div class="line">5</div><div class="line">6</div><div class="line">7</div><div class="line">8</div></pre></td><td class="code"><pre><div class="line"><span class="keyword">var</span> t = <span class="keyword">new</span> <span class="built_in">Date</span>();</div><div class="line">setTimeout(<span class="function"><span class="keyword">function</span> (<span class="params"></span>) </span>&#123;</div><div class="line">    <span class="built_in">console</span>.log(<span class="string">"waiting time: "</span>, <span class="keyword">new</span> <span class="built_in">Date</span>() - t);</div><div class="line">&#125;, <span class="number">1000</span>);</div><div class="line"></div><div class="line"><span class="keyword">while</span>(<span class="keyword">new</span> <span class="built_in">Date</span>() - start &lt; <span class="number">1000</span>);</div><div class="line">=&gt;</div><div class="line">waiting time: <span class="number">1094</span> <span class="comment">//大于我们设置的1000毫秒</span></div></pre></td></tr></table></figure></p>
<h3 id="返回值"><a href="#返回值" class="headerlink" title="返回值"></a>返回值</h3><p>我们分别使用同步和异步实现一个函数，判断当前目录下的文件是否都是File，最终程序返回一个布尔值的数组，如：<code>[true, false]</code><br>当前目录文件结构如下：<br><figure class="highlight 1c"><table><tr><td class="gutter"><pre><div class="line">1</div><div class="line">2</div><div class="line">3</div></pre></td><td class="code"><pre><div class="line"><span class="string">|_async.js</span></div><div class="line"><span class="string">|_sync.js</span></div><div class="line"><span class="string">|_ dir/</span></div></pre></td></tr></table></figure></p>
<h4 id="比较中学习"><a href="#比较中学习" class="headerlink" title="比较中学习"></a>比较中学习</h4><ul>
<li>同步方式下</li>
</ul>
<figure class="highlight javascript"><table><tr><td class="gutter"><pre><div class="line">1</div><div class="line">2</div><div class="line">3</div><div class="line">4</div><div class="line">5</div><div class="line">6</div><div class="line">7</div><div class="line">8</div><div class="line">9</div><div class="line">10</div></pre></td><td class="code"><pre><div class="line"><span class="keyword">const</span> fs = <span class="built_in">require</span>(<span class="string">'fs'</span>);</div><div class="line"><span class="keyword">const</span> path = <span class="built_in">require</span>(<span class="string">'path'</span>);</div><div class="line"></div><div class="line"><span class="keyword">const</span> dirs = fs.readdirSync(<span class="string">'.'</span>);</div><div class="line"><span class="keyword">const</span> areFiles = dirs.map(<span class="function">(<span class="params">dirName</span>) =&gt;</span> &#123;    </div><div class="line">    <span class="keyword">return</span> fs.statSync(path.join(<span class="string">'.'</span>, dirName)).isFile();</div><div class="line">&#125;);</div><div class="line"></div><div class="line"><span class="built_in">console</span>.log(areFiles);</div><div class="line">=&gt; [<span class="literal">true</span>, <span class="literal">false</span>, <span class="literal">true</span>]</div></pre></td></tr></table></figure>
<ul>
<li>异步方式下<br><em>失败的尝试</em></li>
</ul>
<figure class="highlight javascript"><table><tr><td class="gutter"><pre><div class="line">1</div><div class="line">2</div><div class="line">3</div><div class="line">4</div><div class="line">5</div><div class="line">6</div><div class="line">7</div><div class="line">8</div><div class="line">9</div><div class="line">10</div><div class="line">11</div><div class="line">12</div><div class="line">13</div><div class="line">14</div><div class="line">15</div></pre></td><td class="code"><pre><div class="line"><span class="keyword">const</span> fs = <span class="built_in">require</span>(<span class="string">'fs'</span>);</div><div class="line"><span class="keyword">const</span> path = <span class="built_in">require</span>(<span class="string">'path'</span>);</div><div class="line"></div><div class="line">fs.readdir(<span class="string">'.'</span>, (err, dirs) =&gt; &#123;</div><div class="line">    <span class="keyword">const</span> areFiles = [];</div><div class="line"></div><div class="line">    dirs.forEach(<span class="function">(<span class="params">dirName</span>) =&gt;</span> &#123;  </div><div class="line">        fs.stat(path.join(<span class="string">'.'</span>, dirName), (err, stat) =&gt; &#123;</div><div class="line">            areFiles.push(stat.isFile());</div><div class="line">        &#125;)</div><div class="line">    &#125;);</div><div class="line"></div><div class="line">    <span class="built_in">console</span>.log(areFiles);</div><div class="line">&#125;);</div><div class="line">=&gt; [] <span class="comment">//思考为何是空数组？</span></div></pre></td></tr></table></figure>
<p><em>成功的尝试</em></p>
<figure class="highlight javascript"><table><tr><td class="gutter"><pre><div class="line">1</div><div class="line">2</div><div class="line">3</div><div class="line">4</div><div class="line">5</div><div class="line">6</div><div class="line">7</div><div class="line">8</div><div class="line">9</div><div class="line">10</div><div class="line">11</div><div class="line">12</div><div class="line">13</div><div class="line">14</div><div class="line">15</div><div class="line">16</div><div class="line">17</div></pre></td><td class="code"><pre><div class="line"><span class="keyword">const</span> fs = <span class="built_in">require</span>(<span class="string">'fs'</span>);</div><div class="line"><span class="keyword">const</span> path = <span class="built_in">require</span>(<span class="string">'path'</span>);</div><div class="line"></div><div class="line">fs.readdir(<span class="string">'.'</span>, (err, dirs) =&gt; &#123;</div><div class="line">    <span class="keyword">const</span> areFiles = [];</div><div class="line"></div><div class="line">    dirs.forEach(<span class="function">(<span class="params">dirName</span>) =&gt;</span> &#123;  </div><div class="line">        fs.stat(path.join(<span class="string">'.'</span>, dirName), (err, stat) =&gt; &#123;</div><div class="line">            areFiles.push(stat.isFile());</div><div class="line"></div><div class="line">            <span class="keyword">if</span>(areFiles.length == dirs.length) &#123; <span class="comment">//使用标志来位判断所有的回调都已经调用完毕</span></div><div class="line">                <span class="built_in">console</span>.log(areFiles);</div><div class="line">            &#125;</div><div class="line">        &#125;)</div><div class="line">    &#125;);</div><div class="line">&#125;);</div><div class="line">=&gt;[ <span class="literal">true</span>, <span class="literal">true</span>, <span class="literal">false</span> ] or [<span class="literal">true</span>, <span class="literal">false</span>, <span class="literal">true</span>]</div></pre></td></tr></table></figure>
<h4 id="总结"><a href="#总结" class="headerlink" title="总结"></a>总结</h4><ol>
<li>同步方法顺序取返回值，而异步方法总是在回调函数的取返回值</li>
<li>循环遍历中调用同步方法很容易，但是同样地在异步方法中，需要使用<strong>标志位</strong>来判断是否所有回调函数都已经调用完毕</li>
<li>异步函数的执行回调是无序的</li>
</ol>
<h3 id="数组的串行处理"><a href="#数组的串行处理" class="headerlink" title="数组的串行处理"></a>数组的串行处理</h3><p>我们看到上个例子里的异步的写法，最后的返回结果其实是无序的。使用标志位只能保证数组中的所有数据对应的回调函数都得以执行，但不能保证哪个回调函数先返回。要想顺序执行，那么必须是一个回调函数中包含另一个回调函数。拿上面的例子尝试：</p>
<figure class="highlight javascript"><table><tr><td class="gutter"><pre><div class="line">1</div><div class="line">2</div><div class="line">3</div><div class="line">4</div><div class="line">5</div><div class="line">6</div><div class="line">7</div><div class="line">8</div><div class="line">9</div><div class="line">10</div><div class="line">11</div><div class="line">12</div><div class="line">13</div><div class="line">14</div><div class="line">15</div><div class="line">16</div><div class="line">17</div></pre></td><td class="code"><pre><div class="line"><span class="keyword">const</span> fs = <span class="built_in">require</span>(<span class="string">'fs'</span>);</div><div class="line"><span class="keyword">const</span> path = <span class="built_in">require</span>(<span class="string">'path'</span>);</div><div class="line"></div><div class="line">fs.readdir(<span class="string">'.'</span>, (err, dirs) =&gt; &#123;</div><div class="line">    (<span class="function"><span class="keyword">function</span> <span class="title">iterate</span>(<span class="params">index, areFiles, callback</span>) </span>&#123;</div><div class="line">        <span class="keyword">if</span>(index &lt; dirs.length) &#123;</div><div class="line">            fs.stat(path.join(<span class="string">'.'</span>, dirs[index]), (err, stat) =&gt; &#123;</div><div class="line">                areFiles.push(stat.isFile());</div><div class="line">                iterate(index + <span class="number">1</span>, areFiles, callback);</div><div class="line">            &#125;);  </div><div class="line">        &#125; <span class="keyword">else</span> &#123;</div><div class="line">           callback(areFiles);</div><div class="line">        &#125;</div><div class="line">    &#125;(<span class="number">0</span>, [], (result) =&gt; &#123;</div><div class="line">        <span class="built_in">console</span>.log(result);</div><div class="line">    &#125;));</div><div class="line">&#125;);</div></pre></td></tr></table></figure>
<h4 id="在场景中学习"><a href="#在场景中学习" class="headerlink" title="在场景中学习"></a>在场景中学习</h4><p>假如我们有这样一个场景：有一系列的HTTP请求的URL构成的数组和一个初始值。这些HTTP请求是有依赖的，后一个的执行必须依赖前一个HTTP请求的响应。如果只是两个请求，我们可以很轻松地写出这样的代码：</p>
<figure class="highlight javascript"><table><tr><td class="gutter"><pre><div class="line">1</div><div class="line">2</div><div class="line">3</div><div class="line">4</div><div class="line">5</div><div class="line">6</div><div class="line">7</div><div class="line">8</div><div class="line">9</div><div class="line">10</div><div class="line">11</div><div class="line">12</div><div class="line">13</div><div class="line">14</div><div class="line">15</div><div class="line">16</div><div class="line">17</div><div class="line">18</div><div class="line">19</div><div class="line">20</div><div class="line">21</div><div class="line">22</div><div class="line">23</div><div class="line">24</div><div class="line">25</div><div class="line">26</div><div class="line">27</div></pre></td><td class="code"><pre><div class="line"><span class="keyword">const</span> http = <span class="built_in">require</span>(<span class="string">'http'</span>);</div><div class="line"><span class="keyword">const</span> urls = [<span class="string">'localhost'</span>, <span class="string">'www.baidu.com'</span>]; <span class="comment">//多个urls的数组</span></div><div class="line"></div><div class="line"><span class="keyword">const</span> req = http.request(optionsWithHostname(urls[<span class="number">0</span>]), (res) =&gt; &#123; <span class="comment">//第一次请求</span></div><div class="line">    res.on(<span class="string">'data'</span>, (chunk) =&gt; &#123;</div><div class="line">        <span class="keyword">const</span> req2 = http.request(optionsWithHostname(urls[<span class="number">1</span>]), (res2) =&gt; &#123;  <span class="comment">//第二次请求</span></div><div class="line">            res2.on(<span class="string">'data'</span>, (chunk2) =&gt; &#123;</div><div class="line">                <span class="comment">//dosometing here...</span></div><div class="line">            &#125;);</div><div class="line">        &#125;);</div><div class="line">        req2.write(chunk.toString() + <span class="string">'agian'</span>);</div><div class="line">        req2.end();</div><div class="line">    &#125;);</div><div class="line">&#125;);</div><div class="line"></div><div class="line">req.write(<span class="string">'hello world'</span>);</div><div class="line">req.end();</div><div class="line"></div><div class="line"><span class="function"><span class="keyword">function</span> <span class="title">optionsWithHostname</span>(<span class="params">hostname</span>) </span>&#123;</div><div class="line">    <span class="keyword">return</span> &#123;</div><div class="line">    <span class="attr">hostname</span>: hostname,</div><div class="line">    <span class="attr">port</span>: <span class="number">12306</span>,</div><div class="line">    <span class="attr">method</span>: <span class="string">'POST'</span>,</div><div class="line">    <span class="attr">headers</span>: &#123;</div><div class="line">        <span class="string">'Content-type'</span>: <span class="string">'text/plain'</span></div><div class="line">    &#125;&#125;;</div><div class="line">&#125;</div></pre></td></tr></table></figure>
<p>但如果是十个或者更多，这样的写法就不好使了。</p>
<p>我们知道异步函数必须在回调中才能使用其返回值，这样会很容易写出类似于<code>&gt;</code>形状的回调套回调的写法。而递归的写法也正好符合这样的形状，所以尝试一下：</p>
<figure class="highlight javascript"><table><tr><td class="gutter"><pre><div class="line">1</div><div class="line">2</div><div class="line">3</div><div class="line">4</div><div class="line">5</div><div class="line">6</div><div class="line">7</div><div class="line">8</div><div class="line">9</div><div class="line">10</div><div class="line">11</div><div class="line">12</div><div class="line">13</div><div class="line">14</div><div class="line">15</div></pre></td><td class="code"><pre><div class="line"><span class="keyword">const</span> urls = [<span class="string">'localhost'</span>, <span class="string">'www.baidu.com'</span>]; <span class="comment">//多个urls的数组</span></div><div class="line"></div><div class="line">(<span class="function"><span class="keyword">function</span> <span class="title">next</span>(<span class="params">i, len, initValue, callback</span>) </span>&#123;</div><div class="line">    <span class="keyword">if</span> (i &lt; len) &#123;</div><div class="line">    <span class="comment">// 将http请求过程简化成了async</span></div><div class="line">        <span class="keyword">async</span>(urls[i], (value) =&gt; &#123;</div><div class="line">            <span class="built_in">console</span>.log(value);</div><div class="line">            next(i + <span class="number">1</span>, len, value, callback);</div><div class="line">        &#125;);</div><div class="line">    &#125; <span class="keyword">else</span> &#123;</div><div class="line">        callback();</div><div class="line">    &#125;</div><div class="line">&#125;(<span class="number">0</span>, urls.length, <span class="string">'hello world'</span>, () =&gt; &#123;</div><div class="line">   <span class="comment">//dosomething here...</span></div><div class="line">&#125;));</div></pre></td></tr></table></figure>
<h4 id="总结-1"><a href="#总结-1" class="headerlink" title="总结"></a>总结</h4><ul>
<li>在异步函数想要保证执行的顺序，就必须一个回调套一个回调</li>
<li>可以利用递归的写法，在保证执行顺序的同时，处理系列或者不定长度的数据</li>
</ul>
<h3 id="异常处理"><a href="#异常处理" class="headerlink" title="异常处理"></a>异常处理</h3><h4 id="在比较中学习"><a href="#在比较中学习" class="headerlink" title="在比较中学习"></a>在比较中学习</h4><ul>
<li>同步方式下</li>
</ul>
<figure class="highlight javascript"><table><tr><td class="gutter"><pre><div class="line">1</div><div class="line">2</div><div class="line">3</div><div class="line">4</div><div class="line">5</div><div class="line">6</div><div class="line">7</div><div class="line">8</div></pre></td><td class="code"><pre><div class="line"><span class="comment">//try ... catch ...</span></div><div class="line"></div><div class="line"><span class="keyword">try</span> &#123;</div><div class="line">    x.func();</div><div class="line">&#125; <span class="keyword">catch</span> (err) &#123;</div><div class="line">   <span class="built_in">console</span>.log(<span class="string">"I catch you "</span>, err);</div><div class="line">&#125;</div><div class="line">=&gt; I <span class="keyword">catch</span> you  [<span class="built_in">ReferenceError</span>: x is not defined]</div></pre></td></tr></table></figure>
<ul>
<li>异步方式下</li>
</ul>
<figure class="highlight javascript"><table><tr><td class="gutter"><pre><div class="line">1</div><div class="line">2</div><div class="line">3</div><div class="line">4</div><div class="line">5</div><div class="line">6</div><div class="line">7</div><div class="line">8</div><div class="line">9</div><div class="line">10</div><div class="line">11</div><div class="line">12</div><div class="line">13</div></pre></td><td class="code"><pre><div class="line"><span class="keyword">try</span> &#123;</div><div class="line">    setTimeout(<span class="function"><span class="params">()</span> =&gt;</span> &#123;</div><div class="line">        x.func();</div><div class="line">    &#125;, <span class="number">0</span>);</div><div class="line">&#125; <span class="keyword">catch</span> (err) &#123;</div><div class="line">   <span class="built_in">console</span>.log(<span class="string">"I catch you "</span>, err);</div><div class="line">&#125;</div><div class="line">=&gt; C:\Users\qianyan\Projects\lesson6\exception\<span class="keyword">async</span>.js:<span class="number">5</span></div><div class="line">        x.func();</div><div class="line">        ^</div><div class="line"></div><div class="line"><span class="built_in">ReferenceError</span>: x is not defined</div><div class="line">    at <span class="literal">null</span>._onTimeout (C:\Users\qianyan\Projects\lesson6\exception\<span class="keyword">async</span>.js:<span class="number">5</span>:<span class="number">9</span>)</div></pre></td></tr></table></figure>
<p>可以看到，同步方式下异常会沿着代码执行路径一直冒泡，直到遇到第一个try语句时被捕获住。但由于异步函数会打断代码执行路径，异步函数执行过程中以及执行之后产生的异常冒泡到执行路径被打断的位置时，如果一直没有遇到try语句，就作为一个全局异常抛出。</p>
<p>解决方式就是在异常被作为全局异常抛出之前，try-catch住，如下：</p>
<figure class="highlight javascript"><table><tr><td class="gutter"><pre><div class="line">1</div><div class="line">2</div><div class="line">3</div><div class="line">4</div><div class="line">5</div><div class="line">6</div><div class="line">7</div><div class="line">8</div></pre></td><td class="code"><pre><div class="line">setTimeout(<span class="function"><span class="params">()</span> =&gt;</span> &#123;</div><div class="line">    <span class="keyword">try</span> &#123;</div><div class="line">        x.func();</div><div class="line">    &#125; <span class="keyword">catch</span> (err) &#123;</div><div class="line">        <span class="built_in">console</span>.log(<span class="string">"I catch you "</span>, err);</div><div class="line">    &#125;</div><div class="line">&#125;, <span class="number">0</span>);</div><div class="line">=&gt; I <span class="keyword">catch</span> you  [<span class="built_in">ReferenceError</span>: x is not defined]</div></pre></td></tr></table></figure>
<p>这样异常又被捕获了。不妨，对<code>setTimetout</code>做一次封装</p>
<figure class="highlight javascript"><table><tr><td class="gutter"><pre><div class="line">1</div><div class="line">2</div><div class="line">3</div><div class="line">4</div><div class="line">5</div><div class="line">6</div><div class="line">7</div><div class="line">8</div><div class="line">9</div><div class="line">10</div><div class="line">11</div><div class="line">12</div><div class="line">13</div><div class="line">14</div></pre></td><td class="code"><pre><div class="line"><span class="function"><span class="keyword">function</span> <span class="title">wrapSetTimeout</span>(<span class="params">fn, callback</span>) </span>&#123;</div><div class="line">    setTimeout(<span class="function"><span class="params">()</span> =&gt;</span> &#123;</div><div class="line">        <span class="keyword">try</span> &#123;</div><div class="line">            callback(<span class="literal">null</span>, fn());</div><div class="line">        &#125; <span class="keyword">catch</span> (err) &#123;</div><div class="line">            callback(err);</div><div class="line">        &#125;</div><div class="line">    &#125;, <span class="number">0</span>);</div><div class="line">&#125;</div><div class="line"></div><div class="line">wrapSetTimeout(<span class="function"><span class="params">()</span> =&gt;</span> &#123;x.func()&#125;, (err, data) =&gt; &#123;</div><div class="line">    <span class="keyword">if</span>(err) <span class="built_in">console</span>.log(<span class="string">"I catch you again"</span>, err);</div><div class="line">&#125;)</div><div class="line">=&gt;I <span class="keyword">catch</span> you again [<span class="built_in">ReferenceError</span>: x is not defined</div></pre></td></tr></table></figure>
<p>Node.js的整个异步函数的异常设计都是如此，callback的首个参数都是err。</p>
<h4 id="总结-2"><a href="#总结-2" class="headerlink" title="总结"></a>总结</h4><ul>
<li><em>try-catch</em>在同步方式下很有效，但在异步方式下做不到</li>
<li><em>callback</em>首个参数是err，是因为大多数API都遵循了一致的风格</li>
</ul>
<h2 id="小结-5"><a href="#小结-5" class="headerlink" title="小结"></a>小结</h2><ul>
<li>不掌握异步编程就不算学会NodeJS</li>
<li>异步编程依托于回调来实现，而使用回调不一定就是异步编程</li>
<li>异步编程下的函数间数据传递、数组遍历和异常处理与同步编程有很大差别</li>
</ul>
<hr>
<p>参考链接<br>[1] <a href="https://nqdeng.github.io/7-days-nodejs/" target="_blank" rel="external">七天学会NodeJS</a><br>[2] <a href="http://es6.ruanyifeng.com/" target="_blank" rel="external">ECMAScript 6入门 - 阮一峰</a></p>
]]></content>
    
    <summary type="html">
    
      &lt;h2 id=&quot;搭建-Node-js-开发环境&quot;&gt;&lt;a href=&quot;#搭建-Node-js-开发环境&quot; class=&quot;headerlink&quot; title=&quot;搭建 Node.js 开发环境&quot;&gt;&lt;/a&gt;搭建 Node.js 开发环境&lt;/h2&gt;&lt;h3 id=&quot;NVM安装&quot;&gt;&lt;a hre
    
    </summary>
    
      <category term="技术" scheme="https://lameta.com/categories/%E6%8A%80%E6%9C%AF/"/>
    
    
      <category term="node" scheme="https://lameta.com/tags/node/"/>
    
  </entry>
  
  <entry>
    <title>字符集和字符编码</title>
    <link href="https://lameta.com/2015/12/21/Character-set-and-encoding/"/>
    <id>https://lameta.com/2015/12/21/Character-set-and-encoding/</id>
    <published>2015-12-21T13:40:57.000Z</published>
    <updated>2016-09-06T14:41:36.000Z</updated>
    
    <content type="html"><![CDATA[<h2 id="什么是字符集"><a href="#什么是字符集" class="headerlink" title="什么是字符集"></a>什么是字符集</h2><ul>
<li><p>ASCII(American Standard Code for Information Interchange)</p>
<figure class="highlight lsl"><table><tr><td class="gutter"><pre><div class="line">1</div><div class="line">2</div></pre></td><td class="code"><pre><div class="line">man ascii</div><div class="line"><span class="number">0</span> ~ <span class="number">2</span>^<span class="number">7</span> = <span class="number">128</span></div></pre></td></tr></table></figure>
</li>
<li><p>欧洲语系 ISO8859</p>
<ul>
<li>ISO8859-1英语、法语、德语；ISO8859-5 俄语</li>
</ul>
</li>
<li>中国的GB2312和GBK，以及中国台湾的Big5</li>
<li>多语系 Unicode<ul>
<li>最初的目的是把世界各地的语言都映射到16位空间</li>
<li>8位转换为16位，称为UCS-2 (2 byte Universal Character Set), 也叫做Basic Multilingual Plane (BMP)</li>
<li>16位到21位，有效编码区间0 ~ 0x10ffff</li>
</ul>
</li>
</ul>
<h2 id="什么是编码方式"><a href="#什么是编码方式" class="headerlink" title="什么是编码方式"></a>什么是编码方式</h2><h3 id="取“鄢”这个字的Unicode编码"><a href="#取“鄢”这个字的Unicode编码" class="headerlink" title="取“鄢”这个字的Unicode编码"></a>取“鄢”这个字的Unicode编码</h3><hr>
<figure class="highlight 1c"><table><tr><td class="gutter"><pre><div class="line">1</div><div class="line">2</div></pre></td><td class="code"><pre><div class="line">echo <span class="string">"鄢"</span> <span class="string">| native2ascii -encoding utf8</span></div><div class="line">;; \u9122</div></pre></td></tr></table></figure>
<h3 id="一段查看汉字编码的Java代码"><a href="#一段查看汉字编码的Java代码" class="headerlink" title="一段查看汉字编码的Java代码"></a>一段查看汉字编码的Java代码</h3><figure class="highlight java"><table><tr><td class="gutter"><pre><div class="line">1</div><div class="line">2</div><div class="line">3</div><div class="line">4</div><div class="line">5</div><div class="line">6</div><div class="line">7</div><div class="line">8</div><div class="line">9</div><div class="line">10</div><div class="line">11</div><div class="line">12</div><div class="line">13</div><div class="line">14</div><div class="line">15</div><div class="line">16</div><div class="line">17</div><div class="line">18</div><div class="line">19</div><div class="line">20</div><div class="line">21</div><div class="line">22</div><div class="line">23</div><div class="line">24</div><div class="line">25</div><div class="line">26</div><div class="line">27</div><div class="line">28</div><div class="line">29</div><div class="line">30</div></pre></td><td class="code"><pre><div class="line"><span class="keyword">import</span> java.nio.charset.Charset;</div><div class="line"><span class="keyword">import</span> java.util.Arrays;</div><div class="line"></div><div class="line"><span class="class"><span class="keyword">class</span> <span class="title">Main</span> </span>&#123;</div><div class="line">	<span class="function"><span class="keyword">public</span> <span class="keyword">static</span> <span class="keyword">void</span> <span class="title">main</span><span class="params">(String[] args)</span> <span class="keyword">throws</span> Exception </span>&#123;</div><div class="line">      unicode(<span class="string">"鄢"</span>, <span class="string">"utf8"</span>);</div><div class="line">      <span class="comment">// How many bytes: 3 What are they: [-23, -124, -94]</span></div><div class="line"></div><div class="line">      unicode(<span class="string">"鄢"</span>, <span class="string">"utf16"</span>);</div><div class="line">      <span class="comment">// How many bytes: 4 What are they: [-2, -1, -111, 34]</span></div><div class="line"></div><div class="line">      unicode(<span class="string">"鄢"</span>, <span class="string">"utf-16BE"</span>);</div><div class="line">      <span class="comment">// How many bytes: 2 What are they: [-111, 34]</span></div><div class="line"></div><div class="line">      unicode(<span class="string">"🐶"</span>, <span class="string">"utf-16BE"</span>);</div><div class="line">      <span class="comment">// How many bytes: 4 What are they: [-40, 61, -36, 54]</span></div><div class="line"></div><div class="line">      p(<span class="string">"🐶"</span>.length());</div><div class="line">      <span class="comment">// 2</span></div><div class="line">	&#125;</div><div class="line"></div><div class="line">	<span class="function"><span class="keyword">public</span> <span class="keyword">static</span> <span class="keyword">void</span> <span class="title">unicode</span><span class="params">(String s, String encoding)</span></span>&#123;</div><div class="line">		p(<span class="string">"How many bytes: "</span> + s.getBytes(Charset.forName(encoding)).length);</div><div class="line">		p(<span class="string">"What are they: "</span> + Arrays.toString(s.getBytes(Charset.forName(encoding))));</div><div class="line">	&#125;</div><div class="line"></div><div class="line">	<span class="function"><span class="keyword">public</span> <span class="keyword">static</span> <span class="keyword">void</span> <span class="title">p</span><span class="params">(Object s)</span> </span>&#123;</div><div class="line">		System.out.println(s);</div><div class="line">	&#125;</div><div class="line">&#125;</div></pre></td></tr></table></figure>
<h2 id="UTF-8变长编码方式"><a href="#UTF-8变长编码方式" class="headerlink" title="UTF-8变长编码方式"></a>UTF-8变长编码方式</h2><figure class="highlight lsl"><table><tr><td class="gutter"><pre><div class="line">1</div><div class="line">2</div><div class="line">3</div><div class="line">4</div><div class="line">5</div><div class="line">6</div><div class="line">7</div><div class="line">8</div><div class="line">9</div><div class="line">10</div><div class="line">11</div><div class="line">12</div><div class="line">13</div><div class="line">14</div><div class="line">15</div><div class="line">16</div><div class="line">17</div><div class="line">18</div><div class="line">19</div><div class="line">20</div></pre></td><td class="code"><pre><div class="line">\u9122</div><div class="line"><span class="number">1110</span>xxxx <span class="number">10</span>xxxxxx <span class="number">10</span>xxxxxx</div><div class="line"></div><div class="line"><span class="number">9</span> -&gt; <span class="number">1001</span></div><div class="line"><span class="number">1</span> -&gt; <span class="number">0001</span></div><div class="line"><span class="number">2</span> -&gt; <span class="number">0010</span></div><div class="line"><span class="number">2</span> -&gt; <span class="number">0010</span></div><div class="line"></div><div class="line"><span class="number">10010001</span> <span class="number">00100010</span></div><div class="line"></div><div class="line">原码</div><div class="line"><span class="number">11101001</span> <span class="number">10000100</span>  <span class="number">10100010</span></div><div class="line"></div><div class="line">取反</div><div class="line"><span class="number">10010110</span> <span class="number">11111011</span> <span class="number">11011101</span></div><div class="line"></div><div class="line">补码</div><div class="line"><span class="number">10010111</span> <span class="number">11111100</span> <span class="number">11011110</span></div><div class="line"><span class="number">16</span>+<span class="number">4</span>+<span class="number">2</span>+<span class="number">1</span>=<span class="number">23</span> <span class="number">127</span><span class="number">-3</span>=<span class="number">124</span> <span class="number">64</span>+<span class="number">16</span>+<span class="number">8</span>+<span class="number">4</span>+<span class="number">2</span>=<span class="number">94</span></div><div class="line">[<span class="number">-23</span>, <span class="number">-124</span>, <span class="number">-94</span>]</div></pre></td></tr></table></figure>
<ul>
<li>从程序运行的结果来看，<code>UTF-8</code>中，“鄢”占据了3个字节，且在计算机中的补码表示分别为<code>-23, -124, -94</code>；</li>
<li><code>\u9122</code>占据2个字节，但是带入<code>UTF-8</code>编码提供的<code>1110xxxx 10xxxxxx 10xxxxxx</code>模板中就变成了3个字节，我们是从低位带入模板的，<code>x</code>的个数刚好是16位、2个字节；</li>
<li><code>UTF-8</code>编码除了和ASCII码兼容部分(以0开头，<code>0xxxxxxx</code>)，其余都遵循一个简单的标准：<ul>
<li>其模板遵循<code>110xxxxx 10xxxxxx</code>，高位1的个数即是总的字节数，这里<code>110xxxxx</code>中11表示总共有两个字节；</li>
<li>低位的<code>10xxxxxx</code>始终以10开头；</li>
<li>这样的编码方式，使得程序清楚知道那个地方是字符开始的地方，所以才说<code>UTF-8</code>的单字节的编码方式。</li>
</ul>
</li>
</ul>
<p>但是UTF-8也有自己的缺点</p>
<ul>
<li>浪费内存，几乎所有的汉字都占三个字节</li>
<li>随机访问，同字符串的长度成正比</li>
</ul>
<p><strong>Tips</strong>:</p>
<blockquote>
<p>正数的补码就是其本身<br>负数的补码是在其原码的基础上, 符号位不变, 其余各位取反, 最后+1</p>
</blockquote>
<h2 id="大端字节序和小端字节序-主机字节序和网络字节序"><a href="#大端字节序和小端字节序-主机字节序和网络字节序" class="headerlink" title="大端字节序和小端字节序(主机字节序和网络字节序)"></a>大端字节序和小端字节序(主机字节序和网络字节序)</h2><p>我们知道<code>UTF-16</code>的目的和Unicode本来的目的是一致的 —— “把世界各地的语言都映射到16位空间”。<br>以<code>\u9122</code>为例，我们运行程序的结果应该是2个字节，但是事实上是4个字节（<em>How many bytes: 4 What are they: [-2, -1, -111, 34]</em>），这多出来的2个字节是怎么回事？<br>这里就是字节序在作祟。在内存存储时，如果最低有效位在最高有效位的前面，则称小端序；反之则称大端序。大端字节序又称网络字节序。<br>举个例子<br><figure class="highlight livescript"><table><tr><td class="gutter"><pre><div class="line">1</div><div class="line">2</div><div class="line">3</div></pre></td><td class="code"><pre><div class="line"><span class="string">\u9122</span> ;;;<span class="function">-&gt;</span>鄢</div><div class="line"><span class="number">91</span>是最高有效位</div><div class="line"><span class="number">22</span>是最低有效位</div></pre></td></tr></table></figure></p>
<p>我们观察一下返回的4个字节中的前两个字节<code>-2, -1</code>，它们其实不是字符“鄢”的一部分，它们是Unicode中的大端字节序的标识BOM(Bytes Order Marks)<br><figure class="highlight lsl"><table><tr><td class="gutter"><pre><div class="line">1</div><div class="line">2</div><div class="line">3</div><div class="line">4</div><div class="line">5</div><div class="line">6</div><div class="line">7</div></pre></td><td class="code"><pre><div class="line"><span class="number">0xfeff</span></div><div class="line"><span class="number">11111110</span> <span class="number">11111111</span></div><div class="line">反码</div><div class="line"><span class="number">10000001</span> <span class="number">10000000</span></div><div class="line">补码</div><div class="line"><span class="number">10000010</span> <span class="number">10000001</span></div><div class="line"><span class="number">-2</span> <span class="number">-1</span></div></pre></td></tr></table></figure></p>
<p>Unicode在对待字节序时，采取了大端和小端序共存的方式。<code>0xfeff</code>这个编码在对调字节序后得到的<code>0xfffe</code>在Unicode不存在，Unicode借助这个特点来判断读入的字符到底是大端序还是小端序。<br>另外，从程序中可知，Java会默认将读入的字符串当做大端序处理；所以当我们明确指定<code>UTF-16BE</code>后，程序就会返回2个字节的结果了。</p>
<h2 id="UTF-16不足以表示世界所有的字符"><a href="#UTF-16不足以表示世界所有的字符" class="headerlink" title="UTF-16不足以表示世界所有的字符"></a>UTF-16不足以表示世界所有的字符</h2><p>正如前面提及的UCS-2 (2 byte Universal Character Set)不足以表示世界上所有的字符，比如“🐶”这种emoji的字符需要4个字节才能表示，两个字节的<code>UTF-16</code>就无能为力了。<br>Java默认采用了<code>UTF-16</code>编码，结果导致<code>&quot;🐶&quot;.length()</code>不是返回1，而是2。不得已，Java引入了codePoint这种处理方式，有点无奈。</p>
<h2 id="UCS和CSI"><a href="#UCS和CSI" class="headerlink" title="UCS和CSI"></a>UCS和CSI</h2><p>我们计算机的程序会采用两种方式来处理不同的字符集</p>
<ul>
<li><p>UCS：（Universal Character Set，泛用字符集）程序输入输出的时候，需要将文本数据变成UCS，统一处理。</p>
</li>
<li><p>CSI：（Character Set Independent 字符集独立），不对文字集做变换，直接处理。</p>
</li>
</ul>
<h2 id="结论"><a href="#结论" class="headerlink" title="结论"></a>结论</h2><ul>
<li>积极采用UTF-8。</li>
</ul>
]]></content>
    
    <summary type="html">
    
      &lt;h2 id=&quot;什么是字符集&quot;&gt;&lt;a href=&quot;#什么是字符集&quot; class=&quot;headerlink&quot; title=&quot;什么是字符集&quot;&gt;&lt;/a&gt;什么是字符集&lt;/h2&gt;&lt;ul&gt;
&lt;li&gt;&lt;p&gt;ASCII(American Standard Code for Information 
    
    </summary>
    
      <category term="技术" scheme="https://lameta.com/categories/%E6%8A%80%E6%9C%AF/"/>
    
    
      <category term="thinking" scheme="https://lameta.com/tags/thinking/"/>
    
  </entry>
  
  <entry>
    <title>Why we are learning Clojure</title>
    <link href="https://lameta.com/2015/11/26/Why-are-we-learning-Clojure/"/>
    <id>https://lameta.com/2015/11/26/Why-are-we-learning-Clojure/</id>
    <published>2015-11-26T05:37:56.000Z</published>
    <updated>2016-09-06T14:56:24.000Z</updated>
    
    <content type="html"><![CDATA[<p>在Gaslight，我们广泛地在工作中使用Ruby和Rails，也充分地经历了从JQuery到Ember再到AngularJS及其他所有关于JavaScript的一切。不过有时候我们为了学习和成长，需要脱离舒适区。我们如何赶上未来的趋势和机会呢？</p>
<p>那就是绝不停止学习。在过去的几个星期里，我们的开发团队决定作为一个小组去掌握Clojure语言。我们尝试构建了关于这门技术的主要知识点，所以当我们在一个客户项目中采用Clojure的时候，对自己技术的深度和熟悉程度很有自信。</p>
<p>为什么我们会选择Clojure呢？</p>
<h2 id="填补了Ruby和Rail没有顾及的空白"><a href="#填补了Ruby和Rail没有顾及的空白" class="headerlink" title="填补了Ruby和Rail没有顾及的空白"></a>填补了Ruby和Rail没有顾及的空白</h2><blockquote>
<p>我知道我们不可能永远用Ruby编程。另一个语言和环境一定会到来并取代它。我只是希望它真的比我们现在所拥用的更好。 —— Doug Alcorn</p>
</blockquote>
<p>我们依然热爱Ruby，依然认为Rails通常是构建web应用的最佳方案。但是，它和其他工具一样，有长处，也有短处。</p>
<ol>
<li>Ruby的并发并不健壮。是的，你可以使用JRuby和Celluloid，但是可变性在根本上妨碍它走向并发。同时，很多你想要使用的库都不是线程安全的。</li>
<li>Ruby和Rails几乎总是足够快的。几乎总是，但不尽然。</li>
<li>对象关系映射（ORM）不总是正确的。</li>
<li>面向对象的语言也不总是最佳的解决方案。</li>
</ol>
<h2 id="函数式编程是一种不同的思考方式"><a href="#函数式编程是一种不同的思考方式" class="headerlink" title="函数式编程是一种不同的思考方式"></a>函数式编程是一种不同的思考方式</h2><blockquote>
<p>函数式编程是一种编程范式，一种构建计算机程序的结构和元素，它把计算看做数学函数的求值，同时禁止改变状态和可变的数据。——Wikipedia</p>
</blockquote>
<p>函数式编程语言是一种有别于我们平常在Ruby和JavaScript中编程的编程范式。学习这一范式使得我们能学习崭新且不同的技术来解决问题。这些技术可能不太容易去学习，如果我们沉浸在自己舒适的命令式和面向对象的语言范式当中。</p>
<h2 id="Clojure运行在服务器端"><a href="#Clojure运行在服务器端" class="headerlink" title="Clojure运行在服务器端"></a>Clojure运行在服务器端</h2><blockquote>
<p>Clojure实现它的目标，通过：拥抱一个行业标准的开放平台——Java虚拟机；现代化一门古老的语言——Lisp；树立带有不可变持久化数据结构的函数式编程；同时通过软件事务内存以及异步代理提供内建的并发支持。这一结果是健壮的，顺手的和快速的。—— Clojure.org</p>
</blockquote>
<p>Clojure运行在Java虚拟机（JVM）上并且利用了其提供的优势：高质量的垃圾回收，高性能的本地线程以及可扩展的移植。Clojure又在其上增加了高效的不可变持久化数据结构。Clojure带来了有趣的并发模型Core.Async。另外，这里还有很多其他有趣的库供开发，逻辑编程，模式匹配，optional typing和好多我们想要尝试并理解的东西。</p>
<h2 id="Clojure运行在浏览器端"><a href="#Clojure运行在浏览器端" class="headerlink" title="Clojure运行在浏览器端"></a>Clojure运行在浏览器端</h2><blockquote>
<p>一个具有扩展，移植和多供应商支持的开发平台，一场在各个新设备上实现精巧工具的优化军备竞赛以及一个对更丰富且更精巧应用的呐喊。开发者们还有什么想要的呢？一个不同的语言，这就是了。—— ClojureScript Wiki</p>
</blockquote>
<p>ClojureScript编译成JavaScript运行于浏览器。它提供了一种全新的方式去思考编写客户端应用程序。JavaScript并不是开发者社区里广泛青睐的语言。但是Clojure是设计良好，精密且具有很好异步支持的成熟的Lisp。它也可以和其他的JavaScript类库交互。所有这些都为我们提供了一个在客户端和服务端使用相同的语言却不需要做任何妥协的机会。</p>
<p>管理本来就具有状态的客户端应用是件尴尬的事，不过像React和Ember这些JavaScript框架正在着手解决这个问题，我们开始猜想如果一种更加基本的改变方式值得深入研究，那么是否可能有一种更好的语言和这其中的任一已经存在的框架配合来为我们提供一种途径使得可以更好地控制复杂性呢？Clojure允许我们朝着这个问题寻找答案。</p>
<p>我们不相信银弹，但是我们真的喜欢这样的事实，Clojure包含了客户端的需求。</p>
<h2 id="朝冰球要去的地方滑"><a href="#朝冰球要去的地方滑" class="headerlink" title="朝冰球要去的地方滑"></a>朝冰球要去的地方滑</h2><blockquote>
<p>我追逐冰球要去的地方，而不是它所在的地方。—— Wayne Gretzky</p>
</blockquote>
<p>Clojure会帮助我们填补创建特定类型的应用程序的沟壑。那些需要响应或者处理大规模数据的程序。我们已经身处多核CPU的纪元，但是传统的语言还未追赶上这些计算机的架构。函数式编程提供了一种新的编程范式允许我们更简单地使用这种架构写程序。Clojure提供允许我们高效地处理大规模数据的工具。它使得传递和转换数据接近实时。这些特性会使我们比从前更高效地解决问题。</p>
<h2 id="不过不是Bleeding-Edge"><a href="#不过不是Bleeding-Edge" class="headerlink" title="不过不是Bleeding Edge"></a>不过不是Bleeding Edge</h2><blockquote>
<p>“什么会被认为是bleeding-edge的技术，”sez Lucas。 “没被证实的用法，高风险，只有早期采用成瘾者才感到舒服的东西”—— Thomas Pynchon, Bleeding Edge</p>
</blockquote>
<p>此时，Clojure被发布超过5年之久并且似乎在开发者社区里获得了不错的想法分享。在新锐技术上掉坑的采纳曲线上这不算太早了。Clojure利用了已存在的技术如已经被开发超过15以上年头的java虚拟机（JVM）。它也可以利用很多已经存在的高质量的Java类库。</p>
<h2 id="一些新东西让我们保持兴奋"><a href="#一些新东西让我们保持兴奋" class="headerlink" title="一些新东西让我们保持兴奋"></a>一些新东西让我们保持兴奋</h2><blockquote>
<p>自从学Ruby以来，还没有学习一门新技术让我如此激动！ —— Michael Guterl</p>
</blockquote>
<p>在Gaslight，我们想在行业中吸引和保留最好的技术。为了做到这一点，我们需要给大家提供学习和提高其技能的机会。Clojure帮助我们提供了这样的机会。它是一个带有一系列工具的崭新范式。不过最重要的是，它允许我们处理新的且有趣的问题。还没有什么能比解决有意思的问题更让我们高兴的呢。</p>
]]></content>
    
    <summary type="html">
    
      &lt;p&gt;在Gaslight，我们广泛地在工作中使用Ruby和Rails，也充分地经历了从JQuery到Ember再到AngularJS及其他所有关于JavaScript的一切。不过有时候我们为了学习和成长，需要脱离舒适区。我们如何赶上未来的趋势和机会呢？&lt;/p&gt;
&lt;p&gt;那就是绝不停
    
    </summary>
    
      <category term="Clojure" scheme="https://lameta.com/categories/Clojure/"/>
    
    
      <category term="thinking" scheme="https://lameta.com/tags/thinking/"/>
    
  </entry>
  
  <entry>
    <title>Monad</title>
    <link href="https://lameta.com/2015/08/04/Monad/"/>
    <id>https://lameta.com/2015/08/04/Monad/</id>
    <published>2015-08-04T15:09:06.000Z</published>
    <updated>2016-09-06T14:56:16.000Z</updated>
    
    <content type="html"><![CDATA[<blockquote>
<p>Monad不就是个自函子范畴上的幺半群，这有什么难理解的(A monad is just a monoid in the category of endofunctors)<br>—— Phillip Wadler</p>
</blockquote>
<h2 id="自函子-Endofunctor"><a href="#自函子-Endofunctor" class="headerlink" title="自函子(Endofunctor)"></a>自函子(Endofunctor)</h2><p>什么是函数（Function）？<br>函数表达的映射关系在类型上体现在特定类型（proper type）之间的映射。</p>
<p>什么是自函数（Endofunction）？<br><figure class="highlight julia"><table><tr><td class="gutter"><pre><div class="line">1</div></pre></td><td class="code"><pre><div class="line">identity <span class="type">::</span> <span class="built_in">Number</span> -&gt; <span class="built_in">Number</span></div></pre></td></tr></table></figure></p>
<p>自函数就是把<strong>类型映射到自身类型</strong>。函数identity是一个自函数的特例，它接收什么参数就返回什么参数，所以入参和返回值不仅类型一致，而且值也相同。</p>
<p>接下来，回答什么是自函子（Endofunctor）之前，我们先弄清什么是函子（Functor）？</p>
<p>函子有别于函数，函数描述的是特定类型（proper type）之间的映射，而函子描述的是<strong>范畴（category）</strong>之间的映射。</p>
<p>那什么是范畴（category）？</p>
<p>我们把范畴看做一组类型及其关系态射（morphism）的集合。包括特定类型及其态射，比如<code>Int、String、Int -&gt; String</code>；高阶类型及其态射，比如<code>List[Int]、List[String]、List[Int] -&gt; List[String]</code>。</p>
<p>接下来看看函子是如何映射两个范畴的，见下图：<br><img src="http://upload-images.jianshu.io/upload_images/217988-81dc30327d901c40.png?imageMogr2/auto-orient/strip%7CimageView2/2/w/1240" alt="范畴"></p>
<p>图中范畴C1和范畴C2之间有映射关系，C1中Int映射到C2中的List[Int]，C1中String映射到C2中的List[String]。除此之外，C1中的关系态射<code>Int -&gt; String</code>也映射到C2中的关系<code>List[Int] -&gt; List[String]</code>态射上。</p>
<blockquote>
<p>换句话说，如果一个范畴内部的所有元素可以映射为另一个范畴的元素，且元素间的关系也可以映射为另一个范畴元素间关系，则认为这两个范畴之间存在映射。所谓函子就是表示两个范畴的映射。</p>
</blockquote>
<p>澄清了函子的含义，那么如何在程序中表达它？</p>
<p>在Haskell中，函子是在其上可以<em>map over</em>的东西。稍微有一点函数式编程经验，一定会想到数组（Array)或者列表（List），确实如此。不过，在我们的例子中，List并不是一个具体的类型，而是一个类型构造子。举个例子，构造List[Int]，也就是把Int提升到List[Int]，记作<code>Int -&gt; List[Int]</code>。这表达了<em>一个范畴的元素可以映射为另一个范畴的元素。</em></p>
<p>List具有map方法，不妨看看map的定义：<br><figure class="highlight xl"><table><tr><td class="gutter"><pre><div class="line">1</div><div class="line">2</div></pre></td><td class="code"><pre><div class="line"><span class="function"><span class="title">f</span> :: A -&gt;</span> B</div><div class="line"><span class="function"><span class="title">map</span> :: f -&gt;</span> L<span class="function"><span class="title">ist</span>[A] -&gt;</span> List[B]</div></pre></td></tr></table></figure></p>
<p>具体到我们的例子当中，就有：<br><figure class="highlight julia"><table><tr><td class="gutter"><pre><div class="line">1</div><div class="line">2</div></pre></td><td class="code"><pre><div class="line">f <span class="type">::</span> <span class="built_in">Int</span> -&gt; <span class="built_in">String</span></div><div class="line">map <span class="type">::</span> f -&gt; List[<span class="built_in">Int</span>] -&gt; List[<span class="built_in">String</span>]</div></pre></td></tr></table></figure></p>
<p>展开来看：<br><figure class="highlight xl"><table><tr><td class="gutter"><pre><div class="line">1</div></pre></td><td class="code"><pre><div class="line"><span class="function"><span class="title">map</span> :: Int -&gt;</span> S<span class="function"><span class="title">tring</span> -&gt;</span> L<span class="function"><span class="title">ist</span>[Int] -&gt;</span> List[String]</div></pre></td></tr></table></figure></p>
<p>map的定义清晰地告诉我们：<code>Int -&gt; String</code>这种关系可以映射为<code>List[Int] -&gt; List[String]</code>这种关系。这就表达了<em>元素间的关系也可以映射为另一个范畴元素间关系。</em></p>
<p>所以类型构造器List[T]就是一个函子。</p>
<p>理解了函子的概念，接着继续探究什么是自函子。我们已经知道<em>自函数就是把类型映射到自身类型</em>，那么自函子就是把范畴映射到自身范畴。</p>
<p>自函子是如何映射范畴的，见下图：<br><img src="http://upload-images.jianshu.io/upload_images/217988-7d3053ee4f51a15e.png?imageMogr2/auto-orient/strip%7CimageView2/2/w/1240" alt="Identity自函子范畴"></p>
<p>图中表示的是一个将范畴映射到自身的自函子，而且还是一个特殊的Identity自函子。为什么这么说？从函子的定义出发，我们考察这个自函子，始终有<code>List[Int] -&gt; List[Int]</code>以及<code>List[Int] -&gt; List[String] -&gt; List[Int] -&gt; List[String]</code>这两种映射。<br>我们表述成：<br><figure class="highlight zephir"><table><tr><td class="gutter"><pre><div class="line">1</div><div class="line">2</div></pre></td><td class="code"><pre><div class="line">类型<span class="keyword">List</span>[<span class="keyword">Int</span>]映射到自己</div><div class="line">态射f :: <span class="keyword">List</span>[<span class="keyword">Int</span>] -&gt; <span class="keyword">List</span>[String]映射到自己</div></pre></td></tr></table></figure></p>
<p>我们记作：<br><figure class="highlight stylus"><table><tr><td class="gutter"><pre><div class="line">1</div><div class="line">2</div><div class="line">3</div></pre></td><td class="code"><pre><div class="line"><span class="function"><span class="title">F</span><span class="params">(List[Int])</span></span> = List[Int]</div><div class="line"><span class="function"><span class="title">F</span><span class="params">(f)</span></span> = f</div><div class="line">其中，F是Functor.</div></pre></td></tr></table></figure></p>
<p>除了Identity的自函子，还有其它的自函子，见下图：<br><img src="http://upload-images.jianshu.io/upload_images/217988-5143675acb7bab44.png?imageMogr2/auto-orient/strip%7CimageView2/2/w/1240" alt="自函子范畴"></p>
<p>图中的省略号代表这些范畴可以无限地延伸下去。我们在这个大范畴所做的所有映射操作都是同一范畴内的映射，自然这样的范畴就是一个自函子的范畴。</p>
<p>我们记作：<br><figure class="highlight zephir"><table><tr><td class="gutter"><pre><div class="line">1</div><div class="line">2</div><div class="line">3</div></pre></td><td class="code"><pre><div class="line"><span class="keyword">List</span>[<span class="keyword">Int</span>] -&gt; <span class="keyword">List</span>[<span class="keyword">List</span>[<span class="keyword">Int</span>]]</div><div class="line"><span class="keyword">List</span>[<span class="keyword">Int</span>] -&gt; <span class="keyword">List</span>[String] -&gt; <span class="keyword">List</span>[<span class="keyword">List</span>[<span class="keyword">Int</span>]] -&gt; <span class="keyword">List</span>[<span class="keyword">List</span>[String]]</div><div class="line">...</div></pre></td></tr></table></figure></p>
<p>所以<code>List[Int]、List[List[Int]]、...、List[List[List[...]]]</code>及其之间的态射是一个自函子的范畴。</p>
<hr>
<h2 id="幺半群"><a href="#幺半群" class="headerlink" title="幺半群"></a>幺半群</h2><blockquote>
<p>[幺半群][1]是一个带有二元运算 <em>: M × M → M 的集合 M ，其符合下列公理：<br>结合律：对任何在 M 内的a、b、c， (a</em>b)<em>c = a</em>(b<em>c) 。<br>单位元：存在一在 M 内的元素e，使得任一于 M 内的 a 都会符合 a</em>e = e*a = a 。</p>
</blockquote>
<p>接着我们看看在自函子的范畴上，怎么结合幺半群的定义得出Monad的。</p>
<p>假设我们有个cube函数，它的功能就是计算每个数的3次方，函数签名如下：<br><figure class="highlight javascript"><table><tr><td class="gutter"><pre><div class="line">1</div></pre></td><td class="code"><pre><div class="line">cube :: <span class="built_in">Number</span> -&gt; <span class="built_in">Number</span></div></pre></td></tr></table></figure></p>
<p>现在我们想在其返回值上添加一些调试信息，所以返回一个元组（Tuple），第二个元素代表调试信息。函数签名如下：<br><figure class="highlight plain"><table><tr><td class="gutter"><pre><div class="line">1</div></pre></td><td class="code"><pre><div class="line">f :: Number -&gt; (Number,String)</div></pre></td></tr></table></figure></p>
<p>结合前面所讲，我们很容易知道元组构造子(Number,String)是一个自函子。Number所在的范畴并不同于元组(Number,String)所在的范畴。换句话说，f的入参和返回值属于两个范畴。那么这会产生什么影响？我们看看幺半群的定义中规定的结合律。对于函数而言，结合律就是将函数以各种结合方式嵌套起来调用。我们将常用的compose函数看作此处的二元运算。</p>
<figure class="highlight javascript"><table><tr><td class="gutter"><pre><div class="line">1</div><div class="line">2</div><div class="line">3</div><div class="line">4</div><div class="line">5</div><div class="line">6</div><div class="line">7</div></pre></td><td class="code"><pre><div class="line"><span class="keyword">var</span> compose = <span class="function"><span class="keyword">function</span>(<span class="params">f, g</span>) </span>&#123;</div><div class="line">  <span class="keyword">return</span> <span class="function"><span class="keyword">function</span>(<span class="params">x</span>) </span>&#123;</div><div class="line">    <span class="keyword">return</span> f(g(x));</div><div class="line">  &#125;;</div><div class="line">&#125;;</div><div class="line"></div><div class="line">compose(f, f)</div></pre></td></tr></table></figure>
<p>从函数签名可以很容易看出，右边的f运算的结果是元组，而左侧的f却是接收一个Number类型的函数，它们是彼此不兼容的。</p>
<p>有什么好办法能消除这种不兼容性？假如输入和输出都是元组，结果会如何呢？<br><figure class="highlight javascript"><table><tr><td class="gutter"><pre><div class="line">1</div><div class="line">2</div><div class="line">3</div></pre></td><td class="code"><pre><div class="line">F :: (<span class="built_in">Number</span>,<span class="built_in">String</span>) -&gt; (<span class="built_in">Number</span>,<span class="built_in">String</span>)</div><div class="line"></div><div class="line">compose(F, F)</div></pre></td></tr></table></figure></p>
<p>这样是可行的！在验证满足结合律之前，我们引入一个bind函数来辅助将f提升成F.</p>
<figure class="highlight javascript"><table><tr><td class="gutter"><pre><div class="line">1</div><div class="line">2</div><div class="line">3</div><div class="line">4</div><div class="line">5</div><div class="line">6</div><div class="line">7</div><div class="line">8</div><div class="line">9</div><div class="line">10</div><div class="line">11</div><div class="line">12</div><div class="line">13</div></pre></td><td class="code"><pre><div class="line">f :: <span class="built_in">Number</span> -&gt; <span class="function">(<span class="params"><span class="built_in">Number</span>,<span class="built_in">String</span></span>) =&gt;</span> F :: (<span class="built_in">Number</span>,<span class="built_in">String</span>) -&gt; (<span class="built_in">Number</span>,<span class="built_in">String</span>)</div><div class="line"></div><div class="line"><span class="keyword">var</span> bind = <span class="function"><span class="keyword">function</span>(<span class="params">f</span>) </span>&#123;</div><div class="line">  <span class="keyword">return</span> <span class="function"><span class="keyword">function</span> <span class="title">F</span>(<span class="params">tuple</span>) </span>&#123;</div><div class="line">    <span class="keyword">var</span> x  = tuple[<span class="number">0</span>],</div><div class="line">        s  = tuple[<span class="number">1</span>],</div><div class="line">        fx = f(x),</div><div class="line">        y  = fx[<span class="number">0</span>],</div><div class="line">        t  = fx[<span class="number">1</span>];</div><div class="line"></div><div class="line">    <span class="keyword">return</span> [y, s + t];</div><div class="line">  &#125;;</div><div class="line">&#125;;</div></pre></td></tr></table></figure>
<p>我们来实现元组自函子范畴上的结合律：<br><figure class="highlight javascript"><table><tr><td class="gutter"><pre><div class="line">1</div><div class="line">2</div><div class="line">3</div><div class="line">4</div><div class="line">5</div><div class="line">6</div><div class="line">7</div><div class="line">8</div><div class="line">9</div><div class="line">10</div><div class="line">11</div><div class="line">12</div><div class="line">13</div><div class="line">14</div><div class="line">15</div><div class="line">16</div></pre></td><td class="code"><pre><div class="line"><span class="keyword">var</span> cube = <span class="function"><span class="keyword">function</span>(<span class="params">x</span>) </span>&#123;</div><div class="line">  <span class="keyword">return</span> [x * x * x, <span class="string">'cube was called.'</span>];</div><div class="line">&#125;;</div><div class="line"></div><div class="line"><span class="keyword">var</span> sine = <span class="function"><span class="keyword">function</span>(<span class="params">x</span>) </span>&#123;</div><div class="line">  <span class="keyword">return</span> [<span class="built_in">Math</span>.sin(x), <span class="string">'sine was called.'</span>];</div><div class="line">&#125;;</div><div class="line"></div><div class="line"><span class="keyword">var</span> f = compose(compose(bind(sine), bind(cube)), bind(cube));</div><div class="line">f([<span class="number">3</span>, <span class="string">''</span>])</div><div class="line"></div><div class="line"><span class="keyword">var</span> f1 = compose(bind(sine), compose(bind(cube), bind(cube)));</div><div class="line">f1([<span class="number">3</span>,<span class="string">''</span>])</div><div class="line">&gt;&gt;&gt;</div><div class="line">[<span class="number">0.956</span>, <span class="string">'cube was called.cube was called.sine was called.'</span>]</div><div class="line">[<span class="number">0.956</span>, <span class="string">'cube was called.cube was called.sine was called.'</span>]</div></pre></td></tr></table></figure></p>
<p>这里<code>f和f1</code>代表的调用顺序产生同样的结果，说明元组自函子范畴满足结合律。</p>
<p>那如何找到这样一个<code>e</code>，使得<code>a*e=e*a=a</code></p>
<figure class="highlight javascript"><table><tr><td class="gutter"><pre><div class="line">1</div><div class="line">2</div><div class="line">3</div><div class="line">4</div><div class="line">5</div><div class="line">6</div></pre></td><td class="code"><pre><div class="line"><span class="comment">// unit :: Number -&gt; (Number,String)</span></div><div class="line"><span class="keyword">var</span> unit = <span class="function"><span class="keyword">function</span>(<span class="params">x</span>) </span>&#123; <span class="keyword">return</span> [x, <span class="string">''</span>] &#125;;</div><div class="line"></div><div class="line"><span class="keyword">var</span> f = compose(bind(sine), bind(cube));</div><div class="line"></div><div class="line">compose(f, bind(unit)) = compose(bind(unit), f) = f</div></pre></td></tr></table></figure>
<p>这里的<code>bind(unit)</code>就是<code>e</code>了。</p>
<p><em>Monads for functional programming</em>一书中介绍说<em>monad</em>是一个三元组<code>(M, unit, *)</code>，对应此处就是<code>(Tuple, unit, bind)</code>.</p>
<p>参考链接：</p>
<ol>
<li><a href="https://blog.jcoglan.com/2011/03/05/translation-from-haskell-to-javascript-of-selected-portions-of-the-best-introduction-to-monads-ive-ever-read/" target="_blank" rel="external">Translation from Haskell to JavaScript of selected portions of the best introduction to monads I’ve ever read</a></li>
<li><a href="http://hongjiang.info/understand-monad-5-what-is-endofunctor/" target="_blank" rel="external">我所理解的monad</a></li>
<li><a href="http://homepages.inf.ed.ac.uk/wadler/papers/marktoberdorf/baastad.pdf" target="_blank" rel="external">Monads for functional programming</a></li>
</ol>
]]></content>
    
    <summary type="html">
    
      &lt;blockquote&gt;
&lt;p&gt;Monad不就是个自函子范畴上的幺半群，这有什么难理解的(A monad is just a monoid in the category of endofunctors)&lt;br&gt;—— Phillip Wadler&lt;/p&gt;
&lt;/blockquote
    
    </summary>
    
      <category term="技术" scheme="https://lameta.com/categories/%E6%8A%80%E6%9C%AF/"/>
    
    
      <category term="language" scheme="https://lameta.com/tags/language/"/>
    
      <category term="FP" scheme="https://lameta.com/tags/FP/"/>
    
  </entry>
  
  <entry>
    <title>聚合，组合与IoC</title>
    <link href="https://lameta.com/2015/07/26/aggregation-composition-IoC/"/>
    <id>https://lameta.com/2015/07/26/aggregation-composition-IoC/</id>
    <published>2015-07-26T14:12:44.000Z</published>
    <updated>2016-09-06T14:58:50.000Z</updated>
    
    <content type="html"><![CDATA[<h4 id="为什么我们说多用组合，少用继承？"><a href="#为什么我们说多用组合，少用继承？" class="headerlink" title="为什么我们说多用组合，少用继承？"></a>为什么我们说多用组合，少用继承？</h4><hr>
<p>继承具有强耦合(strog coupling)特点。根据里氏替换原则(Liskov Substitution Principle LSP)，基类出现的地方，子类一定能透明地替代。本质上来讲，LSP的这种特性致使基类具有很强的病毒蔓延性(invasive)，因为如何基类设计的不好，会侵蚀所有的子类。举个例子，基类是鸟，同时具有<code>fly</code>这样的方法，子类中如果有鸵鸟或企鹅，就必须继承<code>fly</code>方法，然而这显然是没有意义的。</p>
<p>继承因为强耦合，所以不好测试。举个例子，有一个<code>RemoteXMLFile&lt;Res&gt;</code>基类，它内部有个<code>Res to()</code>方法，旨在把远程的XML文件转换成具体的Response。在我们准备测试<code>to</code>方法的转换逻辑的时候，每个对应子类的测试类里都不得不准备丑陋冗余的<code>XML</code>字面值字符串，尤其是在Java里。之所以这样，是因为无法隔离出一个专门读取<code>XML</code>的抽象屏障，然后使用Mock隔离这层抽象。</p>
<p>组合的好处自然是松耦合(loose coupling)。如果严格遵循依赖倒置原则(Dependency Inversion Principle),那么类本身一定是高能聚，类之间是松耦合的。除此之外，测试起来，各层之间的逻辑清晰明了，易于隔离开来做单元测试(Unit test)。</p>
<h4 id="谈谈聚合和组合的区别"><a href="#谈谈聚合和组合的区别" class="headerlink" title="谈谈聚合和组合的区别"></a>谈谈聚合和组合的区别</h4><hr>
<p>UML里有很多表示关联的线段，有两个关系十分相近，它们是聚合(aggregation)和组合(composition)</p>
<blockquote>
<p>聚合：表示两个对象之间是整体和部分的弱（弱拥有）关系，部分的生命周期可以超越整体。如电脑和鼠标。</p>
<p>组合：表示两个对象之间是整体和部分的强（强拥有）关系，部分的生命周期不能超越整体，或者说不能脱离整体而存在。组合关系的“部分”，是不能在整体之间进行共享的。</p>
</blockquote>
<p>以上是网络上的标准解释，应该是没错。但是实践中依然让人困惑不已。汽车和引擎，这个还比较好理解，汽车报废了，引擎还是可以换给别的汽车用的嘛。这就实现了整体之间的共享——聚合无疑。但是值得注意的一点是：从未说过部分的生命周期小于整体，也就是一定是大于或者等于整体的生命周期的。所以千万别和别人争论，引擎报废了，汽车就没用了，因为在程序中一定是某个第三方供应商维护引擎的生命周期，所以引擎和汽车没有相互维持对方生命周期的联系。</p>
<p>但是比较困惑就是这个生命周期理论！IoC容器提供给我们使用的注入方法无非3种，接口注入、构造函数注入、setter方法注入。每一种无不是聚合，因为产生和管理对象的控制权移交到IoC容器了。那我们口口声声的说组合优于继承其实一直都是聚合优于继承。</p>
<p>那是不是说，有了IoC之后，组合就应该消失呢，当然不是的。例如，某个场景里，一个类确实需要分离出一部分职责交给另外一个类，比如Builder，这部分构建实例的职责就可以通过<code>new Builder()</code>的方式组合应用起来。</p>
<figure class="highlight java"><figcaption><span>Builder_Pattern</span></figcaption><table><tr><td class="gutter"><pre><div class="line">1</div><div class="line">2</div><div class="line">3</div><div class="line">4</div><div class="line">5</div><div class="line">6</div><div class="line">7</div><div class="line">8</div><div class="line">9</div><div class="line">10</div><div class="line">11</div><div class="line">12</div><div class="line">13</div><div class="line">14</div><div class="line">15</div><div class="line">16</div><div class="line">17</div><div class="line">18</div><div class="line">19</div><div class="line">20</div><div class="line">21</div><div class="line">22</div><div class="line">23</div><div class="line">24</div><div class="line">25</div><div class="line">26</div><div class="line">27</div><div class="line">28</div><div class="line">29</div><div class="line">30</div></pre></td><td class="code"><pre><div class="line"><span class="keyword">public</span> <span class="class"><span class="keyword">class</span> <span class="title">Person</span> </span>&#123;</div><div class="line">	<span class="keyword">private</span> String name;</div><div class="line">	<span class="keyword">private</span> <span class="keyword">int</span> age;</div><div class="line"></div><div class="line">	<span class="function"><span class="keyword">public</span> <span class="keyword">static</span> <span class="title">builder</span><span class="params">()</span> </span>&#123;</div><div class="line">		<span class="keyword">return</span> <span class="keyword">new</span> Builder();</div><div class="line">	&#125;</div><div class="line"></div><div class="line">	<span class="keyword">public</span> <span class="keyword">static</span> Builder &#123;</div><div class="line">		<span class="keyword">private</span> String name;</div><div class="line">		<span class="keyword">private</span> <span class="keyword">int</span> age;</div><div class="line"></div><div class="line">		<span class="function"><span class="keyword">public</span> Builder <span class="title">withName</span><span class="params">(String name)</span> </span>&#123;</div><div class="line">			<span class="keyword">this</span>.name = name;</div><div class="line">		&#125;</div><div class="line"></div><div class="line">		<span class="function"><span class="keyword">public</span> Builder <span class="title">withAge</span><span class="params">(<span class="keyword">int</span> age)</span> </span>&#123;</div><div class="line">			<span class="keyword">this</span>.age = age;</div><div class="line">		&#125;</div><div class="line"></div><div class="line">		<span class="function"><span class="keyword">public</span> Person <span class="title">build</span><span class="params">()</span></span>&#123;</div><div class="line">			Person person = <span class="keyword">new</span> Person();</div><div class="line">			person.name = name;</div><div class="line">			person.age = age;</div><div class="line">			<span class="keyword">return</span> person;</div><div class="line">		&#125;</div><div class="line">	&#125;</div><div class="line">&#125;</div><div class="line">&gt;&gt;&gt;</div><div class="line">Person person = Person.builder().withName(<span class="string">'Ryan'</span>).withAge(<span class="number">18</span>).build();</div></pre></td></tr></table></figure>
<h4 id="结论"><a href="#结论" class="headerlink" title="结论"></a>结论</h4><hr>
<ul>
<li>组合优于继承</li>
<li>因为IoC的存在，上句话得改成聚合优于继承</li>
</ul>
]]></content>
    
    <summary type="html">
    
      &lt;h4 id=&quot;为什么我们说多用组合，少用继承？&quot;&gt;&lt;a href=&quot;#为什么我们说多用组合，少用继承？&quot; class=&quot;headerlink&quot; title=&quot;为什么我们说多用组合，少用继承？&quot;&gt;&lt;/a&gt;为什么我们说多用组合，少用继承？&lt;/h4&gt;&lt;hr&gt;
&lt;p&gt;继承具有强耦合(s
    
    </summary>
    
      <category term="技术" scheme="https://lameta.com/categories/%E6%8A%80%E6%9C%AF/"/>
    
    
      <category term="thinking" scheme="https://lameta.com/tags/thinking/"/>
    
  </entry>
  
  <entry>
    <title>ThoughtWorks training</title>
    <link href="https://lameta.com/2015/07/18/ThoughtWorks-training/"/>
    <id>https://lameta.com/2015/07/18/ThoughtWorks-training/</id>
    <published>2015-07-18T14:58:17.000Z</published>
    <updated>2016-09-06T22:42:46.000Z</updated>
    
    <content type="html"><![CDATA[<h2 id="ThoughtWorks培训"><a href="#ThoughtWorks培训" class="headerlink" title="ThoughtWorks培训"></a>ThoughtWorks培训</h2><p>在过去的几年里，为了进一步提升ThoughtWorks在业界的影响力，扩张无疑是最为有效的策略。但是，以提供知识流程外包（KPO）作为主要业务的ThoughtWorks，人员的成长变成了业务增长的主要瓶颈。构建“学习型组织”，减少从0(新人)到1(能够提供专业服务)经历的时间变得迫在眉睫。</p>
<h3 id="现状和思考"><a href="#现状和思考" class="headerlink" title="现状和思考"></a>现状和思考</h3><p>对于培训，公司投入了大量的人力物力，我们仍然在思考和摸索什么是最有效的培训方式。如何快速培养一名合格的TWer？在当前招聘策略下，这是一个严峻的问题。由什么来指导我们做“正确“的培训，培训什么、怎么培训以及能不能达到我们的预期这些都尚未可知。</p>
<p>培训是需要反馈周期的，假设首次教学目的(收集受众能力metrics，得出初步结论)，导入教学形式(是讲理论，做实践还是改变习惯)并对当前的产出做出量化评估(学员能力分级)。形成教学目的&gt;教学形式&gt;量化评估&gt;修改当前教学目的/下次教学目的的闭环，逐步提高培训的质量和效率。</p>
<p>以学前班的郑大夜校为例，我们已经实践了很多期。不否认地，很多毕业生从中获益颇丰，入了ThoughtWorks的门坎。但是，我们也不得不正视每期结束后褒贬不一的反馈。我们不妨试着剖析反馈背后的问题，从一个个点切入，顺藤摸瓜，反观教学的形式、目的和整个郑大夜校编排的合理性。</p>
<p>首先我们回顾一下本期郑大夜校的阶段安排</p>
<blockquote>
<p>阶段一（毕业到年前）：<br>静态语言阶段</p>
<p>阶段二（年后到入职前）：<br>动态语言阶段</p>
<p>阶段三（入职后一个月内）：<br>工程实践阶段</p>
</blockquote>
<p>第一阶段主要囊括了Java语言，面向对象编程，设计原则和模式等内容，采取了讲师讲解，学生提问交流和练习的方式；</p>
<p>第二阶段预计采用Javascript语言，应用动态语言的特性重新实现第一阶段的练习。但是事实上，由于培训人员变动等原因，该阶段没有遵循预期计划，而是直接进入了阶段三。</p>
<p>第三阶段，毕业生应用我们平时工作的流程和使用的工具，参与实现一个公司内部的固定资产管理系统，用XR的话来说就是将思想上的东西应用落地。</p>
<p>经过两期的培训，通过毕业生自己的在retro上的反馈以及各位mentor的观察，大体上可以看出毕业生对于第一阶段的培训内容和形式还是很认可的。比较有趣的是培训中就已经有人明确告诉我他自己越来越不会写代码了，这并不是说他能力不行，反而恰恰说明他正在提升，也证实我们培训的目的达到了——对于整个软件行业以及自身能力有清晰的认识。但是必须承认这个例子不是普遍现象，思想上的东西不是一蹴而就的，而且也不能轻松量化以待检验，至少基于当前的教学形式还做不到。</p>
<p>对于第二阶段的培训，学生普通反映引入的工具和框架过多，再加上课程安排松散，每个迭代的目的不甚明确，严重拖延了项目的进度，结果距离完成的目标尚远。而从mentor的角度，理论到实践的落地过程预估得还是过于乐观，不过这个罅隙在和senior dev的结对过程中得到了有效的弥合。</p>
<p>那么问题是我们现在的问题是什么？</p>
<p>在回答这个问题之前，我们先问自己一个问题：</p>
<h4 id="公司想要把毕业生培养成什么人？"><a href="#公司想要把毕业生培养成什么人？" class="headerlink" title="公司想要把毕业生培养成什么人？"></a>公司想要把毕业生培养成什么人？</h4><p>如果答案是一群高级技工。那么我觉得郑大夜校可以不存在，直接找一群senior dev和这些新人pair，手把手教授过程和工具，模仿着码代码。因为基于我们的培训结果，这样的效果是最快最直接的。</p>
<p>若答案是对技术有兴趣、有想法和自主学习的geek。我们就要真的从毕业生角度出发，明确他们缺少什么能力以及如何弥补这些短板。</p>
<p>那么我就以答案二为假设，结合本期培训，展开问题的论述：</p>
<h3 id="第一阶段"><a href="#第一阶段" class="headerlink" title="第一阶段"></a>第一阶段</h3><hr>
<p>验证理解是否到位最好的方式是做练习。问题是一些学生没能完成课上的配套练习，课后也未必补上。这点与课程设置缺少检验环节必然相关，但归根结底是学生缺乏主动性。那什么叫做有主动性？同样是布置POS机的作业，有学生在每一次培训后都会持续改进自己的程序，将新的知识点实现一遍。没人强制这么做，但他做了就是主动。</p>
<p>问题一来了，学生缺乏主动性。该如何解决这一问题？</p>
<blockquote>
<p>第一，我不认为学生缺少学习的动力，那么可能的原因是没有明确的期望和目标。期望和目标应当来自于未来的雇主。<a href="http://mindhacks.cn/2011/11/04/how-to-interview-a-person-for-two-years/" target="_blank" rel="external">这里</a>有详细的论述，我就不再赘述。</p>
<p>第二，组内职责轮换。基于本期分组教学的模式，我们已经尝试分派一些职责，效果良好。职责轮换可以让学生因感受到压力而成长。</p>
<p>第三，建立惩罚制度。对于mentor而言，我们并没有有力的手段给予学生压力，我们至多会告诫说若表现不好，会影响其过试用期，但是，这样的压力距离太远。所以建议提前建立因技能等评估维度（后面会详细讨论）严重不足而拒绝offer的制度。</p>
</blockquote>
<p>以上解决问题的建议是基于现有的教学形式提出的。经过和DW的讨论，这一问题或许反映了原有的教学形式本身就存在漏洞。</p>
<blockquote>
<p>我们依旧遵循大学的填鸭式教学模式。要知道没有哪个行业像IT行业这样特殊：没有什么东西不能够（应该）在互联网上学到的。我们何妨换个思路，将以前的课上讲授，课后练习，下次课上检验的方式改变成提前提供课题和所用教学资源，课前学生自己学习，课上解惑的模式。这样可以最大化调动学生自主学习能力，而且学生带着问题上课的效率也远高于直接教学。</p>
</blockquote>
<h3 id="第二阶段"><a href="#第二阶段" class="headerlink" title="第二阶段"></a>第二阶段</h3><hr>
<blockquote>
<p>问题二：我们第二阶段的初衷其实是将第一阶段的思想方面的东西落地。但是大量的工具和框架，这些干扰因素严重分散了学生的注意力，原本检验学生设计和创造能力的时间被极大地缩短了。</p>
<p>问题三：课程安排松散，迭代的目的不清则更为严重。这反映了郑大夜校本身就是不规范的，集中体现在讲师变动频繁、能力良莠不齐，授课内容临时起意，内部的目标不清不楚等。</p>
</blockquote>
<p>针对问题二，我们首先再次明确公司需要的是对技术有兴趣、有想法和自主学习的geek。所以建议不强制学生学习使用工具和框架，尽量选用简单的技术栈，重点检验设计能力。</p>
<p>问题三，这样的现象一直存在，而且涉及的问题有点多，我们慢慢剖析。</p>
<blockquote>
<p>第一，讲师变动频繁、能力良莠本身不应该是个问题，这点可以参考TWU的办学模式。关键还是如何标准化。</p>
<p>第二，授课内容不固定，典型反映在临时找的session上，涉及Agile演化、scrum workshop、业务、QA和Dev技能等各个方面。考虑时间限制和学生的接受能力，对于学前班的郑大夜校而言，这样的scope未免过于庞大。或许需要重新定义郑大夜校的scope，例如：将培养卓越的软件能力提到首位，而将P1和P3的侧重弱化，延迟到TWU和On Board Training上。</p>
<p>第三，相较于TWU，郑大夜校缺少了一个P2P feedback的环节，我们也不知道经过培训什么样的人就满足公司的期望。这就引出了量化指标，可想而知，量化指标不是绝对量，它应该是相对于以前能力模型的增长量。举个例子：从70分上升到80分和从10分上升到60分体现得是后者成长空间更大。量化指标需要根据夜校的scope，划分出能力象限，因为我们能考察的不应该越界。</p>
</blockquote>
<p>孔子曰：温故而知新，可以为师矣。我们做个recap。</p>
<h3 id="教学目的"><a href="#教学目的" class="headerlink" title="教学目的"></a>教学目的</h3><hr>
<ul>
<li>拓宽毕业生的软件开发的视野，明确自身能力</li>
<li>帮助公司尽早筛选可用之才</li>
</ul>
<h3 id="形式"><a href="#形式" class="headerlink" title="形式"></a>形式</h3><hr>
<ul>
<li>颠覆填鸭式教学，自学为主，解惑为辅</li>
<li>分组，轮换职责</li>
<li>入职前惩罚制度</li>
<li>制定标准的教学流程</li>
<li>限定夜校教学范围</li>
<li>制定量化指标</li>
</ul>
<h3 id="量化评估"><a href="#量化评估" class="headerlink" title="量化评估"></a>量化评估</h3><hr>
<ul>
<li>有待确定能力象限。</li>
</ul>
]]></content>
    
    <summary type="html">
    
      &lt;h2 id=&quot;ThoughtWorks培训&quot;&gt;&lt;a href=&quot;#ThoughtWorks培训&quot; class=&quot;headerlink&quot; title=&quot;ThoughtWorks培训&quot;&gt;&lt;/a&gt;ThoughtWorks培训&lt;/h2&gt;&lt;p&gt;在过去的几年里，为了进一步提升Thought
    
    </summary>
    
      <category term="生活" scheme="https://lameta.com/categories/%E7%94%9F%E6%B4%BB/"/>
    
    
      <category term="thinking" scheme="https://lameta.com/tags/thinking/"/>
    
  </entry>
  
  <entry>
    <title>Transaction</title>
    <link href="https://lameta.com/2015/07/18/transaction/"/>
    <id>https://lameta.com/2015/07/18/transaction/</id>
    <published>2015-07-18T11:33:38.000Z</published>
    <updated>2016-09-06T14:40:57.000Z</updated>
    
    <content type="html"><![CDATA[<h3 id="什么是事务"><a href="#什么是事务" class="headerlink" title="什么是事务"></a>什么是事务</h3><hr>
<p>事务是一组不可分割的SQL query语句，或者说是<strong>一个最小的工作单元</strong>。</p>
<h3 id="事务与锁的关系"><a href="#事务与锁的关系" class="headerlink" title="事务与锁的关系"></a>事务与锁的关系</h3><hr>
<h4 id="为什么提出这个问题"><a href="#为什么提出这个问题" class="headerlink" title="为什么提出这个问题"></a>为什么提出这个问题</h4><p>在阅读《Java虚拟机并发编程》(Programming Concurrency on the JVM - Materning Synchronization, STM and Actors)中STM(Software Transaction Memory)时，我看到transaction特征在concurrency中的神奇应用场景：</p>
<ul>
<li>原子性：涉及一组操作，这组操作具有原子特征，比如存款和取款的组合操作。这组操作内部的所有更改要么全部成功，要么全部失败。</li>
<li>一致性：所有并行的事务所造成的变更，从外部来看，都是一个接一个发生的。比如：存款，取款这两个独立事务。如果存款的过程中间，取款操作接入，那么取款读取的数据是旧的。待存款恢复并执行完毕，取款想要写回的数据必然无效。取款事务需要重做！所以外观来看这是存款到取款的序列，反之亦然。其实本质上，就是可见性的问题。</li>
<li>不需要显式地运用锁，不论是读锁还是写锁。这样就为程序员提供了比较好的抽象屏障(abstract barrier)。</li>
<li>隔离性：事务在未提交之前，所做的任何更改都不能被其他事务看到。</li>
</ul>
<p>我看到了很多STM的好处，但是看到处理写偏斜异常(Handling Write Skew Anomaly)（可以简单理解为两个事务修改的变量不是同一个，但是两个变量之间又有约束关系）一章时，作者使用<code>ensure</code>函数给约束变量加了读锁。加读锁的意义在于本事务之外，其他事务无法获得该变量的写锁，自然无法修改它的值。但是这里显式地使用了锁，所以可以明确事务不是锁无关的，而且这让我联想到了数据库事务隔离级别中的可重复读(REPEATABLE_READ)。可重复读也是使用在特定记录行上使用读锁，来防止外部事务修改了该条记录行。</p>
<h4 id="微妙的关系"><a href="#微妙的关系" class="headerlink" title="微妙的关系"></a>微妙的关系</h4><p>有趣的事情来了，事务原子性能确保数据的完整性，而事务的一致性和隔离性则侧重于数据的可见性。可见性的保证在并发当中绝对和锁相关。我刚说了，事务给锁提供了抽象屏障，而且事务的隔离级别依旧仰仗锁的粒度，所以不要将事务看做银弹，以为有了事务，锁就不值一提。</p>
<h3 id="数据库事务的隔离级别"><a href="#数据库事务的隔离级别" class="headerlink" title="数据库事务的隔离级别"></a>数据库事务的隔离级别</h3><hr>
<h4 id="为什么提出这个问题-1"><a href="#为什么提出这个问题-1" class="headerlink" title="为什么提出这个问题"></a>为什么提出这个问题</h4><p>一直被《高性能MySQL》里的解释弄得稀里糊涂，纠结于脏读、不可重复读和幻读之间的关系。而且某些解释看似合理，但完全没有指导价值。比如：阐述隔离级别，却没法从中得出我们如何结合应用场景选择合适的隔离级别。</p>
<h4 id="隔离级别"><a href="#隔离级别" class="headerlink" title="隔离级别"></a>隔离级别</h4><ul>
<li>Uncommited Read<br>一个事务未提交，另一个事务却能读到该事务所做的更改。因为有可能读到未提交到数据库里的脏数据，这一级别会导致脏读。适用于只读场景下。</li>
<li>Commited Read<br>未提交之前，事务之间是不可见的，所以可以阻止脏读。<br>但是会导致不可重复读问题，也就是在本事务内，读取一条记录，另一个事务修改了此条记录并提交，本事务再读取同一条记录时，发现得到记录和前一条不一致的场景。这一级别也被称为不可重复读级别。<br><strong>但上述场景没有半点指导意义！</strong><br>如果你的应用场景是这样的——你想查询的变量是通过本事务里只读但是对于外部事务可写的变量作为条件查询出来的，那么这个只读变量很可能不可重复读，导致这个事务会失效。举个例子：如果你的查询语句是这样的<code>SELECT USER.age into age FROM USERS name=&#39;YOU&#39;;</code><br>这时候外部事务修改了名字<code>UPDATE SET name=&#39;ME&#39; WHERE name=&#39;YOU&#39;;</code>并提交。那么这时候，<code>age</code>是无效的状态，你再拿来用就有问题了。这一级别适合于读多写少且写偏斜不存在的场景。</li>
<li>Repeatable Read （MySQL的默认隔离级别）<br>可重复读，可以理解给只读的记录行加了读锁。这样，外部事务无法获得写锁，本事务内部这条记录始终有效，待事务结束即可解锁。反之，外部事务先得写锁，那么本事务无法获取记录行的读锁，导致重试发生。显然，如果你的应用场景里，读多写少且读写操作同一条记录的可能性很大的时候适合。</li>
<li><p>幻读<br>可重复读级别无法防止幻读。幻读是这样一种场景，本事务读取一个<strong>范围内，范围内，范围内（重要的事情写三遍）</strong>的数据集，但是另一事务又向这个范围内插入一条记录，导致数据集发生变化了，像是出现了幻觉，所以称为幻读（我很痛恨一些奇葩的科学家起的不合理的名字，这就是其一。按着这种逻辑，不可重复读不也可以说是出现幻觉吗？）。那么为什么会出现这种情况，原因是新插入的记录以前不存在于数据库中，所以你没法为它加锁。而且可重复读只是为每行记录加锁，没有用到Range Lock，这一幻影插入操作总能成功。<br>不过MySQL中InnoDB存储引擎提供了MVCC（多版本并发控制）技术，为每条记录设置一个递增的事务编号，大于本次事务编号的记录，不准插入记录。<br>可重复读+MVCC即可解决并发中的大部分问题。</p>
</li>
<li><p>Serializable Read<br>顾名思义，串行读，事务之间是串行的，同步的。换言之，并发性剧减。</p>
</li>
</ul>
<h3 id="STM的隔离级别"><a href="#STM的隔离级别" class="headerlink" title="STM的隔离级别"></a>STM的隔离级别</h3><hr>
<ul>
<li>隔离级别处于提交读。</li>
</ul>
<h3 id="事务的级别"><a href="#事务的级别" class="headerlink" title="事务的级别"></a>事务的级别</h3><hr>
<p>描述的是事务本身的属性</p>
<ul>
<li>ReadOnly：所有的操作都是读取操作，不涉及任何产生副作用的操作。</li>
</ul>
]]></content>
    
    <summary type="html">
    
      &lt;h3 id=&quot;什么是事务&quot;&gt;&lt;a href=&quot;#什么是事务&quot; class=&quot;headerlink&quot; title=&quot;什么是事务&quot;&gt;&lt;/a&gt;什么是事务&lt;/h3&gt;&lt;hr&gt;
&lt;p&gt;事务是一组不可分割的SQL query语句，或者说是&lt;strong&gt;一个最小的工作单元&lt;/strong&gt;。
    
    </summary>
    
      <category term="技术" scheme="https://lameta.com/categories/%E6%8A%80%E6%9C%AF/"/>
    
    
      <category term="transaction" scheme="https://lameta.com/tags/transaction/"/>
    
  </entry>
  
  <entry>
    <title>Concurrency in action</title>
    <link href="https://lameta.com/2015/07/16/concurrency-in-action/"/>
    <id>https://lameta.com/2015/07/16/concurrency-in-action/</id>
    <published>2015-07-16T00:25:58.000Z</published>
    <updated>2016-09-06T14:58:32.000Z</updated>
    
    <content type="html"><![CDATA[<h3 id="什么是竞态条件？"><a href="#什么是竞态条件？" class="headerlink" title="什么是竞态条件？"></a>什么是竞态条件？</h3><hr>
<blockquote>
<p><strong>tips</strong>: The situation where two threads compete for the same resource, where the sequence in which the resource is accessed is significant, is called race conditions. A code section that leads to race conditions is called a critical section. In the below example the method add() is a critical section, leading to race conditions. Race conditions can be avoided by proper thread synchronization in critical sections.<br>两个线程竞争同一个资源，而该资源的访问顺序十分重要的情况就被称为竞态条件。导致竞态条件的代码区就被称为临界区。下面的例子中的add()方法就是一个导致竞态条件的临界区。临界区上合适的线程同步能避免竞态条件。<br>Race conditions arise in software when an application depends on the sequence or timing of processes or threads for it to operate properly.<br>race conditions often happen when the processes or threads depend on some shared state.<br>软件里的竞态条件发生在一个应用程序依赖进程或者线程的执行顺序和时间以确保该程序执行正确的时候。竞态条件总是发生在进程或线程依赖某些共享资源的时候。</p>
</blockquote>
<h4 id="一个临界区的例子："><a href="#一个临界区的例子：" class="headerlink" title="一个临界区的例子："></a>一个临界区的例子：</h4><figure class="highlight axapta"><table><tr><td class="gutter"><pre><div class="line">1</div><div class="line">2</div><div class="line">3</div><div class="line">4</div><div class="line">5</div><div class="line">6</div><div class="line">7</div></pre></td><td class="code"><pre><div class="line"><span class="keyword">public</span> <span class="class"><span class="keyword">class</span> <span class="title">Counter</span> </span>&#123;</div><div class="line">    <span class="keyword">protected</span> <span class="keyword">long</span> <span class="keyword">count</span> = <span class="number">0</span>;</div><div class="line"></div><div class="line">    <span class="keyword">public</span> <span class="keyword">void</span> add(<span class="keyword">long</span> value)&#123;</div><div class="line">        <span class="keyword">this</span>.<span class="keyword">count</span> = <span class="keyword">this</span>.<span class="keyword">count</span> + value;</div><div class="line">    &#125;</div><div class="line"> &#125;</div></pre></td></tr></table></figure>
<h3 id="什么是线程安全？"><a href="#什么是线程安全？" class="headerlink" title="什么是线程安全？"></a>什么是线程安全？</h3><hr>
<blockquote>
<p><strong>tips</strong>: Code that is safe to call by multiple threads simultanously is called thread safe. If a piece of code is thread safe, then it contains no race conditions.<br>能被多个线程同时安全地调用的代码就是线程安全。如果一段代码是安全的，那么它就不存在竞态条件。</p>
<ul>
<li>Thread safe: Implementation is guaranteed to be free of race conditions when accessed by multiple threads simultaneously.</li>
<li>Conditionally safe: Different threads can access different objects simultaneously, and access to shared data is protected from race conditions.</li>
<li><p>Not thread safe: Code should not be accessed simultaneously by different threads.</p>
</li>
<li><p>线程安全：实现以保证多个线程同时访问不存在竞态条件</p>
</li>
<li>条件安全：不同的线程可以同时访问不同的对象，且在访问共享资源时保护以免于竞态条件</li>
<li>非线程安全：代码不能被多个线程同时访问</li>
</ul>
</blockquote>
<h3 id="可见性和竞态条件的关系？"><a href="#可见性和竞态条件的关系？" class="headerlink" title="可见性和竞态条件的关系？"></a>可见性和竞态条件的关系？</h3><hr>
<blockquote>
<p>如果存在竞态条件，那么就必须保证变量的可见性</p>
</blockquote>
<h3 id="可见性和线程安全的关系？"><a href="#可见性和线程安全的关系？" class="headerlink" title="可见性和线程安全的关系？"></a>可见性和线程安全的关系？</h3><hr>
<blockquote>
<p>严格意义上，线程安全其实是相对于非线程安全的，即包含了线程安全和条件安全两部分。</p>
<p>Below we discuss two approaches for avoiding race conditions to achieve thread safety.</p>
<p>The first class of approaches focuses on avoiding shared state, and includes:<br>也就是纯粹的线程安全。</p>
<ul>
<li>Re-entrancy<br>  Writing code in such a way that it can be partially executed by a thread, reexecuted by the same thread or simultaneously executed by another thread and still correctly complete the original execution. This requires the saving of state information in variables local to each execution, usually on a stack, instead of in static or global variables or other non-local state. All non-local state must be accessed through atomic operations and the data-structures must also be reentrant.</li>
<li>Thread-local storage<br>  Variables are localized so that each thread has its own private copy. These variables retain their values across subroutine and other code boundaries, and are thread-safe since they are local to each thread, even though the code which accesses them might be executed simultaneously by another thread.</li>
</ul>
<p>The second class of approaches are synchronization-related, and are used in situations where shared state cannot be avoided:<br>也就是条件安全部分。</p>
<ul>
<li>Mutual exclusion<br>  Access to shared data is serialized using mechanisms that ensure only one thread reads or writes to the shared data at any time. Incorporation of mutual exclusion needs to be well thought out, since improper usage can lead to side-effects like deadlocks, livelocks and resource starvation.</li>
<li>Atomic operations<br>  Shared data are accessed by using atomic operations which cannot be interrupted by other threads. This usually requires using special machine language instructions, which might be available in a runtime library. Since the operations are atomic, the shared data are always kept in a valid state, no matter how other threads access it. Atomic operations form the basis of many thread locking mechanisms, and are used to implement mutual exclusion primitives.</li>
<li>Immutable objects<br>  The state of an object cannot be changed after construction. This implies both that only read-only data is shared and that inherent thread safety is attained. Mutable (non-const) operations can then be implemented in such a way that they create new objects instead of modifying existing ones. This approach is used by the string implementations in Java, C# and Python.<br>所以可见性只是针对于条件安全部分而言的。</li>
</ul>
</blockquote>
<h3 id="竞态条件和线程安全的关系？"><a href="#竞态条件和线程安全的关系？" class="headerlink" title="竞态条件和线程安全的关系？"></a>竞态条件和线程安全的关系？</h3><hr>
<blockquote>
<p>避免竞态条件以获得线程安全。换句话说，避免竞态条件是线程安全的充要条件。</p>
</blockquote>
<h3 id="为什么说跨越内存栅栏（可见性）和避免竞态条件是同步相关的两大主要问题？"><a href="#为什么说跨越内存栅栏（可见性）和避免竞态条件是同步相关的两大主要问题？" class="headerlink" title="为什么说跨越内存栅栏（可见性）和避免竞态条件是同步相关的两大主要问题？"></a>为什么说跨越内存栅栏（可见性）和避免竞态条件是同步相关的两大主要问题？</h3><hr>
<blockquote>
<p>跨越内存栅栏（可见性）是应对竞态条件的一种方式，出现竞态条件必须确保可见性。举个例子：一个写线程写完之后，要保证所做更改对其它线程可见，否则会让其它线程读到脏数据。<br>避免竞态条件的方式有很多种，包括上述的线程安全的各种手段。</p>
</blockquote>
<h3 id="相关术语"><a href="#相关术语" class="headerlink" title="相关术语"></a>相关术语</h3><hr>
<blockquote>
<ol>
<li>livelock（活锁）：请求一个锁的时候不断失败。</li>
<li>starvation（饿死）：无法定期访问共享资源来执行，发生在某个线程长期霸占共享资源的时候。</li>
</ol>
<ul>
<li>Starvation<br>Starvation describes a situation where a thread is unable to gain regular access to shared resources and is unable to make progress. This happens when shared resources are made unavailable for long periods by “greedy” threads. For example, suppose an object provides a synchronized method that often takes a long time to return. If one thread invokes this method frequently, other threads that also need frequent synchronized access to the same object will often be blocked.</li>
<li>Livelock<br>A thread often acts in response to the action of another thread. If the other thread’s action is also a response to the action of another thread, then livelock may result. As with deadlock, livelocked threads are unable to make further progress. However, the threads are not blocked — they are simply too busy responding to each other to resume work. This is comparable to two people attempting to pass each other in a corridor: Alphonse moves to his left to let Gaston pass, while Gaston moves to his right to let Alphonse pass. Seeing that they are still blocking each other, Alphone moves to his right, while Gaston moves to his left. They’re still blocking each other, so…</li>
</ul>
</blockquote>
]]></content>
    
    <summary type="html">
    
      &lt;h3 id=&quot;什么是竞态条件？&quot;&gt;&lt;a href=&quot;#什么是竞态条件？&quot; class=&quot;headerlink&quot; title=&quot;什么是竞态条件？&quot;&gt;&lt;/a&gt;什么是竞态条件？&lt;/h3&gt;&lt;hr&gt;
&lt;blockquote&gt;
&lt;p&gt;&lt;strong&gt;tips&lt;/strong&gt;: The s
    
    </summary>
    
      <category term="技术" scheme="https://lameta.com/categories/%E6%8A%80%E6%9C%AF/"/>
    
    
      <category term="concurrency" scheme="https://lameta.com/tags/concurrency/"/>
    
  </entry>
  
  <entry>
    <title>Java陷阱和缺陷</title>
    <link href="https://lameta.com/2015/03/11/pit-and-trap-in-Java/"/>
    <id>https://lameta.com/2015/03/11/pit-and-trap-in-Java/</id>
    <published>2015-03-11T15:16:08.000Z</published>
    <updated>2016-09-06T14:58:29.000Z</updated>
    
    <content type="html"><![CDATA[<h4 id="String-valueOf"><a href="#String-valueOf" class="headerlink" title="String.valueOf"></a>String.valueOf</h4><hr>
<figure class="highlight java"><figcaption><span>example.java</span></figcaption><table><tr><td class="gutter"><pre><div class="line">1</div><div class="line">2</div><div class="line">3</div><div class="line">4</div></pre></td><td class="code"><pre><div class="line">String.valueOf(<span class="keyword">null</span>);</div><div class="line"></div><div class="line">-&gt;</div><div class="line">java.lang.NullPointerException</div></pre></td></tr></table></figure>
<p>why does it cause excepation? Java call the wrong method because type match!</p>
<figure class="highlight java"><figcaption><span>String.java</span></figcaption><table><tr><td class="gutter"><pre><div class="line">1</div><div class="line">2</div><div class="line">3</div><div class="line">4</div><div class="line">5</div><div class="line">6</div><div class="line">7</div></pre></td><td class="code"><pre><div class="line"><span class="function"><span class="keyword">public</span> <span class="keyword">static</span> String <span class="title">valueOf</span><span class="params">(<span class="keyword">char</span> data[])</span> </span>&#123;</div><div class="line">    <span class="keyword">return</span> <span class="keyword">new</span> String(data);</div><div class="line">&#125;</div><div class="line">...</div><div class="line"><span class="function"><span class="keyword">public</span> <span class="title">String</span><span class="params">(<span class="keyword">char</span> value[])</span> </span>&#123;</div><div class="line">    <span class="keyword">this</span>.value = Arrays.copyOf(value, value.length); <span class="comment">//=&gt; cause exception.</span></div><div class="line">&#125;</div></pre></td></tr></table></figure>
<p>how to correct?</p>
<figure class="highlight java"><table><tr><td class="gutter"><pre><div class="line">1</div><div class="line">2</div><div class="line">3</div></pre></td><td class="code"><pre><div class="line">String.valueOf((Object)<span class="keyword">null</span>)</div><div class="line">-&gt;</div><div class="line"><span class="keyword">null</span></div></pre></td></tr></table></figure>
<h4 id="Varargs-bug"><a href="#Varargs-bug" class="headerlink" title="Varargs bug"></a>Varargs bug</h4><hr>
<p>Changes in Most Specific Varargs Method Selection<br>Description: The overload resolution algorithm in the javac compiler has been fixed in how it selects the most specific varargs method when more than one method is applicable to a given call-site (see the JLS, Java SE 7 Edition, section 15.12.2.5). Because of a bug, both JDK 5.0 and JDK 6 compilers reject the following legal code:</p>
<figure class="highlight cs"><table><tr><td class="gutter"><pre><div class="line">1</div><div class="line">2</div><div class="line">3</div><div class="line">4</div><div class="line">5</div><div class="line">6</div><div class="line">7</div><div class="line">8</div></pre></td><td class="code"><pre><div class="line"><span class="keyword">class</span> <span class="title">Test</span> &#123;</div><div class="line">    <span class="function"><span class="keyword">void</span> <span class="title">foo</span>(<span class="params"><span class="keyword">int</span>... i</span>) </span>&#123;&#125;</div><div class="line">    <span class="function"><span class="keyword">void</span> <span class="title">foo</span>(<span class="params"><span class="keyword">double</span>... d</span>) </span>&#123;&#125;</div><div class="line"></div><div class="line">    <span class="function"><span class="keyword">void</span> <span class="title">test</span>(<span class="params"></span>) </span>&#123;</div><div class="line">       foo(<span class="number">1</span>,<span class="number">2</span>,<span class="number">3</span>);</div><div class="line">    &#125;</div><div class="line">&#125;</div></pre></td></tr></table></figure>
<p>In the above example, both methods are applicable (because you can pass an int where a double is expected). Since both methods are applicable, the compiler must select the so-called most-specific method, that is, the best candidate among the two. This is done by looking at the signatures of both methods; in this case, since one method (foo(double…)) is accepting an argument that is more general than the other (foo(int…)), the answer is straightforward: the most specific method is foo(int…).<br>While the javac compiler accepts more code than it did prior to JDK 7, this fix also results in a slight source incompatibility in the following case:</p>
<figure class="highlight cs"><table><tr><td class="gutter"><pre><div class="line">1</div><div class="line">2</div><div class="line">3</div><div class="line">4</div><div class="line">5</div><div class="line">6</div><div class="line">7</div><div class="line">8</div></pre></td><td class="code"><pre><div class="line"><span class="keyword">class</span> <span class="title">Test</span> &#123;</div><div class="line">    <span class="function"><span class="keyword">void</span> <span class="title">foo</span>(<span class="params"><span class="keyword">int</span>... i</span>) </span>&#123;&#125;</div><div class="line">    <span class="function"><span class="keyword">void</span> <span class="title">foo</span>(<span class="params">Object... o</span>) </span>&#123;&#125;</div><div class="line"></div><div class="line">    <span class="function"><span class="keyword">void</span> <span class="title">test</span>(<span class="params"></span>) </span>&#123;</div><div class="line">       foo(<span class="number">1</span>,<span class="number">2</span>,<span class="number">3</span>);</div><div class="line">    &#125;</div><div class="line">&#125;</div></pre></td></tr></table></figure>
<p>This code compiles in JDK 6 (the most specific method is foo(int…)). This code does not compile under JDK 7. As per 15.12.2.5, it is not possible to choose between foo(int…) and foo(Object…) as neither int is a subtype of Object, nor Object is a subtype of int. This program should be disallowed (in fact, it should never have been allowed in the first place).</p>
]]></content>
    
    <summary type="html">
    
      &lt;h4 id=&quot;String-valueOf&quot;&gt;&lt;a href=&quot;#String-valueOf&quot; class=&quot;headerlink&quot; title=&quot;String.valueOf&quot;&gt;&lt;/a&gt;String.valueOf&lt;/h4&gt;&lt;hr&gt;
&lt;figure class=&quot;highl
    
    </summary>
    
      <category term="技术" scheme="https://lameta.com/categories/%E6%8A%80%E6%9C%AF/"/>
    
    
      <category term="java" scheme="https://lameta.com/tags/java/"/>
    
      <category term="Mistakes I Hav Made" scheme="https://lameta.com/tags/Mistakes-I-Hav-Made/"/>
    
  </entry>
  
  <entry>
    <title>Javascript Promise</title>
    <link href="https://lameta.com/2015/02/03/Javascript-Promise/"/>
    <id>https://lameta.com/2015/02/03/Javascript-Promise/</id>
    <published>2015-02-03T15:33:40.000Z</published>
    <updated>2016-09-06T14:58:25.000Z</updated>
    
    <content type="html"><![CDATA[<p>Promise对象最早被C++工程师使用，接着以Deferred对象出现在Python中。随着NodeJS的兴起，Promise在javascript中的应用越来越广。</p>
<p>大凡技术成熟之后都会逐渐形成一个标准，Promise的标准定义在<a href="http://wiki.commonjs.org/wiki/CommonJS" target="_blank" rel="external">CommonJS</a>中，全称是<a href="http://wiki.commonjs.org/wiki/Promises/A" target="_blank" rel="external">Promise/A</a>，不过新近出现了改良版的<a href="https://promisesaplus.com/" target="_blank" rel="external">Promise/A+</a>，只不过它仅仅对原来的标准部分澄清，同时依据实践稍作扩展。</p>
<blockquote>
<p>The core Promises/A+ specification does not deal with how to create, fulfill, or reject promises, choosing instead to focus on providing an interoperable(协作的) <strong>then</strong> method. Future work in companion specifications may touch on these subjects.</p>
</blockquote>
<p>但是使用Promise还是有些地方值得留意的。</p>
<h4 id="1-Then的Resovler-Function不返回任何值"><a href="#1-Then的Resovler-Function不返回任何值" class="headerlink" title="1. Then的Resovler Function不返回任何值"></a>1. Then的Resovler Function不返回任何值</h4><hr>
<p>举个例子，打印deferred数组的首个元素</p>
<figure class="highlight javascript"><figcaption><span>promise_sample.js</span></figcaption><table><tr><td class="gutter"><pre><div class="line">1</div><div class="line">2</div><div class="line">3</div><div class="line">4</div><div class="line">5</div><div class="line">6</div><div class="line">7</div><div class="line">8</div><div class="line">9</div><div class="line">10</div><div class="line">11</div><div class="line">12</div><div class="line">13</div><div class="line">14</div><div class="line">15</div><div class="line">16</div><div class="line">17</div><div class="line">18</div><div class="line">19</div><div class="line">20</div><div class="line">21</div><div class="line">22</div><div class="line">23</div><div class="line">24</div><div class="line">25</div><div class="line">26</div><div class="line">27</div><div class="line">28</div><div class="line">29</div><div class="line">30</div><div class="line">31</div><div class="line">32</div><div class="line">33</div><div class="line">34</div></pre></td><td class="code"><pre><div class="line"><span class="function"><span class="keyword">function</span> <span class="title">printFirstAndLast</span>(<span class="params">itemsDeferred</span>)</span>&#123;</div><div class="line">    findFirst(itemsDeferred).then(util.puts); <span class="comment">//=&gt; 0</span></div><div class="line"></div><div class="line">    last(itemsDeferred).then(<span class="function"><span class="keyword">function</span>(<span class="params">last</span>)</span>&#123;</div><div class="line">        <span class="keyword">var</span> deferred = defer();</div><div class="line">        deferred.resolve(last);</div><div class="line">        findFirst(deferred).then(util.puts) <span class="comment">//=&gt; 1</span></div><div class="line">    &#125;).then(<span class="function"><span class="keyword">function</span>(<span class="params">result</span>)</span>&#123;</div><div class="line">        <span class="built_in">console</span>.log(<span class="string">'result:'</span>, result); <span class="comment">//=&gt; 2</span></div><div class="line">    &#125;, <span class="function"><span class="keyword">function</span>(<span class="params">reason</span>)</span>&#123;</div><div class="line">        <span class="built_in">console</span>.log(<span class="string">'reason:'</span>, reason);</div><div class="line">    &#125;);</div><div class="line">&#125;</div><div class="line"></div><div class="line"><span class="function"><span class="keyword">function</span> <span class="title">last</span>(<span class="params">itemsDeferred</span>) </span>&#123;</div><div class="line">    <span class="keyword">return</span> itemsDeferred.then(<span class="function"><span class="keyword">function</span>(<span class="params">items</span>)</span>&#123;</div><div class="line">        <span class="keyword">return</span> items.slice(<span class="number">1</span>, items.length);</div><div class="line">    &#125;);</div><div class="line">&#125;</div><div class="line"></div><div class="line"><span class="function"><span class="keyword">function</span> <span class="title">findFirst</span>(<span class="params">itemsDeferred</span>)</span>&#123;</div><div class="line">    <span class="keyword">return</span> itemsDeferred.then(<span class="function"><span class="keyword">function</span>(<span class="params">items</span>)</span>&#123;</div><div class="line">        <span class="keyword">return</span> items[<span class="number">0</span>];</div><div class="line">    &#125;);</div><div class="line">&#125;</div><div class="line">---</div><div class="line"><span class="keyword">var</span> deferred = defer();</div><div class="line">deferred.resolve([<span class="number">1</span>, <span class="number">2</span>, <span class="number">3</span>, <span class="number">4</span>, <span class="number">5</span>]);</div><div class="line">printFirstAndLast(deferred);</div><div class="line"></div><div class="line">-&gt;</div><div class="line"><span class="number">1</span></div><div class="line">result: <span class="literal">undefined</span></div><div class="line"><span class="number">2</span></div></pre></td></tr></table></figure>
<p>从输出看，由于我故意没有返回1处的Promise对象，最后一个<em>then</em> 的2处代码先被执行，打印出undefined，然后才会执行完1处的代码，打印出数字2。</p>
<p>这样的现象有两处值得注意的地方</p>
<ul>
<li>执行顺序混乱</li>
<li>最后then的结果是undefined</li>
</ul>
<p>顺序混乱是由于标识为1处的resolve的时间较长，回调的时间较久，所以后执行。</p>
<p> 2处为undefined，是因为1处没有返回值，<strong>我们知道javascript的function始终会返回值且这两种方式：<code>return;</code>(返回void)和不显式写return语句都会返回undefined</strong>。所以将值为undefined的promise对象传给下一个<em>then</em>方法。</p>
<h4 id="2-Then的Rejector-Function不返回Reject-Promise"><a href="#2-Then的Rejector-Function不返回Reject-Promise" class="headerlink" title="2. Then的Rejector Function不返回Reject Promise"></a>2. Then的Rejector Function不返回Reject Promise</h4><hr>
<figure class="highlight javascript"><figcaption><span>reject.js</span></figcaption><table><tr><td class="gutter"><pre><div class="line">1</div><div class="line">2</div><div class="line">3</div><div class="line">4</div><div class="line">5</div><div class="line">6</div><div class="line">7</div><div class="line">8</div><div class="line">9</div><div class="line">10</div><div class="line">11</div><div class="line">12</div><div class="line">13</div><div class="line">14</div><div class="line">15</div><div class="line">16</div><div class="line">17</div><div class="line">18</div></pre></td><td class="code"><pre><div class="line"><span class="function"><span class="keyword">function</span> <span class="title">printFirstAndLast</span>(<span class="params">itemsDeferred</span>)</span>&#123;</div><div class="line">        <span class="keyword">return</span> itemDeferred.then(<span class="function"><span class="keyword">function</span>(<span class="params"></span>)</span>&#123;&#125;, <span class="function"><span class="keyword">function</span>(<span class="params">reason</span>)</span>&#123;</div><div class="line">        util.puts(<span class="string">'1:'</span>+reason);</div><div class="line">        <span class="keyword">return</span> <span class="string">'error-1'</span>;</div><div class="line">    &#125;).then(<span class="function"><span class="keyword">function</span>(<span class="params">result</span>)</span>&#123;</div><div class="line">        util.puts(<span class="string">'resolve 2:'</span>+result);</div><div class="line">    &#125;, <span class="function"><span class="keyword">function</span>(<span class="params">reason</span>)</span>&#123;</div><div class="line">        util.puts(<span class="string">'reject 2:'</span>+reason);</div><div class="line">    &#125;);</div><div class="line">&#125;</div><div class="line"></div><div class="line"><span class="keyword">var</span> deferred = defer();</div><div class="line">deferred.reject(<span class="string">'error-0'</span>);</div><div class="line">printFirstAndLast(deferred);</div><div class="line"></div><div class="line">-&gt;</div><div class="line"><span class="number">1</span>: error<span class="number">-0</span></div><div class="line">resolve <span class="number">2</span>:error<span class="number">-1</span></div></pre></td></tr></table></figure>
<p>从输出看，如果reject function不返回reject promise，而只是<code>error-1</code>这样的值，那么调用then方法，执行的还是resolve方法。</p>
<p>从Promise/A+参考描述可证明上述</p>
<blockquote>
<p>promise2 = promise1.then(onFulfilled, onRejected);</p>
<p>If either onFulfilled or onRejected returns a value x, run the Promise Resolution Procedure [[Resolve]](promise2, x).</p>
<p>If x is a thenable, it attempts to make promise adopt the state of x, under the assumption that x behaves at least somewhat like a promise. Otherwise, it fulfills promise with the value x.</p>
</blockquote>
<h4 id="3-Then的Resolver-Function返回的是对象，而且包含then方法"><a href="#3-Then的Resolver-Function返回的是对象，而且包含then方法" class="headerlink" title="3. Then的Resolver Function返回的是对象，而且包含then方法"></a>3. Then的Resolver Function返回的是对象，而且包含then方法</h4><hr>
<figure class="highlight javascript"><figcaption><span>obj_then.js</span></figcaption><table><tr><td class="gutter"><pre><div class="line">1</div><div class="line">2</div><div class="line">3</div><div class="line">4</div><div class="line">5</div><div class="line">6</div><div class="line">7</div><div class="line">8</div><div class="line">9</div><div class="line">10</div><div class="line">11</div><div class="line">12</div><div class="line">13</div><div class="line">14</div><div class="line">15</div><div class="line">16</div><div class="line">17</div><div class="line">18</div><div class="line">19</div><div class="line">20</div><div class="line">21</div><div class="line">22</div><div class="line">23</div></pre></td><td class="code"><pre><div class="line"></div><div class="line"><span class="keyword">var</span> obj = &#123;<span class="attr">then</span>: <span class="function"><span class="keyword">function</span>(<span class="params">resolver, reject</span>)</span>&#123;</div><div class="line">    <span class="keyword">return</span> resolver(<span class="string">'i am an obj.'</span>);</div><div class="line">&#125;&#125;;</div><div class="line"></div><div class="line"><span class="function"><span class="keyword">function</span> <span class="title">printFirstAndLast</span>(<span class="params">itemsDeferred</span>)</span>&#123;</div><div class="line">        <span class="keyword">return</span> itemDeferred.then(<span class="function"><span class="keyword">function</span>(<span class="params"></span>)</span>&#123;&#125;, <span class="function"><span class="keyword">function</span>(<span class="params">reason</span>)</span>&#123;</div><div class="line">        util.puts(<span class="string">'1:'</span>+reason);</div><div class="line">        <span class="keyword">return</span> obj; 						<span class="comment">//=&gt; 1</span></div><div class="line">    &#125;).then(<span class="function"><span class="keyword">function</span>(<span class="params">result</span>)</span>&#123;</div><div class="line">        util.puts(<span class="string">'resolve 2:'</span>+result);</div><div class="line">    &#125;, <span class="function"><span class="keyword">function</span>(<span class="params">reason</span>)</span>&#123;</div><div class="line">        util.puts(<span class="string">'reject 2:'</span>+reason);</div><div class="line">    &#125;);</div><div class="line">&#125;</div><div class="line"></div><div class="line"><span class="keyword">var</span> deferred = defer();</div><div class="line">deferred.reject(<span class="string">'error-0'</span>);</div><div class="line">printFirstAndLast(deferred);</div><div class="line"></div><div class="line">-&gt;</div><div class="line"><span class="number">1</span>: error<span class="number">-0</span></div><div class="line">resolve <span class="number">2</span>:i am  an obj</div></pre></td></tr></table></figure>
<p>从结果看，obj这个含有then方法的对象，在1处被调用了then，同时将<code>resolver(&#39;i am an obj.&#39;)</code>作为Promise返回。</p>
<p>从Promise/A+可知</p>
<blockquote>
<p>If then is a function, call it with x as this, first argument resolvePromise, and second argument rejectPromise, where:</p>
<p>If/when resolvePromise is called with a value y, run [[Resolve]](promise, y)</p>
<p>If/when rejectPromise is called with a reason r, reject promise with r</p>
</blockquote>
<p>那么如果是以下这些对象</p>
<ul>
<li>{then: ‘something’}</li>
<li>{then: function(){}}</li>
<li>{A: ‘A’}</li>
<li>{then: function(resolver, rejector){return rejector(“error”);}}</li>
</ul>
<p>结果会是</p>
<ul>
<li>resolve 2:[Object Object]</li>
<li>忽略</li>
<li>resolve 2:[Object Object]</li>
<li>reject 2: error</li>
</ul>
]]></content>
    
    <summary type="html">
    
      &lt;p&gt;Promise对象最早被C++工程师使用，接着以Deferred对象出现在Python中。随着NodeJS的兴起，Promise在javascript中的应用越来越广。&lt;/p&gt;
&lt;p&gt;大凡技术成熟之后都会逐渐形成一个标准，Promise的标准定义在&lt;a href=&quot;http
    
    </summary>
    
      <category term="技术" scheme="https://lameta.com/categories/%E6%8A%80%E6%9C%AF/"/>
    
    
      <category term="javascript" scheme="https://lameta.com/tags/javascript/"/>
    
      <category term="promise" scheme="https://lameta.com/tags/promise/"/>
    
  </entry>
  
</feed>
